# Zero-dep allowlist for the dowiz-kernel DEFAULT no-dev build (roadmap items 1+13).
# INVARIANT: this file may only SHRINK. CI compares HEAD against origin/main and
# fails if any name present here is absent there (= growth). The ONLY sanctioned
# growth path is editing the `zero-dep-gate` job in .github/workflows/ci.yml in
# the same reviewed diff — that edit IS the explicit exception record.
# Rationale + retirement plan: docs/design/SPACE-GRADE-KERNEL-ARCHITECTURE-SYNTHESIS-2026-07-19.md §0.1.
#
# 2026-07-19 (roadmap items 4+29): the `tracing` + `tracing-subscriber` pair was RETIRED
# (kernel now owns src/fdr/). That dropped these 19 crates from the default no-dev tree:
#   tracing tracing-subscriber tracing-attributes tracing-core tracing-log
#   proc-macro2 quote syn unicode-ident matchers nu-ansi-term sharded-slab thread_local
#   cfg-if lazy_static log once_cell pin-project-lite smallvec
# The default tree is now exactly 6 crates (kernel + regex's subtree); allowlist = 5 external.
#
# --- 1 direct offender remaining (retirement: item 5 kills regex) ---
regex
# --- transitive closure of regex (locked by kernel/Cargo.lock) ---
aho-corasick
memchr
regex-automata
regex-syntax
