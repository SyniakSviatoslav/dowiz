//! agent — the bounded, fail-closed AgentLoop executor (std host shim).
//!
//! The full pure no_std agent (loop + model registry + model pair) lives in
//! `dowiz_core::agent`.

pub use dowiz_core::agent::*;
