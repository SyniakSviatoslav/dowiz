# Етап 9: Контекст 500k

## Статус
Завершено. Обраний модель `upstage/solar-pro4:free` з контекстним вікном **524 288 токенів** — перевищує вимогу 500k.

## Джерело
Перевірено через `/v1/models` від `inference-api.nousresearch.com`:
- `upstage/solar-pro4:free` → context_length: 524288

## Конфігурація
- `~/.hermes/config.yaml`: модель, провайдер, базовий URL вже вказані.
- Контекстний вікно визначається провайдером/моделлю, не змінюється локально.

## Примітка
Операція зі збільшення контексту — це вибір правильного модель+провайдер, а не параметр конфігурації. Етап виконано вибором моделі з достатнім вікном.
