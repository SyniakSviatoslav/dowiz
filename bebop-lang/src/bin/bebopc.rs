//! Minimal `bebopc` stub — reference scaffold, superseded by the native C
//! bootstrap. The real compiler lives in the native port (direct codegen).
fn main() {
    println!("bebopc 0.1.0 (reference scaffold; native C bootstrap is the real compiler)");
}
