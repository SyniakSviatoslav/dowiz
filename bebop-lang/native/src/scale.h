/* Bebop scalability benchmark — 1/2/4/8-core throughput on parallel NTT. */
#ifndef BEBOP_SCALE_H
#define BEBOP_SCALE_H

/* Run the scaling benchmark, print the speedup/efficiency table. Returns 0. */
int scale_run(void);

#endif /* BEBOP_SCALE_H */
