/* Bebop zlib — native deflate/gzip compression (no libc).
 * Minimal CRC32 + adler32 + LZ77-style Huffman. Arena-backed. */
#ifndef BEBOP_ZLIB_H
#define BEBOP_ZLIB_H
#include <stdint.h>
#include <stddef.h>
u...[truncated]