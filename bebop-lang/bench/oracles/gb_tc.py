#!/usr/bin/env python3
"""B3 prep gate oracle (pending, see gb_bfs.py header). Exact triangle count over the 3
standard graphs, combined fold."""
import lag_common as L
print(L.run_over_graphs(L.triangle_count))
