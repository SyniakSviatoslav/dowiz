bebop bcond 1223 T118 capacity traps (one b.ls per zeros / array literal / enum ctor site)
c13_array bcond 1 T118 capacity traps (one b.ls per zeros / array literal / enum ctor site)
c20_deep bcond 2 T118 capacity traps (one b.ls per zeros / array literal / enum ctor site)
c23_spillcall bcond 3 T118 capacity traps (one b.ls per zeros / array literal / enum ctor site)
c24_ifspill bcond 2 T118 capacity traps (one b.ls per zeros / array literal / enum ctor site)
c25_matchtail bcond 1 T118 capacity traps (one b.ls per zeros / array literal / enum ctor site)
c31_nested_lit bcond 2 T118 capacity traps (one b.ls per zeros / array literal / enum ctor site)
c33_loopalloc bcond 3 T118 capacity traps (one b.ls per zeros / array literal / enum ctor site)
c34_loopescape bcond 4 T118 capacity traps (one b.ls per zeros / array literal / enum ctor site)
c36_break bcond 5 T118 capacity traps (one b.ls per zeros / array literal / enum ctor site)
io_probe bcond 10 T118 capacity traps (one b.ls per zeros / array literal / enum ctor site)
k5 bcond 12 T118 capacity traps (one b.ls per zeros / array literal / enum ctor site)
k6 bcond 7 T118 capacity traps (one b.ls per zeros / array literal / enum ctor site)
k7 bcond 15 T118 capacity traps (one b.ls per zeros / array literal / enum ctor site)
k7neon bcond 14 T118 capacity traps (one b.ls per zeros / array literal / enum ctor site)
bebop bcond 1266 T122 reserved-word checks in compile_fn_at
bebop bcond 1280 T43 rest: struct-literal guard + find_struct line-start test
bebop cbz 142 T43 rest: struct-literal guard + find_struct line-start test
c40_struct bcond 4 T43 rest: new construct (struct literals + 2 frame traps + loop)
bebop bcond 1339 T47 use_expand (scan + dedup loops in cli_compile)
bebop cbz 149 T47 use_expand (scan + dedup loops in cli_compile)
bebop bcond 1341 T48a emit_offsets token buffer
bebop cbz 150 T48a emit_offsets token buffer
bebop bcond 1373 T101 left-operand slide (hazard scan loops)
bebop bcond 1377 T101 left-operand slide
bebop bcond 1387 T102 condition fusion
bebop bcond 1401 T104 madd fusion
bebop bcond 1403 T72 sys_setaffinity emitter (is_aff dispatch)
bebop bcond 1405 T105 clz emitter (is_clz dispatch)
bebop bcond 1459 T108 .becache (chunk_eq/cache_hit/cache_write + cli_compile cache path)
bebop cbz 157 T108 .becache (cache_hit/cache_write read loops)
bebop bcond 1461 T109 crc32 emitter (is_crc dispatch)
bebop bcond 1462 T126 x27/x28 pair never saved (k == 4 arm in OPT-A)
bebop bcond 1464 T109b crc32x emitter (is_crx dispatch)
bebop bcond 1469 T110 sys_msync emitter + T129 exit-90 guards on unwritable outputs
bebop bcond 1471 T47b nested use (use_scan recursion + use_comment)
bebop cbz 158 T47b nested use (use_scan recursion + use_comment)
bebop bcond 1477 T130 unresolved-call trap + T118b entry stub
bebop cbz 160 T130 unresolved-call trap + T118b entry stub
bebop bcond 1544 T80 cas:// imports (cas_prefix/cas_path/cas_verify in use_scan, cli_cas verb, sha256 prelude compiled into the compiler)
bebop cbz 177 T80 cas:// imports (cas_prefix/cas_path/cas_verify in use_scan, cli_cas verb, sha256 prelude compiled into the compiler)
bebop bcond 1568 T104b shl_try + addshift_try (peephole matchers: 24 b.cond in the emitter, every construct shrinks)
bebop bcond 1509 speed-up: slen (source length carried in pos[1], validated) replaces 43 per-token str_len scans
bebop bcond 1536 T90 step 1: diag_exit/put_num/diag_text (line:col diagnostics before exit 95-99) compiled into the compiler
bebop cbz 113 T90 step 1: diag_exit loops
bebop bcond 1542 roadmap 1b: em bounds check (n[0] >= n[1] -> cap_exit, trap 83) + cap_exit's loops
bebop cbz 114 roadmap 1b: cap_exit
bebop bcond 1559 T104b step 2: mulc_try (x*c for c = 2^k+1 / 2^k-1 / 2^j+2^k -> add-lsl / lsl+sub / add-lsl+lsl, 17 b.cond of selector ifs)
bebop bcond 1575 T90 step 2: call-argument delimiter checks (emit_bl_call / emit_call: only a comma or the closing paren after an argument) + a body let must end in semicolon, closing brace or `in` (lax d08/d10 now exit 95)
bebop bcond 1588 T90 step 2b: `check` verb (cli_check: argc / open guards, main: is_check selector + dispatch if)
bebop cbz 119 T90 step 2b: cli_check + the check dispatch in main
bebop bcond 1604 T96 P3: cmp_try (cmp x0,x1 with a movz/mov right operand and a mov left operand -> cmp xR,#imm / cmp xR,xS; the 16 b.cond of its selector ifs)
bebop bcond 1610 B1 prologue/epilogue/call-site right-sizing (emit_prologue_sized/emit_epilogue_sized conditional pair/x15/x14 emission, fntab_fact_lookup, the planning-pass arithmetic size correction)
bebop cbz 120 B1 fntab_fact_lookup's is_match loop test
bebop bcond 1629 B5: bottom test emits one b.<inv cond> per loop instead of b.cond + b
bebop bcond 1762 register model: cbz/cbnz on register conditions at if/while, b.cond on FLAGS
bebop cbz 124 register model: cbz/cbnz on register conditions at if/while, b.cond on FLAGS
c04_cmp cbz 5 register model: cbz/cbnz on register conditions at if/while, b.cond on FLAGS
c05_if cbz 2 register model: cbz/cbnz on register conditions at if/while, b.cond on FLAGS
c20_deep cbz 1 register model: cbz/cbnz on register conditions at if/while, b.cond on FLAGS
bebop bcond 1071 A2 commit 1 (csel): net vs the true HEAD baseline (1762) this is a big DROP -- pure if-arms now emit csel instead of b.cond/b/join; 1071 is only an increase relative to the previously (mistakenly) frozen 1056, which was recorded while bebop.bp:1's `use sha256.bp` line was accidentally left commented out and sha256.bp's own branches were never compiled in
bebop cbz 126 A2 commit 1 (csel): the 5 new scanner fns (arm_end/arm_is_pure/kw_bad_at/kw_term_at/ident_call_at) add a few register-condition cbz sites of their own; net +2 vs the previously (mistakenly) frozen 122, well within a real csel win vs the true HEAD baseline of 124
bebop bcond 1113 A2 commit 2 (constant hoisting): the 6 new fns compiled into the compiler (hoist_lookup/hoist_mask/find_body_end/hoist_record/hoist_scan/hoist_release, each with their own while loop(s)) add their own b.cond selector/loop-test sites; this is a one-time cost of the compiler compiling ITSELF, not per-target-program growth -- k8h_loopwords itself drops 22 -> 14 (docs/PERF.md)
bebop cbz 126 A2 commit 2 (constant hoisting): unchanged from A2 commit 1's frozen 126 (the new hoist scanners' own loop tests compile to b.cond, not cbz -- no new register-condition branches)
bebop bcond 1116 A2 commit 2 follow-up: hoist_scan's nested-`while` guard (word-boundary is_whilekw check + the has_nested-gated vals-clear) adds 3 more b.cond selector sites, compiled into the compiler; needed to keep k1ht/k4t/ptrless/deltasync (all nest a `while` inside a `while`) from tripping the cs-mask-must-be-0 entry assertion when the outer loop's own depth-0 literal would otherwise have hoisted
bebop bcond 1120 A11(b) standalone: 2 new emit_call_or_ctor dispatch conditions (sys_run/sys_wait4, each `name == <64-bit hash>` against a large constant that needs its own cmp_try selector compare) add 2 b.cond each
bebop bcond 1122 A4 fuzz DIVERGE hunt RC2 (2026-09-07): check_reg_collision gained 2 new IMPURE `if`s over the arm_spanning/do_reloc guards (one guards diag_exit(89), one guards the existing vs_park_to_cs/slot relocate) -- impure arms compile to a real b.cond/join, not csel; the pure arm_spanning/do_reloc value computation itself (nested `if .. then 1 else 0`) is all-constant-arm and csel's for free
bebop bcond 1129 ROADMAP A14 (2026-09-07): the path-independent if-park adds 4 impure `if`s (vs_park_move to_cs guard, emit_cond pure-gated span park, vs_span_to_slots kind test + emit_body slot-cursor reset) -- was 1125 A4 fuzz DIVERGE round 2 (2026-09-07): RC5 -- vs_bind/vs_to snapshot guard over pending lazy SYM window entries when a sibling operand writes the same symbol (emit_let_chain / emit_compound_stmt paths) adds 3 impure `if`s; RC4 (T118 heap-trap scratch via vs_alloc instead of a hardcoded x2) adds none
bebop bcond 1141 ROADMAP A13+A15 (2026-09-08): the two compile-time diagnostics add 12 impure `if`s to the compiler compiled into itself -- diag_exit gains 3 new code-selector arms (100 "fn with more than 14 parameters", 101 "unbound symbol", 104 "too many fns (cap 512)") plus their 3 mlen terms, parse_params gains the first-param-position capture and the > 14 check, and the 5 sym_lookup < 0 guards (emit_var_or_ctor, emit_let_chain, emit_array_index, emit_let_stmt's chain head, emit_compound_stmt) are each an impure `if` around a diag_exit call; emit_let_stmt also gains the `in`-consuming if that the unbound diagnostic exposed (c14_string). Offset by A13 DELETING the two silent `brk #8` clamp ifs in emit_call/compile_fn (nparams > 14 -> em(...) and the nb clamp), so the net is +12 over A14's 1129
bebop bcond 1142 ROADMAP A9 (2026-09-08, `scan` builtin; rebased onto A13+A15's 1141): +1 b.cond selector site, net, from the two new large-constant compares against the read_ident hash 260242221 -- one in the emit_call_or_ctor dispatch arm, one in compile_fn_at's rsv3 reserved-word guard; the 46 emitted words of emit_sys_scan are `em()` literals, not compiler branches, so they add none of their own
bebop bcond 1143 ROADMAP B1 step 1 (2026-09-08, `sys_fsync` builtin; rebased onto A9's 1142): +1 b.cond, net, from the two new large-constant compares against the name hash -8392076202161113213 -- the emit_call_or_ctor dispatch arm and the compile_fn_at rsv3 reserved-word guard (added on merge: every other sys_* builtin is a reserved word, the lane had left sys_fsync out). emit_sys_fsync's own body is 2 em() literals (mov x8,#82 / svc #0), not compiler branches
bebop bcond 1145 ROADMAP A14b (2026-09-08, rebased onto B1 step 1's 1143): +2 b.cond from the two new IMPURE `if`s in vs_span_to_slots -- `if is_sym_r == 1 then vs_park_to_slot(...)` and `if is_sym_s == 1 then (vs_alloc + ldr + vs_park_to_slot + vs_mask_free)`. Both arms call functions, so neither csel's away; the pure kind==3 / p0<100 tests that compute is_sym_r/is_sym_s are all-constant-arm and cost none. CORRECTION to the A14b card, which predicted no allow line would be needed: that prediction confused the words this change EMITS (only str/ldr, no branch) with the compiler's OWN branch census, which counts the source `if`s the change adds to bebop.bp
bebop bcond 1150 ROADMAP A2b (2026-09-08, rebased onto A14b's 1145): +5 b.cond in the compiler itself. Four come from lifting A2's nested-`while` hoist ban: emit_while_stmt gains two `while` loops (the release scan over the 4 hoist pairs at a nested loop's entry, and the re-take loop after its code) and the release scan's `if olive == 1 then (... vs_cs_free ...)` arm is impure. The fifth is vs_settle_flags' new `if fntab[4450] == 1` suspension test. Offset by DELETING hoist_scan's is_whilekw/has_nested guard and its vals-clearing arm, which is why the net is +5 and not +7
bebop cbz 127 ROADMAP A2b (2026-09-08): +1 cbz, the bottom test of one of the two new `while` loops in emit_while_stmt (the re-take loop `while orst < onh`, whose counter is a plain register the loop-branch lowers to cbnz rather than cmp+b.cond)
bebop bcond 1154 ROADMAP A2b step 2a (2026-09-08, MADD fold; rebased onto A2b step 1's 1150): +4 b.cond in the compiler itself, all from vs_try_madd and its dispatch arm. Three are inside vs_try_madd -- measured, not predicted, by disassembling that fn's own 216 words out of gen4 8966a903: b.ne at fn+0x238 is the `direct` test `if okind == 3 then (if op0 < 100 then 1 else 0) else 0`, which branches because its then-arm is itself an `if` expression and so is not csel-able; b.ne at fn+0x2ac is `if ok == 1 then insns[lidx] = ... else 0`, an array STORE arm; b.ne at fn+0x2e0 is `if ok == 1 then (vs_pop/vs_pop/vs_push) else 0`, which calls. The fourth is vs_binop's new `let d7 = if d1+..+d6 > 0 then 1 else vs_try_madd(...)` dispatch arm, impure for exactly the reason every other d1..d6 arm is. The word-identification tests (bits 31:21 == 1240, bit 15 == 0, Ra == 31, Rd == mr) are all-constant-arm and csel away, so they add none
bebop bcond 1160 ROADMAP A2b step 2b (2026-09-08, AND-immediate fold; on top of step 2a's 1154): +6 b.cond. Five are inside vs_try_and_imm -- measured by disassembling that fn's own 144 words out of gen4 dfdd6c6a, b.ne at fn+0xdc, +0x104, +0x138, +0x174, +0x1c0: one per guarded arm, `let k = if ok == 1 then 64 - clz(c) else 1` (its then-arm is an expression, not a constant, so it does not csel), then the four side-effecting arms vs_pick_dest / vs_reg / em / (vs_pop,vs_pop,vs_push). The sixth is vs_binop's new `let d8 = if d1+..+d7 > 0 then 1 else vs_try_and_imm(...)` dispatch arm. The mask test itself (`c > 0 && (c & (c + 1)) == 0`, and the kind == 1 and op == 12 tests) is all-constant-arm and csels away
bebop bcond 1163 ROADMAP A2b step 2c (2026-09-08, shift-then-mask -> UBFX; on top of step 2b's 1160): +3 b.cond. Two are inside vs_try_ubfx -- measured by disassembling that fn's own 201 words out of gen4 7939ad7e, b.ne at fn+0x264 and fn+0x2a4: the `if ok == 1 then insns[lidx] = ... else 0` array-STORE arm and the `if ok == 1 then (vs_pop,vs_pop,vs_push) else 0` call arm. It has one FEWER than vs_try_and_imm because it needs no `let k = if ok == 1 then ... else ...` guard (its width/lsb arithmetic is unconditional and only ever read under `ok`). The third is vs_binop's new `let d8 = if d1+..+d7 > 0 then 1 else vs_try_ubfx(...)` dispatch arm. Every word- and fit-test (bits 31:22 == 845, imms == 63, Rd == r, lsb + width < 65) is all-constant-arm and csels away
bebop bcond 1164 ROADMAP C1 steps 1+2 (2026-09-08, re-derived on the post-dedup base 7ef614f1, NOT copied from the lane which measured 1163 -> 1172 before the duplicate-scan deletion): +9 b.cond, attributed per fn by counting (w & 0xFF000010) == 0x54000000 inside each fn's own word range -- kernel_marked +3 (the backward same-line scan for the `kernel ` marker: crosses spaces and tabs only, never a newline, and requires a non-identifier byte before the `k`), kernel_word_at +1, emit_ident +2 (the `sys_` prefix test under the dialect bit), diag_exit +2 (the new exit-102 row), emit_call_or_ctor +1. sys_prefix_at, emit_sys_mprotect and compile_fn_at contribute 0. The two emitted words of emit_sys_mprotect are em() literals (mov x8,#226 / svc #0, as+objdump-verified as d2801c48 / d4000001), not compiler branches
bebop bcond 1167 ROADMAP A5 step 1b (2026-09-08): +3 b.cond, one per new IMPURE guarded arm introduced by the index model's `index 0 needs no add` fast path -- `if idxc > 0 then em(insns, n, ...) else 0` in vs_array_get_imm, the same arm in vs_array_set_imm, and `if idx > 0 then em(insns, n, ...) else 0` in emit_field_access. Each calls em(), so none of the three can csel; the companion `let b = if idxc > 0 then d else base` selects between two plain registers and DOES csel away, which is why the increase is 3 and not 5. cbz 123 and tbz 0 are unchanged -- the step adds no loop and no zero-test. Dropping the fast path would keep bcond at 1164 at the price of one extra `add` on every `a[0]`/`n[0]`/`pos[0]` in every emitted binary
c84_run bcond 13 ROADMAP A5 step 1b (2026-09-08): +1 b.cond in the ONE construct that calls sys_run. It is not a compiler branch at all -- it is the `b.ge` of the argv-vector conversion loop emit_sys_run now emits inline (mov x9,xzr ; cmp x9,x2 ; b.ge +6 ; ldr x10,[x3,x9,lsl #3] ; add x10,x17,x10,lsl #3 ; str x10,[x3,x9,lsl #3] ; add x9,x9,#1 ; b -6), all eight words as+objdump-derived. argv cells hold `str` values, which are still raw pointers under A5, while the caller can only build them out of [i64] arrays, i.e. out of indices; sys_run therefore converts the vector at its own boundary like every other builtin with a cell argument. c84_run's own words go 462 -> 470, exactly the eight
bebop bcond 1189 ROADMAP A6 step 1 defect fix (2026-09-09): +22 b.cond, all of them IMPURE `if` arms in the runtime-match path that did not exist before -- `match <var>` used to emit NOTHING, so this is a whole consumption path, not a cost added to existing code. Re-derived by the main session at SOURCE level, not from a per-fn disassembly of gen4, and stated as such: the seven new/changed fns carry 50 `if` arms, of which 20 are emit_match_lit -- the old emit_match body split out BYTE-IDENTICAL, so already inside the 1167 baseline. That leaves 30 genuinely new arms (emit_match_rt 24, emit_match_var 3, emit_match 1, emit_match_rt_arm 1, patch_b 1; vs_restore and emit_match_rt_bind carry none), and 22 of them survive as b.cond because they call a function or store to an array; the 8 pure selects in the same loops (`stop`, `adv`, `prevb`, `is_rb` and their kin) csel away. cbz 123 and tbz 0 are UNCHANGED, so no new loop was introduced.
bebop bcond 1191 ROADMAP A6 step 2 commit A (2026-09-09, computed frames): +2 b.cond, and the attribution is DERIVED from the diff rather than asserted -- commit A adds exactly six `if` arms and four of them csel away because both sides are pure. The two that survive are `if needs15 == 1 then em(insns, n, 2432697327 + frame_x15off(marks) * 1024) else 0` (a CALL) and `if ldepth + 1 > fntab[4800] then fntab[4800] = ldepth + 1 else 0` (an array STORE, the marks high-water bump beside the live-depth bump in emit_while_stmt). The four that vanish are `let s0 = if vc > 8 then vc - 8 else 0`, `let real_marks = if fntab[4800] > 21 then 21 else fntab[4800]`, and the two `if is_emission == 1 then emit_*_sized(...) else emit_*(...)` arms, which are not new at all -- they are the pre-existing planning/emission split with new arguments, so they carry no new branch. cbz 123 and tbz 0 are UNCHANGED, so no new loop and no new zero-test was introduced. fntab[4800] is the per-fn marks HIGH-WATER and cannot reuse fntab[4411], which is the live depth and is restored on the way out of every loop, so it reads 0 exactly when the frame is sized.
bebop bcond 1194 ROADMAP F2 prerequisite (2026-09-09, exit-code partition): +4 b.cond and +1 cbz over the step-2B baseline of 1190/123, and the attribution is MEASURED, not argued. The lane ran a four-arm census -- baseline, partition only, the seven T122 hashes only, and both -- and the arms are exactly additive with the ENTIRE branch delta belonging to the partition arm: the reserved-table hashes cost ZERO branches. Reproduced here on a different baseline (the lane cut its tree at 5be7adf, before A6 step 2B, so its absolute numbers were 1191 -> 1195; rebased onto 2B the same change gives 1190 -> 1194, the same +4). The branches are `selfcheck_exit(kind, site)`'s own guards: eighteen self-check sites that used to exit `sys_exit(99+site)`, `(100+site)`, `(119+site)` or `(120+site)` now route through ONE code, 201, with `compiler self-check <kind>.<site>` on stderr. That deletes the aliasing outright -- the compiler could previously emit ANY code in 99..140, so 102 meant both the kernel-fn diagnostic and a window-mask self-check, and 104 meant both "too many fns" and a cs-mask self-check. Verified independently after the merge: the only literal in `sys_exit()` is 201, and `diag_exit` uses only 99, 100, 101, 102 and 104 in that range, each with exactly one meaning, leaving 103, 105, 106 and 107 free for F2's new diagnostics. Zero `base + site` families remain.
bebop cbz 124 ROADMAP F2 prerequisite (2026-09-09, exit-code partition): the single new cbz belongs to the same change and the same four-arm measurement -- see the bcond 1194 line above. It is `selfcheck_exit`'s own zero-test; the seven T122 reserved-word hashes add no branch of any kind, which the arm that changed only the table proved by leaving cbz at 123 and bcond at 1191.
bebop bcond 1212 ROADMAP F3 commit 1 (2026-09-09, static bounds checks): +18 b.cond for exit 65 `index past the end`, and the row is a HYBRID rather than the census's "needs emitted words" -- where the length is fixed by the `let` AND the index is a literal, both operands are TEXT at compile time, so the check discharges in the compiler and emits ZERO words in any compiled program. fntab[5410..5537] holds each symbol's static cell length + 1 keyed by its REGISTER, which sym_lookup already returns at both the `let` and the index site, so the key costs no extra scan; only `zeros(<literal>)` and array literals publish a length, while a parameter, a call result or `zeros(n)` record 0 and are NEVER guessed. The +18 is the same figure measured against the pre-relayout base, which is itself the evidence that rebasing onto cap 768 changed nothing but the base. Static discharge is 88.6 pct literal x 92.3 pct known-length = 81.7 pct IN bebop.bp; the deduped corpus is 58.4 pct and excluding bebop.bp 49.2 pct, so user-shaped programs pay roughly THREE TIMES this rate and the bebop.bp-only figure must never be quoted alone.
bebop bcond 1213 ROADMAP A7 step 1 (2026-09-09, crc32b string builtin): +1 b.cond, the new `else if name == 3853165909311 then emit_crc32b(...)` dispatch arm in emit_call_or_ctor -- a genuine conditional on the 131-rolling name hash, so it cannot csel away. The rsv3 reserved-table addition (`if selfname == 3853165909311`) costs zero branches (arithmetic sum, same as the F2 measurement). emit_crc32b itself is straight-line (one cbz loop test, no `if`), so cbz is unchanged.
bebop bcond 1214 ROADMAP A7 step 3 (2026-09-12, retro-attributed): +1 b.cond over the frozen 1213, and the attribution is DERIVED, not asserted. The +1 is NOT the A7 step-2 revert -- that change is branch-neutral, measured: the four functions it touches (emit_crc32b, emit_str_len_fn, emit_char_fn, emit_str) and the two it adjusts (scan_literals, scan_one_lit) carry exactly 11 `if` arms and 2 `while` loops both before and after, 11 -> 11 and 2 -> 2. The +1 belongs to A7 step 3, which landed in 6fb9830 WITHOUT this allow line: the builtin dispatch chain in emit_call_or_ctor went from 37 `else if name ==` arms at A7 step 1 (2ada1bb, where 1213 was frozen) to 38, and the one added arm is `emit_sys_mapb`. That is a genuine conditional on the 131-rolling name hash, so it cannot csel away -- the same shape as the crc32b arm the 1213 line itself documents. cbz stays 124 and tbz 0, so no new loop and no new zero-test. NOTE the emitter that arm dispatches to is itself defective: sys_mapb hands openat a pointer to a word holding an address instead of NUL-terminated path bytes, which is how 9 pointer-named blobs, ~260 MB, got committed in 4286ec1.
bebop bcond 1217 ROADMAP B6 (2026-09-12, parallel region live-symbol check): +3 b.cond for the sys_clone spawn overflow diagnostic (exit 103). Two are from emit_sys_clone: the live-symbol count computation and the check `if total_live > 8 then diag_exit(s, srcpos, 103)` (impure then-arm); one more is added to diag_exit itself for the new `if code == 103` arm. The diagnostic refuses any sys_clone with fntab[5393] (spilled symbols) + fntab[5391] (maximum cs register count) exceeding 8, which would cause a silent child-thread death at runtime (CORRECTNESS failure); this bound is conservative (function-wide counters rather than point-specific) and will refuse some working programs with complex register allocations alongside sys_clone.
bebop bcond 1216 ROADMAP A7 (2026-09-13, sys_mapb refused): +2 b.cond over the frozen 1214, and both are DERIVED rather than asserted. The emitter's 26 emitted words and its own straight-line body are DELETED and replaced by a single `diag_exit(s, pos[0], 108)` tail, which removes no branch (that body had no `if`) and adds none. The +2 are both in diag_exit, the one function every new diagnostic widens: one `if code == 108 then diag_text(...)` arm for the message text, and one `(if code == 108 then 32 else 0)` term in the hand-summed `mlen` expression below it. That second term is the reason the first attempt printed `9:19: ` with an EMPTY message -- the text and its length are two separate conditionals in two places, which is exactly the defect ROADMAP A17 exists to end, reproduced within an hour of that row being written. cbz stays 124 and tbz 0: no new loop, no new zero-test. When A17 step 1 lands and the texts become `str` literals, BOTH of these conditionals disappear together and this line should go down, not up.
bebop bcond 1219 ROADMAP A19 step 1 (2026-09-13, sys_clone REFUSED when > 8 symbols are bound at the spawn point, exit 109): +3 b.cond over the frozen 1216, and the attribution is MEASURED by five additive arms compiled by the promoted 7bdc0f11, not read off the diff. armA = HEAD's own source with no A19 reads bcond 1216 EXACTLY, reproducing the frozen number, and that is what licenses the other arms. armD = armA + only the `if code == 109 then diag_text(...)` arm in diag_exit -> 1218, so the text arm alone costs +2. armE = armD with the 35-element array literal replaced by a ONE-element one -> 1218 as well, so neither branch belongs to the literal's size (only its 68 words do: 43426 -> 43494, ~2 words per element, which is the figure docs/blueprints/A17-diagnostics-as-data.md §4.1 derives); both are structural to one impure `if` arm whose then-side calls. armB = armD + the `(if code == 109 then 35 else 0)` term in the hand-summed mlen -> 1218, so that term costs ZERO: it is a pure select between two constants and csels away. armC = armB + `if stab[0] > 8 then diag_exit(s, srcpos, 109) else 0` in emit_sys_clone -> 1219, so the check costs +1; its then-arm calls, so it cannot csel. cbz stays 124 and tbz 0: no new loop and no new zero-test. words 43405 -> 43513 (+108). THIS CORRECTS THE `bebop bcond 1216` LINE ABOVE, which attributed its +2 to one text arm AND one mlen term: the mlen term is measurably free, so a new diagnostic code costs 2 branches in the text arm alone, and A17 step 1's saving is whatever the `str`-literal path costs instead -- to be MEASURED when it lands, not predicted from this line. Unrelated to the stale `bebop bcond 1217` line (ROADMAP B6, 2026-09-12), which belongs to the WITHDRAWN trap 103: that check counted FUNCTION-WIDE fntab[5393]+fntab[5391] rather than a point-specific count, refused working programs, and was removed; 1217 was never frozen, it licenses only the exact value 1217, and it is kept as the record of an increase that did not survive
bebop cbz 125 ROADMAP A17 step 1 (2026-09-13, diagnostics as data: the 15 compile-time texts become `str` literals): +1 cbz over the frozen 124, and it is pinned to ONE builtin call by five arms compiled by the promoted 7bdc0f11, not attributed by reading the diff. The baseline arm is the licence: HEAD's own source (`332602c`) recompiles to 43513 / bcond 1219 / cbz 124, reproducing the frozen row exactly. armP = HEAD plus the new `diag_str` helper **never called by anything** -> cbz 125, which already proves the increase lives in the helper's own body and not in any call site (the compiler emits every fn whether or not it is reached; that is also why `arch_check` CHECK 19 ratchets dead functions). armQ = the same helper with the length passed in as a parameter instead of computed, i.e. `while i < n { buf[at+i] = char(m,i) }` with NO `str_len` call -> cbz 124, UNCHANGED. So the loop, the `char()` reads and the array store contribute nothing and **the single new cbz is the zero-test emitted for `str_len(m)`**. Two further arms rule out the rest of the change: armV (the new source with HEAD's `cap_exit`/`selfcheck_exit` restored) and armW (the new source with the `error[E`/`]: ` prefix written back as hand-stored ASCII bytes) both still read cbz 125. tbz stays 0: no new bit-test. **bcond FALLS 1219 -> 1203 (-16) and that is the row paying for itself**: armP shows the helper's own `while` adds +1 bcond, so the thirteen converted arms plus the deleted hand-summed `mlen` and the two deleted array literals in `cap_exit`/`selfcheck_exit` give back 17. That also settles, by measurement, the question the `bebop bcond 1219` line above left open: an i64-array arm cost 2 b.cond (measured there as armD 1216 -> 1218 for ONE arm), the `str`-literal arm costs about 1, so the saving is ~1 branch per diagnostic code and was NOT the 2 per code that the withdrawn reasoning in the `bebop bcond 1216` line would have implied. words 43513 -> 42491 (-1022, against the -400..-800 the A17 blueprint §4.1 derived, so the blueprint under-predicted the saving), b 938 -> 939, bl 1930 -> 1933, ret 300 unchanged
bebop bcond 1207 ROADMAP A17 follow-on (2026-09-13, the three silent compiler exits gain diagnostics: codes 64, 88, 90 route through a new `cli_exit(code, m: str)` and cli_compile's SOURCE open gains the guard it never had): +4 b.cond over the frozen 1203, split 3 + 1 and both halves MEASURED, not argued. The baseline arm licences the rest: `ef4c37f`'s own source recompiles to 42491 / bcond 1203 / cbz 125, reproducing the frozen row exactly. armZ = that base plus the `cli_exit` helper **present but NEVER called by anything** -> bcond 1206, so ALL THREE of the first group belong to the helper's own body and **the nine converted call sites cost ZERO branches** -- each `sys_exit(N)` they replaced was already inside an `if`, and swapping in a call does not add a test. Three further arms say what those 3 are NOT: armAB (the `diag_str(buf, a3, m)` call on the str PARAMETER replaced by `a3 + str_len(m)`) and armAC (the str parameter left unused entirely) both still read bcond 1206, so the str argument and its marshalling are not the cost; the remaining candidate inside that straight-line body is the `zeros(256)` arena guard (the trap-80 `zeros crossed x28` test) plus the put_num/store/sys_write sequence, and that is named as the candidate rather than asserted, because no arm isolated it further. The FOURTH b.cond is mine and is exact: `let _ = if fd < 0 then cli_exit(90, ...) else 0;` added to `cli_compile`, an impure then-arm that calls, so it cannot csel -- measured as the only difference between the 42594/1206/126 chain and the 42605/1207/126 one. words 42491 -> 42605 (+114, of which +83 is the helper and +11 the guard), b 939 -> 941, bl 1933 -> 1947, ret 300 -> 301 (the one new fn)
bebop cbz 126 ROADMAP A17 follow-on (2026-09-13, same change as the `bebop bcond 1207` line above): +1 cbz over the frozen 125, and it is the `cli_exit` helper's own body again -- armZ, with the helper present and NEVER called, already reads cbz 126. It is NOT the str parameter: armAC, which does not use `m` at all, still reads 126. What the arms DO pin is the per-call cost of `str_len`: armAB replaces the helper's `diag_str(buf, a3, m)` with `a3 + str_len(m)`, putting a second `str_len` call directly in `cli_exit`, and cbz rises to **127**. That independently confirms the mechanism the `bebop cbz 125` line above established for A17 step 1 -- one cbz per `str_len` call site, the zero-test it emits -- and it is why the final form routes the text through `diag_str` (one `str_len`, inside the helper) instead of calling `str_len` again at the outer level. tbz stays 0: no bit-test anywhere in this change
bebop bcond 1223 ROADMAP A24 step 1 (2026-09-13, a top-level `test NAME { ... }` block is SKIPPED by brace matching in every whole-source scan): +16 b.cond over the frozen 1207, and the shape of the change makes the attribution structural rather than per-branch: FOUR scanners each gained the same guard -- detect `test ` at a top-level position, then skip to the matching close brace with `find_body_end` (`bebop.bp:4962`, the matcher fn bodies already use) -- so the cost is four copies of one keyword test plus its skip call, and `collect_fns` `scan_literals` `find_struct` `find_enum` are the four. Each keyword test is a chain of `char(s, pos+k) == <letter>` products whose arms are impure (they call `find_body_end`), so none csel away. cbz stays 126 and tbz 0: no new loop and no new zero-test, which is the evidence that the skip REUSES `find_body_end` rather than rolling a second brace matcher. words 42605 -> 43023 (+418). **The gate this pays for was measured RED before the change and is load-bearing after it:** on the previous compiler a file with a `test` block compiled and RAN correctly yet came out 756 bytes against its twin's 724 -- a 32-byte (8-word) leak, `scan_literals` collecting the string literal inside the block -- and disabling the `scan_literals` skip alone brings that 32-byte divergence straight back. **`collect_ctors` is NOT in the list and does not need to be, which CORRECTS the blueprint's §3 table:** it is not a whole-source scanner at all (`:745`) -- it calls `find_enum` for the enum's position and reads ctor names from inside those braces, so it inherits the skip transitively once `find_enum` has it. Verified beyond the row's own two constructs by three further probes, all byte-identical to their twin at 724: a test block whose string contains `struct pt { x: i64, y: i64 }`, one containing `enum opt { none, some }`, and -- the one c133 and c134 do not cover, now kept as `c135_testblock_braces` -- one whose string contains a CLOSING BRACE followed by a whole `fn sneaky() -> i64 { 9 }`, which a pattern-matched skip would end early on, collecting `sneaky` as a real function
bebop bcond 1241 ROADMAP F8 step 0 compiler half (2026-09-13, garbage in the requires/ensures and top-level theorem positions is REFUSED with exit 110): +18 b.cond over the frozen 1223, and the split is MEASURED by an isolating arm, not read off the diff. armP = the merged source with both `scan_inert(...)` calls replaced by the constant -1, so the validator is DEFINED but NEVER CALLED at either site, everything else byte-identical: it reads words 43553, bcond 1240, cbz 127, b 970, bl 1955, ret 303. So SEVENTEEN of the eighteen belong to the new code's OWN BODIES and only ONE to the two call sites -- and that one is not a guard, because armP keeps both `if bad >= 0 then diag_exit(s, bad, 110)` guards; what armP removes is the two `bl scan_inert` (bl 1955 -> 1957) and, by making each guard's condition the constant -1, it lets one of them fold. The bodies are four things and the arm does not split them further: `scan_inert`'s `while` over [from, to), `find_line_end`'s `while`, the `theorem ` keyword chain in `collect_fns` (whose arms call `find_body_end`/`diag_exit`, so they cannot csel), and the `if code == 110` text arm in `diag_exit`. What the +17 is NOT is the allow-list itself: scan_inert tests ~20 characters as `if c == N then 1 else 0` terms summed into one value, and the `bebop bcond 1219` line established by armB that a pure select between two constants csels away -- so the twenty comparisons are free and the two loops plus the impure keyword chain carry the cost. The arm that would split them is scan_inert reduced to a single `return -1` with its loop deleted, which was not run. words 43023 -> 43566 (+543), of which +530 is the bodies and +13 the two call sites; b 954 -> 971, bl 1951 -> 1957, ret 301 -> 303 (the two new fns, scan_inert and find_line_end). The gate this pays for: tools/f8_dt.py's ratchet falls 4 -> 0 and all four of its POSITIVE probes still compile and run (7, 10, 42, 7), including `theorem add_zero : forall x:i64. x + 0 = x := refl` -- the `:` `.` `+` `=` on the allow list are exactly what keeps that one legal while `requires @@@ %%%` is refused.
bebop cbz 127 ROADMAP F8 step 0 compiler half (2026-09-13, same change as the `bebop bcond 1241` line above): +1 cbz over the frozen 126, and armP already reads 127 with the validator never called, so the zero-test lives in a new BODY and not at either call site. Candidates, named rather than asserted because no arm isolated further: the `str_len` inside the `diag_str(buf, at, "invalid syntax in contract position -- use only identifiers, type names, and operators")` arm added to diag_exit, which is the mechanism the `bebop cbz 125` and `bebop cbz 126` lines both established by measurement -- one cbz per `str_len` call site, the zero-test it emits -- and, second, `find_line_end`'s `let adv = 1 - is_nl - at_end` loop. The first is the strong candidate on precedent: every previous diagnostic code added to diag_exit through diag_str cost exactly this one cbz. The arm that would settle it is the merged source with the code-110 text arm deleted and everything else kept. tbz stays 0: no bit-test anywhere in this change.
c70_qdsl bcond 110 ROADMAP A26/B7 (2026-09-13, same change as the `c70_qdsl 3462` word_budget line): +1 b.cond over the frozen 109, and it is the new `qdsl_and` helper's OWN BODY, not the call sites. The split is MEASURED, not read off the diff: bl 85 -> 156 is +71, and the file contains 72 occurrences of `qdsl_and(` of which one is the definition, so exactly 71 call sites -> 71 bl. ret 21 -> 22 and b 102 -> 103 are the same one new function. cbz stays 5 and tbz stays 0 -- no new zero-test and no bit-test, which is the evidence the cost is one function body and not per-site branching. What this replaces is 70 `&&` OPERATORS (the "35" in earlier reports was `grep -c`, a LINE count); each becomes a call, and a call is a bl, not a branch. `&&` in this language is a CONSTANT ZERO binding tighter than comparison, so every one of those 70 sites was a defect: measured on this construct's own qdsl_ident against "q { from T }", the identifier scan stops at position 1 with the fix and ran to 12 without it. words 3143 -> 3284
c70_qdsl_neg bcond 95 ROADMAP A26/B7 (2026-09-13, same change as the `c70_qdsl_neg 3010` word_budget line): +1 b.cond over the frozen 94, same mechanism as the `c70_qdsl bcond 110` line above and the same arithmetic independently: bl 76 -> 128 is +52, and the file has 53 occurrences of `qdsl_and(` minus the definition = 52 call sites. ret 20 -> 21, b 87 -> 88, cbz stays 5, tbz stays 0. 51 `&&` operators replaced. words 2708 -> 2828
c_st_bytes bcond 135 ROADMAP B3 (2026-09-13, the branch half of commit 8040103, whose +52-WORD half this file's sibling word_budget.txt lines already isolate): +1 b.cond over the frozen 134, and it is `st_alloc`'s bounds test -- `if (off + 2 + len) > arena_end then sys_exit(80)` at selfhost/prelude/store.bp:333, the test the allocator never had. ISOLATED, not inferred: an arm with that one line neutralised to `if 0 == 1 then sys_exit(80) else 0` and everything else byte-identical recompiles c_st_bytes at bcond **134**, exactly the frozen value, against **135** with the test in place (words 5055 -> 5053). The same +1 lands in all four store-using constructs and no other bin moves, which is the tell that the cost is the SHARED PRELUDE and not any construct. Caveat on the arm, stated because it is visible in its census row: neutralising the condition to `0 == 1` ADDS a cbz (8 -> 9), so the arm isolates bcond cleanly and is NOT clean on cbz; the cbz column of the real change does not move and needs no line
c_gb_degree bcond 281 ROADMAP B3 (2026-09-13): +1 b.cond over the frozen 280, same single cause as the `c_st_bytes bcond 135` line above -- `st_alloc`'s new bounds test in the shared prelude (selfhost/prelude/store.bp:333). Not separately armed: the arm on c_st_bytes moves bcond by exactly 1 and the prelude is common to all four, so a per-construct arm would be measuring the same line four times. cbz and tbz do not move
c_gb_kernel_accessors bcond 245 ROADMAP B3 (2026-09-13): +1 b.cond over the frozen 244, same cause and same evidence as the `c_gb_degree bcond 281` line above -- the shared-prelude st_alloc bounds test. cbz and tbz do not move
c_gb_vec_setbit bcond 282 ROADMAP B3 (2026-09-13): +1 b.cond over the frozen 281, same cause and same evidence as the `c_gb_degree bcond 281` line above -- the shared-prelude st_alloc bounds test. cbz and tbz do not move
c70_qdsl bcond 112 ROADMAP B7 (2026-09-13, the fingerprint change recorded in the `c70_qdsl 3510` word_budget line; SUPERSEDES the `c70_qdsl bcond 110` line above, which covered the A26 `&&` sweep alone): +2 b.cond over the 110 that sweep left, and the two are named individually rather than lumped -- one is `qdsl_ident_hash`'s own loop test `if byte == 0 then h else ...`, the other the `if kind == 10 then qdsl_ident_hash(...) else h` guard that only folds identifier nodes. The rest of the cost is calls, not branches, which the other columns confirm: bl 156 -> 158 (+2 = the guarded call plus the recursive one), ret 22 -> 23 (+1 = the one new function), b 105. cbz stays 5 and tbz stays 0. words 3284 -> 3332
c70_qdsl_neg bcond 97 ROADMAP B7 (2026-09-13, SUPERSEDES the `c70_qdsl_neg bcond 95` line above): +2 b.cond over 95, identical mechanism and identical arithmetic to the `c70_qdsl bcond 112` line above -- bl 128 -> 130, ret 21 -> 22, cbz 5, tbz 0, words 2828 -> 2876. Worth stating: in THIS construct the new code is never reached at runtime (the negative query is rejected before any fingerprint is computed), so these two branches are paid for and not exercised here; they exist to keep the two constructs' parsers identical
bebop bcond 1271 ROADMAP A18 + the lit_table guard (2026-09-14). The +30 over the frozen 1241 is FOUR causes and only TWO of them are this commit's; they are separated by measurement, not by reading the diff. (1) INHERITED, +23: the frozen table was last written at ddae413, and TWO later commits changed bebop.bp without re-freezing it or writing a line here -- e08194f (traps 111 and 112) and c24088d (trap 113). Proven with a control rather than inferred: HEAD's OWN committed bebop.bin (072c01d1) censuses at 44020 1264 131, so the gate was already RED at HEAD by bcond 1241 -> 1264 and cbz 127 -> 131 before a byte of A18 existed. That binary IS the fixpoint of HEAD's source (compile(head.bp) under it = 072c01d1), so this is not a stale-artifact artefact. (2) A18, +5: `return`/`break` in expression position -- two keyword tests in emit_factor, two more in hoist_scan, and the jump-list caps 17/18 -> 64/64. ISOLATED under a FIXED compiler (the same binary compiling both sources): head.bp 44020 1264 131 vs the A18 source 44432 1269 131, so A18 is +412 words, +5 bcond and ZERO cbz. The hoist_scan bail on bodies containing return/break was armed separately and costs 4 words and 0 branches (44432 vs 44428 with the bail removed), which is why it is not named as a cause of its own. (3) the lit_table overflow guard, +1: `if 6000 + lcnt[0] >= 7000 then diag_exit(s, start, 114)` in scan_one_lit, one b.cond, 44432 -> 44447. (4) the E114 message row in diag_exit, +1: one more `if code == 114` link in the message chain, 44447 -> 44460. Every one of those four numbers is a gen4 fixpoint binary, so they are comparable to each other and to the frozen row
bebop cbz 131 ROADMAP A18 + the lit_table guard (2026-09-14): the +4 over the frozen 127 is ENTIRELY INHERITED and none of it is this commit's. Same control as the `bebop bcond 1271` line above: HEAD's own committed bebop.bin (072c01d1) already censuses cbz at 131, from e08194f and c24088d, which never re-froze this table. A18 itself moves cbz by ZERO -- head.bp and the A18 source compiled by the SAME binary both read 131 -- and so do the trap-114 guard and its message row, which are a b.cond and a message link, not zero-tests. Recorded rather than absorbed: the honest number for what A18 cost on this column is 0
bebop bcond 1272 ROADMAP A27 (2026-09-14): +1 b.cond over the 1271 the A18 commit left, and it is ONE named line -- the `is_mulc_cs` guard added to `vs_pop`, `if kind == 6 then ((if p0 >= 19 then 1 else 0) * (if p0 <= 26 then 1 else 0)) else 0`, which is the callee-saved branch that function was missing. ISOLATED by what it fixes rather than by an arm: before it, `let v0 = 1; v0 *= (let t1 = clz(1) in 17); v0` exits 201 (compiler self-check 7.1) and after it the same five lines compile rc=0 and print 17. The cost is one conditional because the guard is one conditional; words 44460 -> 44489 (+29) covers the guard plus its eleven-line comment's effect on nothing -- comments cost zero, so the 29 is the guard's own emission in a function called from every pop site. cbz does not move (131), which is the check that this is a b.cond and not a zero-test: the 19..26 range test is two comparisons folded to a product, never a cbz
bebop bcond 1431 GATE CATCH-UP (2026-09-14): +159 b.cond over the 1272 the A27 commit froze, and it is EIGHT commits, not one. ISOLATED PER COMMIT and not read off a diff, by the method this file's earlier lines already use -- a FIXED compiler compiling each historical source. No arm edits bebop.bp (this lane does not own it): each source came from `git show <commit>:bebop-lang/bebop.bp` and every one was compiled by the SAME promoted bebop.bin, so all nine rows are comparable to each other and to the frozen row. THE CONTROL THAT MAKES IT BELIEVABLE: the base commit 07eb18a recompiles to words 44489 bcond 1272 cbz 131 -- exactly the frozen row, to the digit -- so the method reproduces the number it is explaining before it explains anything. Per-commit b.cond deltas: 0cb2c23 A26 compiler half +10; 541f034 A25 step 1 +2; 88d7d06 A22 step 1 +14; 13f0a24 the slurp-cap raise +0; b7d025d A21 steps 1+2 +74; 194b2a3 A16 step 1 +17; 988191e A16 steps 2+3 +25; fa6345c A8 section 3.1 +17. They sum to exactly 159. Two of those numbers are worth naming: the slurp-cap raise costs ZERO on every column, which is the direct confirmation of A28's claim that it is six lines changing no emission; and A21 alone is +74, nearly half the total, matching its own commit's admission that it spent +1579 words against a +150..250 estimate, all of it three standalone scanners. What this line does NOT do: attribute any commit's own +N to named lines inside it. That needs arming the source of each commit, i.e. editing bebop.bp, which another lane owns -- so the isolation here is per-commit and stops there, and it is labelled as such rather than presented as line-level
bebop cbz 133 GATE CATCH-UP (2026-09-14): +2 zero-tests over the frozen 131, and the same nine-source fixed-compiler sweep places BOTH of them exactly. Only two of the eight commits move this column at all: 541f034 (A25 step 1) +1 and b7d025d (A21 steps 1+2) +1; the other six -- A26's compiler half, A22 step 1, the slurp-cap raise, both A16 steps and A8 section 3.1 -- move it by ZERO. That asymmetry is the useful part and it is the tell this project has established on this column before: a cbz is what a `str_len`-style zero-test emits, and a b.cond is what a range or equality comparison emits, so a change that adds comparisons (A22's struct table, A16's arity guards, A8's type checks) buys b.cond and no cbz. A25's +1 is the `oob > 0` bounds test in `src_copy_range`; A21's +1 is in the arity-reading path. Both named as candidates from the per-commit deltas, NOT isolated to a line, for the same reason as the bcond line above -- line-level arming needs bebop.bp edits and this lane does not own that file
