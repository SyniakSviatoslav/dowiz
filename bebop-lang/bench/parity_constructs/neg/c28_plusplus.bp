// T42(d) 2026-09-04: `++` (string concat) is NOT in the surface and there is
// no third state -- `"ab" ++ "cd"` used to compile silently to a binary
// that crashed; the compiler now exits 96 and writes no .bin.
// EXPECT COMPILEFAIL:96 from: string concat operator not in surface
fn main() -> i64 { let s = "ab" ++ "cd"; str_len(s) }
