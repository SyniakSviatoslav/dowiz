// A2 commit 1: then-arm calls g(i) -- must stay on the branch path (WORD_DELTA vs c70)
// EXPECT -164832 from: hand: impure conditional select
fn main() -> i64 { let a = 7; let b = 3; let i = 0; let acc = 0; while i < 1000 { let acc = acc + (if i * 7 % 3 == 1 then g(i) else b - i); let i = i + 1; 0 }; acc }
fn g(x: i64) -> i64 { x + 1 }
