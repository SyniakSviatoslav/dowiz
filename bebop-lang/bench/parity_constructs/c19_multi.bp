// multi-fn + mixed constructs
fn main() -> i64 {
  let total = helper(5);
  let i = 0;
  let acc = 0;
  while i < total {
    let acc = acc + step(i);
    let i = i + 1;
    0
  };
  acc
}
fn helper(x: i64) -> i64 { x * 2 + 1 }
fn step(i: i64) -> i64 { if i % 2 == 0 then i * 3 else i }
