# Ratchet values for tools/arch_check.py. These may only ever go DOWN, in the commit
# that earns the reduction. Raising one is how a codebase rots; if a change needs a
# higher number, split the file instead.
#
# The cap a NEW .bp file must satisfy. Small files with named helpers, not one long one.
max_new_bp_lines = 800
#
# The largest .bp we currently have. bebop.bp is the self-hosting compiler and is the
# reason this is not 800 yet; every extraction out of it should lower this line.
max_bp_lines = 8500
#
# Files that exceed max_new_bp_lines today, each with the reason it is allowed to.
bp_exempt:bebop.bp = 1                       # the self-hosting compiler; split is task T-split
bp_exempt:gb_pool_build.bp = 1               # generated aggregate, not hand-written
bp_exempt:bench/traps/t114_literal_table_full.bp = 1  # GENERATED, and its size IS the assertion:
#   trap 114 fires only past 1000 string literals (fntab[6000..6999]), so a probe under the 800-line
#   cap cannot trigger it. A 1000-literal version was tried first and compiles rc=0 -- measured -- which
#   is why this file carries 1001. Splitting it would destroy the property under test.
bp_exempt:selfhost/std/sgraph2.bp = 1        # B3c graph kernels; split tracked separately
bp_exempt:bench/vs_rust/std_tests/sgraph2.bp = 1
#
# Over the cap as of 2026-09-12, each with the split that retires it. These are DEBT:
# the exemption is the record of it, not permission. Remove the line when the file is split.
bp_exempt:selfhost/prelude/store.bp = 1      # SPLIT: superblock+PartTab / objects+alloc / commit paths / compaction
bp_exempt:selfhost/prelude/gb.bp = 1         # SPLIT: matrix build / traversal kernels / semiring plumbing
bp_exempt:selfhost/std/gen_gb.bp = 1         # SPLIT: template emission / semiring tables
#
# selfhost/tcheck.bp is the F6 certificate checker: a STANDALONE trusted artifact with its own
# 511-fn cap, deliberately never inside bebop.bp, and the only new thing in the trust root.
# It is exempt on a different ground from the debt lines above -- not "we will split this
# later" but "splitting it would enlarge the trusted surface". A checker's trust argument is
# per-ARTIFACT: one file, one digest, one thing to read end to end. Splitting it into four
# files means four digests to pin and four places a reader must look before believing a
# verdict, which is a worse trade than the line count. Added 2026-09-14 at 3566 lines; it
# grew from ~1000 as F6 landed bb_mul64, the two floor lemmas, lemma D3 and the case splits,
# and it will keep growing while F6 does. The line count is therefore NOT the number to watch
# here -- the fn count against its own 511 cap is, and that has its own guard inside the file.
bp_exempt:selfhost/tcheck.bp = 1
#
# Scripts allowed to discard a run's stderr, each with the reason. A fuzzer or a delta
# debugger runs thousands of programs whose failure IS the signal and is counted, not read.
loud_exempt:tools/fuzzd.sh = 1            # fuzzer: failures are classified by exit code, counted, not printed
loud_exempt:tools/ddc.sh = 1              # delta debugger: every candidate is expected to fail
loud_exempt:tools/mutate_gate.sh = 1      # mutation gate: a mutant that dies is the PASS condition
#
# Documents citing files that do not exist. 1 as of 2026-09-14 (false positive: regex matches .sh from .sha256).
# A citation that points at nothing reads as evidence; every one removed lowers this line.
# 2026-09-14, 20 -> 0. Paths corrected (docs/AGENTS.md -> AGENTS.md), a moved file re-cited
# (expr_compile.bp -> selfhost/attic/), and the rest MARKED for what they are rather than de-named.
# TWO defects in the check itself were found doing this and both are fixed: the extension group had
# no right boundary, so `bebop-f86bee7.sha256` matched as `.sh` and was reported missing; and a design
# document NAMING A FILE IT PROPOSES TO BUILD was flagged as a false citation, which pushed a pass to
# DELETE the names -- `tools/perf_report.py` became 'a Python dashboard tool'. A plan is not a claim.
max_missing_citations = 0        # 20 -> 0, 2026-09-14. Sixteen were fixed at the source: selfhost/expr_compile.bp -> selfhost/attic/, docs/AGENTS.md -> AGENTS.md, and the rest MARKED for what they are (planned / hypothetical / external) rather than de-named -- an earlier pass deleted the filenames to satisfy the check, which made the design docs vaguer to make a number smaller.
#
# A gate comparison below this value is a fold or a small count, not a layout assumption.
magic_literal_min = 1000
#
# DEBT recorded 2026-09-12, each line may only go DOWN.
# 11 -> 1, 2026-09-14, EARNED not moved. Of the 12 the check reported, one was a FALSE
# POSITIVE the check itself caused: gb_reduce_scalar has three callers under bench/wip/, and
# bp_sources() stripped that directory before counting USES even though CHECK 19's own comment
# says "USES are counted over the whole corpus". Fixed by adding bp_call_sites(). The other
# ten were real and were WIRED UP, not deleted, because each belongs to a planned row:
#   6 qplan_* -> bench/vs_rust/std_tests/qplan_cover.bp (B7 step 2's planner had never
#     executed a line; the whole tree had exactly one mention of it, its own file)
#   3 gb_*    -> bench/vs_rust/std_tests/gb_orphan_cover.bp (gb_apply, gb_build_grid10x10,
#     gb_reduce_col; first execution in the tree's history, and it took two wrong assertions
#     of mine to get right -- see that file's header)
#   1 st_commit_c -> the same file. C3's row claims the `schain` gate covers it; schain names
#     it in a COMMENT and calls st_cobj/st_publish directly, because st_commit_c returns
#     st_publish's value instead of the commit offset its only plausible caller needs.
# The remaining 1 is src_rename_idents (bebop.bp:7786), A25's deliberate loud stub -- it is
# `cli_exit(84, "src_rename_idents: NOT IMPLEMENTED")` and is owed an implementation, not a
# caller. It stays at 1 until A25 step 1b lands, and it is in bebop.bp, which the lane that
# lowered this line did not own.
max_dead_functions = 1             # uncalled library/compiler functions; wire them up or delete them
max_uncovered_builtins = 1         # sys_mapb has no corpus caller: A7 step 3 landed it untested
max_syscall_no_regtable = 8        # emit_sys_{write,close,exit,exit_thread_guard,slurp,ftruncate,munmap,mmap}
max_malformed_journal = 519        # entries predating the H:/DID:/GOT:/VERDICT: format
max_unasserted_patchers = 0        # tightened 2026-09-14: the check now looks only at a .replace() whose result is WRITTEN to a repo file, and none of the six previously-listed tools do that. The old 5 was the count under the broad text heuristic; leaving it there would have permitted five real regressions.
max_unbounded_waits = 0
#
# Journal entries written from this unix time onward must carry COST:<seconds from first
# symptom to named cause>. That number is what turns "loud failures make us faster" from a
# claim into a curve. Older entries predate the field and are not retro-fitted.
journal_cost_since = 1789229455

# a gate that reads a producer's .store/.gbpool must assert it exists (need_file);
# a missing prerequisite otherwise surfaces as trap 82 and names the wrong subsystem
max_unguarded_prereqs = 0
