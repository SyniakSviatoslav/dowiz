# Bebop — THE Roadmap (single source of truth)

Status: 2026-09-12 -- rows re-verified against git in `docs/VERIFIED-STATE-2026-09-12.md`; READ THAT FIRST, because the row bodies below were last revised 2026-09-09 (`ae1cfb4`) and twelve commits landed after it, and because a circulating status summary cites a commit `c2f943e` that does not exist. (D11-L split: this file = thesis, TG-DONE table, critical
path, open decisions, measured table; HISTORY.md = every pull, task body, decision
D1-D11 with evidence and the superseded 2026-08 vision text; TASKS.md = the generated
ledger; docs/exp.journal = one line per experiment; AGENTS.md = the laws L1-L17)

## Thesis (binding since D11-A, 2026-09-05)

Bebop is ONE language for everything, and the thesis is that three things usually built as
separate systems are one system here (operator, 2026-09-09): it **compiles itself**, it
**proves itself**, and it **is its own database** -- no C anywhere in the toolchain and no
dependency outside the tree. A 1.5 KB assembly loader runs `bebop.bin`, which compiles `.bp`
to raw machine words and compiles itself to a byte-exact fixpoint (DONE, gated: gen3 == gen4).
Its object model IS its persistent store: persisted objects are the in-memory objects, same
layout, object-relative offsets, no pointers on disk, queries are ordinary compiled functions,
and compiled code runs within a small factor of Rust on honest kernels (measured, G1-G8). And
its claims are machine-checked theorems -- dependent types over an infinite universe hierarchy
with higher-order abstraction, ONE grammar, elaboration a compiler PHASE and never a second
language, the checker written in Bebop itself (Phase F, OPEN). Lean 4 and any SMT solver are a
CROSS-CHECK ORACLE only, never load-bearing, and every Phase F artifact is tested by "if Lean
disappeared tomorrow, what would still hold?". Everything in the plan serves that sentence and
is judged by a number in a committed script:

1. a one-pass compiler whose loops carry no stack-machine words (T96 done: K1 loop
   51 -> 14 words), then the register model (2026-09-06: tags over x0..x7, no runtime push/pop, retarget
   instead of peepholes -- docs/REGISTER-MODEL-BLUEPRINT.md), csel, LIN recurrence folding,
   pointer-free data and a flat per-fn IR only where the self-compile measurement earns it;
2. a store (docs/LANG-DB-DESIGN.md §4 + §9.5, amended by D11-H): two superblocks +
   append-only arena of self-describing objects, `ref T` = object-relative offset,
   commit = root swap, Cheney compaction, sha256-named migrations, CSR/bucket indexes,
   edge log + tiered CSR for graphs — gated G1-G8 against sqlite on a real workload
   (D11-I; D14 item 7 drops LMDB and native Rust — neither is measured by any script);
3. cores only for parallel scans (T106), the sweep engine only where activity is sparse
   (T107); no runtime cells for ordinary code (T55 spike: 41x/740x against);
4. an honesty floor that cannot be talked around: every gate has an oracle and a
   mutation test, every codegen change re-freezes constructs under a written word
   budget, every branch-census increase is a committed ALLOW line, every capacity is a
   loud trap, every speed number is pinned, in-process, against an honest twin.

The 2026-08 vision (post-von-Neumann substrate, no program counter, eigentime,
SME/SVE2 fusion, reversible logic, multiversal superposition, an x9-x13 typed register
bank, "no SQL, no WAL locks") is preserved verbatim in HISTORY.md as superseded text;
its mathematics survives as 91 oracle-backed gates; its operational claims were measured
and retired by decisions D8-D10.

## TG-DONE (falsifiable; every row is a number in a committed script)

| # | criterion | script / gate | today (2026-09-05) | target |
|---|---|---|---|---|
| 1 | honest kernels, in-process pinned, bebop / Rust twin (D11-C) | bench/vs_rust/honest.sh | 2026-09-06 (REPORT-honest.md, 94e47998, REPS=100): K1H 1.8x, K2H 3.3x, K3H 3.4x, K4 1.6x | every row <= 2.0x after T101-T104; <= 1.0x is the D1(a) stretch, decided on the numbers |
| 2 | linear self-hosting fixpoint | every codegen commit (AGENTS law) | gen3 == gen4 c3f58e8e (2026-09-06) | invariant, never a goal |
| 3 | one oracle per gate, none self-frozen | bench/oracles/run_all.sh | ok=117 self-frozen=0 (re-derived 2026-09-12: `grep -c '^gate ' bench/vs_rust/std_golden.sh` = 117, and the 'every gate has an oracle' half from tools/arch_check.py's gate-oracle note -- 117 gates, 0 without an oracle, ratchet 0. The two added since 115 are `smw` (`2e27578`) and `b6core` (`05fcc0f`); BOTH landed without refreshing this cell, so tools/roadmap_check.sh had been RED on this row since. Was 115: the `_addr` gen_gb gate from `7eafa38`, 2026-09-09) (was ok=99; the count drifted as gates were added and tools/roadmap_check.sh went RED on it -- it greps `ok=$(grep -c '^gate ' std_golden.sh)`, so this cell must be refreshed in the same commit as any new gate); every gate mutation-sensitive (T123 sweep 2026-09-06, tools/mutate_gate.sh) | 0 self-frozen; T124 fold specs written (docs/FOLDS.md) |
| 4 | zero tolerated miscompiles -- **the zero-arg-builtin miscompile is CLOSED 2026-09-12, on the PROMOTED compiler `08cd9e46`.** It was: a zero-argument builtin used as the RIGHT operand destroyed an already-computed left operand (`str_len("ab") - sys_arena_base()` gave 0 instead of -8205), because every builtin that takes arguments calls `vs_deliver` first while these three emitted straight into x0. Fixed by delivering in `emit_clock_ms` / `emit_sys_arena_base` / `emit_sys_arena_end` (bebop.bp:1210,1246,1261). Guarded by three constructs that all MATCH on the promoted binary: c97_arena_operand_miscompile (4), c98_arena_base_operand (11), c99_arena_end_operand (11). The fuzz gap that hid it -- gen.py emitted none of the three builtins -- is closed too: gen.py now emits `clock_ms() * 0` / `sys_arena_base() * 0` / `sys_arena_end() * 0` in right-operand position, so the emit path is covered while the program value stays deterministic. **Still open on this row: nothing in the compiler, one defect in the ORACLE was found and fixed the same day** -- tools/bpref.py copied an array on every write, so a callee's write into an `[i64]` parameter was invisible to its caller (`fn poke(a: [i64]) -> i64 { let _ = a[0] = 9; 0 }` gave 0 against bebop's 9). That is what made c70_qdsl / c70_qdsl_neg red since `c99cddb`, and it is an oracle this row depends on: bpref is the fuzz DIVERGE judge. Fixed; construct parity is 104/104 | construct_parity (104 constructs incl. neg), fuzz | R3.x deleted; DIVERGE-20056 (2026-09-06) was an undefined program (loop-release rule now in LANGUAGE.md, bpref raises, gen.py never emits it); DIVERGE-42122 (2026-09-06) WAS a miscompile — a 9+-param callee whose body never touched x15 wrote its 9th param into the caller's spill slot (OPT-G1 scan started after the param stores) — fixed, construct c53_param9 | fuzz >= 10^5 programs, 0 CRASH / DIVERGE, traps only (TG-DONE 8) |
| 5 | single compiler, single language | attic, construct_parity | expr_compile.bp in attic; 38 constructs | every accepted construct in construct_parity; struct literals + `use` + `ref T` landed (T43 rest, T47, T48) |
| 6 | hardware claims measured, never projected | bench_pinned.sh, REPORT-pinned.md, Measured table below | no projected row remains | stays true |
| 7 | the store | G1-G8 (T112-T117) | library + G1-G6 green in std_golden (99 gates; 100/100 SIGKILL trials); G7 sbench measured vs sqlite (17x insert, 450 ns PK lookup, 30x window scan, 2.5x size loss); G8 stage 1 measured (BFS 187 ns/edge vs sqlite 10.8 us), stage 2 running | all eight green with numbers; G7/G8 thresholds a,b,c frozen by the operator before the run (D11-I) |
| 8 | fuzz at scale | bench/fuzz/fuzz.sh | 150-seed batches; gen.py widened 2026-09-05 (large loops, literals in loops, recursion 127, return/break); 2026-09-06 generator fixed (fresh loop names, big-loop vars never reassigned, loop-bound literals released) -> 1.5 programs/s, 0 timeouts on 20 seeds; 2026-09-06 in-process shards (fuzz_batch.py): 3.5 programs/s on 3 cores, 300-seed batch found 42122 | >= 10^5 seeds ON THE PROMOTED BINARY (D12-C: docs/PERF.md `fuzz_seeds_on_bin`, keyed by md5; 28.5k on d785e062, e14dd55e counting), 0 CRASH/DIVERGE/COMPILEFAIL, TRAP-82 = 0 (ALERT class since D12-C), capacity traps 80/81/83 only |

Row 1's target (D14 item 10, wasmtime's own baseline-vs-optimising numbers): one-pass compiler tiers land at
1.1x-1.5x of an optimising tier generally, so <= 2.0x per honest row is the real target for this compiler
shape; D1(a)'s <= 1.0x stays report-only (D12-G), not a gate.

## Critical path (in order; one writer, one commit per single-variable step)

Rewritten 2026-09-06 (session 18) by the main session after the four research reports
(docs/RESEARCH-DEPS, -TENSOR, -NOPOINTERS-SQL, -GRAPHBLAS -2026-09-06.md) and the operator's
decisions of the same day: the operand-tag-stack IR rung is replaced by the register model in one
commit; the Salsa-like per-fn memo is required; harness dogfooding is measured-first; the pointer-
free data model, u32 cells and the tensor-graph DB over the store are on the path; and -- operator
2026-09-06, binding -- **multi-writer parallelism, instant start (no first-shape compile latency),
multi-core execution, rank-n tensors and GraphBLAS-speed transformations are MANDATORY properties of
the store, not optional "after" items**. Every task below has an executable blueprint in
docs/blueprints/NN-<name>.md written by the main session (AGENTS.md L22); the Sonnet worker executes
them in this order, one chain-gated commit per step, and never two codegen tasks in flight.
Duplicates removed: B4 (frame size) is inside A6; T48 types are inside A8; the NEON items of the
tensor report are one task A9; the specialise-then-run twin, the SpGEMM-join twin and B8 (profile the
CSR build) are one measured-first task B2; the compiled planner (14a-3), the DSL (6b-4) and item 6 are
one task B7; group commit / st_verify / recovery rows are inside B1; "multi-writer only if W demands"
is superseded by B5 (mandatory).

### Phase A -- compiler substrate (every task through `tools/chain.sh --codegen`, constructs re-frozen)

| # | task | blueprint | gate (the number) | depends on |
|---|---|---|---|---|
| A1 | register model, one commit (landed 2026-09-06, fixpoint 831a357c) | docs/REGISTER-MODEL-BLUEPRINT.md | push_words == 0; k4 <= 13, k3h <= 10, k1h <= 8, k2h <= 30 loop words; k4_ms <= 1.15x Rust twin; c55-c61 | -- |
| A1b | LANDED (verified 2026-09-07: 3b58f34 fn cap 512 + eb73c29 zone relayout; probe 300 fns compiles and runs, 520 fns exits 89 -- but with the wrong text `let binding while a call temp is live`, fix the text under A13) -- fntab relayout + fn cap 512 (the old compiler's `offs/fnames/fpos/sizes/starts = zeros(256)` and the 3*cnt+1 zones; bebop.bp is at ~250 fns and every later task adds fns) -- one byte-identical commit (gen2 == gen3 == gen4 md5 unchanged) | docs/blueprints/A2-csel-and-const-hoist.md step 0 | fixpoint md5 unchanged; a 300-fn synthetic program compiles; exit 89 above 512 | A1 |
| A2 | LANDED 2026-09-07 (71eaeed csel, 0f29018 hoist): K8H 2.2x -> 1.9x honest, gate 1.2x UNMET -> follow-up A2b (operator 2026-09-07); k8h_loopwords 25 -> 14, bcond 1762 -> 1116. Was: T52 `csel` on FLAGS/REG tags for pure `if` arms; + hoisting of 64-bit loop-invariant constants as the optional second commit if K8H > 1.2x after csel | docs/blueprints/A2-csel-and-const-hoist.md | K8H <= 1.2x Rust (honest.sh row) | A1b |
| A2b | **CLOSED 2026-09-08, gate MET (bebop.bin 7939ad7e): K8H 1.1x against the `<= 1.2x` gate**, reproduced OLD 1.8x/1.8x vs NEW 1.1x/1.1x in one interleaved A/B taken inside a slot so no other lane's chain shared cores 4-6; bebop's absolute fell 0.042 -> 0.025 ms/rep while the unchanged Rust twin held 0.023-0.025. step 2 LANDED (worker lane): the remaining gap was per-iteration WORDS, not the branch -- the data-dependent `if` was already a csel on both sides. Both inner loops were extracted before anything was changed: bebop 12 words vs the rustc -O twin's 8. Three binop-level peepholes close it to 9 -- `vs_try_madd` (folds `a*b+c` backwards into the multiply's own emitted word: MUL *is* MADD with Ra=xzr), `vs_try_and_imm` (`e & (2^k-1)` as a logical immediate instead of mat_const + and), `vs_try_ubfx` (`(e>>lsb) & (2^w-1)` by rewriting the shift's UBFM imms). Each declines unless the producing word is still the last emitted one and its own bits confirm the opcode. Guards c74_madd 82837312, c75_andimm 1477639, c76_ubfx 2271612, all from bpref, each carrying shapes that must DECLINE. census 38800 -> 39340 (+540 net = 634 words of new folder against 94 the folds remove from the compiler compiling itself), k8h 38 -> 35, k7 309 -> 305. docs/PERF.md's k8h_loopwords 14 was STALE: re-derived baseline 12, now 9. One word still separates the loops -- `cmp x22,#1`, which `tst`/`ands xzr` would absorb; that needs a COMPARISON-level fold in vs_cmp (2-word lookback plus a condition flip), not another binop fold, and is NOT required by the gate. Known word-neutral limitation: vs_try_madd lands the result in the multiply's own Rd rather than via vs_pick_dest, so a consumer wanting the other operand's register gets a `mov` (c32_asr, and the two changed words in k2ht). step 1 LANDED 2026-09-08 (bebop.bin 29fe5c72): the nested-`while` hoist ban is lifted -- hoist_scan drops its has_nested guard and emit_while_stmt releases the enclosing loop's 4 hoist pairs at its own entry and re-takes them (one mat_const each) after its own backward branch, so the re-take is paid once per OUTER iteration; guard construct c73_hoistnest EXPECT=24000282. The SAME commit fixes a bigger thing found on the way: vs_settle_flags never READ the `fntab[4450]` suspension emit_cond_csel had been setting since A2, so every pure `if` did cmp -> cset -> cmp -> csel instead of cmp -> csel -- honouring it shrank the compiler 40511 -> 38800 words (-4.2 %, spread over ~every fn that compiles a pure `if`), k8h 40 -> 38, k5 278 -> 268, k7 317 -> 309. bcond 1145 -> 1150, cbz 126 -> 127 (allow rows written). STILL OPEN: the row's own gate is a TIME, not a size -- K8H <= 1.2x on honest.sh, last measured 1.9x, unmeasured since the shrink; and the remaining gap profile (data-dependent `if` already a csel; per-iteration mul/add/shift/and vs the Rust twin) is not started. Was: K8H follow-up: lift the hoist limitation for loops with a nested `while` (release/re-take the loop cs temps around the inner loop), then profile the remaining K8H gap (data-dependent `if` already a csel; per-iteration mul/add/shift/and vs the Rust twin) and close it or record why it cannot close | docs/blueprints/A2-csel-and-const-hoist.md §3 "Commit 2 as landed" | K8H <= 1.2x Rust (honest.sh row) | A2 |
| A3 | **REFUTED 2026-09-08 by its own coverage census, with the implementation finished and GREEN.** The transform works: fixpoint 4c9e0a4e, battery GREEN, 84 constructs, bpref parity across six shapes / two directions / four comparison operators / steps 1-3 / symbolic bounds, trip counts 0..19 and ~1000. Its gates would also have PASSED -- predicted k1h ~0.31x against 0.5x and k4 ~0.28-0.33x against 0.6x. It is refuted anyway, because passing them would have asserted something false. MEASURED coverage (tools/lin_census.py, re-run independently by the main session): **bebop.bp 144 while loops -> 0 affine -> 0 folded**; selfhost/std + prelude 1476 loops -> 5 affine -> 1 folded, and that one fold is `whileb.bp main#6`, a two-iteration test loop; the 12 MACHINE-GENERATED gb_* kernels 1093 loops -> 0; csr/sgraph/gb_run/gb_compile1/ntt/attn/snn/tq 141 -> 0; the honest kernels 12 -> 6. Outside k1/k1h/k3/k3h/k4/k5 the only things in the tree that fold are c07_while, c73_hoistnest's inner loop and bitset.bp:pow2. The decisive test is that widening cannot help: **raising the accumulator cap from 3 to 64 recovers exactly ZERO additional loops.** Of 338 non-affine RHS nodes, 150 are array stores, 99 calls, 47 array reads, 42 `if`s. Affine folding needs the body to be a function of the loop index ALONE, and the moment a loop reads memory its future is data-dependent -- so the class is empty here by construction, not by detector narrowness. B7 is answered directly: it routes every query through gen_gb, the generator whose 1093 loops fold none. B2's finding agrees -- a CSR build spending 96 % of its time in the store's page path is exactly a loop A3 can never see. Cost refused: **+2858 words, +7.27 % of the self-compiler, permanently**, for a feature that fires on zero of its own 144 loops (the three A2b peepholes recovered only 10 of those words -- the code is branch dispatch, not arithmetic; two thirds is the detector and parser). Precedent: A12 was dropped for a measured 0-2 % against a >= 5 % gate; this is stronger evidence, being an empty applicable set rather than a small win. KEPT: `tools/lin_census.py` (446 lines, zero words in the binary) -- the instrument that made this measurable, and the general way to ask "is this class empty?" BEFORE the next optimisation blueprint is written rather than after it is built. PRESERVED, not deleted: the full working implementation is `docs/blueprints/attic/A3-lin-folding-on-1ed3719.patch` (532 lines, two purely additive hunks, applies to 1ed3719). If it is ever landed, apply the accumulator-last reorder first -- one line in lin_emit_fold, 9 -> 8 words per trip and the loop-carried chain collapses to a single MADD. Two of A3's own acceptance lines did not survive contact and are corrected here: `WORD_DELTA 0 on c07_while` is impossible (c07_while IS the foldable shape, 150 -> 156), and k1h/k3h/k4_loopwords cannot move because perf.py's loop_words takes min(loops) and the tail loop becomes the smallest. Was: LIN tag: folding of linear recurrences over k = 2/4 iterations on tags (exact in wraparound i64) | docs/blueprints/A3-lin-recurrence-folding.md | k1h_ms <= 0.5x Rust, k4_ms <= 0.6x Rust, bpref parity on std_tests | A1 |
| A4 | DIVERGE round 2 LANDED 2026-09-07 (bebop.bin 05aa91ac: RC4 heap-trap scratch clobber, RC5 lazy-SYM snapshot; 32 repros: 24 match, 8 fail-closed rc=89 -> A14, 0 diverge) -- DIVERGE round 1 LANDED 2026-09-07 (bebop.bin aa807ab9: RC1 compound-assign snapshot, RC3 array-literal flags clobber fixed; RC2 if-arm cs relocation fail-closed as exit 89; 31 repros: 7 match, 8 fail-closed, 16 STILL DIVERGE + new 148648 -> round 2) -- window RESTARTS after the register-model DIVERGE fix (ALERT 2026-09-07 19:20: batches 145600-148100 on e9159318 = 11 DIVERGE + 376 COMPILEFAIL rc=89; bisect: pre-A1 bin 9e53878 matches bpref, every bin from a77adc6 diverges -- the register model was never fuzzed, fuzzd had been paused since session 18; fuzzd paused again, one Sonnet worker on the miscompiles, rc=89 class -> A14) -- window OPENED 2026-09-07 ~18:00 on e9159318 (operator: before A2b/A3; any later codegen promotion restarts the count on its own md5) -- operator 2026-09-07 (session 25): NO fuzz batches until every other ROADMAP row is done, this window runs LAST -- 24 h fuzz freeze window (process, no code): fuzzd on the promoted md5 until fuzz_seeds_on_bin >= 10^5 | docs/blueprints/A4-fuzz-freeze.md | 0 CRASH/DIVERGE, TRAP-82 = 0 | A1-A3 |
| A5 | **STEP 1b LANDED 2026-09-08 (bebop.bin `15e272fc`) -- the row is COMPLETE: every `[i64]` VALUE is an x17-relative CELL INDEX, and an address exists only inside the instruction that reads or writes.** `emit_zeros` and all four aggregate sites end with `sub x0,x0,x17 ; lsr x0,x0,#3`; `emit_array_get` becomes `add xd,xbase,<idx> ; ldr xd,[x17,xd,lsl #3]` with the **DESTINATION register as scratch** (an `add` reads both operands before writing d, so d may alias base or idx, and `vs_alloc` at a 3-entry window could have parked the array base itself), `emit_array_set` uses **x16**, the register model's parallel-move scratch, dead at every set site, and **index 0 keeps its single word** in both. 21 `emit_sys_*`/builtin sites convert each cell argument with one `add xa,x17,xa,lsl #3`; `sys_slurp`/`sys_readbuf` are deliberately NOT converted (their results are `str`, raw until A7); `sys_mmap` converts its result with `sub ; asr #3` -- ARITHMETIC, because a file map can land below the reserve and the inverse `add x17,..,lsl #3` is exact for either sign while a negative errno stays negative. **The entry stub became IDEMPOTENT** (`cbnz x17,+37` at word 5 into the existing failure landing pad), within the same 172 words, by folding `mov x5,x0`+`mov x9,x5` into `mov x9,x0` and reusing a dead `mov x10,x6` -- required because a `sys_run` child re-enters the stub with the caller's registers and under step 1a reserved a SECOND 4 GiB and mremap'd the arena, moving x17 out from under the caller's indices; `cbnz` and not a compare against the arena end, because `sys_run` is always issued from a `sys_clone` child whose x28 is `sp+12 MiB`. `emit_sys_run` also converts the argv vector's cells in place. `tools/bpref.py` grew one `Interp.arena` cell list with `class Cells` as an offset into it, so `a + k` is real cell stepping and the `arena_cells` capacity check and its `SystemExit(80)` are untouched; `tools/typecheck.py` types `arr +/- i64` as `[i64]` (the blueprint's legal class-(a) stepping) and keeps every other array arithmetic a finding. 14 corpus files had `sys_arena_base() + <bytes>` rewritten to cells. Gates, re-derived in the main tree: **fixpoint gen2 == gen3 == gen4 == 15e272fc**, battery GREEN (std_golden 114/0, constructs **85**/0 including `c69_index_roundtrip` EXPECT=3969009064380 and `c92_ptrfree` EXPECT=777920, diag 16/0, parity 13/0, pool 5/0, oracles mismatch=0 missing=0, ABI ok with NO check_abi.py change, invariants GREEN, words PASS with 61 new literals), census 41217 **1167** 123 0, bin_words 39907 -> 41217 (+3.28 %, 22 word_budget lines), **K5 +3.52 %** against <= +8 % (767 -> 794 ms, 7 interleaved reps), **K6 nn 1.067x** against <= 1.2x (4.5 -> 4.8 ms/query, nnidx flat, all folds identical), RSS +2.06 %. **The bcond +3 is a choice, not a leak**: one `if idxc > 0` per index-0 fast path in `vs_array_get_imm`/`vs_array_set_imm`/`emit_field_access`; dropping the fast path returns bcond to 1164 at the price of one extra `add` on every `a[0]` in every emitted binary. The blueprint's §3 frame-heap carve, its `saved_x14`/`saved_rel` correction and the rewritten `emit_heap_trap` were all made OBSOLETE by A6 and were correctly never written -- that deletion removed the blueprint's own "single most likely RED". Was: **STEP 1a LANDED 2026-09-08 (bebop.bin 3a827a3f) -- the address space only; step 1b, the index model, is NOT started and is the open half of this row.** What landed: the entry stub reserves 4 GiB `PROT_NONE` and `mremap`s the seed's own arena onto its base (so the shipped mapping is the SAME `MAP_PRIVATE|MAP_ANONYMOUS` VMA object seed.S:55 created, not a new one -- the preservation K9 asks for), `x17` = arena base, and `emit_sys_rename` stops using x17 as scratch (retargeted onto x11, dead from the copy loop's terminator on, inside the bank check_abi.py already allowlists) -- because sys_rename is on the compiler's own atomic-publish path and would otherwise destroy the base on every compile. Gates: fixpoint gen3 == gen4 3a827a3f, battery GREEN, invariants GREEN, construct parity 83/0, std_golden 112/0, census 39907 **1164 123 0** (bcond/cbz/tbz UNCHANGED -- the step adds no branches), **K5 +2.10 %** against a <= +8 % gate (7 interleaved reps, medians 564.6 -> 576.4 ms; against the committed 623 ms row it is 7.5 % FASTER), **K6 nn.bp -3.41 %** and nnidx +0.00 % with folds identical, **RSS +0.07 %**, and the row's own **K9 warm-scatter 0.97x** the in-run `MAP_PRIVATE|ANON` control. The stub grew 131 -> 172 words, which is +41 on EVERY binary this compiler emits: 70 word_budget lines, all re-derived here, all exactly +41. **The gate constructs `c69_index_roundtrip` / `c92_ptrfree` are UNMET and structurally so** -- `zeros` still returns an address, `emit_array_get/set`, the `emit_sys_*` conversions, the frame-heap carve and `tools/bpref.py` are untouched, `x17` is written and read by nothing, and neither construct exists. **What the step cost, and what it caught:** the stub-append recipe turned out to be duplicated in FOUR places the patch never touched (`gb_compile1.bp` x3, including `gb_pack_stub`'s loop bound, and `gb_run.bp:415`), still doing `zeros(131)`/`stub[42]`/`n0 + 131` against a 172-word stub -- a 41-cell OOB store past the array plus a truncated, mis-patched stub in every in-process-compiled kernel, which is why `gb_pool` failed with 212 x trap 82. The long-`BEBOP_TMP` artifact of std_golden.sh:26 was REFUTED as the cause by its own signature (it yields `got=` empty; this was `got=0`) and by a base control that passed 111/111; the fault was then bisected to the stub LENGTH alone, with variants proving the reserve and the mremap both innocent. The length is now a literal in FIVE places -- a follow-up should derive it from `entry_stub`'s own return value. Was: **UNBLOCKED 2026-09-08 by deleting ONE SENTENCE: `arena image = file` does NOT survive; everything else in the row does, and is measured free.** A 2x2 factorial over one 512 MiB mapping and a 128 MiB working region, same LCG store stream in every arm, fold 29203473000 identical in all six, medians of 3 interleaved with the arm order reversed in rep 2 -- warm-scatter ms: MAP_PRIVATE |ANON (today's arena, seed.S:55) **79**; MAP_SHARED|ANON **82** (1.04x); MAP_PRIVATE file-backed **81** (1.03x); **MAP_SHARED file-backed 41,431**; PROT_NONE reserve + MAP_FIXED PRIVATE|ANON **81** (1.03x); reserve + MAP_FIXED SHARED file **29,068**. So **neither `shared` nor `file-backed` costs anything alone -- the tax is their CONJUNCTION**, which is the variable R1 could not separate. First touch is now refuted by direct measurement rather than arithmetic: the penalty is LARGER warm (41,431 ms, 10.4 us/store) than cold (23,584), i.e. 524x the 19.8 ns/store control. It is also superlinear -- 1.4x at 16 MiB, 8-11x at 32-64 MiB, 315x at 128 MiB -- which is new evidence against a per-store constant and leans toward the reclaim hypothesis, though the fix is the same either way. **The reserve itself is free** and the 4 GiB PROT_NONE reserve SUCCEEDS on this box (`/proc/self/maps` shows one `---p` row of exactly 4294967296 bytes), so the blueprint's 1 GiB fallback risk row closes GREEN. Design: the committed arena window is `MAP_PRIVATE|MAP_ANONYMOUS|MAP_FIXED` inside the reserve -- which is what `seed.S:55` already does, so this is a PRESERVATION, not a change -- while store file maps stay MAP_SHARED inside the reserve. No syscall is added to any commit path, so the D1 mistake is not repeated in the other direction. **NEW GATE K9** (A5's own gates are read-shaped and streaming and register NONE of this, which is how a 500x regression could have shipped): warm-scatter ms of the shipped arena mapping <= **1.5x an in-run MAP_PRIVATE|ANON control**, pinned at lg=24 -- a RATIO because the failing arm swings 30-108 s run to run, and lg=24 because below it the effect is intermittent and a green would be luck. Today 79 -> ceiling 119; the recommendation passes at 81 (1.03x); A5-as-written fails at 41,431. Probe: `bench/vs_rust/std_tests/arena_map_modes.bp`. -- (constructs renamed 2026-09-08: `c70_ptrfree` collided with the existing `c70_csel` -> `c92_ptrfree`; `c69_index_roundtrip` kept) pointer-free step 1: x17 arena-relative addressing over one PROT_NONE reserve (`zeros` returns an index; `ldr xd,[x17,xidx,lsl #3]`; file maps placed inside the reserve so store bases are indices too; the frame heap is carved from the reserve here so aggregate indices are never negative; arena image = file) | docs/blueprints/A5-arena-relative-addressing.md | chain GREEN; c69_index_roundtrip, c70_ptrfree; K5 <= +8 %, K6 ns/row <= +20 % | A1 (was A3, corrected 2026-09-08 when A3 was refuted: arena-relative addressing has no mechanical dependency on recurrence folding -- the edge was ORDERING, not a real dependency, and it was checked rather than inherited) |
| A6 | **STEP 1 IS NOW COMPLETE: the open defect is CLOSED 2026-09-09 (bebop.bin `a5858877`), and it was TWO faults, not the one this row described.** (i) `emit_enum_ctor` / `emit_enum_ctor_nullary` stored the tag and payload to the dead frame-heap `x14` while RETURNING the x27 block. (ii) **`match <var>` emitted NO CODE AT ALL** -- `emit_match` compared each arm's name against the scrutinee's TEXT, so a variable never matched any arm and the match evaluated to whatever was left in the result register. The row's "the runtime-match path is the other half" understated it: that path did not exist and had to be built. **RED -> GREEN re-derived on the COMMITTED base, not inherited:** the repro `enum opt{none,some} fn main()->i64{let v=some(7); match v {none=>0,some(x)=>x+1}}` gives **8209** on 15e272fc and **8** on a5858877. The row's old 494710833280 was measured on 5df5037 and is stale -- A5 step 1b already moved the symptom, because the ctor returns a cell INDEX now and 8209 is that index left in x0 by the preceding `let`. `emit_enum_ctor` now evaluates the payload FIRST and only then reserves the block (a NESTED ctor bumps x27 out from under an earlier reservation -- `c60_nestctor` is exactly that program), snapshotting the base into the parallel-move scratch with `mov x16,x27`: +1 word per payload ctor, the only word-budget line owed. `emit_match` splits into `emit_match_lit` (the old body, byte-identical) plus `emit_match_var` / `emit_match_rt` / `emit_match_rt_arm` / `emit_match_rt_bind` and the helpers `vs_restore` / `patch_b`, reading tag and payload through the A5 step-1b INDEX form (`ldr xtg,[x17,xsc,lsl #3]`), never an address; `sc`/`tg`/`d` are pinned in the free mask and are not window entries, so no arm can park a pre-existing value inside its own conditionally-executed code (the `fntab[4591]` arm-spanning fault), and arm exit branches are CHAINED so no fixup list is needed. `tools/bpref.py` gained a `('match', cname, arms)` node for the no-payload variable case. New construct **`c96_enumpay`** EXPECT=8503009 (three arms: the roadmap repro as `ra`, a second payload tag, the nullary tag, so the tag COMPARE is covered and not just the payload load). Fixpoint gen2 == gen3 == gen4 == a5858877 reproduced INDEPENDENTLY by the main session; battery GREEN post-promotion (std_golden 114/0, constructs 86/0, diag 16/0, parity 13/0, pool 5/0, oracles mismatch=0 missing=0, ABI ok, invariants GREEN, words PASS with all SEVEN new literals as+objdump-derived -- the lane reported six and missed the `b .+0` placeholder `patch_b` back-patches); census 42078 **1189** 123 0, bcond +22 over 30 genuinely new impure `if` arms (the 20 arms of `emit_match_lit` are the old body and were already inside the 1167 baseline), cbz/tbz UNCHANGED so no new loop; bin_words 41217 -> 42078 (+2.09 %) for a consumption path that did not exist before. KNOWN AND DELIBERATELY UNTOUCHED: `self_check()`'s c29/c30/c31/c32 goldens are now stale; it has no caller and bench/SELFHOST.md:215 records that no gate invokes it, so nothing is RED -- re-freeze them if it is ever wired up. **STEP 2 IS PARTLY LANDED -- this row's "the frame is still the flat 16 KiB" sentence went stale hours after it was written (re-verified against git 2026-09-12):** `7704210` (2026-09-09 10:41, step 2A -- computed frames; `c67_deeprec`'s leaf frame 16384 B -> 96 B) and `fe06c54` (2026-09-09 12:09, step 2B -- `emit_bl` saves x15 alone, and the parent's branch over the child had to shorten with it). Both predate this file's own last edit (`ae1cfb4`, 16:25 the same day) and were never folded in. STILL OWED on step 2: the fn-mark LIFO release of literal-only fns (no commit found), and step 2's gate re-run on the promoted binary (c67_deeprec at 10^5 recursion, fuzz TRAP-81 = 0, TRAP-82 = 0, the RSS row) -- no post-promotion battery output for it exists in the journal. Step 1's earlier half, for the record: array/struct/enum-ctor literals bump the ARENA CURSOR x27 and inherit `zeros`'s trap, `emit_heap_trap` and the T118 brk #81 recipe are DELETED so `brk #0x51` = 0 and exit 81 is gone as a CLASS, A14b's two parked seeds run (100671 -> 6, 100828 -> 4), and `neg/c38_frameheap` moved to the positive set. Was: (2026-09-08: inherits A14b's trap-81 pair as its acceptance -- seeds 100671 -> 6 and 100828 -> 4 must run, and `brk #0x51` count 0; the litrelease construct is `c93_litrelease`, c69/c70 being taken) pointer-free step 2: delete x14 (aggregates already index the reserve after A5) + fn-mark LIFO release of literal-only fns + computed frames (B4): T118 / exit 81 deleted, frame = 80 + 8*marks + 8 + 8*slots, emit_bl saves x15 only | docs/blueprints/A6-aggregates-in-arena-and-frames.md | step 1 MET: brk #0x51 = 0, seeds 100671/100828 run, fixpoint + battery GREEN. Step 2 gate unchanged: c67_deeprec (10^5 recursion), fuzz TRAP-81 = 0, TRAP-82 = 0, RSS row | A5 |
| A7 | **THIS ROW BROKE SELF-HOSTING AND THE ROADMAP CLAIMED IT LANDED. Measured 2026-09-12: step 2's `str`-as-handle migration (`7c03111`) landed the CONSUMERS and never landed the PRODUCER.** `emit_str` mints `((128 + boff) << 32) | blen` where 128 is `LIT0`, "the byte-region start the stub copies literals into" -- and `LIT0` occurs in exactly two places in the whole tree, that comment and that formula: **nothing copies literal bytes into the arena**. So `char` (`lsr x4,x0,#32; add x4,x4,x1; ldrb w0,[x17,x4]`) reads zeros. `fn main(){char("A",0)}` prints 65 built by the committed `bebop.bin` and **0** built by gen2, whose emitter is this source; same for `char(argv[2],0)`. `str_len` hides it by extracting only the low 32 bits and never dereferencing. Consequence: **every generation built from the current source cannot read a single string byte**, gen3 prints 64 from its own argc guard, and `chain` reports NO FIXPOINT -- the invariant of TG-DONE row 2. Two more defects in the same change: `scan_one_lit` writes offsets to `fntab[7000+i]` and lengths to `fntab[7016+i]`, SIXTEEN apart, while `bebop.bp` holds ~100 literals (literal 16 overwrites literal 0's length), so the handle form cannot work for the compiler's own source even with the missing copy added; and `emit_crc32b` puts the base in x1 while its loop still reads `ldrb w2,[x0],#1`. DECISION 2026-09-12: revert the migration to restore the invariant, then re-land step 2 WHOLE (producer copy + a literal table that does not collide + every consumer) as its own single-variable step. The other half of the row, also found 2026-09-12: `emit_sys_mapb` hands `openat` a pointer to a word holding an ADDRESS instead of NUL-terminated path bytes.** It has no copy loop at all -- 7 words between `vs_deliver` and `movz x8,#56; svc` -- while `emit_sys_open` and `emit_sys_rename` both pack a byte-per-cell path into the 8 KiB IO scratch zone below x28 and NUL-terminate it first; mapb's own comment says its path is a str handle `(off<<32|len)`, the byte-arena form, not the form `sys_open` packs. Evidence: it created 9 files in the repo root on 2026-09-11 17:32, each named by exactly 5 bytes with a top byte in 0x71-0x7c -- a live 40-bit user-space address in little-endian, its zero upper bytes acting as the NUL -- sized 64 KiB / 4 MiB x5 / 8 MiB / 64 MiB x2 / 96 MiB, i.e. the map lengths they were created for, ~260 MB committed as binary (removed 2026-09-12; they were in no other commit, so the amend of `4286ec1` takes them out of history). OWED: the scratch-pack-and-NUL path in mapb, plus a NEGATIVE construct that fails if a mapb path ever lands outside the scratch zone, and the 100 MB raw-ingest row itself is still not built (the journal's own A7 line says "raw/sys_mapb row NOT built"). Was: pointer-free step 3: byte arena + `str` as a value `(off<<32 | len)` + raw-byte parser + crc32x per page (= raw ingest) | docs/blueprints/A7-byte-arena-and-str-values.md | c68_strval; 100 MB ingest twin: raw <= 1.5x best Rust, maxrss <= 1.5x file size | A6 |
| A8 | typed tables: T48 checked types in bebop.bp (`[i64]`, `[u32]`, `str`, `ref T`) + u32 cells for CSR/store | docs/blueprints/A8-typed-tables-u32.md | G7 file size <= 1.2x sqlite; K6 ns/row -2x; typecheck oracle == bpref | A7 |
| A9 | `scan` LANDED 2026-09-08 (bebop.bin 26f90c4c, session-26 Lane D merged in session 27): `scan(s, pos, class) -> new pos` as a 46-word scalar builtin (emit_sys_scan), classes 0 = whitespace, 1 = ident, 2 = not-quote-not-backslash, anything else = not-newline; the scan STOPS at the caller's bound pos[1], so no byte past it is ever read and the blueprint's over-read slack is not needed for the scalar form; `scan` is now a reserved word (compile_fn_at rsv3, bpref RESERVED) and selfhost/std/rewrite.bp + bench/tq_sqlite/nn*.bp renamed their local `scan` fns; gate c78_scan EXPECT=-6715473280576199194 and a bpref stub that mirrors bound/class/write-back exactly. cmp_mask/sum64/umulh still OPEN and still wait for A8. Was: NEON/hardware builtins: `scan` (parser loops), `cmp_mask`/`sum64`/`fill` (scans, CSR build), `umulh` (Q32 plus-times) | docs/blueprints/A9-neon-builtins.md | K5 -10 % (scan); K6 ns/row <= 4 with a Rust scan twin; each builtin a construct + bpref stub | A1 (scan any time), A8 (cmp_mask on u32) |
| A10 | **REFUTED 2026-09-08 on a floor that no longer exists -- RE-EXAMINE.** The refutation was correct when taken: the fixed part was 1,899 ms of a 2,423 ms self-compile and no memo could touch it. Later the same day a `str_len` hoist removed most of that floor and the self-compile fell to **623 ms** (4.02x from the day's start, measured), and the scan-fusion worker's own probe showed the remaining time is **per-fn work, not a fixed scanning floor**. A10's gate is <= 300 ms; the row is now 2.1x away rather than 6.5x, and a per-fn memo attacks exactly what is left. **Do not revive it without re-measuring the phase split on the current binary** -- the 21.6 %/78.4 % ratio that killed it was measured on a compiler that no longer exists. Original refutation, kept because its method is the point: **REFUTED on its gate, before a memo was written -- and REPLACED by what the measurement found instead.** The row's gate is a one-function-edit self-compile <= 0.3 s. A phase walk (clock_ms markers, median of 5, in a slot) shows the two per-fn passes A10 would memoise are only **524 ms of 2,423 = 21.6 %**; the 1,899 ms that survives a PERFECT memo is six whole-source character scans. Per-fn emission time is median 1 ms, p99 22, **max 41** (`emit_body`), so `fixed floor + max fn = 1,940 ms` against a 300 ms gate -- 6.5x over, and picking a cheaper function cannot help because the floor is 98 % of the sum. Measured end to end rather than summed: a compiler with both per-fn passes skipped for all but one function -- by construction a perfect memo -- ran **2,020 ms against a base of 2,499**, so the whole memo is worth 479 ms. REPLACEMENT, landed the same day: **two of those six scans are exact duplicates** (`collect_ctors` runs twice and the second produces a bit-identical table; `collect_fns` likewise, the second only to find main's index). Deleting both is 4 hunks and net -8 lines, and it makes the compiler SMALLER: fixpoint `7ef614f1` reached at gen2 (gen2 == gen3 == gen4), battery GREEN, census 39340 -> 39265 words with bcond 1163 -> 1155 and cbz 127 -> 124, so no allow line is needed. A rejected first version is worth recording: it parked main's index at `offs[515]` and widened four `zeros(512)` sites, PASSED both self-compile oracles, and would still have corrupted the arena -- `gb_run.bp:383` and `gb_compile1.bp:83/133` pass their OWN `zeros(512)` to `emit_words_offs`, a path self-compilation never exercises. The shipped form packs the index into `offs[0]`, which is written and never read anywhere in the tree. **The roadmap's order was backwards:** to reach 0.3 s the fixed part must fall under ~260 ms, i.e. the four remaining whole-source scans (1,213 ms) fused into ONE pass -- a scan-fusion row, not a memo row. Order is dedup, then fusion, then a memo only if still needed. Second-order: those scan loops cost ~1 us per source byte, ~25x what a plain array loop costs per iteration in the same compiler, so they are individually optimisable and the fusion estimate is conservative. Was: per-fn memo (Salsa-like): fn words memoised by hash(text, facts), `bl` re-link from the layout table | docs/blueprints/A10-per-fn-memo.md | one-fn-edit self-compile <= 0.3 s, fixpoint md5 unchanged | A1 |
| A11 | (b)-standalone LANDED 2026-09-07 cc0144e: sys_run(addr,size,argc,argv) + sys_wait4 + c84_run (unblocks B3 step 4); rest open: harness dogfooding: (a) spawn census + perf rows chain_spawns (codeless), (b) bebop-native std-runner lane (execve/wait4/pipe2/dup3/kill/run builtins) with std_golden.sh as oracle for 3 chains, (c) chain.bp after a golden-runner decision | docs/blueprints/A11-harness-dogfooding.md | (a) decision gate 20 % of battery wall; (b) byte-identical lane summaries x3 | A1; (b) after A7 (str values) |
| A12 | REFUTED 2026-09-08 by its own measure-first probe (Lane B, session 26): the hand-approximation of rung 1 on the two hottest scanner loops (`pos[0]` hoisted into a local, `slen`/`is_alpha` inlined in `skip_ws`+`read_ident`) buys **0-2 % of K5, not the >= 5 % the blueprint's gate required** -- median-of-3 gave +5.08 % but two independent replications and every pooled estimator (n=9 interleaved base/opt, all 18 outputs value-identical) put it at zero, the within-arm user spread (~10 %) swamping the effect; per the blueprint rung 2 alone is not worth it either, so A12 LEAVES the critical path and A2b/A3/A10 resume their order. Was: flat per-fn index IR (rows {op,a,b,aux}, blocks as ranges; passes fold/lin/cse/sroa/inline/tre/dce) then rung 2: graph register allocation (Hack/Goos) replacing the window | docs/blueprints/A12-flat-ir-and-graph-ra.md | rung 1: K5 -15 % (measured by hand first), K2H ~1.0x; rung 2: K5 -3 % more, bin_words -2 % | A5-A8 (alias facts from typed tables) |
| A13 | LANDED 2026-09-08 (bebop.bin f9275e8a, with A15 in one commit): parse_params now captures the first parameter's position and calls diag_exit(s, first_param_pos, 100) `fn with more than 14 parameters`; the two silent `brk #8` clamp sites in emit_call and compile_fn are DELETED (nparams is no longer clamped to 14, so the bind and move-in loops run over the real count and the diag fires before either); the > 512-fn exit reuses its own code 104 `too many fns (cap 512)` instead of 89's text; TRAPS.md row 8 retired; gates neg/c85_param15 EXPECT=COMPILEFAIL:100. Was: compile-time diagnostic for > 14 parameters: today bebop.bp:4759/4936 clamp nparams to 14 and emit a silent `brk #8` prologue (docs/TRAPS.md row 8, found 2026-09-07); replace with diag_exit(<new code>, "fn <name>: > 14 parameters") at parse time, construct probe c85_param15 (EXPECT compile exit code, held out until the chain lands), TRAPS.md row 8 retired; same commit: the > 512-fn exit 89 must say `too many fns (cap 512)` instead of the reused `let binding while a call temp is live` text | (inline; probes in the session-24 scratchpad argcap/p14-16.bp) | c85 gate; gen3 == gen4 fixpoint; census unchanged apart from the diag words | after A4 (codegen) |
| A14 | LANDED 2026-09-07 (bebop.bin 7d8262a1: emit_cond parks every pre-`if` value to temp slots when an arm can bind -- fntab[4592] + vs_span_to_slots; slot bound 64 -> 256, x14 sp+0x300 -> sp+0x900, hoist_scan bails on match/call bodies; c91_letlive EXPECT=12, c87 back to EXPECT=3; corpus sweep 327/330 match bpref, leftovers -> A14b) -- was: lift the register model's `let ... in` restriction: REGISTER-MODEL-BLUEPRINT §1.1 exits 89 (`let binding while a call temp is live`) when a `let` inside an expression needs a register a parked cs temp owns -- the pre-A1 compiler accepted these programs, and the fuzz generator produces them in ~25 % of seeds (fuzzd batches 145600-148100 on e9159318: COMPILEFAIL 131/132/113 of 500, all rc=89). Until lifted, bench/fuzz classifies them as UNSUPPORTED-89 (pass, counted, never an ALERT; blueprint §1.1's "bpref predicts it" was never done). Finding 2026-09-07 (session 25): every corpus exit 89 (742 COMPILEFAIL repros) is check_reg_collision's arm_spanning exit -- a `let` inside an if colliding with a cs temp parked before the if -- i.e. DIVERGE RC2's mechanism, not a general let-while-call-temp case. Fix (default) = park pre-if register-resident cs temps to temp slots at emit_cond_branch entry (both arms + join read the slot; arm_spanning becomes an assert; RC2's fail-closed exit and neg/c87 go away); probe c89_letlive from the shrunk repro, c87 back to EXPECT 3 | (inline; repros in ~/.cache/bebop/fuzzd/repros/COMPILEFAIL-14*.bp) | corpus sweep: all 742 COMPILEFAIL repros compile and match bpref (rc=89 = 0; no fuzz batch until the roadmap is done); c89 + c87 gates; fixpoint; K gates unchanged | after the DIVERGE fix (codegen) |
| A14b | part 2 LANDED 2026-09-08 (bebop.bin 88d3f50c): vs_span_to_slots also demotes kind-3 `SYM` entries -- p0 < 100 reuses vs_park_to_slot's `str`, p0 >= 100 takes vs_alloc + vs_mat_sym's spilled `ldr` + vs_park_to_slot + vs_mask_free -- so no new instruction word. The exit-89 class is now EMPTY over the corpus (sweep 328 match, 0 compile89; the only 2 leftovers are the trap-81 pair 100671/100828, which A6 owns). Seed 100744 runs 10 = bpref; new positive guard c95_symspan EXPECT=17; c92_letlive2 re-derived to COMPILEFAIL:97 (the shrunk program has no tail expression). +93 words (40418 -> 40511), bcond 1143 -> 1145 (the card wrongly predicted no allow line: the two new impure `if`s call functions, so they are real b.cond in the compiler itself). -- part 1 DONE (shrink, journal 1788820570) and part 2 SPEC'd 2026-09-08 -- root cause: vs_settle_sym_alias materialises a PRE-ARM pending kind-3 `SYM` window entry into a register on one if-arm only, that entry then parks to a cs register inside the arm, and the arm's next `let` gets the same register -- check_reg_collision's arm_spanning exit at bebop.bp:1855 (NOT the bit==1/idx<0 assert at :1861 that part 1 named). A14 covered kinds 2/4/6/7 at the pre-`if` park and left kind 3 alone. Fix (spec'd, scratch-verified, NOT yet landed): vs_span_to_slots also demotes kind-3 SYM entries, no new instruction word; new repro c95_symspan (bpref 17), c92_letlive2's EXPECT becomes COMPILEFAIL:97. Executable card: docs/blueprints/A14b-corpus-leftovers.md. Was: the 3 leftovers of A14's 330-repro corpus sweep: (i) seed 100744 still exits 89 at a NON-arm-spanning site (bebop.bp:5:2049 diag, a `let` colliding with a call temp outside an if) -- shrink it and lift or fail-closed-document that case; (ii) seeds 100671 + 100828 compile but trap 81 (frame heap) because A14's wider x15 slot region takes 1536 bytes out of the 16 KiB frame (verified 2026-09-08: x14 0x300 -> 0x900 = 768 -> 2304, slot region 512 -> 2048, frame heap 15616 -> 14080; the earlier 1280 was wrong) -- either a per-fn x14 chosen from the facts pass (two verified encodings, 0x300 for <= 64 slots and 0x900 above) or wait for A6, which deletes exit 81 entirely | (inline; repros in ~/.cache/bebop/fuzzd/repros/UNSUPPORTED-89-1007*.bp) | corpus sweep 330/330 match bpref, rc=89 = 0, TRAP-81 = 0; fixpoint; K gates unchanged | after A14 (codegen) |
| A15 | LANDED 2026-09-08 (bebop.bin f9275e8a, with A13 in one commit): five `sym_lookup < 0` guards call diag_exit(s, npos, 101) `unbound symbol` -- emit_var_or_ctor, emit_let_chain, emit_array_index, emit_let_stmt's chain head (guarded by is_chain) and emit_compound_stmt. The diagnostic exposed a real parser bug it was NOT allowed to keep: emit_let_stmt accepted `in` as a body-let terminator WITHOUT consuming it, so emit_body classified `in <body>` as a fresh expression item and read `in` as an identifier -- pre-A15 that emitted a dead variable with reg == -1 that the item pop discarded, post-A15 it is a hard error (c14_string, `let x = 5 in x`). Fix: emit_let_stmt consumes its own `in` (pos += 2 when inV), one dead value per body-let goes away. Gates neg/c93_unbound EXPECT=COMPILEFAIL:101; construct parity 73/73; battery GREEN. Was: compile-time diagnostic for an unbound symbol: `let _ = v0 = 0 in 0` with v0 never declared compiles on every bebop.bin (pre-A1 prints 0, 05aa91ac prints nothing / traps `brk #0` in emit_let_chain) and bpref answers 0 -- all three must reject it: diag_exit(<free code>, "unbound symbol <name>") at the sym lookup in emit_let_chain / emit_compound_stmt / emit_ident, bpref raises the same, gen.py never emits an undeclared write (found by the round-2 shrinker, repro session-24 scratchpad div3/sh/146987.bp) | (inline) | neg/c93_unbound gate COMPILEFAIL:<code>; fixpoint | with A13 (codegen, diag words only) |
| A16 | **CLOSURES, GENERICS AND HIGHER-ORDER FUNCTIONS become REQUIRED language features (operator, 2026-09-09, binding).** **RE-PLANNED 2026-09-12 (agent-first, docs/RESEARCH-LANGUAGE-ERGONOMICS-2026-09-12.md §3.8): NOTHING of A16 is in the tree -- `bebop.bp` has 0 matches for annotation/generic/closure syntax, and `docs/LANGUAGE.md:135-138`'s "parsed and erased as of A16 Phase 1" is NOT-IN-TREE: `tools/f8_dt.py` passes because `compile_fn_at` discards everything between `)` and `{` and `collect_fns` never sees a top-level `theorem` line (parser laxness, the same class as B7's no-op). Order of delivery: step 1 fn values `&f` + `call_fn` (T50, 0 `blr` sites today, 5-6 words per site, no allocation; gate c135/c136/neg c137); step 2 closures = fn value + EXPLICIT `env: [i64]` (the shape 22 fns and B6 §4 already use; packed `(fn << 40) \| env` sugar; c138); step 3 generics as ERASURE (`[T]` is `[i64]` for every T until cell widths differ; 0 words; c139 + typecheck_neg); step 4 monomorphisation textual (use_expand shape, 8-12 fns) ONLY after A8 lands `[u32]` -- costed, not scheduled. Capture inference (`\|x\| x + a`) costed at ~2 weeks and NOT scheduled: the one human-legibility trade, named. Defunctionalisation (`apply_T` chains) dropped: it needs a whole-program pass and a `blr` costs the same words. Needs A21 (multi-value returns through `call_fn`) and A17 (the range-trap text).** `docs/LANGUAGE.md:132` currently lists closures and generics under "What is NOT in the language"; that line is now a defect, not a specification. **AMENDED THE SAME DAY, and it changes the conclusion:** the operator additionally requires **DEPENDENT TYPES OVER AN INFINITE UNIVERSE HIERARCHY WITH HIGHER-ORDER ABSTRACTION**. That is full CIC, so the "weakest sufficient logic" question is CLOSED BY FIAT rather than by analysis and F7's kernel is CIC-class. Defunctionalisation and monomorphisation SURVIVE but their claim is re-scoped: they no longer shrink the logic -- the operator wants to WRITE dependent types, so the kernel carries higher-order abstraction natively -- they keep the COMPILED ARTIFACT first-order and monomorphic, which is what the translation-validation layer reasons about. **ONE LANGUAGE, NOT TWO -- operator, binding, and the correction matters.** An earlier framing here said "Surface Bebop elaborates to Core Bebop"; that was wrong and is withdrawn. In Lean, Idris, Rust and Swift nobody WRITES the core -- it is a compiler phase with no separate syntax, documentation or users -- but the "core" named here is the language that ALREADY EXISTS, with its own `docs/LANGUAGE.md`, its own 86 constructs, its own gates and a self-hosting compiler written in it, so naming it a core would genuinely make two languages. **The correct form: ONE grammar, ONE `docs/LANGUAGE.md`, ONE set of constructs.** Dependent types, universes, higher-order abstraction, closures and generics are part of Bebop's grammar, not a layer above it, and ELABORATION IS A PHASE inside the compiler producing the internal representation `bebop.bp` already builds -- the AST, the packed fact word, the window -- plus a proof term. A separate PROGRAM is fine and stays (`tcheck.bp` is a program written in Bebop, like `gb_run.bp` or the witness); a separate LANGUAGE is not. **THE COST THE WRONG FRAMING WAS HIDING -- and the number is CORRECTED 2026-09-09 by verification, which reversed this clause:** with one language the elaborator lives inside `bebop.bp` rather than in its own binary. The first estimate here said 200-400 fns and "the cap binds IMMEDIATELY" at 491-691; a checked elaborator is **140-210**, so **291 + 140..210 = 431-501 against a usable cap of 511** (511, not 512). **THE CAP IS LIFTED AND THE ELABORATOR FITS -- fourth and final correction to this clause, 2026-09-09.** The fn zone is FLOATING (`3*cnt + ecnt + 258`, first fixed zone at 2200), so cap 560 cost a census BYTE-IDENTICAL to base -- zero words, zero branches -- and lifted the budget 293 -> **341**. The fntab relayout then took it to cap **768, budget 547 measured by padding** (+256 compiles, +257 refuses), against A16's need of 431..501: **the elaborator fits with 46 spare.** Both steps census-neutral. **And the relayout CORRECTS the plan of record**: the verification report concluded a `zeros(8192)` widening was mandatory, and it is not -- top index 6999 fits, so the relayout costs ~0 `bin_words`. **The raise exposed a defect no gate could catch:** `fntab[2200+i]` is b1_facts, per-fn planning facts indexed by FN INDEX and sized 512, so raising the cap without moving it writes fn indices 512-514 straight through `b1_scratch` -- a wrong `bl` TARGET, silently. Every construct is far under 512 fns, which is exactly why the cap was never exercised. A second miss of the same class: `offs` has FIVE `zeros(512)` sites including `cli_compile`, the ordinary compile path. **The fix is a GATE, not a layout** -- `check_abi.py` now reads the cap out of `bebop.bp` and refuses any per-fn zone narrower than it, negative-controlled (a copy claiming cap 900 yields 14 PERFN FAILs naming b1_facts and all five `offs` sites). Guards `c122_manyfns` (521 fns, old boundary) and `c123_capfns` (756, new). Was:  `bebop.bp`'s EFFECTIVE function budget is **293, not 511**, and it is pinned by a GATE rather than by the compiler: `gb_pool` concatenates a main-stripped `bebop.bp` with `gb_pool.bp` and its six `use` files, so the cap is hit by the CONCATENATION. Measured by padding until it breaks, not by reading a constant: +0/+1/+2 compile and **+3 gives `too many fns (cap 512)`**. It was found the honest way -- an eight-fn draft self-compiled fine and then failed `gb_pool`, which reads as a bounds-check false positive and is not one. So against a real budget of 293 a 140-210 fn elaborator does not fit AT ALL, not with margin; every earlier version of this clause assumed the compiler's own cap was the constraint. Unpinning `gb_pool` is therefore a PREREQUISITE of the elaborator and is scheduled ahead of it.  **And the cheap form is not available:** an "append a second window list above `fntab[5000]`" plan is WRONG, because `fntab[5000+i]` is the STRING-LITERAL OFFSET zone (`bebop.bp:5992`), one cell per literal, and the append would clobber it; at cnt=1023 the `3*cnt+1` zones need 3,070 cells and collide with `fntab[2712..2714]` and the window list at 2800. **A RELAYOUT IS MANDATORY** -- which is exactly what A1b's precedent actually is, and the ~0 `bin_words` conclusion survives by widening `zeros(8192)`. One further correction: `bebop.bp:2703` is the REGISTER-WINDOW cap, not the fn cap (the compiler says so at `:2166`); the fn cap is at `:4236` (exit 89) and `:6010` (exit 104), and exit 89 is shared by three conditions while the fn cap has two codes -- which is why A1b's gate passed with the wrong diagnostic text. FOUR TENSIONS, named before the work starts rather than discovered during it: (i) both transforms need a WHOLE-PROGRAM view, which collides with the one-pass thesis -- the 2026-09-09 amendment permits a second pass "where it buys impossibility of error", and expressiveness is not that, so these features must either justify a second pass on their own terms or find a one-pass form; (ii) monomorphisation MULTIPLIES functions against the 511-fn cap with `bebop.bp` already at 291, and against `bin_words` 42,330; (iii) a closure needs a code reference, which is exactly what A5 step 1b banished -- the candidate that preserves the pointer-free model is ONE i64 packing a function-table index (511 fits in nine bits) with an environment cell index, the environment being an ordinary arena object; (iv) recursive or escaping closures may not defunctionalise cleanly, and a HOF whose function argument is COMPUTED rather than statically known forces a dispatch table the logic must reason about. Costed by `docs/RESEARCH-PROOF-DESIGN-2026-09-09.md` (in flight) against 291 fns, the 511 cap and `bin_words` | docs/blueprints/A16-closures-generics-hof.md (supersedes A16-parsing-annotations.md) | the surface accepts all three with constructs; the core after defunctionalisation and monomorphisation is first-order and monomorphic, PROVEN by a census rather than asserted; fixpoint and battery GREEN; the word cost stated against 42,330 | A7, A8; and it changes F7's kernel cost, so the two must be decided together |
| A17 | **STEPS 1+4 LANDED 2026-09-13 (promoted `f636d4a3`), and the row paid for itself in branches as well as words.** Compile-time messages are i64 array literals inside `diag_exit` (`bebop.bp:92-102`, one cell per character, lengths hand-summed at `:103`): `strings bebop.bin` finds 0 of the 11 compile-time texts and all 4 runtime ones (incl. the RETIRED `trap 81`), and `grep` finds no message in source. Step 1 LANDED: all **15** compile-time texts (13 in `diag_exit`, `cap_exit`'s 83, `selfcheck_exit`'s 201) are now `str` literals via `diag_str(buf, at, m: str)`, the hand-summed `mlen` is GONE (each arm returns its end offset and they chain, `at1..at13`), `diag_text` is deleted, and repair sentences were added to 89, 97, 100 and 101. Verified independently of the lane that built it: **15 of 15 texts are byte-present in `bebop.bin`, was 0**. **The line shape is `<line>:<col>: error[E<code>]: <text>` -- WITHOUT the `<file>:` prefix, which is now step 1c, because the mechanism this row and its blueprint specified for it cannot work.** `fntab[5601]` is free, which is the half that was checked; the half that was not is that `diag_exit(s: str, p: i64, code: i64)` takes no `fntab` at all, so delivering one means threading a parameter through **35 call sites**. The cheap alternative (park the path in the arena, read it back with `sys_arena_base()`, no parameter) was examined and REJECTED for now on two measured facts: `sys_arena_base()` occurs in `bebop.bp` only inside comments (`:1242-1249`) and is never actually called there, and `AGENTS.md:46` records the IO scratch zone below x28 overlapping live data. So `diag_check.sh` keeps `cut -d: -f1,2`, NOT the `-f2,3` the blueprint predicted. Step 4 LANDED in the same commit and HAD to: the diag lane now additionally asserts the `error[E<code>]` token equals the process exit code, and the mutation test is real -- a deliberately mismatched token in a scratch copy makes the lane print `FAIL d01_paren ... token E96`. **Measured cost, all five arms against the promoted compiler:** words 43513 -> **42491 (-1022**, where blueprint §4.1 derived only -400..-800, so it under-predicted), bcond 1219 -> **1203 (-16)**, cbz 124 -> 125 (+1, pinned to `str_len(m)`'s zero-test: the helper never called still shows it, and the same helper with the length passed in does not). That settles a question the A19 allow line left open: an i64-array arm cost **2** b.cond, a `str`-literal arm about **1**, so the saving is ~1 branch per diagnostic code. Step 2 LANDED 2026-09-13 (`10b36b9d`), and the gate REACHES AGREEMENT rather than being wired at a promise -- `diag_texts: 18 codes, 18 in source, 18 in binary, 18 documented`, rc=0, with the no-text ratchet at **0**: `tools/trap_census.py --texts` asserts code == source text == binary string == TRAPS.md row, over BOTH storage forms, with `--src`/`--bin`/`--traps` overrides and fixtures that prove it green AND red. It found three real defects the hand-written table hid: **codes 64, 88 and 90 are emitted by the compiler with NO diagnostic text at all** (`bebop.bp:7261,7300`, `:6826`, and six sites from `:6869`), the withdrawn **103** was still documented as a live compile-time code, and row **90** was invisible to the census because its process cell reads `seed / bebop.bin` and the matcher demanded an exact `bebop.bin`. All three were then given texts in the same session through a new `cli_exit(code, m: str)`, so every compiler exit now prints a line. Two further defects fell out of TRIGGERING them instead of trusting a report: (a) **`cli_compile`'s SOURCE open was the one unguarded file open in the compiler** -- `sys_open` straight into `sys_slurp` with no `fd < 0` test, so `compile /typo.bp out.bin` compiled an EMPTY program and exited 0; guarded now, and row 90's long-claimed `source` case is true for the first time; (b) **code 64's "unknown CLI command" half was never an exit code at all** -- `main` ends `r5 = if known == 0 then 64` and RETURNS it, so the seed prints `64` on stdout and the process exits 0; only `check` without a file exits 64. Both are recorded on their TRAPS.md rows. The diag lane now asserts all three CLI exits (`diag: 19 pass 0 fail`, was 16), and the ratchet stays wired at 0 as a REGRESSION guard: the next bare `sys_exit(N)` with no text breaks the rung instead of shipping silently, which is exactly how 64, 88 and 90 got in. The rung was proved able to fail -- a fourth text-less exit added to a scratch source makes it print `RATCHET BROKEN: was 3` and turns invariants RED. Step 3 (own commit, every binary): the stub's runtime texts gain the token, `trap 81` deleted, 104 word_budget lines like A5's +41. 1-2 days + 1 | docs/blueprints/A17-diagnostics-as-data.md | `diag: N pass 0 fail` with the token asserted == rc; `diag_texts: n codes, n in source, n in binary, n documented`; WORD_DELTA 0 on 104/104 (steps 1-2); `strings bebop.bin \| grep -c 'trap 81'` = 0 (step 3) | none; FIRST in the language order (every later row adds codes through it) |
| A20 | **LANDED 2026-09-12, gate MET AT THE MARGIN.** `tools/fast.sh` exists and is a strict subset: check -> ONE self-compile -> construct_parity vs gen2 (FREEZE=0) -> typecheck over the whole corpus -> check_words, through `tools/slot.sh` like every other heavy job, never promoting. Measured on `1d1e634f`: wall 32153 / 29871 / 26874 ms, **median 29.9 s against the <= 30 s gate -- 0.4 % of margin, and one run of three was OVER** (`perf.csv` fast_wall). construct_parity is ~70 % of it and is near this box's floor (104 constructs compiled and run, ~190 ms each), so the next thing added to the corpus breaks this gate; it is MET, not comfortable. Both mutants behave: a file that does not parse is RED at step (i) in 1.29 s quoting `4:17: expected `)` or `in``, and a flipped `vs_try_madd` entry choice is RED at step (iii) naming `c74_madd`. Three defects were fixed in the delivered version before merging, all of the class this row exists to catch: it bypassed `slot.sh` (two heavy jobs could then run at once, against the one-compile rule); step (iv) ran `typecheck.py` with NO file arguments, which reports `0 findings` over an empty list and therefore checked nothing; and the parity line was `grep -E '^(PASS|FAIL)'`, which matches nothing construct_parity prints, so it fell through to a hard-coded `PASS` on every run. **FAST LANE (language item 7; harness only).** Measured, and CORRECTED by the main session after the research pass: the chain ran **516 s** on 2026-09-12 (`chain.sh` printed `(516 s total)` and `slot.sh` `released 1 after 517 s` in the same run), while `docs/PERF.md:9` still records `chain_wall` 104-139 s. Both are real -- the battery has grown since that row was pinned -- so the loop has regressed ~4x and this row is MORE urgent than drafted, not less. (The research pass read "516" as the milliseconds figure of journal 1789233153, a different closed defect.) Step 0 of this row re-pins `chain_wall` into perf.csv, because neither number may be quoted until it is, of which the three self-compiles are ~5 s and `perf.py` ~60 s (`chain.sh:50`); the gate memo cannot help a codegen edit (every `.bin` md5 changes). a new `fast.sh` under `tools/` (TO BE CREATED by this row -- written as a path it would read as evidence the file exists, arch_check CHECK 9), invoked as `fast.sh <files>`: `bebop.bin check` (T90 2b, sub-second) -> ONE self-compile -> `construct_parity.sh` against gen2 with FREEZE=0 -> typecheck -> check_words; one summary line; never promotes; `PERF=0` on worker chains. 0.5-1 day | docs/blueprints/A20-fast-lane.md | `fast_wall <= 30 s` (perf.csv, median of 3, taskset -c 4, one-line edit to bebop.bp); a syntax-error mutant RED at step (i) in < 3 s; a `vs_try_madd` flip RED at step (iii) naming c74_madd | none; parallel-safe with A17 |
| A19 | **STEP 1 LANDED 2026-09-13 (promoted `7bdc0f11`, exit 109), and the check is CONSERVATIVE in two MEASURED directions.** Measured (journal 1789219189): 8 kept symbols across `sys_clone` -> both children correct, 9 -> one lost SILENTLY, 11 -> both. Mechanism (`bebop.bp:1296-1307`): symbols 9+ are spill slots `[x15,#k*8]` and the child re-homes x15 to `sp+256` on a FRESH 12 MiB stack, so every spilled symbol reads 0 in the child -- NOT a shared frame (`pool.bp:6-7` says so); the "b6core 120 s hang" WAS real and is now measured twice (`bench/perf_fn/gates.txt`: `b6core_test 120199 miss rc=124`; a direct run: rc=143) -- its cause was a join waiting on a flag nothing set, fixed in `132063b`, and it is NOT this row's mechanism, which stands on the 8/9/11 measurements alone. Step 1 LANDED: it refuses when **`stab[0] > 8`** -- the symbols bound SO FAR, which in a one-pass compiler is exactly the set bound BEFORE the spawn, i.e. `tools/arch_check.py` CHECK 20's rule. That metric is NOT the function-wide `vc` this row originally specified and NOT the `fntab[5393]`+`fntab[5391]` pair the **WITHDRAWN trap 103** used: function-wide counters are not point-specific, they refused working programs, and that is why 103 was removed -- re-deriving one here would have repeated the mistake. Boundary pinned from BOTH sides, which is the half the row originally lacked: without a POSITIVE control a compiler that refused EVERY `sys_clone` would have satisfied the row. Two over-approximations, both MEASURED, both in CHECK 20's direction: (i) `stab[0]` counts i64 CONSTANTS, which the register allocator rematerialises and which are therefore safe in practice, so a fn binding 8 constants before a spawn is refused although it would have run; (ii) in the near-universal `let r = sys_clone(...)` idiom the pending binder `r` enters `stab` BEFORE its RHS is emitted and is counted too, so the effective budget is **SEVEN prior symbols, not 8** (8 only with the call in tail position, no binder) -- and the diag text, which names the `stab` count, therefore reads one higher than a reader counting by hand. Correcting (ii) is **step 1b**. **No working program is refused, and that was tested rather than argued:** all 25 `sys_clone` callers in the tree were compiled on `7bdc0f11` with the pre-A19 compiler as a CONTROL arm, and every file that compiled before still compiles. The only movement is `bench/wip/gb_par_{mxm,mxv,reduce}.bp`, already broken at 101 (unbound symbol) before A19 and now reporting 109 first -- the spawn check fires before symbol resolution finishes, so in an ALREADY-BROKEN file it can mask a more fundamental error; no gated file is affected. Cost: +3 b.cond (1216 -> 1219, re-derived by five additive arms, `census_allow.txt`), 0 program words, 0 new loops (cbz 124, tbz 0). Step 2: the child COPIES its parent's `S + tsp` slots before re-homing (2 + 2k words per spawn site; the parent's `cbnz .+16` skip is coupled to the block length -- `as`+`objdump`), then the limit disappears and CHECK 20 retires with its ratchet key (L25). Step 0 first: a 30-min probe that the 9th symbol reads 0 in the child -- if it does not, step 2 must not be built | docs/blueprints/A19-loud-clone.md | `neg/c141_clone9` COMPILEFAIL:109 and `c142_clone8` = 201, both MET (the row first named these `c126`/`c127`; those were never created, the landed pair is c141/c142); `c128_clone12` = 201 (step 2). Battery GREEN on `7bdc0f11`: construct parity 107/0, std_golden 117/0, diag 16/0, parity 13/0 skip 1, pool_parity 5/0, bpref_parity agree 81 unsupported 9 disagree 0, invariants GREEN, arch_check clone-symbols holds | none for step 1 (LANDED); B6 waits on step 2, which retires the limit and CHECK 20 with it |
| A23 | **LANDED 2026-09-13, and the lane went RED on its own first run -- correctly.** `bench/vs_rust/bpref_parity.sh` runs every construct through BOTH implementations and compares their ANSWERS: no EXPECT is read, so the lane cannot be satisfied by editing a table and it needs nothing from A24's headers. Wired into `tools/battery.sh` as its own lane. Measured on `9f744a99`: **agree=81 unsupported=8 disagree=0 error=0 total=89**. The 8 unsupported are named with their reason rather than discarded: `clz`, `crc32`, `crc32x`, `scan` are builtins bpref does not model, `c67_deeprec` exceeds its 5000-call depth cap, and `c84_run` uses `sys_clone` -- bpref has no threads and no processes. **Two defects in the oracle's own failure path were found by building this**: an unmodelled builtin raised `NameError` and came back as a generic error, and a program whose unmodelled semantics led it to `exit` came back rc=0 with EMPTY stdout, which the lane then read as a disagreement -- seven of its first eight findings were that, not divergence. bpref now raises `UnsupportedForm` and exits 3 with `UNSUPPORTED:<form>`; the 9 program-specific `qdsl_*` shims are deleted (verified dead: c70_qdsl still 41000). **Proof the lane works**: re-inserting `arr = list(arr)` into the `set` node takes it to `disagree=6`, naming `c70_qdsl` AND four constructs nobody had noticed the defect touched (c69_index_roundtrip, c70_qdsl_neg, c92_ptrfree); removing it returns `disagree=0`. So the four-day silence that started this row could not have lasted a day. Still open, costed, not scheduled: generating bpref from the compiler's own tables (~2,000 lines, A12-class), and `formal/Bebop/Semantics.lean` is still a third implementation that nothing runs. | **ONE SEMANTICS, MEASURED (language item 4).** The language is implemented FOUR times (`bebop.bp`; `tools/bpref.py` 736 lines, the fuzz DIVERGE judge and "the semantics reference" per LANGUAGE.md:1; `formal/Bebop/Semantics.lean` 607 lines, never run; `tools/typecheck.py`) and bpref's EVALUATOR is executed by NOTHING in `battery.sh` (only its parser via typecheck), which is how the `set`-node copy landed in `c99cddb` and stayed for four days -- and its fix was committed only on 2026-09-12 in `132063b`, four days after landing. bpref also carries 9 program-specific `qdsl_*` shims (`bpref.py:576-620`). "Generate bpref from the compiler's tables": not attempted, costed at a table-driven front end (~2,000 lines, A12-class), not scheduled. Scheduled: commit the fix; delete the shims; bpref REFUSES undeclared forms with `UNSUPPORTED:<form>` rc=3; lane `bpref_parity.sh` in the battery over all 104 constructs; every EXPECT names its derivation (`bpref \| hand:<method> \| native:<why>`), arch_check ratchets `expect-derivation` 0 and `bpref-shims` 0; LANGUAGE.md:1's authority sentence corrected. 2 days, no chain | docs/blueprints/A23-one-semantics.md | `bpref_parity: agree=a unsupported=u disagree=0 native=n` (a+u+n = 89); re-inserting `arr = list(arr)` -> disagree >= 1 naming c70_qdsl | none; every later language row declares `bpref node \| UNSUPPORTED` |
| A18 | **EARLY RETURN IN EXPRESSION POSITION (language item 3).** `return`/`break` exist (T99) but ONLY as body items (`emit_body_classify:5481-5484`); `if` is an expression whose arms are delivered into one register (`emit_cond_branch:3780`), so `if ok == 0 then return 0 else ...` exits 101 today. Measured consequence: 70 no-op `let _ = if X == 0 then 0 else 0;` lines in 4 files (15 in `c70_qdsl.bp`), an accepting parser, `c70_qdsl_neg` frozen at 1 against its own header's 89; `tcheck_kernel.bp:13-15` chose sentinel-propagation for the same reason. Design: `return e`/`break` as expressions of type never (deliver + the existing `b` placeholder + one dummy CONST so the arm's `vs_to(d)` has an operand; `emit_cond_branch` already saves/restores window state per arm), jump-list caps 17/18 -> 64, `arm_is_pure` refuses them (csel path untouched); PLUS an agent-first lint: a discarded `if` with two literal arms is a compile error (free code below 99). `if c { ... }` without else costed ~200 lines, not scheduled. gen.py must emit the shape (`gen.py:10` avoids it today). 3-5 days, two-stage bootstrap | docs/blueprints/A18-early-return.md | `c124_condreturn` MATCH at a hand value (smallest-prime-factor table, not the program's own division); `neg/c125_noopif` COMPILEFAIL:<code>; WORD_DELTA 0 on c05/c35/c36/c70_csel/c71; 300-seed fuzz DIVERGE=0 with arm-returns >= 10 % | A17 (text); unblocks B7 step 1's repair (`c70_qdsl_neg` -> 89 is B7's number) |
| A24 | **THE EXPECTATION HALF LANDED 2026-09-13; the `test` form is in flight.** SELF-DESCRIBING CONSTRUCTS (agent-framing item 11; T81 with a gate). Measured: three constructs had NO EXPECT row and were red for days while the battery reported green (journal 1789244500) -- the expectation lives in a 260-line `case` table (`construct_parity.sh:49-238`) an agent forgets. `test NAME { body }` at top level: SKIPPED by brace matching in all four whole-source scans under `compile` (byte-identity with a twin file is the gate -- "ignored by the `fn ` scanner" is NOT erasure, the f8_dt lesson), and RUN by a new verb `bebop.bin test <src>` (textual `test_expand` in the `use_expand` shape, unchanged emitter). **LANDED:** all **107** constructs (90 positive + 17 `neg/`) now carry `// EXPECT <value> from: <source>`, the 109-entry `case` table is DELETED, and a missing header is a NAMED failure. Every migrated value was checked against the deleted table mechanically, not by eye: 107 headers vs 107 table entries, **0 mismatches, 0 missing, 0 extra**. Ten of the pre-existing headers were inconsistent PROSE (`// EXPECT: 4`, `// EXPECT=3 (the oracle value...)`) and were converted -- a loose line a parser does not match is indistinguishable from a missing one, which is the defect this row exists to end. The gate goes RED for the right reason, verified by removing a header: `NO_EXPECT c99zz_noheader (missing // EXPECT header)` with `pass=107 fail=1 no_expect=1`, rc=1. **STEP 1 LANDED 2026-09-13 (`0780f16f`): a top-level `test` block now costs ZERO bytes under `compile`/`check`, and the ORACLE erases it too.** The skip is brace-matched via `find_body_end` in `collect_fns`, `scan_literals`, `find_struct` and `find_enum` -- and that last one CORRECTS this row's blueprint, which listed `collect_ctors`: `collect_ctors` (`:745`) is not a whole-source scanner at all, it calls `find_enum` for the enum's position and reads ctor names from inside those braces, so it inherits the skip transitively. `c133_testblock`, `c134_testfn_inside` and `c135_testblock_braces` are each byte-identical to their twin at 724 bytes; construct parity 107 -> **113 pass 0 fail no_expect=0**. Disabling the `scan_literals` skip alone brings back the exact 756-vs-724 divergence the row was opened on, so that half is load-bearing and not decoration. **The row was NOT done when the compiler was:** `tools/typecheck.py` (over bpref's AST) reported `UNSUPPORTED:test` and invariants went RED -- a form the compiler ERASES must be erased by the reference implementation too, or the two disagree about what a program contains. Two lines in `bpref.py`'s `program()` fixed it, and because `skip_block` counts TOKENS a brace inside a string cannot miscount. Checked the direction that matters: bpref returns **42**, the erased program's value, not 70, which is what EXECUTING c133's block would give. `bpref_parity` agreement rose 83 -> **87** with unsupported 11 -> 9. **STEP 2 IS COSTED AND DEFERRED, 2026-09-13 -- not abandoned.** Two lanes reached the same wall and both stopped at scaffolding, with the same correct diagnosis: `test_expand` must GENERATE bebop source -- each block into `fn test_NAME()`, the program's own `main` renamed, and a synthetic `main` that writes `NAME <value>` per line. Printing a decimal from a generated program means emitting a `put_num` equivalent INTO that program, in a language where `str` literals are valid only as arguments and there is no string construction at all. `use_expand` (`:6936`) looks like a 20-line precedent, but it is 20 lines only because its work lives in `use_scan` and `use_append_str`; the analogous helpers do not exist, and the missing primitive -- 'append a RANGE of the source' -- is in no file. A few hundred lines of generated-text plumbing. **Marginal value decides it, not cost:** the measured defect this row was opened for -- a construct with NO expectation silently red for days while the battery reported green -- was closed by the header half, which LANDED (`d9890da`); and `construct_parity.sh` already runs every construct by compiling and executing it, so the verb would re-run what is already run. What step 2 would still buy is co-location of the test body with its file: real, but small against that. Revisit when a `test` form is wanted inside std programs (T81's full ask) or once a string-building primitive exists. Its two assertions are recorded so nobody re-derives them: `bebop.bin test c01_lit.bp` prints `c01_lit 1000000065571`, and `c133_testblock` reads **42 compiled and 70 tested** -- the same file, two numbers, which was the point. **The skip is measured RED before any code:** a file with a `test` block compiles and RUNS correctly on `10b36b9d` but is NOT ignored -- 756 bytes against its twin's 724, a 32-byte (8-word) leak that points at `scan_literals` collecting the string inside the block. So byte-identity is a real assertion, not a vacuous one. Three corrections to this row's own blueprint, all measured: `neg/c134_testfn_inside` must NOT live in `neg/` (that lane iterates expecting `COMPILEFAIL:<code>`, and c134 must compile AND run) -- it belongs in the positive set with a twin; a `str` literal is only valid as an ARGUMENT (`LANGUAGE.md:91`) so the `fn ` bytes must sit inside a call, not a bare statement; and c133's block must itself CONTAIN a string literal or assertion A never exercises the `scan_literals` half it is built to test. Dead `self_check()` (`:6368`) deleted. 3-4 days | docs/blueprints/A24-test-blocks.md | `construct parity: pass=n fail=0 no_expect=0`; `cmp` c133 vs its twin identical; `neg/c134_testfn_inside` (a `fn ` inside a test string) runs to its twin's value; a headerless c01 copy -> `no_expect=1` RED | A23 (derivation headers); A18 optional |
| A21 | **MULTI-VALUE RETURNS AS A REGISTER CONVENTION (language item 1).** 75 `<< 32` pack sites and 68 mask sites tree-wide; the compiler packs too (`emit_call_resolve` `pb*2+okres`, five facts in one word `fw_*`). Design: `return (a, b[, c])` and a tuple tail deliver x0..x2 through the SAME `vs_deliver` builtins use (0 new callee machinery); `let (a, b) = f(..)` pushes `REG x1`/`REG x2` after the `bl` (x1..x7 are free and dead there, `emit_bl_call:855-857`; `emit_bl` and the epilogue never touch x1) -- **0 words on both sides**; NOT a first-class value (no store, no argument, no nesting; k <= 3). Arity recorded by `collect_fns` from `-> (` before any fn compiles and CHECKED at every destructuring `let`, every `return`, and every call of an arity>1 fn outside a destructuring (a silent x1 read cannot happen). Answer to the operator's Q1: structs do NOT subsume this -- a returned struct is an arena allocation per call (trap-80 class in loops); a register pair is free. ~1 week, two-stage | docs/blueprints/A21-multi-return.md | `c129_multiret` MATCH at a hand value (residue-class derivation, not the program's division); `neg/c130_multiret_arity` COMPILEFAIL:<code>; WORD_DELTA 0 on c08/c23/c53/c58/c61; a double-x0-push mutant FAILS c129 | A17; feeds A16 (`call_fn` returns) |
| A22 | **STRUCT LAYOUTS AS NAMES (language item 2; step 2 = A8 step 1a).** Structs exist for exactly ONE struct per program (`find_struct:354` returns the first, `fntab[5541]` caches one hash, `emit_field_access:500` resolves every `.f` against it -- silently wrong with two structs; `c10_struct` contains no struct; the compiler declares none). The layout defect measured 2026-09-12 was the +2 object header written over TWICE (store `st_parttab_write`, journal 924; `tools/stdump`, journal 948) and a +21 constant that turned FOUR gates red (slayout/schain/sevolve/scompact; scrash_torn was a harness-model divergence). Step 1: `collect_structs` table + `T.f` / `T.size` as compile-time CONST pushes (0 words; fold into `add #imm`), so `base[pt + 2 + PartTab.used]` replaces "cell 17" on `[i64]`-backed, store and CSR objects alike -- verbose by design (a human-legibility trade until step 2). Step 2: any struct's literal; a per-symbol struct TAG in the F3 side-channel shape (`fntab[5410+lk]`, `emit_let_stmt:5324`) from a literal RHS / a `: T` param (read in `parse_params`, today `skip_to_delim`'d) / a `-> T` return; `.f` on an untagged symbol is a diagnostic, never "the first struct". This IS A8 §3's 4th stab cell restricted to struct names -- A8 inherits it. Step 3: store accessors over `PartTab.*`/`Obj.*` with the six store gates byte-identical. Not: registers (T49), field types, `p.q.f`. 1 week + 2 weeks | docs/blueprints/A22-struct-layout.md | `c131_structs2` MATCH at a hand value (weights chosen so no field permutation is invariant) and equal with the structs declared in the other order; `c40_struct` WORD_DELTA 0; `neg/c132_untagged_field` COMPILEFAIL:<code>; slayout/schain/sevolve/scompact/scrash_torn/smw unchanged after step 3 | A17; A8 for step 2's generalisation; F3 shares the side channel |
**EXECUTION ORDER FOR THE WHOLE ROADMAP (operator 2026-09-12: "посортуй порядок виконання роадмапу логічно, щоб не повторюватись, поки не закінчать весь роадмап"). One row at a time, one lane, never two codegen tasks in flight; a row is DONE only when its gate number is quoted from a real run and the battery is green before the next row starts. This line SUPERSEDES the language-ergonomics order it replaces and the 2026-09-06 ordering above. **WAVE 0, instruments before work** -- nothing measured later is worth anything until these hold: A20 (LANDED 2026-09-12) -> A7 step 3's emitter defect (LANDED 2026-09-12, `bc06e5a`) (a filename argument handed a POINTER wrote 9 pointer-named blobs, ~160 MB, into git history and GitHub warns on every push) -> A17 steps 1+2+4 (LANDED 2026-09-13: `f636d4a3` then `10b36b9d`; step 1c the `<file>:` prefix and step 3 the stub remain open) -> A24 (header half + step 1 LANDED 2026-09-13; step 2 COSTED AND DEFERRED, see the row) -> F8 step 0's compiler half (LANDED 2026-09-13, promoted `89f889a4`, exit 110, ratchet 4 -> 0) -> A23 (LANDED 2026-09-13) -> A19 step 1 (LANDED 2026-09-13, exit 109; step 1b open) -> F8 step 0's negative construct (LANDED 2026-09-13 as `neg/c143_contract_garbage` COMPILEFAIL:110, with the positive twin `c144_contract_ok`). **WAVE 0 IS NOW CLOSED except three named stragglers: A17 step 1c (the `<file>:` prefix, blocked on threading a parameter through 35 call sites), A17 step 3 (the stub), and A19 step 1b (the pending-binder over-count).** Rationale: every one of them makes a FAILURE visible that is currently silent, and three defect classes found on 2026-09-12 alone were invisible gates, not broken code. **WAVE 1, the language an agent can write** -- each unblocks the next and each pays for itself in the rows after it: A18 -> B7 step 1's parser repair AS A18's proof (339 lines of `(p << 32) | node` and 15 no-op `if ok == 0 then 0 else 0` exist because the language had neither) -> A21 -> A22 step 1 -> A16 steps 1-3. **WAVE 2, types, the hinge** -- A8 is the single most-depended-on open row: A8 (with A22 step 2 = A8 step 1a, one commit) -> F3 -> C1 steps 3-4 -> close A7. **WAVE 3, the store earns the thesis**: B3 -> B6 step 4 (the power-of-two mask FIRST: today's gather arm touches 16384 distinct cells and measures L2) -> B7 step 2 -> B4's open half -> B2's measured-first twins -> the measurement rows C5, D4, E4, E1's baseline -> B8 end to end, the capstone that needs A7 and every B. **WAVE 4, proof** -- F7 is load-bearing and F9 cannot be real before it: F8 (a)-(d) -> F2 -> F5 -> F7 -> F6 -> F4 -> F9. **Out of the sequence on purpose**: A10 is REFUTED on a floor that no longer exists and is a RE-EXAMINE, not a task; C6 is CONDITIONAL, do not start; A16 step 4 (monomorphisation) waits for A8 step 2. Single-stage bootstrap: A17, A19 s1, A20, A23, A24. Every other Phase-A row lands WITHOUT `bebop.bp` using the form and may use it from the next commit. Not scheduled, costed, and named so nobody re-derives them: capture-inference closures (~2 wk), `if` without `else` (~200 lines), a table-driven front end that would make bpref generable (~2,000 lines), JSON diagnostics, struct values in registers (T49).**

### Phase B -- the tensor-graph DB over the store (mandatory properties: multi-writer, instant start, multi-core, rank-n, GraphBLAS-speed transformations; zero dependencies)

| # | task | blueprint | gate (the number) | depends on |
|---|---|---|---|---|
| B1 | step 1 LANDED 2026-09-08 (bebop.bin 42ce19e5, session-26 Lane C merged in session 27): `sys_fsync(fd)` builtin -- syscall 82 DERIVED from /usr/include/asm-generic/unistd.h:218, emitted as `mov x8,#82 ; svc #0` (2 as+objdump-verified words), dispatched on name hash -8392076202161113213 and reserved in compile_fn_at like every other sys_* builtin; selfhost/prelude/store.bp gains st_fsync_dir(path, plen), which st_compact calls AFTER its publishing sys_rename so the directory entry -- not just the file data -- survives a power cut (the name lives in the parent's inode block; fsyncing the file alone never flushes it); gate c94_fsync EXPECT=0 fsyncs a directory fd, needing no O_CREAT and leaving no scratch file, and scrash_torn TRIALS=50 stayed 0 invalid reopens (sync-range audit 1000/1000, 49 distinct picked generations). -- steps 2-3 LANDED 2026-09-07 (G5b torn-write gate GREEN 0/1000 bebop and sqlite WAL; the 475/1000 was two harness-model bugs + a missing oracle fallback, the writer already committed through st_commit_sync and st_open already ran st_reopen_verify; st_commit_batch + sbench rows durable batch10 30 us / batch100 7 us (sqlite 44 / 11), scrash_torn std gate; OPEN: step 1 sys_fsync builtin + st_compact directory fsync after A4; **RECOVERY SPLIT AND HALF-CLOSED 2026-09-09, and the reopen row FLIPS.** The scan `st_reopen_verify` ran was two things at once. The arena is append-only and `st_commit_sync` orders msync(range) -> toggle superblock -> msync(superblock), so **when the FALLBACK generation's superblock is crc-valid, every byte below its cursor was durable before the failing commit's crash window even opened**: the only bytes a crash can leave inconsistent are `[fallback_cursor, live_cursor)`. Everything below that was re-crc-ed on EVERY open -- 72.5 MB against a delta of one commit -- which is paranoia, not durability. `st_verify_range` / `st_reopen_lo` bound the primary scan to that delta; `st_verify` is retained unchanged as the full fail-stop check and the FALLBACK is still scanned in full ON PURPOSE, because once a valid superblock has been shown not to imply a durable payload the ordering argument no longer licenses the bound there. **Measured: reopen + first record 18,570 -> 380 us, 48.9x, and the row FLIPS from 7.03x SLOWER than sqlite to 6.95x FASTER**; recover 51,334.6 -> 40,780.4 us, only 1.26x, and not further optimisable without weakening the guarantee. `scrash_torn` stays 50/0 BY CONSTRUCTION: the harness tears only commit k's own appended range, so the injected range IS the delta. Detection scope given up: corruption strictly below the fallback cursor when both superblocks are valid -- a crash cannot produce it, only media rot, for which the store has no repair path anyway, so what is lost is fail-stop on COLD ROT, not recovery. Alternatives costed and rejected BEFORE building: a running superblock crc (checking one number still recomputes over the whole arena, and the full scan already runs at **3.90 GB/s = crc32x bandwidth**, so a flat pass costs the same 18.5 ms); root-reachable-only (needs the refmask at reopen, and the live set is nearly the whole arena after a compaction); lazy-on-first-touch (CANNOT keep the torn gate at 0 -- the fallback decision is made at OPEN time). **NUMBERS TO STOP INHERITING: the store's full-scan recover is 51.3 ms here, not 44.7, and sqlite's recover is not a stable number at all -- 2,462.4 us AND 5,860.8 us in two runs differing only in WAL state, a 2.4x spread. Quote a range.**; sqlite WAL 0/1000 -- so B1 is a real gap, not a formality: fix = payload msync/fsync ordering in st_commit + chain verification at reopen) durability: G5b torn-write harness (SQLite atomiccommit sector model) + `sys_fsync` of the directory after the compaction rename + group commit + `st_verify` + recovery row | docs/blueprints/B1-durability-torn-write.md | 1000 torn trials, 0 invalid reopens (same harness over sqlite WAL); commits/s row; recovery row | none (python part any time; sys_fsync after A1) |
| B2 | (only on the promoted A1 binary; before that the rows measure the stack machine) measured-first twins that decide the thesis: (i) 2-way join as SpGEMM (1M x 1M, uniform + Zipf) vs sqlite native vs Rust HashMap/sort-merge; (ii) specialise-then-run scan vs Rust generic scan (latency to result incl. compile); (iii) B8 profile of the CSR build **RUN FOR THE FIRST TIME 2026-09-09; the gate is UNMET as written and MET on D2's probe.** The row had never produced a report because `gen_scan.py` generated `fn scan`, a BUILTIN whose dispatch ladder runs BEFORE the user-fn lookup -- twin (ii) was silent dead code calling a 3-parameter builtin with 4 arguments, and F2's T122 extension EXPOSED it as exit 99 rather than causing it. **JOIN, plain probe: uniform 235.0 ms vs rust_best 151.8 and sqlite_best 2276.3 = 0.646x / 9.69x UNMET; zipf 566.0 vs 361.1 / 8923.4 = 0.638x / 15.77x UNMET.** With **D2's group-pipelined probe** -- no compiler change, and D2's own prefetch hint measured as a NET COST -- the same binary gives 0.767x / 11.50x and 0.801x / 19.79x = **MET on both distributions**, so the row is adjudicated on the grouped probe and the plain one retires to a baseline. `rust_best` is the MINIMUM over four Rust arms INCLUDING `rust_csr`, which runs bebop's own algorithm -- gating against hash or merge alone would credit an ALGORITHM win as a LANGUAGE win. **sqlite's index FLIPS direction**: helps 3.3x uniform, HURTS 1.37x zipf. **SCAN, first valid measurement:** bebop 3.320 ms/pass vs rust_generic 10.516 and rust_const 1.524 -- **3.17x faster than generic Rust**, and the specialisation pays back after **7.0 executions**. **CSR PROFILE, the thesis's sharpest qualification:** the fill pass is 18,400 ms into the store vs 601 plain = **30.6x and 95.6 pct of the store build**, rescued only by D1's build-in-RAM-then-publish at 1.21x. So *the object model IS the persistent store, and a scatter phase must be staged in RAM and published once* -- a design rule, not a caveat | docs/blueprints/B2-decisive-twins.md | join >= 10x sqlite AND >= 0.7x best Rust on both key distributions; scan twin rows first/repeat; CSR build profile row | A1 (register model) |
| B3 | OPEN DEFECT CANDIDATE 2026-09-08 (found by the A2b worker, harness half fixed the same day): a `.gbpool` file written by ONE compiler and then read by a DIFFERENT one is not rejected -- its kernels are dispatched and the forked children die with `trap 82: SIGSEGV/SIGBUS` while the fold prints 0. Reproduced both directions on the promoted 29fe5c72 itself (fresh $GBT -> golden, reused $GBT -> three trap-82 lines and 0). **DEFECT FIXED 2026-09-08, and this row's own diagnosis was REFUTED in the process.** The `compiler_digest` check exists, is correct, and `gb_pool_abi` was right about it -- two controls killed the identity story: a reused pool with the SAME compiler still traps 7x (so it is not about identity) and a FRESH pool with a foreign digest is golden (so no check is missing). The real cause is two bugs composed. (1) The pool store was a fixed 4 MiB under a comment claiming "plenty for 48 small kernels", but a Kernel's `words` array holds the `.bin` image ONE BYTE PER CELL, so a 29,628-byte kernel costs 237,048 store bytes and the advertised `gb_pool_cap()` = 48 needs **11.4 MB**; one run's 12 kernels fill 2.85 MB, the second run re-appends unconditionally, and `st_alloc` -- a bare bump allocator that never checks its cursor -- walks off the end of the MAP_SHARED mapping and kills the writer children. (2) The MISS answer is never read: `gb_pool_lookup` returns 0 and every dispatch site (`gb_kernel_run`, `gb_kernel_run_fast`, `hit_one`) passes it straight to `gb_kernel_digest(base, 0)`, so store cell 2 is read as a digest and whatever it names is exported, mmap'd and run. That is the "its kernels are dispatched" half -- not a missing check, an UNREAD answer from the check that already exists. Fix: `gb_pool_size()` = 16 MiB as one definition replacing the literal at all four pool sites, `gb_pool_room()` guarding both appends with a loud exit 89, `gb_kernel_require()` exiting 107 rather than dispatching a null Kernel, and `gb_bg_have()` making a rebuild child idempotent so a re-run stops growing the pool at all. New gate `gb_pool_reuse` + oracle: RED before (same=golden, foreign=0), GREEN after on six cells with 0 traps; re-verified by the main session in a clean tree on the committed compiler 3a827a3f -- typecheck 0 findings, oracles ok=113 mismatch=0, std_golden 113/0. **Still open, and 2026-09-13 established WHY it is still open: `st_alloc`'s bounds check is a FORMAT change, not a two-line guard.** The defect is real and confirmed -- `st_alloc` (`store.bp:324`) bumps `tx[2]` and writes `base[off]` with no bound test, while its partitioned twin `st_alloc_p` (`:561`) tests `off + 2 + len >= st_region_end(...)` and exits 80 first. The plain one has ~164 call sites, so passing a capacity in is a signature cascade and is ruled out. **The attempt and what killed it:** the capacity was written to cell 16 of the page-0 remainder (cells 16..511 are unused by the superblock, and the crc covers only 0..14) and read back by `st_alloc`. It WORKS as a guard -- measured: an allocation past the end exits 80, a small one still succeeds -- but `base` is a MAP_SHARED mapping, so the write persists into the FILE, and `bench/oracles/slayout.py` builds the expected store bytes with `struct.pack` **from the layout rules alone**. Measured: `FAIL slayout golden=749213972 got=3747494854` and `FAIL scrash_torn golden=0 got=20`, both PASSing again the moment the change is reverted, plus 4 `WORD_BUDGET_MISSING` rows (+16 words in every store-using construct) and `bpref_parity error=3`. The lesson is one line: **cell 16 is free to the CODE and not free to the FORMAT.** Reasoning about which cells are read is not the same as reasoning about which bytes the format promises. **So the real row is:** capacity becomes a genuine superblock field, the **version cell (1) is bumped** -- the machinery for exactly this already exists, and trap 87 is what makes a schema divergence loud -- `bench/oracles/slayout.py` learns the new layout, the goldens are re-derived, and `scrash_torn`'s tear model is updated with it. Sized as its own row rather than a guard. The 48-kernel cap is separately still reachable with no eviction. Was: `gb_pool_abi` asserts and PASSES that a Kernel whose compiler_digest mismatches is a MISS (rebuilt, never run), so the file-backed lookup path was read as missing that check; std_golden keys the gate's pool file by the compiler md5, which is the harness and never was the fix. -- steps 1-3 LANDED 2026-09-07 (6183ce8 objects + gb_roundtrip; accf51a gen_gb mxv/vxm x 4 semirings + gb_gen/gb_bfs; step 3 tier-0 mxm/eWise/select/apply/reduce + gb_tc/gb_cc/gb_sssp/gb_pr); step 4 BASE LANDED 2026-09-07 (gb_run.bp Pool/Kernel objects + gb_compile1.bp in-process compile + selfhost/tools/gbpool.bp PreJIT 12 kernels + dispatch via sys_run; gates gb_pool -4783772994166464769 / gb_pool_abi 1001; bench/vs_rust/gbpool.sh rows: tier0 1 ms MET, specialised compile 95 ms vs 50 UNMET, pool hit 8 ms vs 0.1 UNMET -- a bare clone+exit+wait4 floor measures 2.0 ms/iter, so a forked dispatch MEASURES 2.0 ms against a 0.1 ms gate, 20x -- a bound on that method, L23); step 4 follow-ups OPEN: (c) LANDED 2026-09-07: forked background compile-on-miss (single-writer child, parent returns the tier-0 fold in 2 ms) + per-process kernel address table + MAP_SHARED result: pool hit 4.35 -> 1.58 ms/call vs a 1.04 ms fork floor; compile-on-miss child 98 ms vs 50 UNMET (gb_gen_and_compile_fast inside an 8 MiB clone arena); (f) LANDED 2026-09-07: gen_gb.bp op 5 = a generated whole-loop Beamer BFS program (push/pull, alpha 14), gate gb_bfs_gen 997 == python oracle, sgraph2.bp frontier phases dispatch it ONCE per BFS (fork + sys_run of ./sgraph2_bfs.bin staged by sgraph2.sh): folds identical, frontier 26 ns/edge vs queue 112 (<= 2x MET; the in-process frontier BFS it replaced ran 23 ns/edge, so the generated kernel is 13 % slower, not faster -- the win is the kernel being generated, not speed). (needs the in-process `run(bin)` builtin of A11(b) FIRST -- land it as a standalone ~15-word builtin before B3; until then the pool falls back to fork+exec, ~8 ms, and the <= 1 ms gate is measured only after it) `gb.bp` + `gen_gb.bp`: GbMatrix/GbVector as store objects, formats CSR/bitmap/iso, masks, and the generated semiring kernels (mxv push/pull, mxm Gustavson+SPA, eWiseAdd/Mult, select, apply, reduce, extract, transpose) with **instant start**: a PreJIT kernel pool (all <= 36 (op, semiring) kernels compiled ahead into the store image, digest-keyed) plus a tier-0 generic kernel (semiring by id, no compile) used until the specialised one exists, compiled in the background | docs/blueprints/B3-graphblas-kernels-prejit.md | G9a round-trip; G9b LAGraph-style folds BFS/PR/TC/CC/SSSP == python oracle; first-query latency <= 1 ms (tier 0) and specialised <= 50 ms; each kernel a construct | B2 gate, A9 (umulh, cmp_mask, sum64) |
| B4 | **STEP 2 REFUTED 2026-09-08 by arithmetic its own probe confirmed exactly, and the row is RE-SCOPED.** The RowBlock/blocktab structures were built and measured before the live read path was switched onto them: 64-row `BK` blocks, a 512-way page tree, `promote_L0` with block AND page CoW sharing, reachable only through probe phase `k` so no existing fold or gate row moved. **Sharing never fires on this workload.** 1M rows / 64 = 15,625 blocks, and 2^18 L0 rows uniform over 1M touch every one of them: E[untouched] = 15625*(1-64/1e6)^262144 = 0.0008, and the probe measured **0 of 15,625 blocks and 0 of 31 pages spared**. So `promote_L0` is a whole-L1 rewrite -- 98.97 MB and 181/193/195/171/203 ms against a 10 ms gate, 377.6 bytes per promoted edge -- and step 2 would REGRESS every row it targets: stall 74 -> >= 185 ms (2.5x worse), ns/edge 985 -> ~1150, point reads 520 -> 750 ns (+45 %), L1 12,093,750 cells vs 11,000,003 flat (+9.9 %). Total promotion volume is `(1e6/B)*(1-exp(-64B/1e6))*99 MB`, **monotone in B** (B=4096 -> 5580 MB, B=32768 -> 2650 MB, B=2^18 -> 376 MB, never-merge -> 99 MB), so no block size and no L0 bound rescues it -- the cheapest schedule is not to merge into L1 during the phase at all, which is what step 1 already does. Block CoW pays only when the touched-row count is well below the block count, and 1M updates into 1M rows is never that. The structures are correct where they were exercised -- block split LOSSLESS (fold 5001253080071 both ways) and the merge MERGE-OK (5132199567371 == expected) -- and one design detail is worth carrying forward: a literal sorted two-pointer merge into one `ci` would have DRIFTED the folds, because `TB` and bench/oracles/sgraph2.py both index L1 by the flat `csr_build` slot number, so renumbering the original edges silently re-points every tombstone; the block therefore carries `base0` and keeps the original region verbatim with merged L0 edges in a separate slot-less region. Probe parked at docs/blueprints/attic/B4-step2-rowblock-cow-on-55bc5eb.patch. **STEP 2' CANDIDATE (a) REFUTED 2026-09-09 by arithmetic, and NOTHING WAS BUILT.** Baseline re-derived on this box at dbea972 / 15e272fc first: 1027 ns/edge (the row's 985 is 4.3 pct low), 74 ms stall (exact), folds nbr0 500446467359 / nbrlog 550529568420 and size 106,859,272 B all unchanged. Keeping the FIVE per-invocation stalls separately decomposes a promotion with no code change at all (invocation j's max stall is its last promotion, at |L0| = 200k*(j+1)): 49/56/62/67/74 ms fits **stall(|L0|) = 43.3 ms fixed + 30.5 ns per L0 edge**, residuals <= 0.7 ms -- so step 1's "~21 ms fixed + ~117 ns/edge marginal" is wrong in BOTH terms (fixed 2.1x larger, marginal 3.8x smaller). Promotions are 600.8 ms of the 1027 ms row (58 pct); the non-promotion floor is 426 ns/edge. An exact replay of `tl_gen`'s LCG-777 row stream gives |D| = 95,150 / 259,037 / 393,195 / 503,351 / 632,129 DISTINCT rows at |L0| = 100k / 300k / 500k / 700k / 1M (matching n(1-e^-k/n) to < 0.4 pct), so the binding promotion's "compact" directory must describe **0.632n rows** -- this workload is no more sparse in ROWS than step 2 found it in BLOCKS, which is the same refutation in another coordinate. Hence the directory is 13.06 MB packed (row||offset in one cell) against `rp0`+`ci0`'s 16.00 MB, and 18.11 MB unpacked, LARGER than today for every promotion past |L0| = n*ln2 = 693,147. `ci0` alone is 50 pct of the promotion write and a row directory cannot touch it BY CONSTRUCTION: granting (a) a FREE zero-byte directory AND no O(n) host work still leaves **30.5 ms stall (3.05x the 10 ms gate) and 594 ns/edge (1.19x the 500 gate)**; the realistic packed form gives 64 ms / 864 ns. Even free, (a) needs |L0| <= 328,000 to reach a 10 ms stall, and the ns/edge half leaves 74 ms for all TEN promotions against today's 600.8 ms. **A SECOND obstacle is named here for the first time:** the 426 ns/edge non-promotion floor is 85 pct of the entire ns/edge budget on its own -- `tail_new` CoWs the shared tail EVERY batch (mean c0 ~ 45k entries copied, plus st_seal's crc32 over ~1+2*want cells, ~230 B of copy+crc per logged edge), so a promotion costing NOTHING still lands at ~426 ns/edge and the ns/edge half does not move by fixing promotion ALONE -- a bound on that method, not on the goal (L23), and candidate (b) later closed the gate at 435. **STEP 2'' CONFIRMED 2026-09-09 (probe only; `selfhost/std/sgraph2.bp` untouched, no gate row moved).** The SEGMENTED TAIL: a header `TH{arr ref TG}` over `TG{arr i64}` segments of capS entries; a batch CoWs the header and ONLY the last, PARTIAL segment, while every FULL segment is immutable and stays shared BY REFERENCE. Arithmetic first, as step 2 taught: arm W (the whole-tail CoW, counting `tail_new`s double seal) is 33.0 cells per entry against 4.15 at capS=128, predicting a 54 ns/edge floor against an irreducible 52. Measured (`bench/vs_rust/std_tests/segtail.bp` + `segtail.sh`, its own store and root, same LCG-777 stream / 100 batches x 10^4 / per-batch st_begin+commit / same cap, interleaved x3 on core 4): whole-CoW **291 ns/entry and 88,013,848 B** against segmented **72 ns/entry and 16,667,768 B** -- 4.0x and 5.3x. capS is flat 72-85 over 64..1024 and degrades to 143 at 16384. **THE RESULT IS THE CAP SWEEP:** the whole-tail floor is LINEAR in the cap (307 / 545 / 1257 ns/entry at cap 100k / 200k / 500k -- which is exactly why step 1 measured cap 200,000 as WORSE than 100,000, 1034 vs 985) while the segmented floor is FLAT across 10x (80 / 72 / 74 / 82 at 100k / 200k / 500k / 1M). Projected onto `phase_log` (R = 426 - 307 = 119 ns/edge of cap-independent other work; promotion 5.855e7/C; the model reproduces step 1s measured cap sweep within 8 %): the log row goes 1012 -> 784 at cap 100k, 957 -> **484 at cap 200,000**, 1493 -> 310 at 500k, 2623 -> 260 at 1M. **This is the first time either half of the B4 gate has been reachable.** Folds are IDENTICAL between arms at every segment size and equal to the python oracle (final 25038854900104992, pinned batch-1 2480532501578455), and invariant (1) is PROVEN rather than asserted: phase `v` pins the header batch 1 committed, runs 99 further batches (99 CoWs, 9 cap resets) on top and re-folds through that old header unchanged -- had any shared segment been written in place, that fold would have moved. **Scope of that proof, stated honestly:** both arms `st_supersede` the pinned object symmetrically and the probe never compacts, so what is proven is "a full segment is never written in place", NOT "a shared segment survives `st_compact`" -- the Cheney case is untested and must be gated when this lands. **Still open:** the <= 10 ms stall half is UNTOUCHED (~74 ms at every cap, since the last promotion always rewrites |L0| = 10^6 at 43.3 ms + 30.5 ns/edge), so it stays with candidate (b); and raising the cap makes the linear tail scan on reads proportionally worse (`tail_row` 6.8 ns/entry -> ~6.8 ms per point read through a full 10^6 tail), so a per-row index over the tail must be costed BEFORE the cap is raised in `phase_log`. **STEP 2''' LANDED 2026-09-09, GATE STILL RED, and the remaining gap is now ONE NAMED TERM.** The segmented tail is wired into `phase_log` (`bench/vs_rust/std_tests/sgraph2.bp`: `TH{arr ref TG}` header over `TG{arr i64}` segments of 256, `tl_open` CoWs only the partial last segment, `tl_count` is O(1) because segments 1..nseg-1 are always full, `tail_drain` ZEROES the header's stale slots so `st_compact`'s mask -1 walk cannot resurrect drained segments, cap 100000 -> 200000). **MEASURED, replacing step 2'''s projection of 484: 909 -> 571 ns/edge against the <= 500 gate, a MISS by 14.2 %.** The control was re-derived from the pristine binary on this box the same minute and is **909** -- not the row's 985, nor step 2''s 1027; the same measurement has now given three values a day apart, so the gate's margin must be read against that spread. Stall 76 -> 72 ms, nbr 780 -> 790 ns, bfs 141 -> 134 ns/slot, size 106,859,272 -> 105,269,680, and every fold -- nbr0 500446467359, nbrlog 550529568420, nbr 502924403814, bfs 21550931 -- IDENTICAL to the committed values and to `bench/oracles/sgraph2.py`, so no gate value moved and no oracle was re-frozen. Decomposition from the five per-invocation stalls: segmented promotion = 39.2 ms + 33.0 ns/edge over 5 promotions = 295 ns/edge with a 276 floor, against the control's 35.5 ms + 37.5 ns/edge over 10 = 561 with a 348 floor -- so **266 of the 338 ns/edge win is HALVING the promotion count** (the cap raise, affordable only because the segmented floor is cap-independent) and only 72 is the floor itself. **THE CHENEY CASE IS NOW GATED AND GREEN**, closing the hole step 2'' left open: pin a version, append, `st_compact`, re-fold -- the pin folds to 2480532501578455 after compaction, equal to the oracle, and retaining the whole pinned version costs **4,472 bytes** where per-referrer copying of its shared segments would cost ~165 KB, a 37x discriminator confirming `st_copy_obj`'s forwarding pointer (`store.bp:465-478`) makes a segment named by k versions copy ONCE. **READ SIDE, costed before building:** `tail_row` stays linear at 6.76 ns/entry so an in-phase point read is 1.35 ms at cap 200000, but NO GATED ROW PAYS IT, because `phase_log` promotes at the end of every invocation and every version a read phase opens has an empty tail -- that is a PRECONDITION of the cap raise, not a free lunch. A per-batch tail CSR is refuted (3779 ns/edge, 7.6x the whole budget); per-segment row ranges are refuted (uniform rows make the range span 99.2 % of the row space, skipping 0.8 % of segments -- **the THIRD structure this workload has killed for assuming sparsity it does not have**, after step 2's blocks and step 2''s rows); a per-segment Bloom survives the arithmetic at 173x for +7.8 % tail bytes and is deliberately NOT built until a mid-log read row exists to gate it. **CANDIDATE (b) RESOLVED 2026-09-09 -- THE ns/edge HALF IS GREEN; THE STALL HALF IS BOUNDED, NOT ABANDONED (L23).** The elaborate form of (b) was REFUTED BY ARITHMETIC BEFORE IT WAS BUILT: lazy sorted runs cost R*(F_run + M*cap) + (F + M*1e6) = 420-577 ns/edge dense and 394-443 with a compact per-run directory, strictly WORSE than eager merging at R=1 (358) at every R -- because F = 39.2 ms is the n-proportional `rp0` write and does NOT shrink with the run size, so k runs cost k*F and **the LSM amortisation constant is NEGATIVE**. A compact per-run directory helps only while |D_run| << n (0.181n at a 200k run) and the final merge always sees 0.632n. What (b) reduces to here is the RUN COUNT itself, swept (medians of 3; cap on argv[6], flush policy on argv[7], one binary reproducing the committed control): R=5 593 ns/edge / 74 ms, R=3 495 / 86, R=2 443 / 101, R=1 370 / 141. **R=2 (tl_cap 500000) SHIPPED: log 566 -> 435 ns/edge, GATE <= 500 GREEN with 13 pct margin, worst rep 437**, stall 70 -> 97 ms, size +0.009 pct, folds nbr0 500446467359 / nbrlog 550529568420 / nbr 502924403814 / bfs 21550931 EXACTLY the oracle. R=1 rejected: it buys 73 ns/edge the gate does not need for +91 pct stall instead of +36. Read rows are reported as NOISE, not a win -- one run measured nbr and bfs identical, another 740->790 and 126->133 with overlapping spreads, and structurally they must be identical because every policy ends in the same single L0 CSR with an empty tail. Two arithmetic errors the sweep caught: the segmented floor RISES with the cap (409 predicted vs 443 measured), and the stall is NOT flat in R (74 -> 141 as R falls) because the last promotion also drains a 1e6/R-entry tail at 61.6 ns per entry. **THE STALL HALF IS CLOSED 2026-09-09, REFUTED BY READ AMPLIFICATION BEFORE ANYTHING WAS BUILT.** The only route to 10 ms is never materialising 10^6 edges in one batch, which leaves R runs live at read time. Costed against the measured 790 ns point read with |D_run| = n(1-e^-m/n), a 10-bit-per-member Bloom (2 probes, ~20 ns) and a log2 binary search at ~30 ns/probe: the read grows **+490 / 538 / 601 / 722 / 949 / 1638 / 2795 ns at R = 2 / 3 / 5 / 10 / 20 / 50 / 100**, i.e. **1.62x at BEST**. NO run count avoids a regression, because the R x 20 ns of Bloom probes never goes away -- **every run must be asked, including the ones that do not have the row** -- so the read penalty grows LINEARLY in R while the merge shrinks, and the two sides never overlap. A 10 ms batch buys 2.96 MB of merge writes, so the final merge to one run (8.0 MB) spans >= 2.7 batches and the runs are live for exactly that window. **The stall half and the read half of this gate are MUTUALLY UNSATISFIABLE on this workload**, which is stronger than unmet. Renegotiated to the measured bound permanently: **97 ms at R=2, 74 ms at the old policy, hard floor 33.0 ms (16.5 ms under A8's u32 cells)**. L23 satisfied: six structures attempted by different methods, this one costed across R = 2..100 before a line was written. **AND A DEFECT FOUND WHILE DIAGNOSING THE FRONTIER ROW: `gen_gb`'s fmt=2 `_addr` variant is UNGATED** -- `gb_bfs_gen.bp` is "PATH main, no sys_run" by its own comment, so it exercises `_path` only and sgraph2's frontier is the sole consumer of `_addr`. That is the structural reason a broken variant sat in a tree reporting 114/0. `bfs_matrix_wrap` also allocated SIX cells for `gb.bp`'s TEN-field GbMatrix, so `vv`/`mask`/`gen`/`prev` read whatever the next arena cells held -- fixed, and exactly the shape F3's `exit 65` turns from silent into loud. Gate the `_addr` variant, then close the frontier row -- the fix is IN THE TREE and NOT GATED as of 2026-09-09.  | docs/blueprints/B4-functional-tensor-updates.md | step 1 ADJUDICATED 2026-09-09, and the 985/74 numbers this column used to carry are SUPERSEDED: ns/edge MET at 435 vs <= 500 (segmented tail R=2, tl_cap 500000, 13 pct margin, worst rep 437); folds MET EXACTLY (nbr0 500446467359 / nbrlog 550529568420 / nbr 502924403814 / bfs 21550931); the <= 10 ms stall clause is RENEGOTIATED to the measured 97 ms bound -- it is not merely unmet, it is jointly UNSATISFIABLE with the read half, which four closed structures (step 2's blocks, 2''s row directory, 2'''s segment ranges, (b)'s lazy runs) now price. STEP 3 IS THE ROW'S ONLY OPEN HALF and G9c applies unchanged to it: `prev` + time-travel + the `u` twin, 1M single-row updates amortised <= 0.5 us, folds == oracle every 10^4, kill -9 during merge (G5 harness); update twin vs sqlite WAL UPDATE | B3 |
| B5 | **G10 GREEN 2026-09-12 (`PASS smw (303000)`): 3 writer THREADS x 10^5 updates on DISJOINT partitions, a cross-partition 2PC transaction every 100th, fold walked out of the arena.** All five properties the row lists are now real, and NONE of them were before -- the code had landed, the behaviour had not. Measured: (i) partitioned writers -- every partition's cursor started at 1024 because st_begin_p's no-PartTab fallback is a single-partition constant, so writers overwrote each other (probe: partition 0's counter read back as partition 1's value); fixed by `st_init_p`, which seeds each cursor at its own `st_region_start`. (ii) per-partition roots under one global root swap -- works via the PartTab, and `st_root` had to be taught to dereference it. (iii) cross-partition atomic commit by 2PC -- `st_commit_2pc` committed NOTHING (probe returned 00, neither root moved); rewritten to take a `roots` array and publish every participant under one superblock toggle, and its PartTab home had to stop being hardcoded to partition 0, which wrote over the superblock for a 1+2 transaction. (iv) STM validation of read-sets -- did not exist; now validates each participant's generation against the one `st_begin_p` observed, and PROVED to fire (a stale cross transaction aborts and the interfering writer's root survives). (v) single-writer degenerate case -- P=1 works. **Not yet: `cross_commit` takes both partition locks pessimistically, so the STM validation cannot fire inside G10 itself -- it is proved by a dedicated probe. Making the cross path optimistic (drop the second lock, validate and retry) is the open follow-up.** Commits f4626a2, a16c2f2, 5ece5fd, 6ae618e, e4ae212, 2e27578, e3d1cf3 | docs/blueprints/B5-multi-writer.md | G10: 3 writers x 10^5 updates on disjoint partitions -- MET, 303000, ~10.5 s, reproduced | B1-B4 |
| B6 | **STEP 3 LANDED PARTLY, 2026-09-12: the gate TERMINATES but its timing arm is RED and one assertion is measuring the wrong thing.** `b6core` had hung for 120 s in every battery: the join waited on `sys_atomic_add(cells, W+w, 0) == 1` and NOTHING ever set that flag, and every post-clone local was spilled into the frame parent and child SHARE. Rewritten to the blueprint's §4 shape (`b6_work` in its own frame, the release publish before the wake); it now terminates at every (shape, W) with ok=pin=noalloc=1 on `1d1e634f`. Two things stay OPEN and are not to be quietly dropped: (a) assertion C is **0.43** against its 0.05 bound (ms0 7, msW(W=1) 10) and scan speedup is **1.00x** against the 2.50x threshold, which is step 4, the actual research; (b) **the gather arm was not measuring a gather, and is now -- FIXED 2026-09-13, and the threshold it had been passing was passing on cache residency.** n is now 1<<23 = 8388608 cells (64 MiB), mask 0x7FFFFF, every index reachable. Measured before/after on `9f744a99`: **2.2 ns/gather -> 19.4 ns/gather** (the blueprint's reference for a real gather is 13.3), and the 3-core ratio **1.83x -> 1.36x** against the 1.80x threshold. So the gather arm is now honestly RED where it had been green on an artefact. The scan arm is unaffected (it never indexed), still 1.00x against 2.50x. Golden re-derived with n: 485239494593740800. The original defect, kept because it is the lesson:  `idx[i] & (n - 1)` with n = 10^7 masks with 0x98967F, which has 14 bits set, so the index takes at most 2^14 = 16384 distinct values -- ~1 MiB of cache lines, resident in L2/L3 by construction. That is why it reads 2.7 ns/gather against the 13.3 ns/gather the blueprint derives its 1.80x threshold from, and why W=2 once read 9x. The mask must be a power of two (n = 1<<23 = 64 MiB) before any gather number on this row means anything. Also, the `ok` check the gate asserted was `fold_par == fold_seq` with both sides computed by the same sequential fold -- a tautology; it now sums the workers' partial sums. **REVIVED and RAISED IN PRIORITY 2026-09-08 (operator: multi-core, async and parallelism are mandatory).** It had been discounted on the ROADMAP's own DRAM row, which claimed one A78 saturates ~12 GB/s and that three do no better. Measured that day, that is false: one sequential 80 MB sum takes 28 ms solo and **28 / 28 / 28 ms when three run concurrently on cores 4,5,6** -- linear 3x with zero contention -- while ~12 GB/s is the AGGREGATE ceiling of all seven cores. Two consequences for how this row is scoped: (a) on SCAN-shaped work the core count and per-core instruction reduction (unrolling, A9 NEON) share ONE budget of about 4x, because 3 cores x 4x would need ~34 GB/s and the ceiling is ~12 -- they do not multiply; (b) **CORRECTED 2026-09-12, and the correction inverts this consequence.** The two numbers behind it were both wrong. `docs/exp.journal:852` says 133 ms for 10^7 gathers = **13.3 ns** each, not 133 ns -- a factor of 10; and the "0.67 ns floor" priced **8 bytes** when the same journal sentence says a gather moves a whole **64-byte line**, so the honest floor is 64 B / 11.05 GB/s = **5.8 ns** -- a factor of 8. The claimed 200:1 headroom is **2.3:1**. Per core it is worse than 1:1: 64 B at that core's own 2.86 GB/s stream rate is 22.4 ns, LONGER than the 13.3 ns the gather took, because the scan loop is instruction-bound and the gather loop is not. **The gather is already closer to the memory system than the scan is.** Three cores demand 14.4 GB/s against ~11 available, so the traversal cap is **2.30x, not 3x** -- and this row refuted itself two sentences later without noticing, by reporting `parscat`'s measured 1.64x. Traversal and scan do NOT multiply; they share the same budget, traversal just reaches it sooner. The four A55 cores measured 0.7-1.0 GB/s each and are the natural home for background work the A78s should not stop for (B3's compile-on-miss, crc verification, compaction) -- but **CPU-free is not memory-free**, and this session overstated it: in the same seven-core run one A78 went from 28 ms to **43 ms** while the A55s were loaded, a 1.5x hit. Background work on the little cores costs the big ones about a third of their throughput. Was: multi-core execution (mandatory): row-range partitioning of mxv/mxm/scan/reduce over 3 A78 via clone/setaffinity work queues, reduce merge, NUMA-free but DRAM-aware (stop scaling at the bandwidth ceiling; step 0 measures whether clone threads count toward the proot/boxguard proc cap, else the battery goes RED under PROC_CAP=30) **MEASURED 2026-09-09: cores pay, they do not multiply.** Step 0 answered -- `slot.sh` counts `ps -e` = PROCESSES while `pool.bp`'s workers are `sys_clone(68864)` = CLONE_THREAD, which `ps -e` does not list and the phantom killer does not count separately, so **threads are FREE against the cap and forks are NOT**; a fork's COW space makes a child's writes invisible anyway. Usable width is **3**, set by the core mask. The mechanism needs NO runtime: `pool.bp` already has clone/futex/atomic workers, and `pool_parity` re-derived 5/0 on this box BEFORE the probe was trusted. **THE NEW DATUM** (`parscat.bp` is **scratch and unreproducible** -- checked 2026-09-12, no such file exists anywhere in the tree and it appears in no journal entry, so this row's headline traversal number has no committed script and cannot be re-run): it measured a random SCATTER (4M destinations over 32 MB, `sys_atomic_add` so the fold is bit-identical at every W) at 100/100/68/61 ms for W = 0/1/2/3 -- **1.64x on three cores, 55 pct of ideal, against T106's measured 2.21x (74 pct) for a SCAN**. W=1 equals W=0 to the millisecond, so the shortfall is RFO traffic, not harness overhead. Amdahl on B2's phase table with these MEASURED efficiencies: fill alone 1.23x (0.94x of the serial plain arm), all but prefix/seal **1.82x** -- where applying the SCAN efficiency would have projected 1.36x and 2.19x, so measuring moved the headline **20 pct pessimistic**. Cores are IRRELEVANT to the 30.6x store scatter because D1's staging already deleted that phase. THREE TRAPS: `pool.bp`'s '<= 8 live symbols across a parallel region' is a CORRECTNESS requirement (14 gave NO OUTPUT at every W >= 1, the child reading spilled per-thread array bases); a clone stack must sit LOW in the arena; and `m/W` truncates, so the parent sweeps the remainder | docs/blueprints/B6-multi-core-kernels.md | K6 scan x1.4-2.2 on 3 cores; BFS/PR rows; no lost updates with B5 | B3, B5 |
| B7 | **STEP 1 HAS AN OPEN DEFECT, found and then RE-DIAGNOSED 2026-09-12: the parser accepts a malformed query, and the obvious fix rejects valid ones.** `qdsl_query` writes `let _ = if ok == 0 then 0 else 0;` **fifteen times** -- in `selfhost/std/qdsl.bp` (the production parser) as well as in both c70 constructs -- where an early return was meant, so `qdsl_parse("invalid query that should fail")` returns a tree and `c70_qdsl_neg`, whose header asks for 89 ("parse correctly rejected"), gets 1 from bebop.bin AND from tools/bpref.py. **Measured, not assumed: honouring the flag at the return (`if ok == 0 then 0 else root`) makes the negative construct give 89 on both sides -- and makes the POSITIVE one give 1 instead of 41000**, because `ok` is overloaded. Each OPTIONAL clause sets `ok = 0` merely by being absent (`if p+5 > n then ok = 0` before the where/group/order/limit keyword test), and the no-op line is where the author meant "clause absent, carry on". So the fix is not a return: each optional clause needs its OWN presence flag, leaving `ok` to mean only "a clause that WAS present failed to parse". Five blocks (join, where, group, order, limit). The constructs stay frozen at 41000 / 1 because construct_parity measures bebop-vs-oracle parity and parity HOLDS; re-derive both goldens in the same commit as the fix. Step 2 must not build a planner on a parser that cannot reject. associative-array DSL + planner (`q { from T where p group by k agg s join U on k }` -> AST -> access path + join order (k <= 4) -> gen_gb -> kernel) + compiled Q6/Q1/join twins; rank-n = mode-ordered CSR chosen by the planner | docs/blueprints/B7-dsl-planner.md | Q6 >= 10x, Q1 >= 5x sqlite native; first/repeat latency rows; rank-3 construct | B3, B6 |
| B8 | (last: needs A7 for ingest and every B item) W end to end: the dowiz-core order log (T66 ordfsm/money oracles) on the tensor-graph DB -- ingest (A7), FSM as a 12x12 matrix, queries via B7, updates via B4/B5, durability via B1; the acceptance of the thesis | docs/blueprints/B8-workload-W.md | byte-exact Rust oracles; the G-rows of this workload in REPORT-honest / RESULT-sgraph | B1-B7 |

### Phase C -- from the 2026-09-08 architecture review

Source: `docs/RESEARCH-BYOK-ECS-DATAFLOW-2026-09-08.md`, seven operator proposals evaluated read-only against
the tree. Net **0 ADOPT-as-stated, 3 ALREADY, 3 NARROW, 2 REFUTE** -- four of seven were, in their load-bearing
half, already in the tree under other names. None is on the A/B critical path; C6 must not be started.

| # | task | blueprint | gate (the number) | depends on |
|---|---|---|---|---|
| C1 | **steps 1+2 LANDED 2026-09-08 (bebop.bin 7649a141); steps 3-4 still need A8.** The hazard is no longer an inference: `probe_hazard.bp` reproduced it end to end -- an unfenced forked child WROTE 987654321 into the parent's store FILE (`hz_a.store`), and with the fence the child died with one trap 82 and the file stayed zero. Step 1 turned out to need a compiler change after all: no `mprotect` builtin existed, so `emit_sys_mprotect` (syscall 226, DERIVED from asm-generic/unistd.h, words d2801c48/d4000001 as+objdump-verified) was added and `gb_fence_ro`/`gb_fence_rw` bracket the clone/wait4 in BOTH kernel launchers -- `gb_bg_launch` deliberately NOT fenced, because its child IS the pool's single writer. The fence needs no branch on the mapping's mode, and that is measured, not assumed: `rw_down=0 rw_up=0 ro_down=0 ro_up=-13`, the EACCES being harmless because MAP_SHARED of an O_RDONLY fd carries no VM_MAYWRITE. Step 2's `kernel fn` marker (read backwards from `fpos`, same line only) sets fntab[4643] and `emit_ident` rejects any `sys_` name under it with exit 102. **Byte-identity of the non-dialect path was demonstrated, not asserted:** all 69 pre-existing frozen constructs unchanged, and the marked/unmarked twins of c111 are the same 652 bytes. Constructs c110_fence, c111_kernelfn, neg/c112_kernelsys; trap code 102. Gate numbers RE-DERIVED on the post-dedup base by the main session (bcond 1155 -> 1164, words 39265 -> 39756), NOT copied from the lane, which had measured 1163 -> 1172 before the duplicate-scan deletion landed. **The fence does NOT close B3's trap-82 defect** -- reproduced identically with and without it, 7 trap-82 lines and fold -367455298937407970 both ways -- because that explanation is REFUTED (2026-09-08, B3's fix): it is neither identity nor permission. The `compiler_digest` check exists and is correct; the pool store was a fixed 4 MiB against an advertised 48-kernel cap needing 11.4 MB, `st_alloc` never checked its cursor, and the resulting MISS (0) was dispatched as a Kernel by every call site. C1's own fence is unaffected -- only this sentence was wrong. The `-367455298937407970` observation recorded here was real and is now reproduced as the once-used-then-foreign cell of B3's 4-cell table. What the fence guarantees is that a stale kernel's wild access is now a LOUD trap rather than a silent write into a writer-parent's mapping. Was: **SAFETY row.** Checked kernel dialect: `kernel fn` emits bounds checks (2 words, 1 on power-of-two tables), rejects `sys_*` at parse time, carries a step budget, and the dispatching parent drops its RW mapping before `sys_clone`. Replaces the operator's WASM/eBPF sandbox: the compiler is the verifier, at native speed, ~300 lines, zero dependencies -- against wasm3's 64 KB interpreter (40 % of bebop.bin, and it interprets) or the eBPF verifier's ~30,000 lines (4.3x all of bebop.bp, and kernel eBPF is unreachable from uid 10546). **Confirmed hazard this row closes**, verified from the flags 2026-09-08: `gb_run.bp:246` forks with `sys_clone(17,...)` (SIGCHLD only, no CLONE_VM) and `store.bp:146` maps prot=3 flags=1, so a kernel dispatched by a process holding a writer's mapping can WRITE the store file; the B3 gate is safe only because `gb_pool.bp:229` happens to map read-only. Steps 1-2 (the mapping fence, then `kernel fn` + `sys_*` reject) do not depend on A8 and can land first | docs/blueprints/C1-checked-kernel-dialect.md | 10^4 hostile generated kernels -> 0 SIGSEGV/TRAP-82 and 100 % loud traps; K6 ns/row with checks <= 1.2x without; sgraph2 frontier fold unchanged | steps 3-4 need A8 (typed tables carry the length the check needs); step 1 none |
| C2 | Per-type column storage (`column` vs `object` in the layout), the survivable half of the operator's ECS proposal. **REFUTATION STEP DONE 2026-09-09 and BOTH HALVES OF THE GATE PASS** (probe `bench/vs_rust/std_tests/scolp.bp` + `bench/vs_rust/scolp.sh`, bebop.bin `15e272fc`, core 4, arms interleaved, reproduced by the main session in its own tree to the byte). Two stores hold the SAME 1M three-field rows (row i is a pure function of i in both arms, so every fold is equal by construction -- measured obj == col == python oracle at k=1/2/3: -232035154245 / -888355404241 / -364181805314). SIZE: objects (`R{i64,i64,i64}` + a `T{arr ref R}` table) = **48,008,240 B**, columns (three `CO{arr i64}` + `K`) = **24,008,336 B**, same-shape sqlite (1M rows, 3 INTEGER cols, VACUUM) = **21,057,536 B**. So columns = **1.140x sqlite** against the <= 1.2x gate and objects = 2.280x. The 23,999,904 B delta closes to the byte: **15,999,928 = HEADER AMORTISATION (66.7 %)** -- 1M x 16 B of `store.bp:3-5` header removed, 3 x 16 B added back, +24 B for the per-column length cells -- and **7,999,976 = the `T` ref table disappearing (33.3 %)**, because a row's identity becomes its index. That second third is a real consequence of columns the row did not previously claim. POINT LOOKUP (10^6 ops, medians of interleaved reps): objects **181 / 186 / 195 ns** and columns **64 / 101 / 138 ns** at k=1/2/3 fields read -- **+8 ns per extra field for objects, +37 ns for columns**, so the "k random cache lines" counter-cost is real, linear and measured, but at k=3 columns are still **1.41x FASTER** because the object arm pays a dependent chase through `T` that the column arm replaces with index arithmetic. Crossover k ~ 4.8; the 2x guard is not reached until k ~ 8.7. THREE NUMBERS THIS ROW USED TO CARRY ARE CORRECTED: (1) "today's 450 ns" is stale by 2.6x -- the sbench PK row re-derived on this box is **170 ns** (RESULT-sbench.md already said 190 on 2026-09-07), and 450 made the regression guard nearly three times too permissive; (2) "72.4 MB / 34.1 MB" are the sbench FOUR-field+index table (reproduced exactly: 72,396,960 / 34,074,624), not 1M three-field records; (3) the per-type rule survives but binds only above **k ~ 5 fields read per point lookup** -- no three-field type can trip it. Already SoA in practice (`gb.bp:8-10`); C2 makes it declarable | docs/blueprints/C2-column-storage.md | REFUTATION STEP GREEN (typecheck 0 findings, oracles ok=114 mismatch=0 missing=0, std_golden 114/0, `bebop.bin` untouched). Gate for the LANDED form, restated against re-derived comparators: 1M 3-field records as columns <= 1.2x SAME-SHAPE sqlite (<= 25.3 MB; measured 24.0 MB) and point lookup <= 2x a re-derived object baseline (<= 362 ns; measured 138 ns at k=3), else that type stays an object | A8 (typed tables) for the surface syntax; the refutation step is DONE |
| C3 | **LANDED 2026-09-08, gate MET (`schain`, std_golden gate 114).** Superblock cell 10 = the live generation's commit object `C{gen, ref root, ref prev}`, cell 11 = a FIXED-width 16-cell heads table `H{ref C x8, i64 name x8}` (one object, one constant refmask, 8 branches), plus `st_cobj`/`st_publish`/`st_commit_c`, `st_commit_at`, `st_croot`, `st_head`, `st_heads_set` and `st_open_at(gen)`. Both cells are ZERO in every store a plain `st_commit`/`st_commit_m` writes, so no existing store's bytes change, and `st_open`/`st_pick`/`st_verify`/`st_reopen_verify` never read them -- the chain is written, never read, on the hot path, which is how "live-generation open no slower than today" is met by construction rather than by a timing. The gate walks 1000 generations (one node committed as each generation's root), reopens at gen 1 / 500 / 1000 through the chain and refolds against the folds recorded at write time, checks the live root/generation and the EXACT arena cost (used 10060, live 9018, superseded 18), then compacts: only what the "main"/"keep" heads reach survives (live 4028) and exactly 5008 cells are freed = 998*5 commit objects no head reaches + the 18-cell superseded table -- every freed cell accounted for by an event, none by a scan. okmask 1023 = all 10 checks; oracle bench/oracles/schain.py. Cell 9 left free for B1's `anc`. | docs/blueprints/C3-commit-chain-and-heads.md | MET: schain = 71563930701023 (okmask 1023), invariants GREEN, typecheck census 0 findings | B4 step 3; cell order vs B1's follow-up card |
| C4 | **CONFIRMED FOR SMALL-K VIEWS, REFUTED FOR DENSE ONES, 2026-09-09.** Unblocked by B4 step 2''' which built the tail delta a view is maintained from. Maintenance is `alpha*d + W*K` per commit (d delta edges, K view cells, W = 27 ns/cell from the measured 3.4 ms/MB), so **a view LOSES when C_r < (B/Q)*(alpha*d + W*K)** -- it turns on the commit-to-query RATIO times the per-commit cost, not on the view being clever. **DENSE per-row views (K = n) refuted by arithmetic before building:** whole-view CoW is 2.70 s over the run = **6.2x the entire 435 ms log row**, and even segmented with step 2'''s own trick at the optimum S=11 it is 527 ms = 1.21x, sparing only 10 pct of segments -- the same not-sparse wall as step 2's blocks, 2''s row directory and 2'''s segment ranges, **the FIFTH structure this workload has killed**. **SCALAR views already exist**: G2 cells 1 and 8 are aggregates rewritten every commit inside a root that is rewritten anyway -- K = 0 extra cells, cost zero. BUILT AND GATED: `VW{arr i64}`, K = 1024 bucketed by `w >> 10`, maintained by ONE increment per appended edge inside `tl_gen`'s existing loop and published per commit. **Maintained fold == recomputed fold == 489029643, EQUAL over all 1024 buckets and still equal AFTER tombstones AND compaction**; existing folds exactly the oracle; **maintenance BELOW the 3-rep noise floor** (no-view 404 vs with-view 435 ns/edge, and a second run gave 430 vs 415 with the view arm nominally FASTER -- the runs disagree in direction) against a predicted 1.33 pct; **recompute 5 ms vs view query 5.0 us = 1000x**; store +8,224 B; `scrash_torn` 50/0 with the view on the commit path. **EXPRESSIBILITY WITHOUT CLOSURES:** a view is a layout digest + a root cell + a reduction chosen by ARITHMETIC on the edge, so aggregates over a bounded arithmetic key are expressible NOW, while a filtered projection or a user-supplied reduction body needs a first-class function value and is **blocked on A16, not on this row**. **TWO TRAPS WORTH CARRYING:** G2's refmask must go 764 -> **1788** (bit 10) or `st_compact` never copies the view and leaves cell 10 pointing into the OLD file -- the symptom was a maintained fold of **3554557677048121137**, the superblock magic's neighbourhood, and a TRAP on the read after compaction. **The VIEW needs no ref discipline (values only, refmask 0); the ROOT does, the moment a root cell is added.** And `root_write` was already at bebop's hard 14-parameter limit, so a 15th argument gives exit 100 WITH NO MESSAGE (docs/TRAPS.md:26) and the view is linked by a separate `root_view()` with a re-seal. **TURN POINT, corrected against the lane's own prediction: predicted break-even 0.052 queries, MEASURED 1.15** -- the recompute had been sized against a full-graph traversal (110 ms) when the view's base is the 1e6-edge DELTA (5 ms). Worth it up to **~87 commits per query** | selfhost/std/sgraph2.bp | fold equality maintained == recomputed, before AND after compaction; maintenance below noise; scrash_torn 0/50 | A16 for user-declared view bodies |
| C5 | Chunk-boundary tier/flavour switching (HyPer/Umbra form), generalising two mechanisms already here: B3's tier-0 -> specialised switch at query granularity, and the Beamer push/pull switch that already changes strategy mid-BFS at alpha 14. Mid-loop OSR as proposed is unbuildable today -- `sys_run` must execute in a forked child because entering another image's entry stub reconfigures the caller's arena registers, and there is no frame-layout side table. NOTE, CORRECTED 2026-09-08: `perf_event_paranoid` reads -1 here, but that is **the wrong lever** -- the Android property `security.perf_harden` reads **1**, and that is what makes `perf_event_open` return EACCES. So `swpmu.bp:1-4`'s record is RIGHT about the symptom and this row was wrong to call it stale. Hardware miss counters need `adb shell setprop security.perf_harden 0` from the operator's side, exactly like the phantom-process cap in docs/BOX.md **REFUTED 2026-09-09 BEFORE BUILDING on three measured grounds; nothing built, no tree file changed.** (1) The deciding statistic does NOT vary by position -- `join_twin.bp`'s `gen()` draws `rk[i]` i.i.d. PER ROW, so the skew is in the KEY distribution and every chunk is a fresh sample: heavy-key fraction varies 1.17/2.43/4.82 pct sd at 64/256/1024 chunks **against a flavour difference of 22-33 pct**. (2) No crossover exists -- grouped wins **6 of 6 cells** (n = 250k/500k/1M x u/z, ratios 1.148-1.364, folds equal), so the best possible switch equals the better FIXED flavour minus its own overhead, and the 1.15-1.36x spread is BELOW this row's own 1.5x bar, making the gate unreachable by construction. (3) On the write path it is a WORSE D1: the 30.6x is a property of the DESTINATION, so every chunk must choose RAM, which IS D1 plus extra publishes. Dispatch cost is NOT the blocker (a branch needs >= 15 rows, a pool lookup >= 2259, against a measured 3.32 ns/row scan); **what is, is that the statistic's sd goes as 1/sqrt(N), so reacting faster makes the decision WORSE**. BFS is the exception because its frontier signal spans FIVE TO SIX ORDERS OF MAGNITUDE within one query against the join's 1-5 pct. Revisit only after landing an input CLUSTERED by key; no workload here produces one | docs/blueprints/C5-chunk-boundary-tier-switch.md | on Zipf input: within 1.1x of the better fixed flavour AND better than 1.5x of the worse one; fold identical to the single-flavour run | B3 (the kernel pool), B6 |
| C6 | **CONDITIONAL -- do not start.** A `persist` builtin (`dc cvap` + `dsb`, and `dcpop` is present on all 8 cores here) replacing `sys_msync` at two call sites, IF a DAX-mapped device ever exists. This is all that survives "abolish the WAL for PMEM": there is no WAL, and the 44.7 ms recovery is `st_reopen_verify` scanning the whole arena, whose fix is B1's follow-up card and needs no hardware. "Recovery = 0" is false even on real PMEM (ADR does not flush CPU caches, so a torn multi-word commit is as possible as a torn page; PMDK replays logs at open), no `/dev/pmem*` or NFIT exists here, Optane was cancelled July 2022, and the target nodes are phones | docs/blueprints/C6-pmem-persist-builtin.md | none until the trigger fires: a target node with a DAX device AND a harness that can cut power to it | B1 follow-up card first |

**REFUTED by the same review, with the evidence, so they are not re-proposed:** racing three JIT'd plans
(cardinalities are already exact from `rp[k+1]-rp[k]`, and each fork costs 1.04 ms against a 32-process cap --
the BANDWIDTH half of that refutation is WITHDRAWN 2026-09-08: it claimed one A78 saturates DRAM so three
racing kernels each run 3x slower, and direct measurement shows three A78s scale linearly, 28/28/28 ms against
a solo 28. The refutation stands on fork cost, the process cap and exact cardinalities, not on bandwidth);
whole-plan racing (Rdb/VMS raced an operator, never whole plans); and abolishing a WAL that does not exist
(C6). Also refuted inside C2: raw-byte components (A7's `(off,len)` handle already gives bytes a home) and
"no records" as a doctrine.

### Phase D -- from the 2026-09-08 literature study

Source: `docs/RESEARCH-LITERATURE-2026-09-08.md` and `docs/RESEARCH-CORPUS-IDEAS-2026-09-08.md` -- two
analysts over 85,663 arXiv records (cs.DS, cs.PL, cs.DC, cs.AR, cs.DB) under different lenses. **Neither
ranking carries a citation signal**: the OpenAlex crawl was discarded (topic ids taken from memory, never
resolved; its 195,600 works were ML/AI), so these rows rest on mechanisms and this box's own measurements.
The headline is a rule-OUT: **one-pass codegen has no more gate to give** -- TPDE and copy-and-patch put
single-pass at LLVM -O0 class and bebop's honest rows against `rustc -O` are past that band, so A2b-class
work no longer moves a gate. Every row below is in the storage or scheduling layer instead.

| # | task | blueprint | gate (the number) | depends on |
|---|---|---|---|---|
| D1 | **LANDED 2026-09-08, gate MET at 1.23x against <= 1.3x.** `csr_build` split into `csr_passes` (three counting-sort passes over plain arrays, no store argument) plus a publish in one ascending pass per object. Measured, fold 5001253080071 identical in every arm: store 9,119 ms, plain 1,086, **ram 1,339** -- and the residual accounts exactly, 1,319 - 1,071 = 248 ms observed against 243 publish + 7 seal = 250. G8 stage 2 build L1 **1,270 ms against a 7,165 ms same-box baseline (5.3x)**, 11.8-12.2x against the committed RESULT-sgraph rows, with compaction sizes BYTE-IDENTICAL so the arena layout did not move. Two things the port taught: (i) the sequential copy pays a SMALLER version of the same tax -- anon copy 13 ms, store copy 218 cold / 116 warm, while a crc32 READ of the same 88 MB through the same mapping is 8 ms, so **the mapping reads at anonymous speed and only WRITES are taxed**; cold-warm is 4.7 us/page (first touch) and the remaining ~103 ms is a per-write tax that survives it. (ii) the naive one-function port read 1.48-1.51x because `base`/`tmp`/`rp`/`ci` are live-out into the publish and the fill loop's inductions SPILL -- plain-array fill 820 ms against a 592 ms control for byte-identical work. Splitting the function fixed it, and that is why the split is load-bearing rather than cosmetic. **Floors, measured:** 1.21x as designed (today is within 2 %, no tuning left) and **1.019x if the file is never mapped writable** -- a 20-point gap that IS the MAP_SHARED write path, and the strongest quantitative case for §1b short of building it. Was: **Build in RAM, publish sequentially.** Its mechanism is MEASURED, not argued: R1 isolated B2's "96 % in the store's page path" to one flag by single-variable elimination -- first touch refuted by 132x on arithmetic, the filesystem refuted by inversion (tmpfs 36.5x vs f2fs 12.8x, the RAM-backed store being 3x SLOWER), and then MAP_PRIVATE 1,239 ms against MAP_SHARED 27,404 ms back to back, identical fold. So the cost is dirty-page tracking on a shared writable file mapping, and MAP_PRIVATE lands within 2x of plain arrays. Build `rp`/`ci` in anonymous memory, copy into `st_alloc`'d objects in ONE sequential pass. The thesis sentence is preserved for every READ and narrowed for exactly one write shape, bulk construction -- which is the design of LMDB, SQLite-WAL, every LSM and Datomic, the survivors LANG-DB §1 lists | docs/blueprints/D1-build-in-ram-publish-sequentially.md | store arm <= 1.3x plain arm on the 1M/10M build (<= ~1 s where today is 18 s), fold identical | none; supersedes part of G8's 44.8 s CSR row and unblocks B8's ingest |
| D2 | **REFUTED 2026-09-08 by its own kill line, and the `prefetch` builtin is dropped as a NET COST.** Measured n=1M, R=15 full-process runs, FOUR probe arms interleaved inside one binary, medians, one A78 core, folds equal to `join_twin.bp`'s throughout (u 8496861157178 / 207305104418158, z 83965256206435 / 1968676132101021). ns per R row, uniform / zipf: plain probe **149 / 476**; D2 as specified (group-pipelined G=8 + `prfm pldl1keep`) **122 / 376**; same template with the hint removed **106 / 360**; a SURGICAL control differing from the D2 arm by exactly the three `prfm`+`mov` word pairs **108 / 364**; Rust `HashMap` probe phase **287 / 558**. So (i) the row's kill line -- "if the probe phase does not drop by >= 25 %, this row dies" -- is UNMET at 18.1 % / 21.0 %; (ii) the **hint itself costs 13.0 % uniform** (reproduced at 12.5 % in an earlier R=15 run) and 3.3 % zipf, i.e. the A78's own reorder window was already doing a `pldl1keep`'s work and the builtin only adds words to the loop; (iii) the `<= 1.2x` gate was never D2's to win -- the COMMITTED plain probe already passes it at **0.52x / 0.85x**. What survives is the RESTRUCTURING, which needs no compiler at all: unroll by G=8 and stage `lo/hi` through a caller-owned `buf` so eight independent misses overlap instead of three dependent ones, **+28.4 % / +25.2 % rebuilt on the committed `bebop.bin` 0ae01bd3** -- clearing the 25 % line on both distributions at 0.38x / 0.65x of the Rust hash probe. Landed as `bench/vs_rust/std_tests/join_twin_grouped.bp` (both arms in one binary, one compiler, one build, one store layout) plus `bench/vs_rust/rust_once/join_hash_split.rs`, which times the Rust BUILD and PROBE halves separately -- without it this gate's denominator is not reproducible. NOT measured, and the row's one open half: the frontier-BFS side (`ns/edge-slot <= 0.6x of 45` on sgraph2). E2 already refuted its per-slot-branch premise; after this result a `prfm` win there is unlikely on the same core, but it is untested. Was: **Memory-level parallelism in the generated probe kernels** -- one 1-word `prefetch` builtin plus an unroll-by-G group template in `gen_gb.bp`, payoff PREDICTED from x86 servers with large L3s and unknown on a 3-core A78 | docs/blueprints/D2-mlp-probe-kernels.md | probe ns per R row <= 1.2x the Rust `HashMap` twin's probe (this is what decides B2 (i) `>= 0.7x best Rust`); frontier ns/edge-slot <= 0.6x of today's 45, fold unchanged | B3's templates; B2's twins for the gate |
| D3 | **REFUTED 2026-09-08 by its own kill test, and the relabelling demonstrably LANDED -- the time just did not follow.** The blueprint's §5 kill line was `< 20 % move => the row dies`. Measured on the 1M/10M sgraph2 graph with NO store change (vertex ids assigned in whole-graph BFS rank by permuting the generated pair list before the CSR build), two arms interleaved in ONE pinned slot run: frontier **25.4 -> 24.8 ns/edge-slot, B/A 0.976, a 2.4 % move** against a gate that wanted <= 0.7x; the queue BFS moved 4.9 % (116.0 -> 110.3). Folds identical in both arms and both kernels (21482396), store size 89259184 both, and the fold is invariant by construction -- the 8 BFS sources are made fixed points of pi, so source s denotes the same vertex in both arms. **The relabelling is not the thing that failed:** mean neighbour rank distance fell 15.0 % (333298 -> 283420) and the fraction of slots within 4096 ranks rose 11.0 % (8153 -> 9053 ppm). The mechanism is that the blueprint aimed its TLB argument at the 10M-slot `ci` array, which BOTH kernels stream SEQUENTIALLY in CSR row order -- relabelling cannot make a sequential stream more sequential. The only random-access structure is the visit state, and in the frontier kernel that is a bitmap of 1M bits = 125 KB, already L2-resident, so there is no TLB pressure to relieve; the queue kernel's `level[]` is 8 MB and genuinely TLB-relevant, which is exactly why it moved twice as much and still only a quarter of the kill line. A uniform ER graph (avg degree 10, diameter ~7) also has no community structure to exploit -- by BFS level 3 the frontier spans nearly all 1M vertices, so a BFS rank puts a vertex's neighbours in the same huge level block, not near it, which is the 2001.08448 warning the blueprint itself quoted. Step 2 (the `st_compact` permutation) was never started and must not be. The measurement harness is parked at docs/blueprints/attic/D3-bfs-relabel-killtest-on-5df5037.patch, not merged: it is 190 lines of relabel/locality phases in a gate source, and keeping it would cost a full std_golden run for a dead row. | docs/blueprints/D3-locality-relabel-at-compaction.md | REFUTED: 2.4 % against a 20 % kill line; the <= 0.7x gate is unreachable from 0.976x | -- |
| D4 | **WRITE SIDE MEASURED 2026-09-09 AND REFUTED ON THE GATED ROW; the size and stall halves CONFIRMED. NOT LANDED.** After the traversal re-target (random gather 133 ms i64 vs 128 ms packed = 1.06x, nothing), the untested claim was the write side: sgraph2's L0 is `rp0` (n+2 entries) and `ci0` (one per edge), both pure indices at n = 10^6 with three orders of magnitude of headroom. Built and measured: `u32_pack`/`u32_at` as value-level helpers in `selfhost/prelude/store.bp` (the ref stays inside `st_get`/`st_put`, so typecheck rung vii needs no new REF_TOLERANT name), L0 published as `RP32{arr u32}`/`CI32{arr u32}` with G2 cells 5/6 retyped, `st_compact` unchanged because a u32 array has refmask 0. Three interleaved A/B runs: **store 105,279,056 -> 97,269,672 B = -8,009,384, EXACTLY the predicted -8.0 MB to the byte; max batch stall ~100 -> ~59 ms (-40 pct), better than the predicted 76; and the LOG ROW 396 -> 508 ns/edge (+28 pct) against a predicted -11 pct**, ratio 1.28-1.33 stable across three runs, nbr in the noise as predicted and bfs 3-19 pct slower. Folds byte-identical to `bench/oracles/sgraph2.py`; typecheck 0, invariants GREEN, oracles mismatch=0, std_golden 114/0 under BEBOP_MEMO=0. Decomposition by the stall ratio: promotion fell 128 -> 82 ns/edge EXACTLY as the arithmetic said, and the FLOOR rose 158 ns/edge -- `l0_load` reads L0 back ~3e6 times per run to rebuild the host mirror, ~53 ns per unpacked entry, and INLINING the helper at those sites did not move the ratio, so the cost is the shift-and-mask arithmetic and not call overhead. **THE RULE, now measured on both sides: sub-32-bit pays where an array is written once and read rarely (the store SIZE row) and loses where it is read back per promotion (the log row).** So D4 has failed twice for the same reason. Answering the question it was sent with: the store's general cells CANNOT be u32 without A8 -- `st_get`/`st_put` stay the i64 cell accessors and every other layout keeps i64 cells -- but ONE declared array type can, which is what was built. **The 158 ns/edge is a measured PRECONDITION for A8**: a typed u32 column needs its unpack EMITTED BY CODEGEN as part of the access, not written in source, and that is the price of doing it in source. The helpers are deliberately NOT landed, because unused prelude fns cost words in every store program; they are in the lane record and land with A8 | selfhost/prelude/store.bp (u32_pack/u32_at, when A8 needs them) | traversal 1.06x REFUTED; write side -8.0 MB and -40 pct stall CONFIRMED; log row +28 pct REFUTED, so nothing lands until A8 emits the unpack | A8 for the typed-column form |
| D5 | **The trust chain (revives T89, OPEN since TASKS.md:100). DOCUMENTATION LANDED 2026-09-09; the DDC half is REFUTED as cheap, and the row's own numbers were stale in both directions.** Re-derived on this box and confirmed independently by the main session: `seed/build/seed` is 1,480 B of ELF around **488 B of executed `.text`** (not "1,480 bytes"), loading a **170,312-byte** `bebop.bin` (not 159,196) checked into git. So the trust root is a committed binary plus CPython for the bpref oracle -- not 159 lines of assembly -- and `gen3 == gen4` is a reproducibility gate, not a trust gate: Thompson survives any fixpoint by construction. **The witness does NOT exist in working order, and the row pointed at the wrong path.** There is no `selfhost/expr_compile.bp`; it is `selfhost/attic/expr_compile.bp`, RETIRED 2026-09-04 by T45, and it targets the retired `exec_words` stack machine -- on `fn main()->i64{42}` it emits 92 bytes that push the result and never pop, leaving `sp` 16 B low across the epilogue, so `ldp` reloads garbage and seed dies SIGBUS. The project`s own success at retiring the stack machine (invariants.sh step (ix) gates `push_words == 0`) is what killed its DDC witness. Measured: **1 of 75 frozen constructs agree, 74 diverge** -- and the one agreement is `c90_symalias` with frozen EXPECT=0, which a broken binary also yields, so the honest count of witnessed constructs is **ZERO**. Two-stage DDC of `bebop.bp` was NOT ATTEMPTED at the time and is NOT impossible (L23): the witness had no `use` handling while `bebop.bp` line 1 is a `use`, and `ec_driver.bp` sized its fn table `zeros(256)` against 287 fns unchecked (rc=124, 600 s timeout). D5b then repaired all three -- `use` is costed at 60-80 lines of driver-level `sys_readbuf` splicing, the tables are raised to 1024, and ONE line to `emit_epilogue` took the witness from 0 to 46 of 73 non-vacuous agreements. `tools/bpref.py` cannot substitute -- `bpref.py:659` stubs every `sys_*` to 0, so it can neither read a source file nor write a `.bin`. **The row also UNDERSTATED what already exists:** `bench/vs_rust/invariants.sh` step (viii) ALREADY rebuilds `seed.S` via `as | ld -static | objcopy -j .text` and `cmp`s it against the committed seed, so seed.S -> seed is already reproducible from source and already gated; `docs/ANALYSIS-2026-09-06.md:165` ("no gate rebuilds it and compares") is stale. **And this blocker was already written down before the row was:** `docs/ROADMAP-AUDIT-2026-09-04.md:316` blocks T89 with "witness compiler must compile current surface -- it does not: F-D". CPython stays in the trust root unchanged: `bpref.py`, `typecheck.py`, `census.py`, `seed/pack.py` | docs/TRUST-CHAIN.md; docs/blueprints/D5-trust-chain-ddc.md | `docs/TRUST-CHAIN.md` names every artifact hash from seed.S to the promoted binary AND states what is NOT witnessed; `tools/ddc.sh --gate` prints the measurement and exits RED with `DDC: NOT ESTABLISHED` (deliberately NOT wired into battery.sh or std_golden.sh, or it would fail every run) | reviving the witness is the real cost and is its own row: repair the calling contract, add `use`, raise the fn table past 287, port every post-T45 builtin |
| D5b | **Revive the diverse witness -- D5's named real cost, and load-bearing for Phase F: the certificate checker is ONE program compiled by ONE compiler, so Thompson applies to it directly and a working diverse witness is the second executor that closes exactly that gap.** Steps 1, 2 and 4 LANDED 2026-09-09, measured after every step with no regression at any: non-vacuous **0 -> 46 of 73** (raw 1 -> 47 of 76; the main session reproduces 46/73 where the lane measured 46/72, because A6 step 2A added `c67_deeprec` to the frozen set). Step 1 was ONE LINE: `emit_epilogue` never popped the body's value off the eval stack, so the teardown ran 16 B low and `ret` branched into the result -- that single `pop` took the witness from SIGBUS on `fn main()->i64{42}` to 43 non-vacuous agreements, and the old lone agreement `c90_symalias` (frozen EXPECT=0) correctly became a divergence, confirming from the other side that it was worth nothing. Step 2 raised the fn tables 256 -> 1024 -- the cap was WRONG in both places against `bebop.bp`'s 287 fns, so the witness could never have read its target even with `use` -- and gave `ec_driver.bp` the loud trap `compile_program_to` already had, one trap and not two. Step 4 added unary `-`/`!`, `0x` hex, `clz` and `>>>`, and fixed `emit_array_lit`'s allocation order (it stored every element at `[x14 + i*8]` and bumped x14 only after the loop, so a nested literal overwrote the outer elements -- exactly the defect `c31_nested_lit` was frozen to catch, still live in the witness); six new words assembler-verified by `as` + `objdump`. `>>>` is reported as a NET ZERO and bought no construct. **Repair beat rewrite on a number:** one line bought 43 agreements where a fresh witness restarts at 0 for the same remaining work (Camlboot's comparable job was ~1 person-month, arXiv:2202.09231). Step 3 (`use`) is scoped and costed but NOT done: ~60-80 lines of driver-level `sys_readbuf` splicing, because the witness scans a `str` and Bebop has no concatenation -- and doing it in the harness would move a language feature into the test rig. **The 16 frame/spill divergences are left STANDING ON PURPOSE**, with the reason written into the document: closing them requires importing the register model, and a construct made to agree that way agrees FALSELY -- it stops being independent evidence about the model under test | docs/TRUST-CHAIN.md §4a (per-step ledger), §4b (what was deliberately not repaired), §7.2a (`use` design and cost) | `tools/ddc.sh --measure` reports raw AND non-vacuous and the non-vacuous number is the one quoted; `--gate` stays RED and OUT of `battery.sh` until it is total | the ceiling of model-preserving repair is now visible and it is the right ceiling: **46/73 with the witness's own stack machine is worth more than 73/73 with the register model imported.** Remaining: `use`, then `return`/`break`, then crc32/crc32x/cas/sys_run/enum payload -- never the frame model |

**Order: D1 (the measured 96 %) LANDED, D2 REFUTED (its surviving half landed without a compiler change), then D5 (documentation-only until the witness runs), then D3/D4 once B4 and A8 exist.**

**Flagged for B7's owner, not a row:** worst-case-optimal / leapfrog joins over sorted CSR (1503.04169, 2301.10841) are a competing ALGORITHM class -- triangle counting as a three-way sorted intersection against `gb_tc`'s masked SpGEMM, same graph. **Two footnotes with no row:** a magic-multiply peephole for division by a literal (1.67-1.98x, ~150 words, one `vs_try_*` arm; `itoa`/`parse` divide by 10 with `sdiv`, so it is measurable on K5), and a rank/select index over the tombstone bitmaps (2605.25528, ARM64-engineered, "1.4x speedup in rank queries", ~80 lines) -- worth it only if a profile shows the bitmap-to-CSR conversion.

### Phase E -- from the 2026-09-08 wild-ideas study

Source: `docs/RESEARCH-WILD-IDEAS-2026-09-08.md`. Its most useful output was a CORRECTION, not an idea: it challenged this session's own R1 attribution and the challenge held (see the DRAM row and D1). Ordered by expected surprise per unit of cost to check.

| # | task | blueprint | gate (the number) | depends on |
|---|---|---|---|---|
| E1 | **LANDED 2026-09-08, folds MET and the wall gate UNMET at 8.6x against <= 4x -- and the miss is the BASELINE's, not the mechanism's.** `gen_gb.bp` gains op 6 `mbfs`: ONE i64 frontier cell per vertex, bit `s` = "v is in source s's frontier", push `nx[u] \|= fr[v]` under (OR, AND), settle/count by SWAR popcount once per discovered cell -- plus a `gb_mbfs_gen` golden (65342 = 64 SEPARATE queue-BFS folds, driver `bench/vs_rust/std_tests/gb_mbfs_gen.bp`, oracle `bench/oracles/gb_mbfs_gen.py`). Measured on 351c288, so E2's branchless push is underneath the baseline: n = 10^6 / 10^7 CSR slots, SHIPPED generated kernels, process-included wall, core 4 in `tools/slot.sh`, 3 interleaved reps, fold **462119596 identical in every arm** (and 65342 across oracle / driver / op6 S=64 / op5 x64 at n=1000, with op6 S=1 == op5 single == 1003). op6 S=64 **2730/2776/2898 ms** against 64 separate op5 runs at **17318/17265/17316** (**6.24x**) and against ONE op5 run at **348/322/308** (**8.6x**, where the gate needs <= 4x). The bit-parallel MECHANISM is not what fails -- it OVER-delivers, and the diagnostic says so exactly: with the pull direction disabled on both sides (alpha = 0) the same kernel runs 763 ms at S=1 and 2583 ms at S=64, i.e. **64 traversals for 3.39x the time, an 18.9x sharing win, ABOVE the gate's own 16x**. What the gate actually measures is the one thing 64 sources cannot have: DIRECTION OPTIMISATION. A single-source pull stops at the FIRST frontier neighbour; a 64-source pull must keep reading until every still-missing source's bit turns up, so it degenerates to a full row scan. That is worth **3.36x** to op5 (227 ms in-process against this kernel's 763 ms push-only S=1), and 18.9 / 3.36 = 5.6x accounts for the measured 4.9x (in-process) to 6.2x (wall). **So the row's gate charges the mechanism for Beamer's win**: against 64 separate traversals the op is a clean 6.2x, against one Beamer traversal it MEASURES 8.6x and the gate's 4x is not met by this mechanism, and both statements are the same measurement. alpha = 2 ships, not op5's 14, on its own grid (3 reps, n = 10^6): alpha 2 beats alpha 14 at EVERY S (S=1 228 against 918 ms, S=4 615 against 1778, S=16 2310 against 3662, S=64 2877 against 4117) and beats alpha 0 by 3.3x at S=1 (228 against 763) while giving up 11 % to it at S=64 (2877 against 2583) -- pull is worth almost nothing once S is large, exactly as the mechanism predicts. Was: **64-source BFS in one i64 cell.** The G8 row is literally "100 sources", and today that is 100 separate traversals each paying its own cache miss per edge. Make the frontier ONE i64 per vertex with bit `s` = "reached from source s"; push becomes `nx[u] \|= fr[v]` under the (OR, AND) semiring -- one more `gen_gb` instance, no compiler change. **64 traversals then share every miss.** This is the first place the "every cell is exactly 8 bytes" constraint becomes a measured ADVANTAGE rather than a cost, and it attacks the traversal wall the right way: a gather is latency-bound at 133 ns against a 0.67 ns traffic floor, so the lever is doing more work per miss, not fewer bytes | (inline; `gen_gb.bp` op instance) | 64-source BFS wall <= 4x a single-source BFS on the same graph (i.e. >= 16x the per-source cost of running them separately), folds identical to 64 separate runs | B3's generator |
| E2 | **LANDED 2026-09-08 -- and its diagnostic REFUTED half of D2's premise.** `gbfs_push` is now an unconditional `nx |= bit` with a new `gbfs_settle` doing `nx &= ~vis; vis |= nx` plus a Kernighan drain once per level. Folds identical. The worker ran THREE variants, not two, and the third decided it -- push-only, 4 sources: A (today, two data-dependent branches around two stores) 31.13 ns/slot; B (E2, one unconditional write, `vis` never touched per slot) **26.25**; C (branch-free but STILL touching both lines) **38.42**. So removing the branches while keeping both line touches is **23.4 % SLOWER**: the branch is load-bearing as a FILTER, suppressing stores in the common already-visited case, and predicting it is cheaper than doing them. The whole E2 win is dropping the `vis` line -- B beats A by 15.7 % and C by 31.7 %. **Therefore D2's misses-in-flight premise survives and its per-slot-branch premise is refuted.** Honest caveat: in the SHIPPED Beamer configuration (alpha=14) the move is 1.4 %, inside a +-2 % spread, because alpha routes most edges to `pull`, which all three variants share unchanged. Was: **Branchless push: always write, subtract later.** The generated `gbfs_push` inner loop has two data-dependent branches per slot, and they are STORES, so A2's csel cannot apply. Replace with an unconditional `nx \|= bit`, then `nx &= ~vis; vis \|= nx; popcount` once per level (~30 us). Twelve lines, same fold. Its value is partly diagnostic: the result says whether misses-per-frontier-vertex or per-slot branches bind, **before** anyone adds a `prfm` under D2 | (inline) | frontier ns/edge-slot improves, fold identical; if it does not move, D2's premise is the surviving explanation | B3's generator |
| E3 | **REFUTED 2026-09-08 by its own gate -- the bits are never set, and the cause is architectural.** The row's own warning was the right one: `/proc/self/clear_refs` accepts `4` because `CLEAR_REFS_SOFT_DIRTY` passes the handler's range check whether or not `CONFIG_MEM_SOFT_DIRTY` sets any PTE bit. Measured over the 2x2 at 64 MiB (16384 pages) per arm, writing exactly 1/3 of pages after `clear_refs=4`: **false negatives 100.0 %, false positives 0.0 % in ALL FOUR quadrants** (MAP_PRIVATE/MAP_SHARED x ANON/file-backed), soft-dirty count 0 everywhere. The negative binds because the POSITIVE CONTROL passes: `clear_refs` type 1 moves smaps `Referenced:` **2048 -> 0 -> 512 kB** after touching exactly 1/4 of a 2048 kB mapping, and type `9` returns EINVAL while `1` and `4` are accepted -- so the write reaches the real kernel handler and proot is NOT swallowing it. Loopholes closed: pagemap indexing verified by a `[mapped, HOLE, mapped]` triple reading present bits `[1,0,1]`; no vendor-relocated flag (over 4096 pages the set of distinct pagemap entries is a SINGLE value, `0x8100000000000000` -- no bit in 48..62 separates written from unwritten); 11,680 pages scanned address-space-wide with 0 carrying bit 55; `VmFlags` advertises `sd` (VM_SOFTDIRTY) on **0 of 51** VMAs. Cause: aarch64 does not select `ARCH_SUPPORTS_SOFT_DIRTY` -- there is no spare PTE bit, and the feature is x86/s390 (inferred from the measurements; `/proc/config.gz` is permission-denied here). **So this is neither an operator setting nor a proot artefact**: unlike the durable-commit row there is no "re-take it outside proot" escape hatch, because it is the same kernel and the same PTEs and the type-1 control proves the proot passes these files through faithfully. Reproduced by the main session in its own tree, numbers identical. **What stays open, now unowned:** the case E3 existed to cover -- in-place tombstone-bitmap writes, which D1's stronger form cannot reach -- has NO user-space dirty-tracking mechanism on this box; the remaining candidates are in-process bookkeeping (a dirty-page set the store's own writer maintains, paying a branch per write instead of a fault per page), which is a new row, not this one. Probe: `bench/e3_softdirty_probe.py` (self-contained, ~1 s, prints all five sections including the control). Was: **Soft-dirty as user-space dirty tracking.** Probed 2026-09-08 and available: `/proc/self/clear_refs` accepts `4` and `/proc/self/pagemap` is readable from this proot. That gives write-protect-once-per-page on the process's own terms -- one fault per page per COMMIT instead of whatever the MAP_SHARED path does per STORE. It covers the case D1's stronger form cannot: writes that are not appends, i.e. the tombstone bitmaps flipped in place. **Verify first that the bits are actually set** -- `clear_refs` accepting the write does not prove `CONFIG_MEM_SOFT_DIRTY` is on, and ten lines of python settle it | (inline) | a commit's dirty set read back from pagemap equals `[old_tail, new_tail)` plus exactly the bitmap pages touched | D1's stronger form first |
| E4 | **Per-column JIT'd decoders, bit widths as `ubfx` immediates.** D4 stalls at 16 bits because a scalar decoder costs more cycles than the bytes it saves. But this compiler generates code at runtime: a column whose values fit `w` bits gets a decoder with `w` baked in as an immediate, not read from a header. That moves the decode ridge below 16 bits **without NEON**, which is what D4 is waiting for | (inline; `gen_gb.bp` + A8) | bytes/slot <= 3 with ns/slot not above A8's u32, fold identical -- the same gate as D4, reached earlier | A8, D4 |

**Two operator-side items found by probing:** (a) `security.perf_harden` reads 1 and is what returns EACCES from `perf_event_open` -- `perf_event_paranoid` = -1 is the wrong lever (C5 corrected); miss counters need `adb shell setprop security.perf_harden 0`. (b) The losing durable-commit row (0.15x vs sqlite WAL) may be a PROOT ARTEFACT -- `bebop.bin` is static and libc-free and `docs/ANDROID.md` says it runs natively, so re-take it in plain Termux before believing it.

**Discarded with a reason** (details in that document's §8), so a week is not spent on them: f2fs transparent compression (`/data` has no `compress_algorithm`); mTHP / 64 KB folios (THP `[never]`, and proot fakes the kernel version); userfaultfd paging (a per-fault round trip -- the 22x made explicit, not removed); a GC read barrier for incremental compaction (two words per deref forever, to shave a stall a fork already removes); the branch predictor as an index (readout costs more than the L2 hit it replaces); batch/erasure codes, k^2-trees, bit-sliced values, hyperdimensional adjacency, BFS by matrix squaring; superoptimisation validated by testing (the honesty floor forbids it).

### Phase F -- verification of the WHOLE language (operator, 2026-09-09: binding, no shortcuts; runs BEFORE B5 and B7). **CERTIFICATE FORMAT DECIDED 2026-09-09: our own, end to end.** 11.3 a custom minimal format, NOT LRAT; 12.1 QF_ABV bit-blasted to SAT (carry makes LIA unsound, arrays eliminated in the VC generator); 13.3 our own term format for the structural layer immediately, not Lean-exported terms; 14.3 both encodings, text as exchange and binary as a derived cache; 15.1 AND 15.3 -- a sha256 of the obligation inside each certificate AND a Merkle root over a commit's obligations, so replay is impossible and the gate is ONE number. **Consequence, stated once: Lean cannot emit our format, so it is DEMOTED from producer to cross-check oracle** -- Bebop produces the proof, Lean independently confirms the theorem, disagreement is a bug in one of them. Thinner scaffolding, bought deliberately for self-sufficiency. Source: `docs/RESEARCH-VERIFICATION-2026-09-09.md`. The ladder is a progress counter to 100 %, never a reduced target: 86/86 constructs, 36/36 builtins, 20/20 traps.
| # | task | blueprint | gate (the number) | depends on |
|---|---|---|---|---|
| F0 | **LANDED 2026-09-09 -- the language surface is now complete and CHECKABLE, which is the precondition for "the whole language" being a number anyone can verify.** The authority is the compiler itself: `emit_call_or_ctor` dispatches every builtin on `read_ident`'s 131-rolling hash, so its dispatch arms ARE the surface and nothing can enter the language without appearing there. `tools/builtin_surface.py` recovers names BY HASH, never by munging the emitter symbol (the symbol only proposes candidates and the hash picks the winner); an arm matching no candidate is UNRESOLVED and fails. Today: **36 arms, 36 resolved, 0 unresolved.** The four lists that are supposed to describe this language had never been diffed and disagreed in four directions: compiler 36, typecheck `BUILTIN` 35 (missing `scan`, now added with the signature taken from the emitter's own comment), bpref 34 (`hvham`/`hvham2` have NO case at all), LANGUAGE.md 31 (missing `scan`, `sys_fsync`, `sys_mprotect`, `sys_run`, `sys_wait4`, and listing `sys_exit_thread_guard` twice). Two numbers corrected against the Fable study by re-measurement: bpref implements **9**, not 10 -- `clock_ms` has an explicit name test but shares the `or name.startswith('sys_')` arm that RETURNS 0, so it is a stub, and 9 + 25 + 2 = 36 is arithmetically forced. **A finding not looked for:** seven builtins are absent from the T122 reserved-word table, and the consequence was probed rather than asserted -- `fn clz(x: i64) -> i64 { 5 }` with `fn main() { clz(8) }` compiles rc=0 and returns **60**, the builtin, because builtin dispatch runs before user-fn lookup, so the user's function becomes silently dead code where T122 intends exit 99. That is a shadowed USER FN, and under the agentic rule it is a defect of the language, not of the author | tools/builtin_surface.py | `builtins known to all four: 36 of 36` wired as a std_golden gate. **34 of 36 today**, so it stays STANDALONE and is deliberately NOT wired in -- wiring it now would make the battery RED on a real gap, and papering over a gap to get a green is the one thing this phase exists to stop | blocked on bpref implementing `hvham`/`hvham2`; and 25 of 36 builtins are bpref STUBS returning 0, so "every builtin verified" cannot lean on bpref for those 25 -- they need native goldens, which is a scoping fact for F4, not a defect |
| F1 | **LANDED 2026-09-09. The census is a script that RE-DERIVES, because the document it was meant to transcribe was not in the tree** -- `docs/blueprints/F0-trap-census.md` does not exist and is not invented here, and `docs/RESEARCH-VERIFICATION-2026-09-09.md` was absent until this commit added it (it had been written into an OUT directory and never moved in). So "24 rows / 20 open / 16 zero-word" was never checkable against its source even in principle. `tools/trap_census.py` derives instead, on every run, from `docs/WORKER-CARD.md`, `docs/LANGUAGE.md`, `docs/TRAPS.md`, `bebop.bp` and `bench/parity_constructs/neg/`: **trap_unrep 10/29, trap_zero_word 16 of 19 open, trap_needs_words 3 of 19, undef_census 2**. Two of the report's four numbers verify EXACTLY (16 zero-word, 2 undefined); open is 19 not 20 and rows 29 not 24, and the census NAMES the three rows it excludes as not-language-defects. A row counts CLOSED only with both a landed mechanism AND a `neg/` construct -- a mechanism with no neg is one refactor from vanishing. All three word-costing rows are ONE defect wearing three hats: unchecked array bounds. **THE FINDING THAT BLOCKS F2: the exit-code space is not partitioned.** `vs_mask_take`/`free` and `vs_cs_take`/`free` (`bebop.bp:2271-2295`) exit `sys_exit(99/100/119/120 + site)` for a caller-chosen `site` in 1..20, so the compiler can emit ANY code in 99..140, aliasing every documented diagnostic; 102 and 104 each mean two things today; 103/105/106/107 are emitted and documented NOWHERE, against a table whose first line says a code not in it is a bug. F2 assigns 105/106/107 to new user diagnostics and all three are taken. Free codes below 99: 65-79, 84, 85, 86, 93 = nineteen against F2's twelve. Builtin shadowing was PROBED not inherited: `fn char` gives exit 99 but `fn clz` compiles rc=0 and `clz(8)` returns 60, so T122's mechanism is CORRECT and its table is short by exactly seven of 36 -- and comparing the literals as written reports 16, because `bebop.bp` spells the same hash SIGNED in the reserved table and UNSIGNED in the dispatch ladder. `docs/TRAPS.md` corrected against `bebop.bp`'s own table: fn cap **512** not 256, nesting cap **512 at fntab[2800..4335]** not 128. The `sgraph2` twin is argued OUT of the census as a harness defect, on the principle that **a count which admits anything that ever cost someone an hour measures nothing** | tools/trap_census.py; tools/undef_census.py; docs/TRAPS.md; docs/RESEARCH-VERIFICATION-2026-09-09.md | `trap_unrep: 10/29` -> 29/29; the zero-word/needs-words split; `shadowable_builtins: 7 of 36` -> 0; `undef_census: 2` -> 0. **NOT wired into battery.sh**: at 10/29 it would make the battery RED on a real gap, and the F0 precedent of holding `builtin_surface.py` out at 34/36 is the right one | the `gen_avoid` counter CANNOT MOVE as worded -- the string `avoid` occurs zero times in `bench/fuzz/gen.py`, the avoidance there is structural not lexical, and the number is reported PROVISIONAL until `# LANG-AVOID:` markers land: one line of convention, not more grepping |
| F2 | the 16 zero-word traps made unrepresentable in the planning scan: definite assignment as one i64 bitmask over the 64-symbol cap (exit 105 read of a never-assigned symbol), loop-literal escape rejected via `loop_alloc_safe` (106; verify A6's current per-iteration status first), `zeros` in a `while` body (107), > 8 live symbols across `sys_clone` (108), true message for the nesting cap at exit 95's sites, `&&`/`||` rejected or short-circuited (112), unresolved call at compile time via `fntab_lookup` at end of program (115, deletes the `brk #87` word), literal `sys_clone(_, 0)` (116), `use` off column 0 (118), **ARGUMENT-COUNT MISMATCH AT A CALL SITE (code 93, free today per this row's own list; operator 2026-09-13 folded it here rather than into a row of its own) -- MEASURED, not proposed:** `fn f(a: i64, b: i64, c: i64) -> i64 { a * 100 + b * 10 + c }` called as `f(1, 2, 3, 4, 5)` COMPILES rc=0 and RUNS, printing **123**; the extra arguments are silently discarded with no diagnostic. It is the same loud-failure class as F8 step 0's contract garbage and F0's builtin shadowing, and unlike those two it is already MISCOMPILING code in the tree: `selfhost/std/qdsl.bp:223,236,265,287` call `qdsl_cc(s, p, c0, c1, c2, c3, kl)` -- SEVEN parameters -- with EIGHT arguments, so `kl` has been receiving the fifth CHARACTER of the keyword (101 for `where`, 112 for `group`) instead of the intended length 5 for as long as the production query parser has existed, and nothing ever said so. A too-FEW-arguments call is the same check and reads the callee's uninitialised registers. Note the cost is not just the check: `collect_fns` records fn names but not arities today, so this needs the arity table A21 also specifies -- **land them together or the table is built twice**; positions on every remaining exit-89 site; one `neg/` construct per code (`EXPECT=COMPILEFAIL:<code>`), gen.py stops avoiding each rejected shape; ~300 lines total, ~3 weeks, bebop.bp lane | docs/blueprints/F1-unrepresentable-zero-word.md | `trap_unrep >= 12/20`; construct parity 86+12/0; fixpoint gen3 == gen4; bin_words delta <= 0 (one `brk #87` word removed) with a word_budget line; `gen_avoid` shrinks by 12; battery GREEN | F0 |
| F3 | `[T]` with length in A8 (this row amends A8's gate, it does not replace A8): arm (i) header cell at `data-1` written by `emit_zeros` and the 4 aggregate sites, check `sub ; ldr ; cmp ; b.hs` (4 words register index, 3 literal) to new `trap 84` with the pc in the stub text; store-level `st_len` check in `st_get`/`st_put` and a cursor check in `st_alloc`; cell stepping (`arr + k`) census then made UNREPRESENTABLE (typecheck finding + rewrite of the stepping sites), because bpref's `Cells(off+k, n-k)` is a fat value native cannot carry; `unchecked fn` keyword counted, `kernel fn` keeps C1's mask; arm (ii) declared lengths `[i64; k]`/`[i64; n]` on params and locals in A8's tag side channel discharge every literal index below a declared length (87.5 % of bebop.bp's sites) at zero words, with the actual's length checked once at the call site; arm (iii) hoisted per-loop check when every index is the loop counter (pre-measure with a `lin_census.py` variant counting counter-indexed loops, ~1 day); `/0` trap 109 unless proven (row 10), `checked` dialect bit for overflow trap 111 (row 12, money.bp compiles under it), clone stack trap 117; ~2 weeks arm (i) + ~3 weeks arms (ii)-(iii), bebop.bp lane | docs/blueprints/A8-typed-tables-u32.md (amended §"length") + docs/blueprints/F2-bounds-by-type.md | `bounds_census: checked+proven+hoisted = 100 % of index sites, unchecked_fn = n` (static census first, F4's trace later); `bin_words <= 48,700` after arm (i) with a word_budget line per construct, `<= 43,400` after arm (ii), `<= 42,600` after arm (iii); `K5 <= +15 %` arm (i), `<= +3 %` after (ii); `K6 <= 1.2x` with the HOISTED check on the sgraph2 frontier loop (C1 §6.3 form); fuzz OOB class -> 100 % trap 84, TRAP-82 = 0; `stepping_sites: 0`; `trap_unrep >= 18/20` | F1; A8 |
| F4 | mechanised semantics of the WHOLE language in Lean 4 (decision argued in report §5.1: re-checkable kernel, native LRAT pipeline; Coq would have served a verified compiler, not chosen): executable definitional interpreter with fuel over the ~45 rules, the one-cell-array memory model, the trap set and the static rejections of F1; 10 executable builtins; 26 `sys_*` AXIOMATISED with declared footprints (7 threading builtins declared outside the single-thread semantics: no theorem crosses a clone); conformance harness runs the 86 constructs and the 121 oracle programs; runs OFF-BOX (first off-box gate in the tree; the box cannot host Lean under its 3 GB/32-process caps -- unmeasured); the Lean run emits a results file bound by sha256 to the `.lean` sources and the toolchain hash, committed, and a ~70-line python step in battery.sh recomputes the comparison against construct_parity.sh and refuses a results file whose source hash does not match; bpref differential over the fuzz corpus via exported bpref results; 8-12 weeks, parallel (new `formal/` directory, no bebop.bp edit) | docs/blueprints/F3-lean-semantics.md | `lean_conformance: c/86` (landable at any c; end state 86/86); `builtin_spec: b/36` (10 exec + 26 axiom; end 36/36); `bpref_diff: 0` over 86 + 121 + >= 10^4 seeds; `lean_results_hash == sha256(formal/*.lean)` checked on-box | F0 (the rule list and rejection codes); parallel to F1/F2 |
| F5 | translation validation from the one-pass trace: emitter writes `(pos, first word, last word, window-before digest, window-after digest, patch list)` per construct into a side zone of the `.bin` (A10's reloc zone; ~30 emitter sites, ~50-100 lines, 0 code words, fixpoint md5 unchanged by the zone); Lean semantics of the emitted AArch64 subset (inventory = check_words.py's allowlist over the 532 emission sites; a few dozen forms); validator symbolically executes each fragment (<= ~20 words; loops as diamonds with the register model's "cs mask 0 at loop entry" invariant), normalises, sends the residual (`vs_try_ubfx`/`and_imm`/`madd`, csel) to QF_BV with certificates through F5; runs as a chain gate, not at compile time; 6-10 weeks: decoder/semantics parallel, trace slice bebop.bp lane after F2 | docs/blueprints/F4-fragment-validation.md | `tv_fragments: v/t` over the 86 constructs (end t/t), then over the self-compile; `trace_zone_words: 0` in code; fixpoint gen3 == gen4 unchanged; `bounds_census` re-derived from the trace agrees with F2's static census | F2 (checks change the fragments), F3 (rules); F5 for the residual |
| F6 | **certificate checker in Bebop, `tcheck.bp`** -- a standalone program with its OWN 511-fn cap, never inside `bebop.bp`, and the ONLY new trusted artifact. Under 12.1 it reads bit-blasted QF_ABV obligations and discharges them: VC reader, bit-blaster for 64-bit add/sub/logic/shifts/compares/mux/mul, Tseitin, and the resolution checker. Under 11.3 the certificate is OURS, so the format is specified here rather than inherited -- and the LRAT-over-DRAT argument survives the format being ours: **hinted steps keep checking LINEAR and the checker small**, which is the whole reason to have hints at all, so an unhinted format must justify itself against that. Under 14.3 exactly one encoding is NORMATIVE and the other is a derived cache re-established by re-deriving, never trusted -- text is the candidate, because `scan` (A9, 46 words) exists for it and a cache that cannot be re-derived is a second source of truth. Under 15.1 each certificate carries the sha256 of the obligation it discharges, so it cannot be replayed against a different claim; under 15.3 a Merkle root over the commit's obligations makes the gate one number instead of N. **THE FORMAT IS NOW EXECUTABLE, MEASURED 2026-09-09, and cadical is reachable in-tree.** CaDiCaL **2.1.2** ships inside the Lean toolchain at `<lean>/bin/cadical` and `--lrat=true --binary=false` emits exactly text-hinted LRAT, so no workaround is needed. `tools/certgen.py` (UNTRUSTED, and deletable the day `bebop.bp` produces this format) takes an obligation DAG through a Tseitin bit-blast to cadical and emits the format as specified -- text normative, hints mandatory, `x <oid>` binding. `tools/certcheck.py` is the reference checker, written because **a format nobody can check is not a specification**, and it turns this row's central claim from an argument into executable evidence: hinted resolution is a loop with NO unit propagation, NO watched literals and NO search, which is precisely what lets `tcheck.bp` be tens of functions instead of a solver. Measured end to end: `shl(x,1) == x + x` is 577 vars / 1794 clauses / **709 hinted steps** / 59,405 B and checks OK; `(x&y)|(x^y) == x|y` is 326 steps and checks OK; the INVALID `shl(x,1) == x + 1` is **refused rc=2 with no file written**, which is right -- a producer that emits a certificate anyway is what the checker exists to catch. Four negatives all exit 2: a corrupted hint id (named by line and missing antecedent), the empty clause removed, the `x` line stripped, and hints removed from one step. **And the replay test, independently reproduced by the main session: a certificate that checks PERFECTLY, filed against the wrong obligation, is rejected on OID MISMATCH** -- 15.1 demonstrated rather than asserted. Costed at 45-65 fns / 1,700-2,900 lines **THE `ring` RULE IS IN, by operator ruling 2026-09-09 -- built even though it is risky and slow.** The study found the three flagship theorems are NOT safe SAT problems: `fp_mul`, `isqrt` and `money.bp`'s `mulov` each compare a 64x64 multiplication or a 64-bit division against a different arithmetic decomposition of the same value, and multiplier-equivalence miters are the known-hard case for CDCL. The bounds, overflow, FSM and store obligations are the easy case and 12.1 is right for them. So the kernel carries ONE word-level rule -- polynomial normal form over Z/2^64, ~8-12 fns -- plus two floor-division lemmas, AND the SAT route is attempted alongside rather than instead: the operator's ruling is to build both and let the measurement say which closes what. | docs/RESEARCH-VERIFICATION-2026-09-09.md | `cert_checked` -- obligations discharged and machine-checked, counting to total; `checker_neg 0/N` on deliberately corrupted certificates | what stays trusted REGARDLESS: `seed/seed.S`, the checker source AND binary (Thompson applies to it directly, which is why D5b's witness is load-bearing), the VC generator, the bit-blaster's operator definitions, and the certificate-to-obligation binding |
| F7 | **THE KERNEL NOW CHECKS SOMETHING (2026-09-12, main session): every one of the 16 `bench/kernel_neg` fixtures produces a verdict, and `kernel_neg` is 0 ACCEPTED of 13.** `selfhost/tcheck_kernel.bp` was rewritten from scratch (14 fns) with a new driver `selfhost/tkernel.bp` (10 fns): one flat `K` array because there are no globals and the 14-param cap forbids a bag of arrays; branchless error propagation with uid 0 as an absorbing sentinel, because `return` is statement-only here; zero allocation, so L8 cannot bite. Results, each a real run: n01 26, n02 26, n04 24, n06 20, n07 12, n08 14, n09 22, n10 26, n11 26, n12 12, n13 23, p01 0, p02 0 -- **13 of 16 agree with the twin**, and all three disagreements (n03, n14, p03) are the SAME named gap reported as code 71, inductives unimplemented, which counts as an INTERNAL and never as a rejection. Two of the rules that mattered needed no typing at all and a skeleton would have missed both: STRICTLY SMALLER argument ids (n12 -- the undefined-node check cannot catch it, because ids may appear out of order) and NO SHARING (n08 -- a refcount per file id; with de Bruijn indices one node at two binder depths means two different things). **So ~170 functions was never the threshold for a kernel that checks something: 24 was.** What the rest of the set buys is `k_decl_inductive`, eliminators, positivity and universe polymorphism, and the operator's 2026-09-12 decision stands: BOTH level rules ship and the corpus runs twice, the predicative arm REJECTING n02 (it does, code 26) and the impredicative arm accepting it and still rejecting a Hurkens witness that has yet to be authored. **BOTH ARMS NOW SHIP AND THE CORPUS RUNS TWICE (2026-09-12), and substitution is in.** 17 fixtures: under predicative `max` all 13 negatives are rejected and all 4 positives accepted; under `imax` the table is IDENTICAL except `n02_hurkens_shape`, which flips 26 -> 0 -- exactly the discriminator its own comment predicts. **`n01` type-in-type is REJECTED BY BOTH ARMS**, so the impredicative arm does not admit Girard: n02 is `Pi (X : Sort 0). X` at `Sort 0`, which is precisely what Lean-style impredicative `Prop` makes valid, and Lean is consistent. `k_subst` landed and is observable: `p04_dependent_app` applies `id : Pi (A : Sort 0). Pi (_ : A). A` to an axiom and checks it at `Pi (_ : T). T`, a type reachable only by substituting into the codomain, and it returns 0. (The first p04 written for this was `(lam (Sort 0) (var 0)) (Sort 0)` claimed at `Sort 0` -- Girard's shape wearing a positive's name, correctly rejected with 24; a positive fixture the kernel must reject is a broken fixture, not a broken kernel.) The impredicative arm stays UNPROVEN until a full Hurkens encoding is authored and shown rejected under it. OWED next: eliminators and recursors, universe polymorphism, that Hurkens witness, and `tools/kcheck.py`'s `kernel_parity` line must become a real measurement instead of the hardcoded `0/%d` it still prints. Previously, and for the record: **`selfhost/tcheck_kernel.bp` DID NOT COMPILE** -- `./seed/build/seed ./bebop.bin compile selfhost/tcheck_kernel.bp` exits **101 = unbound symbol** (`bebop.bp:100`), because `ttag`/`targ1`/`targ2` at lines 23-25 read `tm_tag`/`tm_a`/`tm_b` as globals while every other fn takes them as parameters. So **0 of its 30 functions has ever executed**, `tools/kcheck.py --corpus bench/kernel_neg` reports `kernel_parity: 0/16` from a HARDCODED string, and `synth`/`parse_core`/`whnf` carry self-admitted placeholders (`whnf` also allocates inside a `while` body -- an L8 violation that would SIGSEGV). The instruments, by contrast, are real and better than this row implies: `tools/kcheck.py` (~470 lines) is the differential twin and the authoritative spec of the calculus, and `bench/kernel_neg/` holds 16 fixtures (13 negatives, 3 positives) several of which were added by mutation testing and each names the earlier test it discriminates against. **BLOCKER, operator's to settle: this row binds `imax` while the twin implements predicative `max`, and `n02_hurkens_shape.core` is the deliberate discriminator -- `imax` ACCEPTS it, which makes `kernel_neg: 1 accepted of 13` and fails the gate by its own definition.** Was: **kernel module of `tcheck.bp` for the structural layer** -- under 13.3 this is OURS from the start, not a re-checker for Lean-exported terms, which is the choice that keeps the format producer-agnostic and the trust root free of Lean. A checker for a minimal dependent calculus: terms, substitution, whnf, conversion, a fixed universe hierarchy, inductives with eliminators, with the bit-level obligations of F6 admitted as a certified oracle step. Keep it MINIMAL -- the research costed a Lean-subset kernel at 150-400 fns against `bebop.bp`'s 287 and a 511 cap, and it is the single most expensive item in the phase, which is why it lives in `tcheck.bp` with its own cap rather than inside the compiler **UNIVERSE POLYMORPHISM AND IMPREDICATIVE `Prop` ARE BOTH REQUIRED (operator, 2026-09-09, asked and answered: "both").** The two are SEPARATE losses that a concrete-level kernel would take, and each was surfaced by a different reviewer. Universe POLYMORPHISM is the ability to quantify a statement over levels -- without it no theorem can be stated once for all `u`, only repeated per concrete level; it costs ~8 kernel fns of ~170 (verification estimate). IMPREDICATIVE `Prop` is what `imax` actually buys -- the rule putting `pi A B` in `Prop` whenever `B : Prop` regardless of A's level; dropping it leaves the hierarchy infinite but the system PREDICATIVE throughout. So "infinite universe hierarchy" alone did NOT imply either, and both are now in. **This is maximum power at maximum risk and the risk is named: impredicative `Prop` is exactly what Girard's and Hurkens' paradoxes threaten**, so the kernel must get positivity and the level rules exactly right or it proves False -- which makes `bench/kernel_neg`'s discriminator load-bearing rather than decorative. `n02` is the file that separates the two rules: `Pi (X : Sort 0). X` claimed at `Sort 0` is rejected predicatively (`max(1,0) = 1`) and accepted under `imax`. **SELF-VERIFICATION IS A TARGET, NOT AN IMPOSSIBILITY (operator, 2026-09-09, binding: no specialists, no third parties).** The 2026-09-09 design study filed "the checker verified in its own logic" as multi-year and out of reach, citing MM0 and Milawa. That framing is rejected, and the study itself supplies why: **self-APPLICATION is free** -- `tcheck` checks certificates about its own fns because they are Bebop. What is expensive is the SEMANTICS OF BEBOP IN THE LOGIC, and that is **already row F4**, required for whole-language verification whether or not the checker is ever self-verified. So the costly prerequisite is bought by another row, and what remains is an induction over `tcheck.bp`'s own 137-206 fns over a semantics that has to exist anyway. **The mechanism that makes it incremental is MILAWA'S LADDER**: not one large proof but rungs, each verified by the rung below and the whole re-checked from rung 1 -- which decomposes a monolith into steps that each carry a number, and steps with numbers are this project's entire velocity mechanism. The row must therefore name rung 1 (the smallest checker whose verification is tractable), what each rung buys, which rungs depend on F4 and which can start before it, and the residual cost with the F4 prerequisite REMOVED from the estimate -- which is not the number the study gave. A rung that genuinely does not decompose is to be named as such rather than padded into the ladder. | docs/RESEARCH-VERIFICATION-2026-09-09.md | `kernel_checked` -- theorems whose terms the Bebop kernel accepts, counting to total | 13.3 buys self-sufficiency at the price of the bootstrap: Lean can no longer hand us a term to check, so the elaborator that PRODUCES terms is on the critical path from the start rather than deferred |
| F8 | **THE GATE PASSES VACUOUSLY -- measured 2026-09-12, and the compiler silently accepts garbage.** `tools/f8_dt.py` reports `PASS -- all dependent surface syntax parsed and erased (inert)` in every battery, and `docs/LANGUAGE.md:135-138` states the surface syntax "IS parsed and erased by the compiler as of A16 Phase 1". Neither is true. `bebop.bp` contains ZERO code for `requires` / `ensures` / `theorem` (the three grep hits are comments about unrelated things); the parser simply discards everything between `)` and `{`, and discards unrecognised top-level text. Two probes, both COMPILED and both ran to 7: `fn add(x: i64, y: i64) -> i64 requires @@@ %%% not_a_thing ensures 1 2 3 ][ { x + y }` and a top-level `theorem this_is_utter_nonsense_and_should_not_parse ][ @@@`. So the gate cannot distinguish a parser from a shredder, and the defect is worse than a hollow gate: **arbitrary invalid text is accepted in two syntactic positions with no diagnostic**, which is a loud-failure violation (L1) in the compiler itself. **STEP 0 LANDED 2026-09-13: the gate now says what is true.** `tools/f8_dt.py` no longer prints `all dependent surface syntax parsed and erased (inert)`; it prints `0 parsed`, names the four forms as **DISCARDED unread**, and runs FOUR negative probes -- garbage in the `requires`/`ensures` position and in the `theorem` position -- every one of which the compiler ACCEPTS today with no diagnostic. That count is held by a **ratchet at 4** in the script (not in `tools/arch_ratchet.txt`: `arch_check`'s `ratchet-orphan` fails any number there that no arch_check check reads). The ratchet was exercised in both directions -- lowering it below the real count prints `RATCHET BROKEN: garbage-accepting positions rose to 4 (was 3)` with rc=1, and a count that falls without the ratchet being lowered also fails. The `f8_dt gate:` line survives because `battery.sh:62` asserts `^f8_dt gate:` contains `PASS`; what changed is its MEANING -- PASS now says only that the known-bad state has not got worse, and the line says so in words. LANGUAGE.md was already corrected. **STEP 0 IS NOW COMPLETE: the compiler half LANDED 2026-09-13 (promoted `89f889a4`), and the ratchet is 0.** The fix is a LEXICAL validator, not a parser -- `scan_inert(s, from, to)` (`bebop.bp:4503`) walks a region and returns the offset of the first character that is not whitespace, an identifier character, or one of the fifteen type/contract punctuation marks `- > < = + * / . : , ( ) [ ] ;`; `@ % # $ ! & \| ^ ~ ? \ " '` are refused. It is wired into the two positions the defect named: the fn-header region between `)` and `{` in `compile_fn_at` (replacing the blind `skip_to(s, pos, 123)` that was the shredder) and a top-level `theorem ` line now RECOGNISED by `collect_fns` the way `test ` is, validated to end of line. Both call `diag_exit(s, <offset>, 110)`; **110 is a new code** with its text in `diag_exit` and a row in `docs/TRAPS.md`, and `trap_census.py --texts` reaches agreement at **19 codes, 19 in source, 19 in binary, 19 documented** (was 18). **The boundary is the whole point and it was measured on both sides:** all four f8_dt POSITIVE probes still compile and run (7, 10, 42, 7), including `theorem add_zero : forall x:i64. x + 0 = x := refl` -- `:` `.` `+` `=` are on the allow list for exactly that -- while all four negative probes are REJECTED at exit 110. Constructs `neg/c143_contract_garbage` (COMPILEFAIL:110) and its positive twin `c144_contract_ok` bracket it, because without the twin a compiler that refused every fn header would satisfy the row. **A 544-file control arm** compiled every `.bp` in the tree before and after: exactly ONE file changes, `samples/theorem-sample.bp` 0 -> 110, and it is marked SUPERSEDED in its own first line and uses `(\x:i64. x + 1)` lambda syntax that is not in the language -- it previously 'compiled' only because the parser discarded it unread, which is the defect this row closes. No gate compiles `samples/`. Cost, split by an isolating arm rather than asserted: words 43023 -> 43566 (+543), bcond 1223 -> 1241 (+18) of which **17 are the new bodies and 1 the two call sites**, cbz 126 -> 127 -- see the `bebop bcond 1241` line in `census_allow.txt`. **NOTE FOR STEP (a):** when real contract syntax lands, if it includes lambdas the allow list needs `\`. What remains of F8 is (a)-(d), not step 0. | dependent types directly in Bebop, in the only order with a first theorem under a year: (a) bebop.bp parses `[T; n]`, `{x : T | p}`, `requires`/`ensures`/`invariant`, `theorem` as INERT syntax erased before emission and emits their VCs into F4's side zone (~2-3 weeks, bebop.bp lane); (b) an UNTRUSTED elaborator -- standalone `elab.bp` or Lean itself elaborating Bebop's annotations to exported terms (the cheaper path) -- produces terms that F6's kernel re-checks (8-16 weeks, parallel); (c) the checker renames every `let` (Bebop `let` REBINDS a fn-scoped register, LANGUAGE.md:41-43) and requires a loop `invariant` for every rebound variable that a type mentions -- the Verus/Dafny shape, unavoidable over mutable state; full dependent surface types (`Fin n`, length-indexed tensors for B7) after (c) | docs/blueprints/F7-dependent-types.md | `dt_fns: n/833` selfhost fns with checked types (first: `fp.bp` 2/2, `money.bp` 17/17, `store.bp` 52/52); `elab_neg: 0 accepted of N` ill-typed programs; `K5_typed <= 1.5x K5` (annotation parsing + erasure only; checking is a chain gate); fixpoint unchanged; every rejected program carries `<line>:<col>` | F6; F2 arm (ii) (declared lengths are the first dependent types) |
| F9 | first theorems, machine-checked and certificate-checked: `fp_mul(a, b) = sign * floor(|a| * |b| / 2^32)` and `isqrt(s)^2 <= s < (isqrt(s)+1)^2` (today tested on 1.2M values, fp.bp comment), the B8 money laws of `money.bp` against the Rust oracle in `bench/oracles/rust` for all inputs in the declared range (the parent repo's `eqc_gen` exact-equality discipline lifted to a quantifier), store invariants (`st_len`, cursor monotone, crc); 3-5 weeks after F5-F7 | docs/blueprints/F8-first-theorems.md | `theorems: >= 3` then growing, each with a kernel-checked term and an LRAT certificate; `theorem-sample.bp`/`theorem-false.bp` retired or re-homed on F6 (their C-era kernel is a stub per bench/VERIFICATION.md) | F7 |
### Still open after A/B (design-bound, operator decision first): T61 (pool library + gate), T68-T70, T85 -> T86, T73, T76, T49/T50, T56, T59. Project-sized items stay under HISTORY.md `## PARKED` (D14 item 11).

Parallel-safe at any time: docs, oracles, fuzz batches, honest.sh rows, T78/T79/T81/T82 tooling.

## Open decisions (operator)

- (decided 2026-09-06, HISTORY D13: all 12 retro proposals of docs/RETRO-SESSIONS-2026-09-06.md §5
  are work items; process-count gate, FREEZE-on-codegen, pkill block first)
- (decided 2026-09-06, HISTORY D12: evals E1-E14, P2 = IR rung, TRAP-82 ALERT, hygiene
  commit, a/b/c = 4x/10x/2.5x, 1.0x stays the long target, K8 before csel, T48 into bebop.bp)
- (decided 2026-09-06, HISTORY D14, docs/DECISIONS-RESEARCH-2026-09-06.md: order B1 -> B2 ->
  B5 -> the operand-tag-stack IR rung (not an op-list) -> K8 -> B4 computed frames; window
  x1-x7 with a forced spill path; T53/T54 DELETED, T52 conditional on K8; store's first move
  is B8 (profile the CSR build), workload W = the dowiz-core order log (T66); LMDB and "native
  Rust" leave the thesis sentence; D1(a) 1.0x stays report-only, <= 2.0x is the real TG-DONE 1
  target; the 11 project-sized tasks of report §4 move to HISTORY.md's `## PARKED` heading
  (reverses D11-J); codegen freezes for a 24 h fuzz window after the IR rung lands (TG-DONE 8))
- (operator 2026-09-08, session 27, BINDING: one main agent + ONE worker, no more -- the four-lane
  protocol of docs/blueprints/PARALLEL-LANES-2026-09-08.md is history; its slot.sh semaphore and
  merge recipe stay, its lane list does not)
- (measured 2026-09-08, Lane B: the A12 reorder below is REVERSED by its own gate -- rung 1 buys 0-2 % of K5,
  not >= 5 %, so A12 drops off the critical path and A2b/A3/A10 resume their order; the probe is the
  journal line at 1788821670)
- (decided 2026-09-08: no roadmap rows cut; A12 (flat IR + graph RA) reordered BEFORE A2b/A3/A10;
  A4 fuzz window = foreground 10^4-seed batches, not 24 h daemon at 10^5; up to 4 parallel workers,
  one writer owns bebop.bp; Opus writes blueprint/spec for every row before worker starts it)

## Measured (pinned A78, in-process clock_ms medians; every number has a script)

Per-commit series since D12-A (2026-09-06): docs/PERF.md (generated by tools/perf.py at the end of
every tools/chain.sh run from bench/perf.csv: self-compile wall/utime/stime/maxrss + energy proxy,
bebop.bin/stub/per-fn words with a budget, K1H-K4 ms med/p95 interleaved against the previous
binary + loop words, per-construct words, fuzz seeds per binary; `?` = invalid window, `!` = alert).


Honest twins (bench/vs_rust/honest.sh, D11-C, 2026-09-06, bebop.bin 94e47998, REPS=100 in-process,
pinned core 4, R=11 medians; bench/vs_rust/REPORT-honest.md has the p95 column and the method):

| kernel | bebop ms | Rust honest twin ms | bebop / Rust |
|---|---|---|---|
| K1H s = s*3 + i, 1M | 2.07 | 1.132 | 1.8x |
| K2H fib(25), inline(never) | 1.30 | 0.397 | 3.3x |
| K3H 300x300 nonlinear accumulator | 0.65 | 0.194 | 3.4x |
| K4 (v + i*7)*3 - 11, 2M (twin black_box on input/output only) | 5.26 | 3.259 | 1.6x |
| K5 self-compile of bebop.bp, cold, median of 3 | 1.67 s | | |

| tensor query, 1M points (bench/tq_sqlite/run.sh, T100) | sqlite 3.46 | bebop | bebop faster by |
|---|---|---|---|
| nearest, full scan | 183 ms (python) / ~158 ms native | 18.4 ms | 8.6-9.9x |
| nearest, 3x3 cell index | 55 us (C API incl. ~19 us ctypes) / ~35 us native | 4.0 us | ~9x native |
| nn4 bucketed scan, 1 A78 vs 3 A78 pinned (bench/tq_sqlite/nn4.sh, T106) | seq 219 ms | par 99 ms | 2.21x on 3 cores |

| store vs sqlite 3.46.1 C API via ctypes (bench/vs_rust/sbench.sh, G7/T116, 1M records) | store | sqlite | ratio |
|---|---|---|---|
| insert 1M + index + commit | 880 ms | 15147 ms | 17x |
| PK lookup (10^5) | 450 ns | 10.3 us (ctypes; ~2 us via python's module) | 22.8x / >= 4x native |
| 3x3 cell-window scan (10^4) | 2.7 us | 83 us | 30.7x |
| update 10^5, one transaction | 157 ms | 1000 ms | 6.4x |
| reopen + first record | 590 us | 3600 us | 6.1x |
| logical size after update / after compaction | 85.2 MB / 72.4 MB | 34.1 MB | 2.5x / 2.1x LOSS |
| compaction vs VACUUM | 747 ms | 544 ms | 0.7x |
| durable commit (msync appended pages + superblock) vs WAL NORMAL / FULL | 506 us | 78 us / 567 us | 0.15x / 1.1x |

| graph in the store, 1M nodes 10M edge slots (bench/vs_rust/sgraph.sh + sgraph2.sh, G8/T117) | store | sqlite 3.46.1 | ratio |
|---|---|---|---|
| BFS, ns per edge (bebop 100 sources; sqlite level-synchronous, 3 sources) | 187 | 10758 | 57x |
| BFS on L1, queue vs frontier SpMSpV (push/pull, alpha 14), ns per edge slot, 3 sources | 192 vs 45 (4.3x) | | |
| build the CSR | 44.8 s | 242 s | 5.4x |
| 1M edges through the edge log, 100 L0 rebuilds, 5 compactions | 30 us / edge, max stall 747 ms | | |
| tombstone 10% + commit / BFS with tombstones + log / compaction | 131 ms / 240 ns per slot / 795 ms | | |


| platform | measured |
|---|---|
| usable A78 cores in this shell | 3 (cpus 4-6; cpu 7 refuses taskset) |
| DRAM bandwidth, one A78 / three A78 / aggregate ceiling | **2.9 / 8.6 / ~12 GB/s** -- CORRECTED 2026-09-08. The old row read `~12 GB/s / ~12 GB/s` and was used to argue that memory bandwidth is already saturated by a single core. It is not. Measured that day with one sequential i64 sum over 80 MB, pinned, run solo and then concurrently: solo 28 ms; three instances on cores 4,5,6 **28 / 28 / 28 ms -- identical to solo, zero contention**, i.e. exactly 3x aggregate; seven instances give 28/29/43 on the A78s and 82/83/84/117 on the A55s. So ~12 GB/s is the AGGREGATE ceiling across all seven cores, and one core reaches under a quarter of it. `docs/LANG-DB-DESIGN.md:121` had it right all along -- "4 cores 1.0-1.4x on streams" and "bebop's scan is codegen-bound" -- and this row had compressed that into a claim the source never made |
| process RSS K1-K4, bebop / Rust | 16-17 MB / 16-17 MB |
| self-compile pinned (2026-09-05 re-measured, core 4: T109 binary / T126 binary) | 294.5 s, 94 MB / 292.9 s, 111 MB (the 108.7 s row of 2026-09-04 is not reproducible today; same box, same core) |
| self-compile after the 2026-09-06 speed-ups (third pass removed, slen, str_len hoist a27b594), cold / warm .becache hit | 1.5-1.7 s / 0.07 s |
| compile of a std gate, cold / warm .becache hit / trivial-program floor (T108, becache_gate.sh) | 346 ms / 113 ms / 106 ms |
| page-cache read fault / CoW fault / msync 1 page / rename (proot) | 0.3 us / 3.5 us / ~100 us / ~270 us |

## Where everything else is

HISTORY.md (all pulls, task bodies, decisions D1-D11 and their evidence, progress
log, the 2026-08 vision text), TASKS.md (ledger), docs/SPEEDUP-ANALYSIS.md (plan
P1-P10, physics), docs/LANG-DB-DESIGN.md (store spec, prior art, §9 tensor-graph
indexing), docs/ROADMAP-CRITIQUE-2026-09-04.md (findings F1-F43), docs/LANGUAGE.md,
docs/TRAPS.md, docs/SESSION-HANDOFF.md, docs/exp.journal.
