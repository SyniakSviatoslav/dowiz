// hello.bp — minimal Bebop sample (glyphic surface + ASCII fallback)
module core { }
struct order { id: i64, amount: i64 }
const VERSION: i64 = 1
fn ntt(a: i64) -> i64 { a + 1 }
use core::math
type Amount = i64
