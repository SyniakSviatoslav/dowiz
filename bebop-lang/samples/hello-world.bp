// Status: 2026-09-04 SUPERSEDED — string literals + write compile but the str surface is R3.x(d) territory until ROADMAP T42; run through seed, not bebopc.
// hello-world.bp — Bebop Hello World (working, with I/O syscalls)
//
// Run: ./build/bebopc run samples/hello-world.bp main
// Demonstrates: write syscall, strings with escapes, fn params, while, arrays.
module core { }

fn main() -> i64 {
  write("Hello, World!\n");
  0
}