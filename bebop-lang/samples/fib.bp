// T84: fib.bp (ASCII) and fib_glyph.bp (glyphs) are ONE program in two spellings;
// both compile to a byte-identical binary. Glyphs are canonical, ASCII is the projection.
fn main() -> i64 {
  let n = 30;
  let a = 0;
  let b = 1;
  let i = 0;
  while i < n {
    let tmp = a + b;
    let a = b;
    let b = tmp;
    let i = i + 1;
    0
  };
  a
}
