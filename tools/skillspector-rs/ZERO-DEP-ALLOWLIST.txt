# Zero-dep allowlist (roadmap item 31 — per-crate extension of items 1+13).
# INVARIANT: this file may only SHRINK. CI (scripts/zero-dep-gate.sh <crate>) compares HEAD
# against origin/main and fails if any name here is absent there (= growth).
#
# RULING: item 31 §4.4 Phase-A CUTOVER COMPLETE (2026-07-19). serde + serde_json were REMOVED —
# JSON-RPC serve-mode parsing AND findings/summary OUTPUT now go through `dowiz_kernel::json`
# (differentially proven vs serde_json — kernel/tests/json_oracle.rs; shape locked by engine.rs
# to_value_tests). dowiz-kernel's DEFAULT build is zero-external-dep, adding NO transitive crates.
# The serde subtree dropped (serde/serde_json/serde_core/serde_derive/proc-macro2/quote/syn/
# unicode-ident/itoa/zmij — 10 crates): 15 external → 5 (the regex subtree only).
# `regex` remains a separate named rewrite candidate (synthesis §25) — out of item-31 scope.
# Generated by: cargo tree -e no-dev --locked --offline --prefix none | grep -v " (/" | awk "{print \$1}" | sort -u
aho-corasick
memchr
regex
regex-automata
regex-syntax
