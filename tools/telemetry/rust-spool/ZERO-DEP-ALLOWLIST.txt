# Zero-dep allowlist (roadmap item 31 — per-crate extension of items 1+13).
# INVARIANT: this file may only SHRINK. CI (scripts/zero-dep-gate.sh <crate>) compares HEAD
# against origin/main and fails if any name here is absent there (= growth).
#
# RULING (item 31 §3 — CORRECTED 2026-07-19, evidence over the blueprint's proposal): serde carrier,
# builds the `telemetry-spool` binary. The blueprint PROPOSED deletion ("referenced by no
# script/workflow"). Independent verification REJECTS that: `tools/telemetry/lib.sh:37` hardcodes
# `$TELEMETRY_DIR/rust-spool/target/release/telemetry-spool` and `tg_spool_ensure` (lib.sh:35-39,
# called from tg_deliver:60) LAUNCHES it — this is the currently-DEPLOYED live Telegram telemetry
# drainer (TORVALDS-audit 2026-07-18: drainer PID observed running it). async-spool is the intended
# successor but is NOT yet wired into lib.sh. DELETION DEFERRED — deleting it now would break the
# live telemetry/alerting pipeline. Reopening trigger: delete ONLY after async-spool is deployed as
# the drainer AND tools/telemetry/lib.sh is cut over to it AND the live pipeline is verified.
# Allowlist frozen (shrink-only) until then. serde_json cutover is NOT the reduction path here.
# Generated by: cargo tree -e no-dev --locked --offline --prefix none | grep -v " (/" | awk "{print \$1}" | sort -u
base64
cc
cfg-if
displaydoc
find-msvc-tools
form_urlencoded
getrandom
icu_collections
icu_locale_core
icu_normalizer
icu_normalizer_data
icu_properties
icu_properties_data
icu_provider
idna
idna_adapter
itoa
libc
litemap
log
memchr
once_cell
percent-encoding
potential_utf
proc-macro2
quote
ring
rustls
rustls-pki-types
rustls-webpki
serde
serde_core
serde_derive
serde_json
shlex
smallvec
stable_deref_trait
subtle
syn
synstructure
tinystr
unicode-ident
untrusted
ureq
url
utf8_iter
webpki-roots
writeable
yoke
yoke-derive
zerofrom
zerofrom-derive
zeroize
zerotrie
zerovec
zerovec-derive
zmij
