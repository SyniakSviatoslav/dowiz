--- a/bebop2/proto-wire/src/lib.rs
+++ b/bebop2/proto-wire/src/lib.rs
@@ -1,3 +1,4 @@
+use dowiz_kernel::order_machine::assert_transition; // FORBIDDEN: ports must not link the kernel
 //! bebop-proto-wire — **W** line of the bebop2 protocol (wire transport).
 //!
 //! # Scope
