# NOTIF-MATRIX.md

| event_key | recipients | non-recipients | precondition | trigger action | channels | assertion surface | env requirement | dedup/idempotency |
|-----------|------------|----------------|--------------|----------------|----------|-------------------|-----------------|-------------------|
| order.created | owner (telegram, in-app WS, sound), customer (in-app WS) | courier, other owners/locations | Customer has cart, proceeds to checkout | Customer places order via API or app | Owner: telegram, in-app WS (dashboard), in-app sound; Customer: in-app WS (order status) | Owner telegram: receive Telegram message; Owner in-app WS: new order card appears on dashboard; Owner sound: audio plays (or infer from WS update); Customer in-app WS: order status page updates to show new order | Staging environment (VITE_BASE_URL set to staging) | Each order creation triggers exactly one notification per active target; deduplication via attempt counter and prefs |
| order.delivered | owner (telegram, in-app WS, sound), customer (in-app WS, push) | courier, other owners/locations | Order is in delivery state, courier delivers order | Courier marks order as delivered via API or app | Owner: telegram, in-app WS (dashboard), in-app sound; Customer: in-app WS (order status), push (webpush) | Owner telegram: receive Telegram message; Owner in-app WS: order card updates to delivered; Owner sound: audio plays; Customer in-app WS: order status page updates to delivered; Customer push: receive webpush notification in service worker | Staging environment | Each delivery triggers exactly one notification per active target; deduplication via attempt counter and prefs |
| order.substitution_needs_human | owner (telegram, in-app WS, sound), customer (in-app WS) | courier, other owners/locations | Order has items that need substitution due to stock-out | Customer or agent triggers substitution flow via app or API | Owner: telegram, in-app WS (dashboard), in-app sound; Customer: in-app WS (order status or chat) | Owner telegram: receive Telegram message with substitution request; Owner in-app WS: order card shows substitution needed; Owner sound: audio plays; Customer in-app WS: order status page or chat shows substitution request | Staging environment | Each substitution need triggers exactly one notification per active target; deduplication via attempt counter and prefs |
| order.dwell_escalation | owner (telegram, in-app WS, sound) | customer, courier, other owners/locations | Order remains in PENDING state beyond threshold | System detects order dwell time exceeded via dwell monitor worker | Owner: telegram, in-app WS (dashboard), in-app sound; (Note: push may be sent for tier 1 dwell escalation per code) | Owner telegram: receive Telegram message; Owner in-app WS: dashboard alert or order card highlights; Owner sound: audio plays; Owner push: receive webpush notification (if tier 1) | Staging environment (fault-injection safe: dwell threshold can be reduced via env) | Each dwell escalation triggers exactly one notification per active target per tier; deduplication via attempt counter and prefs |
| order.timeout_cancelled | owner (telegram, in-app WS, sound), customer (in-app WS) | courier, other owners/locations | Order remains in PENDING state beyond timeout threshold | System auto-cancels order due to timeout | Owner: telegram, in-app WS (dashboard), in-app sound; Customer: in-app WS (order status) | Owner telegram: receive Telegram message; Owner in-app WS: order card updates to cancelled; Owner sound: audio plays; Customer in-app WS: order status page updates to cancelled | Staging environment | Each timeout cancellation triggers exactly one notification per active target; deduplication via attempt counter and prefs |
| cash.reconcile_discrepancy | owner (telegram, in-app WS, sound) | customer, courier, other owners/locations | Owner closes shift with cash discrepancy | Owner closes shift and enters cash amount that doesn't match expected | Owner: telegram, in-app WS (dashboard), in-app sound | Owner telegram: receive Telegram message; Owner in-app WS: dashboard shows cash reconciliation alert; Owner sound: audio plays | Staging environment | Each discrepancy triggers exactly one notification per active target; deduplication via attempt counter and prefs |
| delivery.flag_raised | owner (telegram, in-app WS, sound) | customer, courier, other owners/locations | Courier raises a flag during delivery (e.g., problem with address) | Courier raises flag via app or API | Owner: telegram, in-app WS (dashboard), in-app sound | Owner telegram: receive Telegram message; Owner in-app WS: dashboard shows delivery flag alert; Owner sound: audio plays | Staging environment | Each flag raise triggers exactly one notification per active target; deduplication via attempt counter and prefs |
| rating.low_received | owner (telegram, in-app WS, sound) | customer, courier, other owners/locations | Customer leaves a low rating (e.g., 1 or 2 stars) | Customer submits rating via app or API | Owner: telegram, in-app WS (dashboard), in-app sound | Owner telegram: receive Telegram message; Owner in-app WS: dashboard shows rating alert; Owner sound: audio plays | Staging environment | Each low rating triggers exactly one notification per active target; deduplication via attempt counter and prefs |
| ops.worker_liveness | owner (telegram, in-app WS, sound) | customer, courier, other owners/locations | Worker process heartbeat fails (system event) | System detects worker liveness failure | Owner: telegram, in-app WS (dashboard), in-app sound | Owner telegram: receive Telegram message; Owner in-app WS: dashboard shows system alert; Owner sound: audio plays | Staging environment (fault-injection: only in staging with fault-injection enabled) | Each liveness failure triggers exactly one notification per active target; deduplication via attempt counter and prefs |
| ops.backup_failed | owner (telegram, in-app WS, sound) | customer, courier, other owners/locations | Backup process fails (system event) | Backup job fails | Owner: telegram, in-app WS (dashboard), in-app sound | Owner telegram: receive Telegram message; Owner in-app WS: dashboard shows backup alert; Owner sound: audio plays | Staging environment (fault-injection: only in staging with fault-injection enabled) | Each backup failure triggers exactly one notification per active target; deduplication via attempt counter and prefs |
| ops.degradation_changed | owner (telegram, in-app WS, sound) | customer, courier, other owners/locations | System degradation level changes (system event) | System detects degradation change | Owner: telegram, in-app WS (dashboard), in-app sound | Owner telegram: receive Telegram message; Owner in-app WS: dashboard shows degradation alert; Owner sound: audio plays | Staging environment (fault-injection: only in staging with fault-injection enabled) | Each degradation change triggers exactly one notification per active target; deduplication via attempt counter and prefs |
| courier.assigned | courier (telegram, in-app WS), owner (in-app WS) | customer, other owners/locations | Order is ready for courier assignment | Owner assigns courier to order via API or app | Courier: telegram, in-app WS (assignment notification); Owner: in-app WS (dashboard shows assignment) | Courier telegram: receive Telegram message; Courier in-app WS: assignment notification appears; Owner in-app WS: dashboard updates to show courier assigned | Staging environment | Each assignment triggers exactly one notification per active target; deduplication via attempt counter and prefs |
| order.ready_for_pickup | courier (telegram, in-app WS), customer (in-app WS) | owner, other owners/locations | Order is prepared and ready for courier pickup | System marks order as ready after preparation | Courier: telegram, in-app WS (ready notification); Customer: in-app WS (order status shows ready for pickup) | Courier telegram: receive Telegram message; Courier in-app WS: ready notification appears; Customer in-app WS: order status page updates to ready for pickup | Staging environment | Each ready-for-pickup triggers exactly one notification per active target; deduplication via attempt counter and prefs |
| shift.started | courier (in-app WS, telegram?), owner (in-app WS) | customer, other owners/locations | Courier starts shift via app | Courier starts shift via app or /start shift command | Courier: in-app WS (shift started notification), possibly telegram; Owner: in-app WS (dashboard shows shift started) | Courier in-app WS: shift started notification appears; Courier telegram: receive Telegram message if subscribed; Owner in-app WS: dashboard updates to show shift started | Staging environment | Each shift start triggers exactly one notification per active target; deduplication via attempt counter and prefs |
| shift.closed | courier (in-app WS, telegram?), owner (in-app WS) | customer, other owners/locations | Courier ends shift via app | Courier ends shift via app or /end shift command | Courier: in-app WS (shift ended notification), possibly telegram; Owner: in-app WS (dashboard shows shift ended) | Courier in-app WS: shift ended notification appears; Courier telegram: receive Telegram message if subscribed; Owner in-app WS: dashboard updates to show shift ended | Staging environment | Each shift end triggers exactly one notification per active target; deduplication via attempt counter and prefs |
| shift.close_reminder | courier (in-app WS) | owner, customer, other owners/locations | Shift is nearing end time (reminder threshold) | System detects shift close time approaching | Courier: in-app WS (shift close reminder) | Courier in-app WS: shift close reminder notification appears | Staging environment | Each shift close reminder triggers exactly one notification per active target; deduplication via attempt counter and prefs |
| test | owner (telegram, push) | customer, courier, other owners/locations | Owner manually triggers test notification | Owner sends test notification via notifications settings | Owner: telegram, push | Owner telegram: receive Telegram test message; Owner push: receive webpush test message | Staging environment | Each test triggers exactly one notification per active target; deduplication via attempt counter and prefs |
