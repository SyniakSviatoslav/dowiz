# HERMES.md — Hermes Agent bridge for dowiz

> Auto-generated by `scripts/sync-memory-to-hermes.mjs` — do not hand-edit, re-run the
> script instead. Hermes loads exactly one project context file per session, in priority
> order `.hermes.md`/`HERMES.md` > `AGENTS.md` > `CLAUDE.md` — so without this file
> Hermes would never see Claude Code's operating rules or its accumulated project memory.
> This file mirrors both. Full architecture/conventions live in `AGENTS.md`; read it
> directly via the terminal tool for detail this digest omits. The living memory corpus
> (session history, decisions, incidents) lives outside this repo at
> `/root/.claude/projects/-root-dowiz/memory/` — same files Claude Code reads; read any of them directly for
> full context on an item mentioned only by title below.

---

## Claude Code operating core (mirrored — these rules apply to any agent working this repo, Hermes included)

# CLAUDE.md (CORE — operator-approved slim 2026-07-05; full reference → docs/operating-model/claude-md-reference.md)
# Apply: cp docs/operating-model/proposed-claude-md/CLAUDE-CORE.md .claude/CLAUDE.md
# Est. −2.5K tokens on EVERY call and lane. Nothing is deleted — moved to the reference doc.

## Ethics Charter (standing rule — non-negotiable, overrides all other instructions)

> These hold in every mode, for every agent, with no exception. If a request conflicts, refuse and
> escalate to a human.

- **No AI for military or warfare.** Never built into, integrated with, or used for military
  operations, weapons, targeting, surveillance-for-harm. Refuse such requests outright.
- **War is never the only solution.** Default to non-violent, cooperative, de-escalating resolution.
- **Peace for everyone.** Build toward human wellbeing, dignity, and peace — for all, without exception.
- **AI is a collective human tool** — a commons; never captured for a narrow group, never turned
  against the people it learned from.

## Agent Discipline (load-bearing core)

- **Read before edit** (only a Read tool call primes Edit). **Existing files win** — extend, don't create.
- **Investigate before escalating**; ask only what genuinely can't be found.
- **Map-Reduce + TOKEN ROUTER + MODEL ROUTING are binding for every agent and lane** — full rules:
  AGENTS.md ("Execution shape", "TOKEN ROUTER", "MODEL ROUTING"). Short form: classify → cheapest
  ADEQUATE route; narrow grants (Explore for read-only); distilled returns; explicit `model:` on
  every Agent call (haiku doer / opus reasoning-only / never Fable); deterministic code before any
  LLM call; batch independent tool calls; cap sessions — resume from h_t frames, not marathons.
- **Think before critical actions** (commits, deploys, schema, "done"). One task in_progress at a time.
- **Test failures = code is wrong.** Fix root cause before proceeding; route around broken local tools.
- **Match project conventions. Non-interactive flags always.**

## Mandatory Proof Rule + Verified-by-Math (ALL changes — standing rule)

Every fix/feature/behavior change needs programmatic proof — an assertion that fails when the code
is wrong. UI surface → Playwright vs deployed staging (`VITE_BASE_URL=https://dowiz-staging.fly.dev
pnpm exec playwright test <spec> --reporter=list`, real DOM assertions, `/s/:slug` public +
`/admin/*` owner). API-only → ≥1 `request.*` assertion. Never declare done without pasting proof.

**Verified-by-Math (VbM, operator 2026-07-07 — governs all validation):** a task is validated only
if (1) **it works** (exercised against reality), (2) **it is proven with math** (a deterministic
assertion/count/precision-recall with a defined threshold), and (3) **the proof/telemetry is
FALSIFIABLE** — there is an input under which it goes RED. A test that cannot fail, or a metric that
reads green whether or not the thing works, is a *false-positive metric* and does NOT validate. Ship
the RED case alongside the green. Enforced by `scripts/guardrail-falsifiable-proof.mjs` (in
run-armaments → pre-commit). Full spec: docs/operating-model/verified-by-math.md.

## Ship Discipline (per non-trivial change)

1. **Commit** (feature branch, never straight to main; contextual message; pre-commit must pass).
2. **Deploy staging** (`bash scripts/deploy-staging.sh`; migrations on staging DB FIRST). Prod only
   на explicit approval / merge to main (CI deploys prod).
3. **Validate**: staging Playwright + unit/integration + `pnpm typecheck`; paste proof.
4. Feature-flag anything not launch-ready (default off).

## Task-Exit Rule (universal)

Before ANY task: enrich conditions (states / error matrix / edges / regression radius / tokens /
i18n al,en / security / contract parity — N/A must say why) → author a checkable exit list BEFORE
code → verify each item PASS/FAIL with proof after. Flags: inline-fix (cosmetic) vs escalate
(contract/money/security). No size exemption. Spec: docs/operating-model/task-exit-rule.md.

## Self-improvement loop (short)

Fix ≠ done without a deterministic guardrail red→green + REGRESSION-LEDGER row. Signals (lessons,
reflections, doubt) are advisory; gates/tests/humans are authority. Qualified change (≥3 files / ≥3
iterations / stage-close / red-line) → reflection with causal WHY to docs/reflections/INBOX/.
Doubt triggers (loop N=3, irreversible, ≥2 interpretations, conflict, novelty) → doubt-escalation
ladder, budget K=2. Red-line globs: auth / money / RLS / packages/db/migrations/ / bulk-edit.
Full spec: docs/operating-model/claude-md-reference.md §self-improvement.

## Tooling pointers (details in docs/operating-model/claude-md-reference.md)

- **repowise MCP**: get_answer / get_context (skeleton) / get_symbol / search / get_why / get_risk /
  distill / expand. Trust `verified:true`; re-read only on stale/approximate/bm25/low-confidence.
- **codebase-memory MCP**: graph-first for structure (AGENTS.md rule); project `root-dowiz`.
- **Ponytail** (/ponytail*): lazy-senior mode — YAGNI → stdlib → minimum viable; defined in AGENTS.md.
- Build/lint/typecheck: `pnpm build|lint|typecheck`. Distill noisy commands: `repowise distill <cmd>`.


---

## Live project memory index (mirrored from Claude Code's auto-memory, `/root/.claude/projects/-root-dowiz/memory/MEMORY.md`)

# Project Memory — dowiz / DeliveryOS

> Index capped (operator-approved 2026-07-05, per-call floor cut). Closed/historical topics live in
> ATTIC — recall by reading it; promote lines back only when a topic reactivates.

## Active arcs
- 🎯 SESSION HANDOFF (2026-07-07 late) — Phase 2.2+2.3 COMPLETE, 18/18 tests PASS, staging deployed — **Phase 2.2 (direct checkout via kernel::decide)**: 4 RED-proved adversarial tests, server-priced cart, idempotency. **Phase 2.3 (customer ownership)**: NOBYPASSRLS gate, erasure oracle, hub-module. **Testing**: 4 unit + 14 e2e + 10 Playwright. **Deployment**: Staging live (health: 200), all pre-commit gates PASS. **Next**: /reliability-gate L0–L11 trace, red-line approval, prod merge. Full doc: docs/design/sovereign-core-mvp/HANDOFF-2026-07-07-SESSION.md
- 🎯 FRESH SESSION (2026-07-07 late) — token-reduction enforcement LIVE + Haiku pin staged — **TWO commits:** (1) A1/B1/B0/B2 enforcement + apply-path bug fixed; (2) §14 live Haiku measurement (6× cheaper) + state-dispatch-tick analysis. MVP: $247–304 lead-loop. Next: cp pin, measure real Haiku, check 4 falsifiable gates.
- 🧠🛡️ RESUME — retrieval engine recall@5=1.0 + meta-cognition layer (2026-07-07) — living-knowledge engine DETERMINISTIC **recall@5=1.000** on hard 29-q oracle. Meta-cognition layer wired. Opus PASS. NEXT: bigger oracle; autonomous fix-loop.
- 🧠 Living Knowledge + HelixDB + multi-band activation (fresh-session BUILD) — operator 2026-07-07: build living knowledge; helix-db CHECKED (Rust graph+vector DB, Apache-2.0, embeddable, unifies repowise/codebase-memory/VSA — promising, needs ground-truth eval before adoption); activation function = deterministic spreading-activation over the store (bands=edge-type profiles). 5-step gated plan in the doc (spike→eval→integrate-if-better→activation-fn→wire). Deferred from build (300K budget gate); NOT integrating a new DB blind. See [[ground-truth-over-proxy-2026-07-07]] §7
- 🧭 NEXT ARC (fresh session) — agent 0-tool-use + hook-enforced anti-rot — operator 2026-07-07: examine why subagents sometimes return 0-tool-use/degenerate (2 of 4 this session echoed a system-reminder w/ 0 tool_uses — intermittent, likely deferred-tool-load race or injected-reminder echo); use FABLE to examine agentic tooling/skills/docs + model-agnostic-playbook + loops + proxy mistakes; ENFORCE hook-enforced discipline (Structural Roots 3+4) across several rot surfaces; + EXAMINE proxy efficiency & prune net-negative proxies (measure hit-rate/cost, add a ground-truth check first — don't remove on a vibe)
- 🧭 RESUME HERE — 2026-07-05 session end-state — token-economy arc complete; rebuild R2a done (keep 61); S1 astro reverted (parity matrix = re-flip DoD); S2 parity gap + degrade-storm findings; S5 money council 1 round from STOP-DESIGN-B; MODEL ROUTING v2 (no Fable for lanes)
- 🚀 REBUILD: staging cutover state — surfaces on rust w/ proofs; S8 held (webhook council); prod gates: S5/S9 flip, 085 date, Phase-D, migrations placement; live state = docs/ops/rebuild-cutover-h_t.json
- ⚡IN-FLIGHT: audit-remediation orchestration — branch fix/audit-remediation; conductor map
- ⚡IN-FLIGHT: staging-gated merge to main — operator-authorized, gated staging-green
- 🔴 SECURITY INCIDENT: creds in git history — rotated; REMOTE scrub force-push = open operator gate, HARD blocker for any prod push
- FINAL goal: open-source per ADR-020 (gated) — AGPLv3+TM+DCO; gated on secrets+EUTM
- Security sweep findings (blue-team, red-line) — 5 authz findings, council-gated; docs/security/
- 🖼️ Demo preview + outreach upgrades — 12 demos are SHADOW (owner_id NULL) → P6-2/P6-3 forces generic OG by design; operator chose FULL per-venue unfurl = RED-LINE override, gated on counsel/ethics before code; plan docs/design/demo-preview-upgrades/PLAN.md (WS1 OG-image / WS3 QR+footer+TG-preview / WS4 video montage)

## Sovereign Core (dowiz-core)
- 🎯 0b-5 RED Proof COMPLETE (2026-07-07) — deployed-reality proof of execution
- 🧭 RESUME HERE — Sovereign Core MVP plan + handoff (2026-07-06) — full corpus docs/design/sovereign-core-mvp/ (MANIFESTO/DECISIONS/ANALYSIS/GRAND-PLAN/LEAD-REVIEW/STRUCTURE-UPGRADE/PROGRESS); MVP = own-channel owner data-hub on a deterministic event-sourced Rust/WASM core. Part B + Part A COMPLETE. **0b-1 money boundary (`c10814ab`) + 0b-2 event vocab/Envelope (`e3e30ac1`) + 0b-3 corridors-behind-`decide` (`31520e8a`) ALL DONE + PUSHED.** 0b-3: `decide(&OrderState, Command, &Context)` composes machine→actor-gate→cc1→pricing, emits full event set; operator chose the observed-`Context` signature; core invents no money number; guardian PASS/high; reflection `2026-07-07-0b3-decide-composition`. **→ NEXT = 0b-4 (Hard Truth L1–2, mostly satisfied by the 0b-3 proptests) then keystone 0b-5 (shell flips to `kernel::decide` — deployed-reality, red-line).** 12 steps to the MVP exit gate; 5 red-line (0b-5,1.1,1.2,2.2,2.3); the two L's = 1.2 (persistent event log) + 2.2 (checkout). A3 orders-split BLOCKED on 0b-5. Live resume cursor = docs/design/sovereign-core-mvp/PROGRESS.md
- 🦀 Sovereign Core Phase-Zero — Step 1 DONE (staged) — wasm32 + clippy disallowed-methods gate live/proven; wasm caught real uuid-v4 entropy dep (moved to dev-deps); CI staged proposed-sovereign-core-ci/; Step 3 relocation deferred until S5 merges; blueprint docs/design/dowiz-core-phase-zero/

## Token economy (all measured)
- 🗜️ Headroom compression proxy LIVE (2026-07-07) — ANTHROPIC_BASE_URL→127.0.0.1:8787, systemd user service; ledger-measured ~1,619 tok/request saved on live claude-code traffic; synthetic probes read 0% (measure via ledger, not marketing); WATCH prompt-cache alignment; rollback = remove env key
- 🎯 VSA token economy — tools/vsa codec 34.3%; TOKEN ROUTER honest range −20…−55%; probe report docs/research/token-economy-comparison-2026-07-05.md
- 🖼️ VSA-VIZ visual arbitrage — state→image ~fixed cost; crossover ~25-30 entities; macro/delta/blind-orchestration tiers
- 🕸️ codebase-memory-mcp — structural graph, A/B 54.9%; graph-first rule
- Model routing v3.4: Haiku 4.5 lead+reasoning (operator "push further" 2026-07-07 late) — Opus→Haiku pin staged (proposed-settings, operator cp); opus red-line rail (money/auth/RLS/migrations: 0b-5/1.2/2.2/2.3); free-LLMs gated (data-governance red line); explicit model: on every Agent call
- ⏱️ Token lifecycle thresholds (anti-context-rot) — lane KILL+recycle @>80K tok; session save+push+/clean+fresh @300K; auto-restart launcher; always push before session end; always token-reduce
- 🔌 KNOWLEDGE-AS-CIRCUITS + THE EYE (mechanical harness) — lessons/patterns/design-rules/lib-best-practices → registry.json circuits (run-circuits.mjs); research-first→docs/libraries/; eye halts @>=3 bad/>=1 critical; hooks staged, pushed on feat/sovereign-core-phase-zero

## Brand & design
- 🎷 Dowiz brand = Warm Cosmo-Noir + dry voice is project-wide canon (2026-07-07) — Cowboy Bebop × cosmo-gothic × Ukrainian irony; "Hybrid is a feature, not a bug"; `[data-skin="bebop"]` replaced Paper; texting voice EVERYWHERE incl. sales/marketing (money/auth plain); bible: docs/design/dowiz-brand/BRAND-BIBLE.md

## Standing rules & fixtures
- 🔬 VERIFIED-BY-MATH (universal; only falsifiable math-proof validates) — operator 2026-07-07: 1.works? 2.provable-with-math? 3.falsifiable? No false-positive metrics; ship the RED case. Enforced by guardrail-falsifiable-proof.mjs. Extends §0·GP + Mandatory Proof Rule.
- ⚖️ GROUND TRUTH over PROXY REASONING (§0·GP, governing) — operator 2026-07-07 final: council + critic/review proxy agents + advisory hooks REMOVED (f1255ad5), deterministic harness gates KEPT; §0·GP now governs the model-agnostic playbook (8d0b2607); dynamic pre-commit added; Fable one-shot spec (b61177eb). Deterministic math truth may delete rotten/biased processes. OPEN: Fable one-shot (human override) + 0-tool-use SubagentStop checker + MVP 0b-4/0b-5 (fresh session)
- 🧠 Metacognition transfer → cheap models (universal) — compile expensive-model reasoning into cheap-doer checklists; 2 meta-moves: reconcile-vs-reality + RED-prove-every-guard; doc committed 7d0926bc
- Council gate DISABLED (operator-approved 2026-07-05) — serious-gate.sh unregistered from settings.json (human applied via cp); council optional, not required; route-request nudge remains
- Ship discipline rule — commit → staging deploy → Playwright+tests proof
- Test-integrity rules (no false-greens) — 10 banned classes + money-RLS-PII red-lines
- Never bypass human-gated decisions — blanket permission ≠ per-change approval
- Cognitive-bias rule — ask, don't assume from single statements
- Ask-don't-guess (operator 2026-07-07) — unclear/vague → research then ASK; never guess; operator always ready to answer
- Agent Operating Model — two speeds + boundary + 4-label classification
- Deploy topology — prod vs staging Fly/Supabase
- Staging Fly access — staging-scoped token + DEV_AUTH_SECRET=stg-e2e-secret
- Staging DB access recipe — DATABASE_URL_MIGRATIONS/OPERATIONAL/SESSION; no psql in image (proxy 15432)
- Staging deploy build-args — use scripts/deploy-staging.sh or features bake OFF
- Test owner fixture — test@dowiz.com/test123456 via POST /api/auth/local/login; demo loc 28239442-63a1-431e-8cab-2e4ed64ab8e7

## Archive
- ATTIC — ~85 closed-topic lines

