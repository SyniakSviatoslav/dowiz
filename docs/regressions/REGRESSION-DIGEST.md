# Regression Digest — dowiz / DeliveryOS

> GENERATED VIEW. The `REGRESSION-LEDGER.md` is authoritative; this digest is
> a one-line-per-row readable projection. Each row links back via its id.
> Regenerate with `cargo run --manifest-path tools/ops-alert/Cargo.toml --bin regression-digest`.

SUMMARY: 9 live guardrails | 9 green | 0 unverified (generated via regression-digest by regression-digest)

| # | class (≤15 words) | guardrail type | how to run | status | last verified |
|---|---|---|---|---|---|
| 18 | Agent burns turns stuck and the loop-detector never notices: (a) a limit cycle across ≥2 | unit + hook (Markov attractor, red→green + e2e) | path: tools/loop-signals/markov_attractor.py models the tool-outcome stream as a first-order Markov chain (SLP3 App. A: transition matrix A, stationary π) → deterministic signals: entropy-rate, escape-mass (long-run occupancy of a progress state), Foster-Lyapunov drift, + spectral SLEM/period via Faddeev-LeVerrier char-poly + Durand-Kerner roots (closes the hydraulic-loop-v2 μ≈−1 eigensolver gap). Progress-vs-probe fix: benign reads (ls/cat/grep) are neutral probe, not progress. Wired ADVISORY into .claude/hooks/loop-detector.sh (fail-open, dedup'd, never blocks, augments the N=3 counter). Proof: tools/loop-signals/test_markov_attractor.py 8/8 (RED on limit-cycle H=0 + bipartite-star H=0.79-via-spectral + strange-churn H=1.46; GREEN on edit↔run_ok escape=0.5 + cold start) + e2e driver (live hook fires LIMIT_CYCLE/STRANGE_ATTRACTOR once at the window boundary, silent on healthy) | GREEN | 2026-07-13 · a6a299b4 |
| 20 | Agent wastes a commit-retry cycle: errors only surface at git commit (post-edit), and the old | CI-gate (tool, fail-closed) | path: tools/verify-scope.sh — callable on-demand (bash tools/verify-scope.sh [paths]) before staging; mirrors .husky/pre-commit routing so edit→verify errors are caught early and the commit-retry cycle shrinks. 2026-07-17 PRUNE (row 20): the legacy JS/TS (eslint npx) and web/Astro (astro build) branches were removed — the legacy thin-layer (row 21) was deleted 2026-07-13, so those scopes no longer exist. The guardrail now routes only the surviving Rust surface (kernel/ → cargo test, engine/ → cargo test), fail-closed. bash -n clean; run on a kernel path gates cargo test, engine path skipped cleanly. Skill equivalence-proof-before-delete (Hermes runtime) captures the RW-02 rewire-proof pattern so future sessions don't reinvent it. | GREEN | 2026-07-13 · d5e04920 (pruned 2026-07-17, f4802927e-wave) |
| 21 | Legacy JS/TS thin-layer (web/, apps/web, packages/ui, shared-types, domain) duplicated kernel logic and was the only | CI-gate (structural) | path: Removed the entire legacy thin-layer (261 files) in feat/remove-legacy-thin-layer; kernel (Rust/WASM) is now the sole source of truth. The next UI (P6 thin-client per roadmap) is drawn fresh on the kernel wasm surface. .husky/pre-commit dropped eslint/pnpm branches (legacy JS gone), keeps kernel cargo test + python tooling gates; pnpm branch guarded on package.json existing. tools/verify-scope.sh (row 20) updated to match. | GREEN | 2026-07-13 |
| 46 | No signed, subscribable abuse blocklist existed: a Wave-0 hub operator had NO cryptographically-signed, opt-in signal | cargo-test (red→green) + grep-CI-gate (field/type) | path: kernel/src/blocklist.rs — AbuseBlocklist (canonical, order-significant TLV) + SignedBlocklist (RequireBoth hybrid sig via capability_cert::HybridSig, NOT the pq-gated pq/hybrid.rs KEM — principled deviation: the real hybrid signature seam is default-built, so M2 runs under cargo test --lib default features). Tests: m2_sign_verify_roundtrip (green); m2_reorder_entries_fails_verify + m2_truncated_entries_fails_verify (canonical re-encode teeth — stale sig on mutated list rejected); m2_single_half_forgery_rejected (AND-verify — valid-classical/junk-pq or vice-versa both rejected, B4 lesson); m2_over_cap_rejected_at_construction (over MAX_BLOCKLIST_ENTRIES=10_000 refused); m2_is_flagged_returns_option_reason (Option<ReportReason> — compile-proof no number returned); m2_subscription_trust_is_advisory_only (SubscriptionTrust enum has ONLY Advisory — Enforcing/Ranking unrepresentable, so the §16.26/§16.59 auto-filter red line is a type). blocklist::is_flagged is advisory-only membership, never an input to dispatch. | GREEN | 2026-07-18 |
| 47 | A moderation datum could silently reach the HRW matcher / discovery signal, re-introducing a courier-quality/rank | cargo-test (runtime invariance + decode-gate) + grep-CI-gate (no moderation→matching import in bebop2/proto-cap, plus kernel-side field-clean mirror) | path: kernel/src/moderation.rs — ReportReason enum has NO quality/service variant (type-level §16.59 anti-scope; ReportReason::from_u8 returns None→UnknownReason for any reason byte outside the abuse enum, so a quality-code smuggle at the byte boundary is Law-rejected, persisting nothing). m1_ tests: valid abuse report commits + idempotent replay (Duplicate no-op, no re-run of decide, one row); m1_quality_code_rejected_nothing_persists; m1_note_too_long_rejected; m3_exploitative_content_decode_flags_operator_escalation (ExploitativeContent is the ONLY legal-escalation decode-flag → hub operator, never dowiz). kernel/src/blocklist.rs M4 mirror: m4_advisory_returns_no_score_type + m4_blocked_actor_has_no_score_field (no score/rank/rep field — the field-name CI guard §0 auto-covers). m3_kernel_exposes_no_cross_hub_content_api (no cross-hub read/edit/delete surface exists — a "takedown into a hub" is inexpressible; dowiz's only cross-hub lever is liveness/availability). The load-bearing runtime assign-invariance test + ci-no-moderation-in-matching.sh live in bebop-repo (T4, out-of-tree here): assign(order,cands,max) accepts only (order_id, pubkey), so a poisoned moderation input is provably outside its domain → the regression is RED only if someone wires it in. | GREEN | 2026-07-18 |
| RF-1 | A foreign compiled DecisionUnit (Layer F MoE mesh rung-2) could be admitted to Live and | cargo-test (red→green) | path: kernel/src/decision/import.rs — import_unit replays the FULL harvested instance set through the candidate unit AND compares to the local oracle's expected verdict; any single-case disagreement ⇒ ImportReject::ReplayDisagreement, nothing persisted. decision::import::tests::poisoned_unit_rejected_nothing_persisted is the RED→GREEN proof (attacker flips case 1 of 2 ⇒ rejected, log length unchanged). Mirrors EventLog::commit_after_decide_drift_gate (Hermetic RC-2). BLUEPRINT-P-F §4.2 / §6.3 A1. | GREEN | 2026-07-19 |
| RF-2 | A DecisionUnit artifact could exceed the transport bound (1 MiB) or carry a content-id that | cargo-test (red→green) | path: kernel/src/decision/import.rs — import_unit rejects OversizeArtifact (> 1<<20) and MalformedArtifact (sha3_256(artifact) != meta.content_id) BEFORE any replay/persist. Tests: oversize_artifact_rejected, tampered_artifact_rejected. Cross-repo constant-drift guard (BLUEPRINT-P-F D6) keeps MAX_UNIT_ARTIFACT_BYTES == bebop2 sync_pull.rs:159. | GREEN | 2026-07-19 |
| RF-3 | A Pricing (money-gated) DecisionUnit could be admitted Live and start moving money without an explicit | cargo-test (type-level) + grep-CI-gate (domain bar) | path: kernel/src/decision/import.rs — import_unit builds the admitted unit via DecisionUnit::new, which sets operator_activation = Pending for any money_gated unit and Activated otherwise (structurally unreachable to arrive pre-activated; MoneyGateRequired reserved for the operator-activation path's GateError::NotMoneyGated). decision::tests::pricing_not_invocable_before_activation proves decide() answers Escalate(MoneyGateLocked) until the operator event. No Activated/Enforcing/Ranking variant exists in the import surface. BLUEPRINT-P-F §6.3 A6 / §7. | GREEN | 2026-07-19 |
| RF-4 | scripts/verify-kernel-engine.sh (the documented one-shot local/CI-mirroring gate) failed closed at its wasm32 step: cargo build --target | cargo-build-gate (red→green, wasm32 target) | path: kernel/src/bin/markov_attractor.rs — the DOWIZ_FDR_DIR opt-in init block and the trailing emit_verdict_pmu call are now #[cfg(not(target_arch = "wasm32"))]-gated (matching the gate already on their definitions in fdr/mod.rs); this bin was always native-only (check.sh hook consumer) and was never meant to build for wasm32. RED: cd kernel && cargo build --target wasm32-unknown-unknown --release failed with E0425/E0422 (symbols "configured out") at HEAD (6701bbb) before this fix. GREEN: same command exits 0 after the fix; cargo test --lib unaffected (938/938 kernel, 117/117 engine still pass); native markov_attractor --selftest and a stdin functional smoke test produce byte-identical output to before the change (the JSON contract in the file's own doc comment is preserved). Found while investigating why scripts/verify-kernel-engine.sh — the documented "one-shot local gate before pushing" — was red. | GREEN | 2026-07-20 |

---
*Readability sign-off (operator): ___ how many live / which red / re-run row N ___ (DoD-c)
