# Regression Ledger — dowiz / DeliveryOS

> **Tier-1 Regression Ratchet** (HARNESS infrastructure, not product).
> One row per bug class that recurred ≥1× or is clearly recurrence-prone. Each row
> names a **deterministic guardrail** that makes the bug physically hard to reintroduce.

## Ratchet process rule (standing)

> **Every future fix adds a guardrail (with a red→green proof) and a ledger row BEFORE it is "done".**

- **Red→green proof** = the guardrail must FAIL on the bad behavior (the bug), and PASS on
  the corrected / current code. "It should work" is not proof.
- The guardrail must also be **green on the whole current repo** — a rule that flags existing
  legitimate code is mis-scoped; narrow it until it only catches the regression.
- **Monotonic / ratchet-only**: never weaken or delete an existing gate, test, or rule to go
  green. No `skip` / `.only` / `fixme` / inflated timeout / `expect(true)` / commented-out
  assertions.
- **Reversible**: each guardrail is removable, but only with a written justification appended here.
- Guardrail types: `eslint` (code-pattern), `boot-guard` (FATAL on bad env/state at startup),
  `migration` / `release_command` (schema-drift), `E2E` (UI/contract proof), `CI-gate` (pipeline),
  `unit/integration` test.

## Ledger

| # | Symptom | Root cause | Guardrail type | Where (file / test / rule) | Date / commit |
|---|---------|-----------|----------------|----------------------------|---------------|
| 53 | The plane-maintainer's first firing from a genuinely fresh cloud checkout (no `loops/runs/predictions.jsonl` on disk) ran `plane-telemetry.mjs publish`, which silently DELETED `telemetry/predictions.jsonl` from the append-only `telemetry/plane` branch — all 4 outstanding calibration predictions from 2026-07-02 were briefly gone from the durable record | `collectPublishFiles()` enumerated only LOCAL scratch files; `cmdPublish()` rebuilt the branch's whole tree from that local-only set, merging branch-vs-local per already-known file — any branch file with no local counterpart (predictions.jsonl, on a box that never wrote one) was never in the set at all, so it silently dropped out of the new tree on push. The append-only contract was true in a code comment, false in the actual tree-build for the fresh-checkout / partial-local case | `unit` (red→green, real git plumbing fixtures) | `scripts/plane-telemetry.mjs` — new `remoteTelemetryNames(tip)` (git ls-tree at the remote tip) unioned into the publish file set so any branch-only file carries forward unchanged. `scripts/plane-telemetry.test.mjs` new test "publish: a fresh checkout with no local predictions.jsonl must NOT delete…" — RED proven (stashed the fix, test failed with `predictions.jsonl was DELETED`), GREEN after restoring the fix; full suite 23/23. Real data recovered from branch commit `373ca35` and republished with the fixed script — verified via `git ls-tree` that `telemetry/predictions.jsonl` + `telemetry/plane-events-2026-07.jsonl` now coexist on `origin/telemetry/plane`. Found by the plane-maintainer agent during its own SENSE step (self-diagnosed, not human-reported). No product/UI/API surface — governance-plane tooling only, proof is the red→green unit test (Ship Discipline docs-only-mixed-change note: code file present, so this is NOT a docs-only change, but there is no staging deploy surface for a git-plumbing CLI script; N/A recorded explicitly) | 2026-07-03 · this change |
| 38 | "Integrate STORM + Scrapling and scrape the food-delivery market" risked (a) a 3rd redundant research lane (STORM vs DeerFlow vs in-tree deep-research), (b) abusive/ToS-violating/PII-harvesting scraping, (c) pulling the Python research stack into the Node tree | open-ended "scrape all" + stacking overlapping research engines, no conduct boundary | `CI-gate` (exit-1) + `ethics/conduct-gate` (exit-1) + out-of-tree pilot | `scripts/guardrail-license.mjs` FORBIDDEN-DEP += `knowledge-storm`/`scrapling`/`dspy` (out-of-tree only). `scripts/scrape-pilot/scraping-conduct-attest.mjs` (fail-closed: missing robots/rate-limit/public-only/no-PII flag or an auth-walled/private target → exit 1; clean public config → exit 0, red→green proven). Coherence: ONE research lane (deep-research default; STORM/DeerFlow benchmark-then-pick-one); Scrapling = complementary fetch layer. `docs/research/storm-scrapling-pilot.md` + first-pass intel `docs/research/food-delivery-market-brief-2026.md` (cited; dowiz direct-ordering thesis market-validated) | 2026-06-29 · this change |
| 37 | Three external AI tools requested for integration (ByteDance DeerFlow, PurpleAILAB Decepticon, EvoMap) could (a) pull the already-DEFERRED LangGraph/LangChain into the product tree via DeerFlow, (b) run an offensive C2/attack-chain agent (Decepticon) against a non-dowiz target — an Ethics-Charter violation, or (c) egress dowiz data/capabilities to EvoMap's external GEP marketplace | "integrate" of heavyweight/dual-use external frameworks with no scan/boundary; DeerFlow is LangGraph-based (council-DEFERRED), Decepticon is offensive (charter-gated), EvoMap is an external capability network (egress/commons-capture) | `CI-gate` (exit-1) + `ethics-gate` (exit-1) + out-of-tree pilot scaffolds | `scripts/guardrail-license.mjs` FORBIDDEN-DEP extended (`deerflow`/`deer-flow`/`langchain`/`@langchain/`/`decepticon`/`evomap`) — blocks them as a dep/import; green on repo + new tokens trip a planted lock entry. `scripts/decepticon-pilot/authorized-scope-attest.mjs` (fail-closed: non-dowiz target → exit 1; dowiz-owned/loopback → exit 0, red→green proven) enforces the charter's authorized-own-infra-only scope. Pilots are OUT-OF-TREE only (`docs/research/{deerflow,evomap,decepticon}-pilot.md`, STATUS pending operator) + the shared no-credential attestation; never product deps/runtime | 2026-06-29 · this change |
| 36 | The AI menu-parser re-spent LLM tokens on identical inputs — a re-import of the same PDF, a retry after a transient failure, or the same scraped page across runs each paid full prompt+completion tokens, with no reuse (token-economy waste, ADR-0012) | `ai-ocr-parser.ts` called the provider unconditionally; no response cache. Risk if naively added: a stale parse served after a schema/model change, or unbounded memory growth | `unit` (deterministic cache invariants) + token-economy | `apps/api/src/lib/ai-ocr-parser.ts` adds a bounded (cap 200, FIFO-evicted), 24h-TTL'd LLM response cache keyed by sha256(provider+model+**full prompt**) — the prompt embeds the schema, memory examples AND the redacted source, so ANY change misses (never a stale serve); only a Zod-VALIDATED response is cached (never garbage). Guardrail `apps/api/tests/ai-ocr-cache.test.ts` (3/3): key determinism + input-sensitivity, hit-after-set/miss, FIFO eviction past the cap. An identical parse now costs ZERO tokens | 2026-06-29 · this change |
| 35 | A tenant who picks a **pale brand primary** ships an illegible storefront: the CTA fill was the naive `color-mix(primary 85%, black)` with hard-coded WHITE text (`tokens.css`), so white-on-pale buttons/prices fell below AA 4.5:1. `derivePalette` auto-corrected body text + a primary-as-text token, but never the primary *fill* + its text — so the loudest element (the buy button) could be invisible | sellable-polish branding pass. The fill/onPrimary pair was a static CSS `color-mix` default, blind to the chosen hue's luminance; `BrandingPage` only *warned* (at 3:1, not 4.5) and never auto-corrected the fill | `unit` (deterministic contrast, no DB/browser) | `packages/ui/src/theme/palette.ts` now derives `{primaryStrong, onPrimary}` — primary nudged until the text pole clears AA on it — and `ThemeProvider.tsx` applies `--brand-primary-strong`/`--color-on-primary`, auto-correcting every tenant's CTA. Guardrail `packages/ui/src/theme/palette.contrast.test.ts` (node:test): RED arm proves the old naive default is sub-AA on pale brands; GREEN arms prove the derived CTA pair + text/bg + primaryReadable/surface all clear 4.5:1 across light AND dark brands. 64/64 ui tests green | 2026-06-29 · this change |
| 34 | A legitimate **frequent customer** dead-ends at checkout: `POST /api/orders` returns a *soft* velocity speed-bump (`200 {outcome:'soft_confirm', reasons:[{code:'velocity'}], requiresConfirmation:true}`, P26 anti-fake-signals, default 3 orders/hr per device-IP), but the FE only handled `soft_confirm` when `requiresOtp` was true → the velocity case fell through `OrderCreateResponse.parse()`, threw, and the catch surfaced the generic "order failed / call the restaurant" banner. A regular ordering 3+×/hr could not order at all | `CheckoutPage.submitOrder` treated every non-OTP, non-hard-block 200 body as a created order; a `soft_confirm` with no `id` is not, so it threw. A SOFT signal is a confirm-prompt, not a block — the FE had no path to acknowledge it (only the OTP soft-reason was acked) | `E2E` (deterministic, stubbed contract) + real-staging no-regression | `e2e/tests/flow-simpl-s1-velocity-frictionless.spec.ts` — stubs the live soft_confirm body, asserts the FE silently resubmits ONCE with `acknowledged_codes:['velocity']`, lands on the order page, and the failure banner never shows. RED proven vs the deployed (old) FE (URL stays `/s/demo`); GREEN after deploy. Fix: `submitOrder(verifiedToken, ackCodes=[])` auto-acknowledges non-OTP soft reasons on a single bounded retry (`ackCodes.length===0` guard, no loop) — zero added customer friction; `requiresOtp` stays a real gate (never auto-acked), `hard_block` still shows review-cart. Backend order-create verified healthy. Found during the 48h staging verification pass | 2026-06-29 · `3ffedd9a` |
| 33 | A NEW migration adds a `SECURITY DEFINER` Postgres function WITHOUT a pinned `search_path` → privilege-escalation / RLS-bypass vector (a caller who can create an object in an earlier-resolved schema — notably an *implicit* `pg_temp`, searched first for relation names — shadows a relation the definer touches). 4 live fns (incl. the RLS lynchpin `app_member_location_ids()` + `read_public_menu()`) already ran unpinned; the earlier staged remediation pinned the **weaker** `pg_catalog, public`, leaving `pg_temp` implicitly first → still shadowable | pg-privilege-hardening (proposal §6). The definer-search-path rule keyed on nothing deterministic — only a one-off audit/`cleaning-loop` memo had flagged it, so a future `CREATE … SECURITY DEFINER` could silently re-introduce an unpinned fn; and the correctness subtlety (`= public` ≠ `pg_catalog, public, pg_temp` — only the explicit-`pg_temp`-LAST form demotes temp out of first-position) was undocumented in any gate | `CI-gate` (exit-1, static, no DB) + staged `migration` + `verify:rls` follow-on | `scripts/guardrail-definer-search-path.mjs` (`process.exit(1)`, text-level scan of `packages/db/migrations/**/*.ts`: every `SECURITY DEFINER` CREATE header must carry `SET search_path` in the same span; frozen `scripts/definer-baseline.json` exempts the 13 historical occurrences only — new files can't extend it). RED→GREEN proven: fixture DEFINER fn w/o search_path → exit 1; add `SET search_path = pg_catalog, public, pg_temp` → exit 0; repo green (15 occ → 13 keys baseline-exempt, 0 unlisted). Staged for operator: MIG-ITEM1 `docs/security/SECURITY-DEFINER-search-path.migration.ts` (re-pins to the strong triple incl. weak `= public` fns), MIG-ITEM2 `…/OPERATIONAL-ROLE-nobypassrls.migration.ts` (strips BYPASSRLS off `deliveryos_api_user`), boot-guard + `verify:rls` probes in `…OPERATOR-HANDOFF.md`. ADR-pg-privilege-hardening, council `docs/design/pg-privilege-hardening/` | 2026-06-29 · this change |
| 32 | A `MultiEdit` to a `protect-paths` zone (`.claude/**`, contracts/schema/infra/governance) BYPASSED the governance gate entirely, and a `MultiEdit` introducing a code red-line skipped the post-edit red-line gate | Hook-matcher drift: `protect-paths.sh` + `post-edit-gates.sh` matched only `Edit\|Write`, while the newer governance hooks (`serious-gate`/`pre-edit-lessons`/`red-line-doubt-gate`/`loop-detector`) were given `Edit\|Write\|MultiEdit` when MultiEdit support landed — the two older security gates were left behind, opening a silent bypass of the exact gate that requires human approval for protected zones | `CI-gate` (exit-1) + config-fix | Aligned both matchers to `Edit\|Write\|MultiEdit` in `.claude/settings.json` + new `scripts/guardrail-hook-matchers.mjs` (`process.exit(1)`, in `verify:all` + pre-commit) asserting all 5 edit-governance gates (protect-paths/serious-gate/pre-edit-lessons/red-line-doubt-gate/post-edit-gates) cover Edit+Write+MultiEdit so the matchers can't drift again. RED→GREEN proven: re-introducing `Edit\|Write` on protect-paths → exit 1; aligned → exit 0. Found during the self-improve pass (operator-directed "fix the harness regression") | 2026-06-29 · this change |
| 31 | The out-of-band Skyvern pilot (AGPL, out-of-tree sidecar) could be handed a dowiz `DATABASE_URL` / RLS / tenant secret, leaking it into an external AGPL process that ships full scraped pages (incl. incidental third-party PII) to a vision LLM | Council `tooling-integration-eval` (Phase 4 / Breaker H4). Skyvern targets exactly the PII-dense contact/about pages the menu-parser deliberately drops; the only machine-checkable control against credential leakage is an explicit attestation that the sidecar env carries none | `CI-gate` (exit-1) | `scripts/skyvern-pilot/no-credential-attest.mjs` (`process.exit(1)` if the sidecar env file contains any `DATABASE_URL*`/`JWT_*`/`*_ENCRYPTION_KEY`/`DEV_AUTH_SECRET`/`REDIS_URL`/`R2_SECRET_*`/`TELEGRAM_BOT_*`/Supabase/service-role/RLS token). RED→GREEN: a `DATABASE_URL` in the sidecar env → exit 1; clean env → exit 0. Pilot is OUT-OF-BAND/one-shot, never wired (artifact `docs/research/skyvern-pilot.md`, STATUS pending operator run; load-bearing egress allowlist is a network-layer control, host-string/menu-only are best-effort per RA-7). ADR-tooling-integration-eval G4 | 2026-06-29 · this change |
| 30 | Integrating external tooling could silently import (a) a copyleft (AGPL/GPL) third-party dep into the shipped tree, (b) an ADR DEFER/REJECT package (langgraph/@workos-inc/skyvern/recall/moneyprinter/glossopetrae) pulled back in "just for a pilot", or (c) a new external-service env (SKYVERN_BASE_URL/DEEPEVAL_*/CONFIDENT_AI_*) added without a subprocessor entry — each a license / vendor-lock / PII-egress regression on a 🔴 boundary | Council `tooling-integration-eval` (cross-cutting G5). `scripts/compliance-gate.ts` had NO license logic; its check B was a CLOSED `SERVICE_ENV` allowlist a new external env was never examined by (Breaker H2); a naive `*_(URL\|KEY\|TOKEN\|SECRET\|DSN\|ENDPOINT)` pattern detector storms on ~18 internal secrets and a substring `GPL` match denies permissive `LGPL` (RA-1/RA-2) | `CI-gate` (exit-1, whole-tree) | `scripts/guardrail-license.mjs` (`process.exit(1)`, in `verify:all` + pre-commit): (a) scans the installed third-party closure (`node_modules/.pnpm`, first-party `private` exempt, deduped), evaluates SPDX **properly** (OR passes if any disjunct permissive; AND denies if any conjunct copyleft; `WITH` honoured; **LGPL ≠ GPL**, exact IDs) → denies `AGPL-*`/`GPL-2*`/`GPL-3*`; a missing-license third-party dep is cleared only via a reviewed `compliance/license-exceptions.md` row; (b) forbidden-dep list checked in `pnpm-lock.yaml` + source imports; (c) `compliance/env-classification.md` classification gate — every matched config env must be `internal`\|`external-subprocessor`, external → subprocessor must be in `compliance/subprocessors.md`, fail-closed on any unclassified env. RED→GREEN proven: planted AGPL pkg → exit 1; `LGPL-3.0`/`(MIT OR GPL-2.0)` → exit 0; `GPL-3.0-only`/`Apache AND GPL` → exit 1; source import of `skyvern` → exit 1; unclassified `DEEPEVAL_API_KEY` → exit 1; clean tree → exit 0 (found+cleared 3 real missing-license deps + added the Google subprocessor row). ADR-tooling-integration-eval G5 | 2026-06-29 · this change |
| 29 | The AI menu-parser ingests UNTRUSTED scraped/OCR content concatenated into a Claude prompt (`ai-ocr-parser.ts` sink ~:544) with no adversarial corpus continuously proving it ignores injected directives (OWASP LLM01); and an adversarial test corpus is itself an injection-delivery risk if a fixture ever reaches a prompt-assembly path or ships in the runtime image | Council `tooling-integration-eval` (Phase 1) — no inert injection corpus existed, and a naive corpus location (under `apps/api/`) would be swept into the Docker `COPY apps`/`build-apps.ts cpSync` paths, turning inert test data into a live injection; the original eslint-warn "floor" merged green (warn≠block, staged-only) | `CI-gate` (exit-1, whole-tree) + `integration` (behavioural, real parser) | Corpus relocated to **repo-root `tests/injection-corpus/`** (20 synthetic OWASP-LLM01 fixtures across instruction-override/role-play/encoding-obfuscation/token-smuggling/exfil-bait) — structurally outside every `COPY`/`cpSync` path + `.dockerignore`. `scripts/guardrail-corpus-reachability.mjs` (`process.exit(1)`, in `verify:all`+pre-commit, NOT a warn-lint) enforces 4 layers: structural (corpus not under any build-reachable root, read from real Dockerfile/build-apps), sentinel/dir-name content scan of non-test `apps`/`packages`/`scripts` (self-skip own file, RA-8), import-edge, fixture structured-PII gate. Behavioural `apps/api/tests/injection-corpus.test.ts`: deterministic heuristic arm (always-on, no key) asserts every fixture parses as a menu + leak-marker absent; LLM arm gated `RUN_INJECTION_CORPUS_LLM=1`. RED→GREEN proven: non-test source referencing the sentinel → exit 1; an `injection-corpus` dir under `apps/` → exit 1; an email-bearing fixture → exit 1; canonical layout → exit 0. ADR-tooling-integration-eval G1 | 2026-06-29 · this change |
| 28 | flow-simplification §5 honest-dispatch (no-trap red-line F1) had no deterministic guard — an order could in principle reach `IN_DELIVERY` with NO courier bound (an orphan with no recovery affordance), or a double-bind on an already-`offered`/`assigned` order | The dispatch decision lived INLINE in `orders.ts` (the untested-hotspot biomarker) where it could only be exercised through a full HTTP+staging path — no unit-level proof that "no courier → never advance" or "already bound → no double-bind"; a refactor could silently invert the order-of-operations (advance before finding a courier) | `integration` (real PG, red→green) | Extracted the decision verbatim to `apps/api/src/lib/dispatch.ts::attemptHonestDispatch` (runs inside the caller's tx — finds a courier BEFORE advancing; no courier → stay at current status `{dispatched:false,reason:'no_courier'}`; active binding → `{reason:'already_assigned'}`, no double-bind). `apps/api/tests/flow-simpl-honest-dispatch.test.ts` 3/3 vs the real head schema (`DV2_TEST_DATABASE_URL`): no-courier → order stays CONFIRMED + 0 bindings; available → atomic IN_DELIVERY + assignment + shift on_delivery; already-offered → no second binding. RED proven by removing the no-courier guard → test 1 fails (order would orphan). `orders.ts` now calls the extracted fn (−42 lines from the hotspot) | 2026-06-29 · this change |
| 26 | deliver v2 (Cash-as-Proof) red-lines: (a) GDPR GPS-crumb retention silently anonymizes 0 rows (delivery_trace is tenant-scoped FORCE → a context-free operational UPDATE sees nothing → indefinite PII retention while a schedule-existence assert stays green); (b) the cash-as-proof HOLD/trace written on only ONE of two completion paths (owner-proxy wrote none → silent unreconciled debt); (c) an order stranded out of IN_DELIVERY with a dangling active assignment (courier blocked forever); (d) the ethical bond justification leans on a Stage-21 no-auto-deduct/no-courier-scoring invariant that doesn't exist yet | Council-hardened deliver v2 (ADR-deliver-v2-cash-as-proof) — the dangerous Stage-20 correlation engine was never built (delete→never-build); the real risks are tenant-FORCE retention efficacy, two-completion-path parity, the widened IN_DELIVERY edge's unaudited callers, and the carried Stage-21 ethics invariant | `integration` (real PG NOBYPASSRLS) + `unit` + `pending-guardrail` | (a) `anonymize_stale_delivery_trace(interval)` SECURITY DEFINER (migration 073, 7d dispute-window floor) + `delivery-trace-retention.efficacy.test.ts` 2/2 — OUTCOME-based under a real NOBYPASSRLS caller (raw UPDATE sees 0 → RED; DEFINER fn anonymizes ≥2 cross-tenant → GREEN) + boot-assert `assertDeliveryTraceSchedule`. (b) `lib/deliveryCompletion.ts::completeDelivery` single primitive both paths call + `deliver-completion.test.ts` 4/4. (c) the terminalize-fold in `updateOrderStatus` (no order leaves IN_DELIVERY without its active assignment terminalized) — proven in the same suite. (d) `stage21-no-auto-deduct.invariant.test.ts` (RED-on-disk pending-guardrail via node:test `todo` — visible every run, non-blocking, flips to a hard gate when ADR-stage21-reconciliation.md carries NO-AUTO-DEDUCT + NO-COURIER-SCORING). | 2026-06-28 · deliver-v2 |
| 25 | P6 claim phase (shadow→owner) could (a) let a logged-in attacker claim a shadow they don't own (IDOR/enumerate acquisition_source_id), (b) auto-publish on claim → reopen the anonymous-real-order CRITICAL, (c) leave the raw scraped PII blob after claim, (d) launder AI-guessed allergens as owner fact, (e) ambush the restaurant with a passing-off first-contact instead of an honest Art-14 notice | New 🔴 RLS+AUTH ownership-transfer surface — the token-as-sole-authority, no-auto-publish (B3), erase-on-claim, allergen-confirmation, and honest-consent guards are all load-bearing and unbuilt in the proposal | `integration` (real PG NOBYPASSRLS) + `unit` | `claim_transfer(token,user)` SECURITY DEFINER carve-out (migration 071, staged) — token validated INSIDE, touches ONLY the target shadow (the inline RLS UPDATE policy can't work: PG requires SELECT-visibility for an UPDATE target; empirically proven). `claim.ts`: markVerified (VERIFIED floor), mintClaimInvite (one-active, 256-bit opaque sha256 token), acceptClaim, declineAndErase (token-only, no-reg → hardDelete), buildArt14Notice (CC1 honest first-contact). No-auto-publish: transfer leaves published_at NULL + status closed (the existing activation gate + menu_confirmed_at interlock keep it un-publishable until reviewed). H-erase: place_raw/menu_draft cleared on claim. K4/CC3 approve: `routes/owner/menu-confirm.ts` sets allergens_confirmed=true ONLY (never source), owner authors allergens into the write-stripped empty fields. C2 read-gate staged `docs/acquisition/migration-1790000000072-c2-read-gate.ts` (verbatim 065/035 + bom-strip CASE, golden-snapshot proof plan, REQUIRES 068). Public routes `routes/public/claim.ts` (accept verifyAuth, decline token-only). PROOF: `claim-rls.test.ts` 7/7 NOBYPASSRLS (IDOR, double-claim race, no-auto-publish, blob-erase, tenant-flip, decline) + `claim-notice.test.ts` 8/8 (Art-14 honesty + no-reg decline). 35/35 P6 total. Migration staged 071 (REQUIRES 068+069+070). | 2026-06-28 · P6-claim |
| 24 | P6-3 menu extraction→render could (a) leak third-party free-text NAMES (staff/testimonials) to the external LLM (the net-new html-scrape path can't inherit the OCR redaction + the redactor has no name pattern), (b) publish AI-guessed allergens as fact pre-claim, (c) let a valid provisioning token write products into a VICTIM tenant's location_id, (d) re-expose the unconsented venue's real name in unfurl OG, (e) publish a low-confidence/fabricated menu with no human in the loop | New 🔴 RLS+AI/PII surface — scrape→AI→write→public render. The PII-before-AI control, allergen gate, products RLS binding, generic-OG, and no-fabrication gate are all load-bearing and were unbuilt in the proposal | `integration` (RLS, real PG NOBYPASSRLS) + `unit` (red→green) | C1: `apps/api/src/lib/menu-region.ts` (menu-region ALLOWLIST drops About/Team/Reviews/footer + `scanResidualPii` fail-closed) + `pii-redactor.ts` (anchored name pattern) + `ai-ocr-parser.ts` (html/text kind converges on the single :404 redaction) → `menu-region-pii.test.ts` 5/5 (stated recall floor). C2: write-strip `bom[].allergens` (`provisioning.ts`) + read-gate `read_preview_menu` `attributes - 'bom'` (mig 070); live read_public_menu re-version DEFERRED to claim-phase (`docs/acquisition/c2-read-gate-claim-phase.sql`). H2: products/categories `provision_shadow` bound to a shadow location via SECURITY DEFINER `app_is_shadow_location()` (mig 070). H4: `menu-extractor.ts::classifyExtraction` (low-confidence/error/0-items → LOW_QUALITY/MANUAL_REVIEW, ENRICHED is the only door to PROVISIONED) → `menu-extractor.test.ts` 6/6. H3/D: `preview-render.ts` generic-OG + noindex + never-orderable labeled preview → `preview-render.test.ts` 4/4. M1: `hardDeleteShadow` clears place_raw/menu_draft. Full proof: `provision-rls.test.ts` 12/12 (incl. menu write, allergen strip, victim-location rejected) — 27/27 P6-3 total. Migration staged `docs/acquisition/migration-1790000000070-provision-products.ts` (REQUIRES 068+069). SSRF/robots via `menu-source.ts` (reuses brand-extractor `assertPublicUrl`) | 2026-06-28 · P6-3 |
| 23 | P6-2 shadow-spine write could (a) be admitted WITHOUT a valid one-time token (RLS-through write bypassed), (b) be replayed / double-spined for one source (two shadow tenants per place_id), (c) mint a NON-shadow org via the provisioning policy (widening), or (d) leave a shadow with no day-one erasure path | New 🔴 RLS write-authority surface (operator decision 1b — one-time provisioning token + narrow additive `provision_shadow` policy). The policy + the in-tx guarded state-transition (advance ENRICHED→PROVISIONED, state-pinned) + consume-LAST are load-bearing; the operational role bypasses RLS today so the proof must run under a real NOBYPASSRLS role | `integration` (RLS, red→green, real Postgres) | `apps/api/src/modules/acquisition/provisioning.ts` (mint single-use grant + `provisionShadowSpine` one-tx: set_config → FOR UPDATE → INSERT spine → advance chokepoint → consume LAST; `hardDeleteShadow` C2) + migration 069 `provision_grants` (FORCE-RLS, partial-unique one-active-grant, reaper idx) + additive `provision_shadow FOR INSERT WITH CHECK(owner_id NULL/status closed AND token EXISTS)` on organizations/locations/menu_versions (built-in `sha256`, zero search_path dep). Proven under a NOBYPASSRLS role: `apps/api/tests/provision-rls.test.ts` 8/8 — (a) token write admitted, (b) no-token rejected, (c) bogus/expired rejected, (d) single-use, (e) NO widening (valid token cannot create owner_id-NOT-NULL org), (f) B1 dedup chokepoint (already-PROVISIONED → no double-spine), (g) one-active-grant, (h) hard-delete. P6-1 regression 9/9 green. Migration staged `docs/acquisition/migration-1790000000069-provision-grants.ts` (operator places + `migrate:up`, REQUIRES 068). Also B2 shadow render-suppression (`spa-shell.ts`/`ssr-renderer.ts`/`seo.ts` — owner_id NULL → noindex + no real-name OG/sitemap), B4 ops-auth decoupled from dev-login (`ops-auth.ts`, `PROVISION_OPS_SECRET`) | 2026-06-28 · P6-2 |
| 22 | P6-1 acquisition pipeline could (a) create duplicate shadow lifecycles for one `place_id`, (b) let a lifecycle stall in a state with no exit (MANUAL_REVIEW sink), or (c) set a terminal/exit state with no `failure_reason` (silent drop) | New vertical with no invariant guards — the state-machine + dedup anchor are the load-bearing correctness | `unit` + `integration` (red→green) | `apps/api/src/modules/acquisition/state-machine.ts` (assertTransition + `everyNonTerminalHasExit` + `REQUIRES_REASON`) + `service.ts` (single-statement `INSERT … ON CONFLICT (place_id)` dedup anchor). Proven: `acquisition-state-machine.test.ts` 6/6 + `acquisition-service.test.ts` 3/3 vs a throwaway PG (idempotent dedup → exactly 1 row/place_id; legal/illegal advance; reason invariant) + full migration SQL applies clean (UNIQUE + FORCE-RLS + provenance). Migration staged `docs/acquisition/migration-1790000000068-acquisition.ts` (operator places + `migrate:up`) | 2026-06-27 · P6-1 |
| 21 | Tests that pass while the feature is broken — full-surface sweep found 2023 blind-spots (217 CRITICAL); the #1 class is tautological assertions (`expect(true)`, `assert.ok(true)`) that can never go red, and permissive `expect([200,400,500]).toContain(x)` status arrays that accept a 500 | Test files had no lint guard against assertions that cannot fail; the `no-permissive-status-assertion` rule existed but had NO test runner (itself a dead/unverified guard) | `ESLint` (Tier-1, red→green via RuleTester) | `tools/eslint-plugin-local/src/index.js` — NEW `no-tautological-assertion` (bans `expect(<bool literal>)`, `assert(<truthy const>)`, `assert.ok(<truthy const>)`) + `no-swallowed-catch` (`.catch(()=>{})` empty body) + `no-truthy-on-identifier` (`.toBeTruthy()`/`.toBeDefined()` on token/id/url → assert shape) + ACTIVATED `no-permissive-status-assertion`, all with a RuleTester suite (`tools/eslint-plugin-local/__tests__/rules.test.ts`, 4/4). Both wired `warn` in `eslint.config.js` (ratchet → `error` once the existing ~217 violations are fixed). Proven red on real `apps/api/e2e/phase2.spec.ts` (5 caught). Rule: AGENTS.md "Test Integrity"; ledger of all 18 classes: `docs/design-review/test-hardening-findings.md` | 2026-06-27 · this change |
| 20 | §4 order-velocity throttle was phone-only — `clientIpHash` computed but never gated, so a phone-rotating attacker placing many orders from one IP was unbounded (Breaker M5) | The IP half of the "phone+IP velocity" throttle was never wired; only the per-phone block existed | `E2E`/`integration` (psql-seed) | `apps/api/src/routes/orders.ts` — parallel per-(location,IP) gate over `velocity_events.client_ip_hash` (≥20/15min → 429 `IP_THROTTLE`); proven by a psql-seed integration on staging (seed to threshold → next order 429). Limit is per-location so shared-NAT false positives are negligible | 2026-06-25 · `fd3d27e0` |
| 19 | §1.2/§1.3/§1.4 sensor capture could silently regress: (a) a delivered order's normalised baseline is mutated by a re-fired DELIVERED; (b) the public funnel ingest leaks an error to the storefront / enumerates / writes under no tenant; (c) the eta_cap clamp is silent — owner never learns an order's honest ETA exceeded their cap, and a member-only signal would drop in courier-confirm context | (a) baseline must ride the idempotent `ON CONFLICT (order_id) DO NOTHING` so first-observed is immutable; (b) the anonymous ingest must be uniform-204 + per-IP rate-limited + tenant-scoped via `app.current_tenant` (FORCE-RLS WITH CHECK); (c) `eta_cap_exceeded` must use `order_sensor_events` dual-context RLS (not member-only `location_alerts`) to land in BOTH owner-PATCH and courier-accept contexts | `E2E` (staging) + `DB-state` | `e2e/tests/flow-sensor-delivery-baseline.spec.ts` (psql: route_distance_m=2603/expected=7) · `e2e/tests/flow-sensor-funnel.spec.ts` 3/3 (valid→204, malformed→204, unknown-loc→204; psql: valid landed, bad-FK dropped) · §1.4 psql (cap=1→confirm→`eta_cap_exceeded`, window clamped absolute). ADR-0009 v4 | 2026-06-25 · `28ddbb45`, `f82b1473`, `ab58760f` |
| 18 | §1.1 sensor runtime could silently regress three ways: (a) a "frozen" promise that mutates → the customer is shown a different promise than was measured; (b) an ETA window that collapses to a single number or blows past `eta_cap` → dishonest/runaway estimate; (c) a geofence event silently swallowed in the courier empty-context → the §1.1 "exactly once" capture never lands | (a) `promised_window_*` is only set-once by a DB trigger if the app writes it once + `IS NULL`-guarded; (b) R3-M1: width-floor applied AFTER the cap let `hi` exceed `eta_cap` (`lo=92→hi=97>90`); the fix clamps `lo` first → floor → cap LAST; (c) C2: `order_sensor_events` INSERT runs under `app.current_tenant` only (courier ping), so a member-only RLS `WITH CHECK` denies it and the best-effort SAVEPOINT eats the loss — needs the dual-context policy | `unit` (red→green) + `E2E` (staging) + `DB-state` | `apps/api/tests/eta-synthesis.test.ts` — `clampWindow` cap-last absolute + width-floor + never-point (7/7, incl. the `lo=92→hi≤90` R3-M1 regression arm) + `e2e/tests/flow-sensor-eta-window.spec.ts` (range-never-point + set-once frozen promise across PREPARING/READY) + `e2e/tests/flow-sensor-geofence.spec.ts` (geofence path on the courier's OWN server-derived order_id) + psql DB-state proof (exactly-one geofence row under dual-context RLS; re-cross is a no-op). ADR-0007/0008/0009 v4 | 2026-06-25 · `0f4e8618`, `d359c195` |
| 17 | Checkout shows a delivery total that differs from what the server charges (worst at the free-over-threshold boundary — the hardcoded fee billed in full exactly when delivery should be free); for 77% cash-on-delivery that is the wrong sum collected at the door | `CheckoutPage.tsx:342` hardcoded `deliveryFee = 200`; the server is authoritative (`orders.ts:518-565`: min-order + free-over-threshold + tiered/flat + tax) and `/info` didn't expose the fee inputs, so the client literally could not mirror it | `unit` (parity, red→green) + `E2E` (door-parity) | `apps/api/tests/fee-parity.test.ts` — asserts the client MIRROR (`packages/ui/src/lib/money.ts` `estimateOrderTotal`/`applyTax`) equals the real server `applyTax` + fee ladder across a subtotal×tax×threshold matrix incl. the free-over-2000 boundary (5/5 green; fails red on any drift). Runtime backstop: `CASH_AMOUNT_TOO_LOW` 422 handler. ADR-0005 (Approach M — server math untouched). NOTE: CI wiring of the test needs a `package.json` script (protected → manual approval) | 2026-06-25 · this change |
| 1 | Live prod auth backdoor: `POST /api/auth/local/login` minted a real owner JWT; `/dev/mock-auth` minters rode the same path | `devLoginAllowed` returned `!!DEV_AUTH_SECRET` (no flag/`NODE_ENV` gate); staging secret leaked to prod; dev tokens accepted on prod kid | `boot-guard` + `CI-gate` + `E2E` | `ALLOW_DEV_LOGIN` flag folded into `devLoginAllowed`+`isDevRequestAuthorized`; dev-kid segregation via `signDevToken`/`verifyAuthToken`; boot-guard D in config `loadEnv`; `scripts/migrate-runner.ts` release guard; auth E2E | 2026-06-22 · `5da9d136`, `ef0954c9` (ADR-0003) |
| 2 | Predictable / forgeable security tokens, OTPs, nonces, session ids (same trust-boundary class as #1) | `Math.random()` is non-CSPRNG; if used to mint a token/otp/secret it is guessable | **`eslint`** | **`local/no-insecure-random`** — flags `Math.random()` assigned to a security-named identifier (token/otp/secret/nonce/session/passwd/salt/csrf/api-key/verif/recovery/reset/magic); requires `crypto.*`. Fixtures: `__fixtures__/{bad,good}-insecure-random.ts` | 2026-06-22 · this change |
| 3 | Prod outage — API crash-loops at boot, both machines `stopped`, 000 timeouts | Schema drift: image boot-guard FATAL-exits when DB head < expected migration; CI deployed code needing migrations 032–040 but ran NONE (no `release_command`) | `release_command` + `boot-guard` | `release_command` wired in `fly.toml` to auto-migrate before rollout; pre-existing boot-guard that asserts DB head ≥ expected | 2026-06-20 / 2026-06-21 · `a91e78e9`, `5e2bc924` |
| 4 | Prod boot hangs before `listen` — `/livez` critical, never binds `:8080` | Worker `start()` calls `boss.createQueue()` → needs `CREATE on schema pgboss`; runtime role had `USAGE` only; schema owned by `postgres` | `migration` / `CI-gate` | `GRANT CREATE ON SCHEMA pgboss`; migration 042 made resilient to `insufficient_privilege`; fresh-provision pre-creates pgboss schema | 2026-06-21 / `929f0282`, `c55074fa`, `9589c2e7` |
| 5 | Out-of-order WebSocket frames flip order status backwards; reconnect-storm bugs | Components subscribing to a raw `new WebSocket()` bypass the shared client that owns reconnect-jitter + frame ordering | **`eslint`** + guard | **`local/no-direct-websocket`** — flags `new WebSocket()` in `apps/web/**` and `packages/ui/src/**` outside the **single** shared client (`useWebSocket.ts`). Fixtures: `__fixtures__/{bad,good}-websocket.tsx`. Complements the runtime status-monotonicity guard (`3b186fcb`). **F14 (2026-06-24):** the dead reconnect-capped `packages/ui/src/lib/websocket.ts` (froze after 10 tries, 0 consumers) was deleted; its rule exception removed so a re-add is now flagged | 2026-06-22 · this change |
| 7 | Cart silently carries stale prices → checkout hard-block ambush (F9); owner card flashes nameless/"0 items" on a fresh WS order (F7); a 2nd reconnect-capped WS client freezes permanently (F14) | Cart never tracked `menu_version`; OrderCard rendered `items.length` before the authed backfill arrived; a dead capped WS client diverged from the reconnect-forever one | **`test`** + `eslint` | `e2e/tests/polish-debt-logic.spec.ts` (11 cases): `reconcileCart` reprice/remove/modifier-skip/version-gate/legacy + `isOrderDetailsPending` + a guardrail asserting the capped `websocket.ts` cannot be reintroduced. Pure cores extracted (`apps/web/src/lib/cartReconcile.ts`, `isOrderDetailsPending`). Tightened `local/no-direct-websocket` (row 5) | 2026-06-24 · this change |
| 6 | Storefront cards unreadable — dark text on dark surface (~1.08:1 contrast) on light tenant themes | Partial tenant theme (primary/bg/text only) merged with default-DARK tokens; `var(...)` placeholders fell through to Food-Dark surfaces | `eslint` (existing) | `local/no-hardcoded-color`, `local/no-hardcoded-tailwind-color` + `derivePalette` coherent-palette util | 2026-06-21 · `4dab5af4` |
| 7b | Money rendered/stored with float drift | Currency handled as float instead of integer minor units | `eslint` (existing) + test | integer-tax fix + money assertions in E2E | 2026-06-20 · `otp-disabled-money-fix` |
| 8 | Hardcoded user-visible strings ship untranslated; Albanian diacritics lost | UI strings not routed through `t('key','fallback')` | `eslint` (existing) | `local/no-hardcoded-string` | 2026-06-21 · `f1f044da`, `be1529c3` |
| 11 | "Fixed A, broke B" — a shared-component change silently regresses another screen; nobody re-checks every critical screen by hand | no automated pixel-level guard on the critical path; phase gates are too coarse to catch per-change visual drift | **`test`/visual net** | Critical-Path Visual Regression Net: `playwright.visual.config.ts` (deterministic — reducedMotion/frozen-tz/perceptual-threshold/`[data-dynamic]` masks) + `e2e/visual/` harness (`loginAs`/`seedVisualState`/`MASK`) + dev-gated `/dev/seed-visual-state` + 180 snapshot tests (client/owner/courier × states × 3bp × 2lang). Baselines locked in CI (pinned Playwright image + PG service) — `docs/operating-model/proposed-visual-ci/APPLY.md`. DoD-prove: intentional shared-component regression → red snapshot (runs at lock). | 2026-06-24 · this change |
| 10 | Visually-wrong-but-valid UI ships (off-scale spacing/off-token colour via arbitrary Tailwind values; silent-English chips); code review + linter don't see pixels | agent reasons in tokens not pixels; arbitrary `p-[13px]`/`text-[#fff]` bypass the scale/tokens; keys used via `t('x','fallback')` but never catalogued render English in all locales | **`eslint`** + gate + loop | `local/no-arbitrary-tailwind` (red→green fixtures `__fixtures__/{bad,good}-arbitrary-tailwind.tsx`); i18n-parity gate extended to flag code-used-but-uncatalogued keys (`--strict`); `scripts/ui-verify-floor.sh` FLOOR runner; UI Build-Verification Loop (agent-as-eye Layer 4) — first run `e2e/journeys/UI_LOOP_RUN_2026-06-24.md` fixed `state.*` English-in-SQ/UK | 2026-06-24 · this change |
| 9 | A key routed through `t()` is added to one locale but **silently missing** in `sq`/`uk` → non-English users get an invisible English fallback, no error | resolver is `messages[locale]?.[key] \|\| fallback \|\| key`; locale-major maps let a locale lag with zero signal | **`test`/gate** + SSOT refactor | single source of truth = key-major `packages/ui/src/lib/i18n-catalog.ts` (`{key:{en,sq,uk}}`); `messages` derived from it (lossless, proven 3237/3237). `scripts/i18n-parity.ts` fails on any missing/empty/TODO locale (red→green proven), wired into `.husky/pre-commit` when i18n files are staged; `scripts/i18n-add.ts` adds a key in one step (untranslated locales become TODO drafts the gate rejects); dev-only missing-key `console.warn` in `translate()` | 2026-06-24 · this change |
| 9b | Raw SQL string interpolation (SQLi surface) | Query built with template-literal/`+` interpolation instead of `$1` params | `eslint` (existing) | `local/no-raw-sql` | existing |
| 10b | Cross-courier / cross-tenant IDOR (assignment-accept) | Handler not scoped by `courier_id` / tenant | `eslint` (existing) + test | `local/require-auth-hook` + scoped query fix | 2026-06-22 · `68c2cc6d` |
| 11b | Permissive test assertions hide contract regressions | `expect([200,400,...]).toContain(status)` passes on the wrong status | `eslint` (existing) | `local/no-permissive-status-assertion` | existing |
| 12 | Unhandled `localStorage` access crashes the SPA (private mode / storage off) | Direct `localStorage` reads not guarded | runtime guard + `E2E` | `safeStorage` wrapper + chaos-monkey harness | 2026-06-22 · `213bdfb5`, `2ed555a9` |
| 14 | Render-time defects invisible to text gates (dark-on-dark contrast #6, silently-dropped CSS rule #13, tenant-theme fall-through) — grep/typecheck/build/lint green, output wrong | Static rules see source, not the COMPUTED outcome; only a live-DOM check catches "renders unreadable / blank" | `E2E` (live-DOM, systemic) | `e2e/tests/behavioural-invariants.spec.ts` — the home for outcome-invariants: WCAG-AA contrast on SOLID-surface text (skips image/gradient-backed text where ratio is undefined) + body paints a resolved opaque brand surface. Red arm proves it flags solid dark-on-dark. Extensible (add `expect`s; never weaken). Closes Council root R1 | 2026-06-23 · this change (systemic ratchet) |
| 13 | A whole CSS rule silently dropped by the browser — present in file/served/dist CSS, invisible to grep/typecheck/build/lint (the `[data-skin="paper"]` token block didn't apply) | A literal `*/` in CSS comment PROSE (`--ink-*/--paper-*`) closed the block comment early; the browser's error-recovery consumed the next rule until resync | `E2E` (live-DOM) + `lesson` | `e2e/tests/paper-skin-tokens.spec.ts` (live-DOM, semantic) + `packages/ui/src/theme/css-comment-integrity.test.ts` (cheap static arm: strip canonical comments → any leftover `/*`/`*/` marker = early-closed comment; red/green arms) + lesson `docs/lessons/2026-06-23-css-comment-star-slash.md` (TRIGGER `packages/ui/src/theme/**.css`) | 2026-06-23 · this change (static arm added by Council ratchet) |
| 16 | Removed/downgraded owner keeps tenant read/WRITE access for the full token life — owner authorization queries filter `role='owner'` but not `status='active'`, and helpers trust the baked JWT `activeLocationId` with no DB re-check (insider-removal window) | A revoked owner's still-valid access token carries `role:owner`+`activeLocationId`; per-request scoping never re-checked live membership status, so revocation didn't bite until token expiry | `CI-gate` (static) + `E2E` (no-regression) | `scripts/guardrail-owner-active-membership.mjs` (fails on any `memberships` query with `role='owner'` lacking `status='active'`; red-proven via a probe file, green on repo; wired into `verify:all` + `pnpm guardrail:owner-active`) + `e2e/tests/owner-revocation.spec.ts` (P-b logout route wired+auth; active owner not locked out). ADR-0004, council `docs/design/owner-token-revocation/` | 2026-06-23 · this change |
| 15 | Storefront "blinks empty" under load — `/public/locations/:slug/menu` returns HTTP 500 for multi-second windows during a concurrent burst; FE renders an empty menu | Operational-pool starvation: no server-side cache on the hottest read + **2 conns/request** (Promise.all of `read_public_menu` + a redundant `locations` lookup) + heavy per-row `product_available_now()`; burst > pool (`max:8`) → checkout waits `connectionTimeoutMillis` (5s) → 500 | `E2E` (load) + `unit`(SQL) + `boot` (env-tunable pool) | `e2e/tests/menu-load.spec.ts` (30-wide burst → zero 5xx, products present — RED reproduced ~15-20×500@5.1s via curl); `packages/db/tests/read-public-menu-availability-equivalence.sql` (F4 set-based predicate ≡ `product_available_now` across 18 cases — proven green); F1 in-proc cache + F2 drop redundant query (migration 1790000000064, real `down()`) + F3 `OPERATIONAL_POOL_SIZE` (default 20) | 2026-06-23 · this change |
| 49 | **Plane telemetry was invisible and its egress unguarded**: maintainer runs left no durable record (ephemeral cloud box ≠ durable FS), Telegram egress had no deterministic secret/PII boundary, and prediction-vs-fact calibration had no persistent record (result-vs-expectation doubt trigger was ephemeral) | no single telemetry choke-point on the governance plane; denylist-only redaction misses shapeless secrets; "advisory stays advisory" was prose-only (repo precedent: gates silently disarmed, #47) | `test` (node:test) + `CI-gate` (plane-guard) | `scripts/plane-telemetry.mjs` (single egress choke-point: field-scoped 2-layer redaction; git-plumbing publish to append-only `telemetry/plane` with fail-closed scan of the exact committed bytes; injection-sanitized advisory-only `inbox`) + `scripts/plane-telemetry.test.mjs` 15/15 (canary + injection RED via test-seams → GREEN; parallel-session dedup; prediction ordering; push-fail-closed; subprocess-safety) + 3 plane-guard checks fixture-proven red→green (`telemetry-liveness` soft anti-silent-skip; `advisory-forever` + `ingestion-authority` hard friction). ADR-plane-telemetry-and-calibration, council `docs/design/plane-telemetry-principles/` | 2026-07-02 · this change |
| 50 · **PENDING** | **C1 anonymous RLS policies fail-open — DESIGNED-NOT-SHIPPED (already orphaned once, in migration 077)** (security-hardening batch finding #2, `docs/design/security-hardening-2026-07/{proposal,resolution}.md` §"#2"). `USING (app_current_user() IS NULL)` on `orders`/`order_items`/`customers` (+ `idempotency_keys` anon SELECT) is table-wide TRUE for any no-`app.user_id` connection → under NOBYPASSRLS it is a live table-wide cross-tenant customer-PII siphon. Latent today ONLY because `dowiz_app` is BYPASSRLS (Case A). **Migration 077 added the anon INSERT siblings but never narrowed the fail-open SELECT/UPDATE — so this class was orphaned once already; this row exists so it cannot be orphaned again.** | Anti-orphan design: handing #2's guardrail bundled INTO #2's own delivery is circular (it can't guard the interim). So the gate must exist BEFORE the fix. **B3-coupled**, gated on **OR-1** (operator must record the live `DATABASE_URL_OPERATIONAL` role + `rolbypassrls` — a LIVE-EXPOSURE gate: if the pool is unexpectedly NOBYPASSRLS today, #2 promotes from latent → live Tier-1). Must land WITH courier/anon GUC seating (077 RC-set) + a COMPLETE anon-read inventory (customer `GET /orders/:id` raw-pool read, spa-proxy order reads, status-poll, track-exchange) **before** the NOBYPASSRLS flip, or legitimate customer order-tracking 404s for everyone (the KNOWN TRAP). Preferred narrowing mechanism = a `SECURITY DEFINER` scoped by order-id/token-hash with pinned search_path (reuse #3's primitive; one isolation primitive, not a new per-request `app.anon_order_id` GUC). | `verify:rls` probe (skip-registered, named — MUST land with Tier 1) + `plane-guard.mjs` pending check + flip-gate lifecycle E2E | **REQUIRED before the flip, ships WITH Tier 1 (not with the #2 fix):** (a) a skip-registered, named probe `test.skip('C1 anon policies fail-closed under dowiz_app', …)` in `packages/db/scripts/verify-rls.ts` — impossible to remove without narrowing C1 — that under `SET LOCAL ROLE dowiz_app` with NO GUC asserts `SELECT` on each of `orders`/`order_items`/`customers` returns **0 rows** (RED today = all rows; GREEN after narrowing flips skip→active); (b) a `plane-guard.mjs` PENDING check keyed to this row; (c) flip-gate E2E covering the customer **status-read** path (not just checkout). Row flips PENDING→closed only when the probe is un-skipped and green, C1 is narrowed, and OR-1 + the anon-read inventory are recorded. | 2026-07-02 · PENDING (security-hardening batch; B3-coupled, OR-1-gated) |

## Guardrails added by this change (red→green proof)

Both new ESLint rules live in `tools/eslint-plugin-local/src/index.js`, are registered in
`eslint.config.js`, and are gated by `pnpm lint:gates` (lints `__fixtures__/*`).

### `local/no-insecure-random` (row 2)
- **RED** — `__fixtures__/bad-insecure-random.ts`: flags `sessionToken`, `otpCode`, `resetToken`,
  `csrfNonce` built from `Math.random()`.
- **GREEN** — `__fixtures__/good-insecure-random.ts`: `crypto.randomBytes/randomInt/randomUUID`
  pass; and legitimate non-security `Math.random()` (jitter, particle hue, toast id) stays clean.
- **Repo-green**: zero hits across `apps/` + `packages/` (all real id/token/otp generation already
  uses `crypto.*`; `Math.random()` only appears in jitter/animation/toast-id contexts).

### `local/no-direct-websocket` (row 5)
- **RED** — `__fixtures__/bad-websocket.tsx`: flags `new WebSocket(url)` in a component.
- **GREEN** — `__fixtures__/good-websocket.tsx`: subscribing via the shared `useWebSocket` client.
- **Repo-green**: the only frontend `new WebSocket(` is the designated shared client
  (`apps/web/src/lib/useWebSocket.ts`), excluded; scope is frontend-only so back-end/test WS
  constructions are untouched. **F14 (2026-06-24):** the second client
  `packages/ui/src/lib/websocket.ts` was deleted and its exception removed — re-adding it (or any
  raw `new WebSocket`) is now flagged.

### `polish-debt-logic` regression test (row 7)
- **RED** — pre-fix, `reconcileCart` didn't exist (cart never tracked `menu_version`) and OrderCard
  rendered `items.length` (→ "0 items" on a fresh WS order); the capped `websocket.ts` existed.
- **GREEN** — `e2e/tests/polish-debt-logic.spec.ts` 11/11: reprice/remove/modifier-skip/version-gate/
  legacy reconcile, `isOrderDetailsPending` truth table, and an `existsSync` guard that fails if the
  capped `websocket.ts` is reintroduced. Runs headless (no browser): `playwright test polish-debt-logic`.

### `eta-range-invariants` regression test (row 8)
- **Bug class:** the customer-facing ETA must NEVER be a single number, NEVER "0", NEVER NaN, never
  `low>high`; must widen with queue depth, floor near the end, and not re-widen on Phase-2 jitter. The
  pre-existing machine (`courier-events.ts` `etaSeconds` via `calculateNaiveETASeconds`) emitted a single
  number that hit 0 on close distance — the exact failure this guards.
- **RED** — a naive `dist/speed` ETA returns one number and 0 near the door (the removed behavior).
- **GREEN** — `apps/api/tests/eta-service.test.ts` (20 cases, node:test) asserts every invariant of
  `computeEtaRange`/`deliveryLegMinutes`: always a range (low<high via minBandMin), never 0 (minLowFloor),
  phase-1 wider than phase-2, deeper queue → higher, near-end floor band, monotonic-but-honest top, and
  NaN/null-pin/negative/Infinity never leaking NaN/0/single/low>high. Run: `node --test --import tsx
  tests/eta-service.test.ts` (headless, no DB). Payload guardrail: `etaSeconds` removed from the customer
  WS payload (`courier-events.ts`); the honest `etaRange` on `/customer/orders/:id/status` is the sole
  customer ETA (D1 kept live position/route). | 2026-06-24 · this change

### deliver v2 — RESOLVE round 5 (implementation-drift) guardrails (row 27)
- **Drift class:** shipped code diverged from items the council marked FIXED (D1 `/cancel` re-created the C-2
  trap + a false `ORDER_CANCELLED`; D2 owner-reassign raw revert bypassed the machine) + two
  named-but-unbuilt authorities (D4 anti-scoring-creep ban; D5 M-3a signal-independence). See
  `docs/design/deliver-v2-cash-as-proof/resolution.md` §"RESOLVE round 5".
- **Fixes:** the `/cancel` and `/abort` exits now share one rail `apps/api/src/lib/bindingRelease.ts::releaseBindingAndReoffer`
  (terminalize-first + order-side action guarded on the locked order status; never a forced transition);
  owner-reassign displaced revert routes through `updateOrderStatus` (`owner/dashboard.ts`); the owner
  `/deliver` body gained the courier-parity Zod schema (`.int().nonnegative()` / `.strict()` / outcome enum).
- **RED→GREEN guardrails:**
  - `apps/api/tests/deliver-drift-resolve5.test.ts` — D1a (IN_DELIVERY+picked_up→CANCELLED), **D1b
    (IN_DELIVERY+accepted→READY, NOT stuck IN_DELIVERY, NO false ORDER_CANCELLED — the C-2/R2-5 fix)**, D1c
    (flag-ON CONFIRMED → no forced-transition throw), D2 (IN_DELIVERY→READY writes `order_status_history`),
    **D5 M-3a (a contradictory pre-existing `delivery_trace` signal does NOT change the completion outcome —
    the "never build the verdict engine" authority)**. 9/9 green vs a head-migrated throwaway.
  - `scripts/guardrail-deliver-v2.mjs` (wired into `verify:all`) now also enforces **(D4 / C1/Q5)** no
    non-`'hold'` `courier_cash_ledger` type write + no penalty/score derived from a signal row — in addition
    to R2-1 completion-parity + R3-3 no-new-raw-cancel (this row also names the previously-unlisted R3-3 gate).
- **GREEN** — `node scripts/guardrail-deliver-v2.mjs`; `DV2_TEST_DATABASE_URL=… node --test --import tsx
  apps/api/tests/deliver-drift-resolve5.test.ts`. | 2026-06-28 · this change
| 39 | LIVE cross-tenant + cross-courier WebSocket leak (B6): the courier `subscribe` branch authorized only the room PREFIX, so any authenticated courier could `subscribe` to another tenant's `location:<id>`/`order:<id>` and receive its live order/assignment/message/GPS stream (owner branch had `ownerCanAccessRoom`; courier branch had nothing) | courier WS room authz never written — only `courier:<self>` was guarded; `location:`/`order:` admitted on prefix alone | `unit` (red→green, mutant) + ADR-0013 council | `apps/api/src/lib/courier-room-authz.ts` — shared `courierCanAccessRoom`/`courierCanReadOrder`/`courierCanSendOrder`: `location:*` denied with zero DB access, `order:` requires a live `courier_assignments` binding under a NOBYPASSRLS-sound `BEGIN`+`set_config('app.current_tenant')` tx (the assignments.ts:80 shape), fail-CLOSED — wired into `apps/api/src/websocket.ts` courier subscribe branch. `apps/api/tests/courier-room-authz.test.ts` 7/7 green; mutant re-opening `location:*` → 3 fail. Council `docs/design/courier-realtime-authz/` + ADR-0013. **Follow-on (same PR DoD):** fan-out revalidation+~60s ceiling (reassign colleague-leak), order-messages N3 send/read split, FE `order:${task.orderId}`, 8 staging E2E. | 2026-06-29 · this change |
| 40 | ADR-0013 DoD: an ADMITTED courier later UN-bound (owner-reassign / decline / sweep) keeps receiving the live `order:<O>` stream until disconnect — the involuntary-reassign colleague leak (Breaker C1); + the intra-tenant colleague-thread leak on the REST message routes (N3, location-wide not assignment-scoped); + the FE subscribing `order:<assignmentId>` (denied by the new gate) | the WS fan-out (role-AGNOSTIC bus handler + both GPS relays) sent raw to every room member with no re-check; `binding_changed` carries `{orderId}` only (no `courierId`) and victim paths emit nothing on `order:<O>`, so event-eviction can't target the evictee → must revalidate at fan-out time | `unit` (red→green, 2 mutants) + `eslint` drift rule (red→green) + ADR-0013 council | `apps/api/src/lib/courier-relay-guard.ts` `createCourierRelayGuard`: tri-state (`courierReadVerdict` ALLOW/DENY/UNAVAILABLE, never throws), relay-only-on-fresh-ALLOW, fixed-TTL (~10s, no refresh-on-access) bounded-LRU cache, ~60s wall-DOMINANT fail-safe ceiling from in-memory state (evicts under sustained DB-UNAVAILABLE with no DB read), `binding_revoked` eviction. Wired over ALL THREE raw courier-send sites in `websocket.ts` (bus handler + `client_location` + `client_location_stop`); subscribe UNAVAILABLE → retryable soft error (no `ws.close` fleet-deny). N3 REST split in `routes/order-messages.ts` (read=`courierCanReadOrder` incl. `offered`, send=`courierCanSendOrder` excl. `offered`). FE `DeliveryPage` → `order:${task.orderId}` (R1). Drift guardrail `local/no-raw-courier-ws-send` (ERROR) bans raw `<member>.ws.send` at the fan-out site — keyed on SITE not `role==='courier'` (Breaker NEW-E). **Proof:** `courier-relay-guard.test.ts` 13/13 (mutant relay-then-revalidate → 6 fail; drop-ceiling → 3 fail); `courier-room-authz.test.ts` 12/12 (tri-state); eslint mutant fixture → 1 error, `websocket.ts` → 0. **Staging E2E (v195, 6/6 green):** `e2e/tests/courier-room-authz-isolation.spec.ts` — positive-control GPS relay to a bound courier, the REAL `/courier/delivery/:id` route + WS, `location:` denial, N3 WS zero-frames + REST 404, and the **C1 reassign eviction** (bound courier receives → reassign → `binding_revoked` + zero post-expiry frames while c2 takes over). Off-staging: UNAVAILABLE/ceiling = unit; NOBYPASSRLS item-8 = tracked integration follow-up. | 2026-06-29 · this change |
| 41 | **B4 BOLA on `/api/admin/*`** (ADR-admin-platform-authz, APPROVED): every admin endpoint gated only by `requireRole(['owner'])` → any of thousands of tenant owners read all-tenant backup inventory / fleet phones+fallback config / cross-tenant notification audit AND weaponize DR drills (`runRestoreVerify(any backupId)`); no platform-admin principal distinct from `owner`. Plus the `notification-audit` double-prefix bug (`/api/admin/admin/...`) made its DoD test a false-green, and the DR `acquireLock` leaked the advisory lock forever (released the pooled client while holding the session lock → every later drill 409s) | "admin" was modeled as "an owner" — the tenant plane and the platform/ops plane collapsed onto one role | `unit`+`integration` (red→green, mutant) + `eslint` drift rule (red→green) + Triadic Council (architect×3/breaker×3/counsel×2, 10→6→1→0) | `apps/api/src/lib/platform-admin.ts` — `platform_admins` NON-RLS global allowlist + `requirePlatformAdmin` per-request point-read (`SELECT 1 … WHERE user_id=$1 AND revoked_at IS NULL`; immediate insider-removal, no token claim, B3-independent), 403 non-admin / **503 fail-CLOSED** on DB blip / 401 no-userId; `registerAdminPlaneGate` = a ROOT-instance `onRequest` hook keyed on the **matched route pattern** (`request.routeOptions.url`, not raw URL) that gates every `/api/admin*` route — child, **sibling**, or future — by construction (structural F1/RA2-5 closure, the route-tree boot-guard was unrealizable). Parent plane `routes/admin/index.ts` (verifyAuth→requirePlatformAdmin); the 3 children drop their `verifyAuth`/`requireRole(['owner'])`; `notification-audit` path de-double-prefixed; DR endpoints get Zod-uuid + rate-limit; `backup-verify.ts` advisory-lock leak fixed (hold the locked client across the drill). Drift guardrail `local/no-admin-prefix-register` (ERROR) bans any `/api/admin` register outside server.ts. Ops-CLI provisioning `scripts/platform-admin-grant.ts` (no API write path). **Proof:** `platform-admin.test.ts` 7/7 (fail-open mutant → 1 fail) + `platform-admin-gate.integration.test.ts` 4/4 (real Fastify: child+sibling gated, lookalike not, revoke-at-entry); empirical Fastify-5.8.5 sibling-closure probe; eslint mutant → 1 error / server.ts → 0; api typecheck clean. Write-ahead audit wired (DR drills `started`→`completed`/`failed`, reads best-effort `completed`; F6 `err.message` leak fixed) — `platform-admin.test.ts` 11/11 incl. audit (hashed ip/ua, best-effort read). **Staging E2E (v197, 3/3 + 2 skipped-pending-admin-token):** `e2e/tests/admin-platform-authz.spec.ts` — owner JWT → NOT 200 on all 6 admin endpoints (503 fail-closed at the canonical paths until the migration lands), unauth → 401, courier/customer-shaped → 401/403. **Caught + fixed a real wiring bug** the unit tests missed: adminPlane forwarded its `{prefix:'/api/admin'}` opts to children → double-prefix (`/api/admin/api/admin/*`, real paths 404'd); no security hole (404/503, never 200). **FULLY PROVEN on staging v197** (migration applied to the staging DB): owner→**403** ×6 (E2E 3/3), platform-admin→**200** ×3, DR rate-limit verify#4→**429**, **audit rows** written (write-ahead `started`→`failed` drills + `completed` reads), **revocation→403 at request-entry** (immediate insider-removal). **For PROD:** the operator must place `docs/security/platform-admins-and-audit.migration.ts` into `packages/db/migrations` (protect-path → tracked + run via release_command), then `platform-admin-grant` ≥2 real admins. Cross-finding R11 → hard B3 blocker. | 2026-06-29 · this change |
| 42 | **WS JWT leaked into logs via `?token=`** (P1 of the WS-token-in-URL escalation, red-line auth surface, ADR-0013): the realtime WS authenticates by `?token=<JWT>` (`apps/web/src/lib/useWebSocket.ts:50`, the order-status widget `apps/api/src/client/status/ws.ts:17`, server `apps/api/src/websocket.ts:155`), and the Pino `req` serializer logged `req.url` **raw** while `redact` covered headers only → the full bearer token (courier 14d / customer 7d / owner 24h TTL) was written to app/Fly/aggregator logs on **every** WS upgrade. Confirmed CRITICAL by security-sentinel; prod affected (`origin/main` carries the client line). | the request-URL serializer was treated as non-sensitive — redaction was scoped to headers/body keys, but auth had quietly moved a bearer credential into the query string for the WS upgrade path | `unit` (red→green, serializer-wiring mutant) + Triadic-style council (security-sentinel CRITICAL / system-breaker / counsel) | `apps/api/src/lib/logger.ts` — new exported `redactUrlSecrets(url)` strips secret-bearing query params (`token`/`access_token`/`refresh_token`/`api_key`/`secret`/`jwt`/`auth`/…) → `<key>=[REDACTED]`, preserving path + benign params; wired into the Pino `req` serializer (`url: redactUrlSecrets(req.url)`). Kills the log vector for BOTH WS clients with **zero auth/contract change**, no test breakage. **Deliberately P1-only:** removing the client/server URL-token channel (browser-console vector) is GATED — breaker proved server removal breaks `StatusWSClient` (URL-only, no message fallback) + the P1-WSDUP churn guardrail + regresses the 5s auth budget → deferred to an ADR-0013 addendum (P2/P3) pending human approval. Escalation doc `docs/design/ws-token-in-url/escalation.md`. **Proof:** `apps/api/tests/logger-url-redaction.test.ts` 6/6 green; mutant (serializer → raw `req.url`) → test 6 `not ok` (fail 1); restored → 6/6. api typecheck clean. Staging deploy + live-log verification. | 2026-06-30 · this change |
| 43 | **Storefront allergen surface was recipe-only on two LIVE surfaces** (menu-characteristics-model FULL-BUILD round, FB-C1/FB-C2): the menu allergen FILTER predicate (`MenuPage.tsx:194`) and the detail-modal's flag-OFF (today's) branch read allergens from `bomToNutrition(p).allergens` (recipe/bom only). An owner who **declared** an allergen (`attributes.allergen_status='listed', declared_allergens=['milk']`) on a dish with **no recipe/bom** was therefore (a) silently DROPPED from a "contains milk" filter view and (b) shown NOTHING in the modal (the legacy branch returned `null` on empty bom, no reliance bound) — a live allergen false-negative on the storefront. The card also passed a recipe-only `allergens` prop (dead — ProductCard already omits it). | the allergen surface was derived from two different functions on two surfaces, and the recipe-only `bomToNutrition().allergens` was treated as the safety source instead of the conservative declared∪recipe union; the honest detail-floor was gated behind a feature flag, so v1 (flag off) shipped the unsafe legacy path | `unit` (red→green, #12 retention) + `eslint` drift rule (red→green, RuleTester) + 5-round + FULL-BUILD council (ADR-0014) | **STEP-0 (no flag, lands first/independent).** `apps/web/src/pages/client/MenuPage.tsx` — single source `allergenSurfaceOf(p)=computeAllergenSurface(p.attributes, recipeAllergens(p))` (declared∪recipe); `bomToNutrition` no longer returns `allergens`; filter predicate converged onto it; the modal renders the honest detail-floor UNCONDITIONALLY (flag gate + recipe-only legacy branch removed) with the #5d reliance bound + "info not provided" floor; the dead recipe-only card prop removed. Allergen FILTER CHIPS gated OFF behind `VITE_MENU_ALLERGEN_FILTER` (default off — FB-C1 recorded human decision; predicate stays converged so re-enable is correct-by-construction). Guardrail **#12**: `tools/eslint-plugin-local` rule `local/no-recipe-only-allergen-read` (error) bans `bomToNutrition(...).allergens` in `MenuPage.tsx` + `packages/ui/src/lib/__tests__/characteristics.test.ts` "#12 STEP-0" (declared-only dish retained by contains-milk + warning surfaced). **Proof:** characteristics unit 17/17; RuleTester `no-recipe-only-allergen-read` 6/6 (invalid=red, valid+prod=green); production `MenuPage.tsx` 0 rule errors; web typecheck clean. Staging deploy + E2E to follow. | 2026-06-30 · this change |
| 44 | **Menu Characteristics layer — compare/filter could fabricate a verdict or mis-rank no-data dishes** (council ADR-0014 build steps 3/4, flag-dark): a 2-dish comparison that put a directional arrow on a macro axis (a kcal "wins" arrow = a regulated lightness verdict, R2-H1) or emitted a global "winner" would assert a health claim the platform must never make; and a macro sort that ranked a **no-bom** dish at its numeric `protein:0`/`kcal:0` would make "nutrition unknown" masquerade as "lowest" (a no-data dish reading as a value dish). | facts-not-verdict: comparison and sort are neutral fact views, but a directional marker on the wrong axis (macros) or an inline numeric-0 placement of a data-absent dish silently converts a fact into a verdict / a no-data state into a value | `unit` (red→green, #11 + #15) + council ADR-0014 | **Flag-dark (default off, prod dark).** Step 3 COMPARE (`VITE_MENU_CHARACTERISTICS_COMPARISON`): `apps/web/src/pages/client/MenuComparePanel.tsx` reuses `compareDishes()` — the SAME single derivation (allergens via `computeAllergenSurface`, #12) — affordance-only entry (no long-press, FB-M4), directional "lower" marker ONLY on price/prep, allergen cells explicit-both with floor + reliance bound (#8). Step 4 FILTER (`VITE_MENU_CHARACTERISTICS_FILTER`): `partitionByMacroLens()` in `packages/ui/src/lib/characteristics.ts` splits no-bom dishes into an explicit "no data" group; MenuPage renders it as a labeled section, never inline-at-0. Guardrails: **#11** (`characteristics.test.ts` "compare arrows only price/prep" — the only `lower`-bearing keys are price+prepTime, no winner/better/healthier field) + **#15** ("macro lens no-data bucket" — no-bom→bucket, real-zero stays ranked, tri-state). **Proof:** characteristics unit 22/22 (incl. #11 + #15); web typecheck clean; eslint 0 errors. Flag-on staging E2E pending the Dockerfile build-args (protected-path, operator approval). | 2026-06-30 · this change |
| 45 | **Voice engine's "holds zero write capability" was import-grep, not enforced against a runtime callback (ADR-0015 §6 / breaker R2-F/M3):** the safety design is source→sink — the voice ENGINE emits readonly `IntentProposal` DATA and holds no mutator, and only the `ConfirmationGate` (with a human confirm for STATEFUL) may apply anything. But nothing stopped a future engine API from taking a **function-typed parameter** (`on(event, cb)`, `subscribe({onResult})`, `new Engine(handler)`), through which a write-capable closure (`addToCart`, `handleUpdateStatus`) could be smuggled across the boundary — silently defeating the no-write guarantee the whole gate rests on. This became live-risk the moment the real ASR engine (`WhisperProvider`/`Transcriber`) landed. | the no-write invariant was asserted by static import-grep + prose, not by a mechanism that covers **injected closures**; a callback param is a write channel that no import check sees | `eslint` (code-pattern, red→green RuleTester) + `unit` (49/49 voice) | `tools/eslint-plugin-local/src/index.js` rule **`local/no-voice-engine-callback`** (error) — flags a function-typed parameter (`TSFunctionType`/constructor-type, incl. unions + TS parameter-properties) in any EXPORTED signature under `packages/voice/src` (functions, class ctor/methods excl. `#private`, interface method sigs). An OBJECT port (`handlers: VoiceHandlers`, a TSTypeReference) is NOT flagged — the sink legitimately holds handlers; a function-typed *property* on an interface is not a param, so `VoiceHandlers` itself is untouched. Fixtures `__fixtures__/{bad,good}-voice-engine-callback.ts`; downgraded to `warn` in the `__fixtures__` override so `lint:gates` stays green while the bad fixture still demonstrates it. **Proof:** RuleTester (TS parser) 7/7 (invalid arms red incl. ctor param-property + union; valid arms green incl. object port, AsyncIterable data, non-exported helper, `#private`, out-of-package); live RED on a real `packages/voice/src/*.ts` callback param (1 error) → 0 on the actual engine; `lint:gates` PASS; voice suite 49/49. | 2026-07-01 · this change |

| 46 | **The test suite was a paper gate: `test:unit` + `verify:all` ran NOWHERE automatically (not CI, not pre-commit), and the suite itself was red/lying** — 24 failures nobody saw (stale money-params expectation after ADR-0016 receiver fields; SSR tests broken by the P6-2 owner_id gate; LLM-cache cross-test poisoning; auth-refresh harness missing `verifyAuth`/`sendError` decorators server.ts provides; live-stack tests hard-crashing without env instead of skipping) plus 8 `assert.ok(true)` "verified by code inspection" false-greens (stage26/28/36), and the `verify:all` i18n-coverage step parsed the LEGACY locale-major `i18n.ts` instead of the key-major `i18n-catalog.ts` SSoT → 1052 phantom misses masking 26 REAL missing keys (claim/fonts/allergen surfaces rendered fallback-language only). | a gate that never runs cannot rot visibly: every consumer (CI validate, pre-commit, deploy) omitted it, so drift accumulated invisibly — and inspection-comment stubs pass by construction | `unit` (857/0 fail on a fresh migrated+seeded DB, serial) + `verify:all --ci` (ALL PASSED) + CI wiring proposal | Stubs → real source-assertions (regex on the actual route/worker source, red when the pattern is removed); `llmCacheClear()` export + per-test cache reset; live-stack tests skip-with-reason via dynamic `@deliveryos/db` import (module-scope `loadEnv` crash); `verify-all.ts --ci` static mode; i18n-coverage parser rewritten for the key-major catalog (red→green: 1052 phantom → 26 real → 0 after adding the 26 keys ×3 locales via `i18n-add.ts`); CI wiring staged at `proposed-ci-test-gates/APPLY.md` (ci.yml is protect-paths — operator applies). **Proof:** `pnpm test:unit` EXIT:0 857/792 pass/64 skip on fresh `dowiz_sag_ci` (migrate→seed→serial run); `tsx scripts/verify-all.ts --ci` → ALL PASSED; i18n parity 1417 keys × 3 locales. | 2026-07-02 · this change |

| 47 | **The governance gates were silently DISARMED for 11 days while looking registered:** `serious-gate.sh` cleared on ANY non-empty `.claude/state/serious-cleared` (7 council slugs accumulated because council.md's post-ship truncate step was never performed) — last DENY 2026-06-21, then 400+ blind ALLOWs incl. money (`plisio.ts`), auth tests, a migration; `red-line-doubt-gate.sh` honored a single 2026-06-23 `redline-confirmed` file forever; and the entire Bash lane had NO gate (`guard-bash.sh` deliberately unregistered in 43a018c1 — its verbatim DANGER list blocked the staging deploys Ship Discipline requires), so a heredoc/`sed -i`/node script via Bash bypassed every edit-governance hook. | a gate whose release condition is a non-expiring state file rots OPEN: nothing measured armament (only registration), so "hook runs" was mistaken for "hook denies"; and the Bash guard was dropped wholesale instead of fixing its one over-broad rule | `hook` (clearance = per-line `slug\|expiry-epoch`, 72h, appended by council GO — legacy bare lines never clear; redline confirm = 60-min window; guard-bash rewritten: protect-paths parity for Bash mutations, HUMAN-ONLY override files, push-main/prod-deploy/dep-mutation blocks, staging deploy explicitly allowed) + `script` red→green | `.claude/hooks/{serious-gate,red-line-doubt-gate,guard-bash}.sh` + `.claude/settings.json` (Bash PreToolUse matcher) + `.claude/commands/council.md` (GO appends expiry) · **`scripts/guardrail-gate-armament.mjs`** — 19 hermetic DENY/ALLOW simulations (stale/legacy/expired clearance must DENY, fresh must ALLOW, Bash protected-writes exit 2), wired into `verify-all.ts` (ci:true) · `scripts/guardrail-hook-matchers.mjs` extended to assert guard-bash stays registered under Bash (proven RED "not registered under PreToolUse" → GREEN). **Live proof:** the freshly registered hook blocked this very session's own Bash mutation of `.github/` mid-change, and the rearmed serious-gate DENIED this very ledger append until a scoped expiring clearance was written. CI enactment of `verify:all --ci` rides `proposed-ci-test-gates/APPLY.md` §1 (operator apply). | 2026-07-02 · this change |

| 48 | **The self-improvement loop's advisory arm was structurally unenforced and unmeasured** (meta-loop audit P1–P3): the reflection→librarian→lesson chain ran on CLAUDE.md prose only (dead since 06-23 — lessons frozen at 4, INBOX stale incl. an unfilled-WHY placeholder); no lesson injection, gate decision, loop escalation, or doubt spend left any record; the ledger's "#N" refs were ambiguous (#7/#9/#10/#11 duplicated); the loop router could RUN-match 2 of 16 registered loops (3 divergent registries); loop run-history was gitignored despite a "permanent+lossless" design and `DEFAULT_BREAKER` shipped infinite budget/time caps; 3 CERTIFIED loops cited certification reports that don't exist on disk. | what is hook-enforced survives, what is discipline-triggered dies — and an unmeasured advisory layer cannot even show it died (no hit-counts → blind pruning; no armament data → gates rot invisibly) | `script` guardrails (red→green) + `hook` v2 (staged, operator-applied) + librarian curation run | **`scripts/guardrail-ledger-integrity.mjs`** (unique row #s; RED on HEAD's dup 7/9/10/11 → GREEN after letter-suffixing) + **`scripts/loops-registry-sync.mjs --check`** (loops/registry.md = SoT → runs/registry.json; router-visible loops 2→20) — both wired into `verify-all.ts` (ci:true) · `scripts/agent-health-pass.mjs` → first-ever `docs/governance/agent-health-2026-07-02.md` (counsel Duty B implemented) · hook v2s staged + armament-proven (V2 24/24 vs staged dir): every gate/nudge/injection/escalation logs one JSONL line to `.claude/logs/harness-events.jsonl`; Stop-gate blocks a QUALIFIED change (≥3 code files or red-line) without a fresh filled-WHY reflection and blocks ANY unfilled placeholder; serious-gate docs/* precision exemption · librarian's first curation since 06-23 (3 reflections archived, 3 lessons promoted, 1 refreshed, INDEX+4 rows) · `DEFAULT_BREAKER` finite ($75 / 4h), metrics.jsonl deduped, `loops/runs/{metrics,routing}.jsonl` + `registry.json` git-tracked, lost cert reports honestly flagged in registry.md. | 2026-07-02 · this change |

## Reversal log

_(none — both guardrails active. To remove a guardrail, delete its rule + fixtures and append a
justification row here.)_
