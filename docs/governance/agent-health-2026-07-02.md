# Agent-harness health pass — 2026-07-02

Generated by `scripts/agent-health-pass.mjs`. Advisory — deterministic gates decide.

## Gates (authority layer)

- serious-cleared: 1 line(s), 1 valid (unexpired), 0 legacy/expiry-less.
- serious-override: absent (good).
- redline-confirmed: absent (good).
- all 9 hook files present (registration is asserted by guardrail-hook-matchers; armament by guardrail-gate-armament).

## Advisory stores (reflections / lessons)

- INBOX: 2 file(s).
- lessons: 7 file(s); INDEX rows: 8.

## Regression ledger

- 50 rows, max #47 (uniqueness is enforced by guardrail-ledger-integrity).

## Loop system

- registry.md: 19 loops; runs/registry.json: 20 (sync via loops-registry-sync).
- metrics.jsonl: 6 run(s), 5 with zero cost/tokens (collector not wired for those runs).
- ⚠️ **only 6 telemetry rows for 19 registered loops — most loop runs still bypass finalize.**

## Harness event log (hook v2 telemetry)

- no harness-events.jsonl yet (hook v2 not applied, or nothing fired) — the advisory layer is still unmeasured.

## Verdict

🟡 **1 warning(s)** — feed these to the weekly librarian/retro run:
  1. only 6 telemetry rows for 19 registered loops — most loop runs still bypass finalize.
