\* TLC config for DecisionImport_BROKEN.tla. Expected outcome: RED —
\* an `EpochNoDowngrade` violation (equal-epoch overwrite of a Live unit).

CONSTANTS
  TagOf = {d1, d2}
  MaxEpoch = 3

SPECIFICATION Spec

INVARIANTS
  TypeOK
  NoDeadlock
  LineageClosed

PROPERTIES
  EpochNoDowngrade
  ReplayBeforePersist
