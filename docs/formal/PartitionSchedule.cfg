\* TLC model config — CORRECT (BROKEN = FALSE).
\* Run:  java -jar tla2tools.jar -config PartitionSchedule.cfg PartitionSchedule.tla
\* Expectation: all four properties GREEN (model is small & finite by construction;
\* the major frame is a true cycle so the state graph is finite and liveness checks).

SPECIFICATION  Spec

CONSTANTS
    T              <- 6
    MaxPartitions  <- 3
    BROKEN         <- FALSE

\* The three safety invariants (golden signature, cf. order_machine.rs fsm_graph_report).
INVARIANT
    SliceSumFitsFrame
    SliceGuarantee
    NoOverrun

\* NoStarvation is a liveness (<>) property — verified under PROPERTY with WF fairness.
PROPERTY
    NoStarvation
