# Design-system-for-all-interfaces task — conspect
Мета: один план+блюпринти дизайну для УСІХ інтерфейсів dowiz (client/owner/courier), на фізичному двигуні,
локально без серверів, кросплатформно (web/laptop/AR-glasses), мультимодально (voice/gesture), реактивно до
intent/event/state/environment. Не забути ЖОДНУ наявну фічу. Когерентність дизайну.

## REFERENCE ARTIFACT (north star, f528e9af) — "Artë Pasta: one journey arrive→choose→receive":
- ПОВНИЙ HTML saved: /root/.claude/projects/-root-dowiz/.../tool-results/artifact-f528e9af-*.html (64KB, self-contained WebGL2, LOCAL no server)
- ТРИ АКТИ: (1) arrival hero над full-screen WebGL2 wave-SEA; (2) choose — paper menu SHEET піднімається над морем (rounded 26px top, spectral edge); (3) receive — море ПОВЕРТАЄТЬСЯ і РОЗВИВАЄТЬСЯ зі станом: wave AMPLITUDE росте, COLOR йде terracotta→gold, waves TURN зі станом
- МЕТА-ПРИНЦИП (backbone): "menu = venue's BRAND; SEA = dowiz — entrance+tracking, tinted to venue. spectral edge threads whole arc". → dowiz володіє ambient physics field (море/переходи/tracking); brand володіє content sheet (menu/colors/type)
- Море шейдер: WebGL2, 84000 points desktop / 26000 mobile, Gerstner waves (5 directional D0-D4, dispersion sqrt(G*k) G=0.30), FBO ping-pong trail (FADE 0.963 + shrink 0.9986), ripples on pointerdown (8 slots, expanding front), COMP composite shader з dive transition (u_dive: zoom push-in→settle, plunge toward click, ring wipe, refraction через trail membrane, chromatic split peaks at surface, Beer-Lambert transmission exp(-depth·vec3(1,1.6,2.6) — stays WARM, caustics). energy/phase drive amplitude+color. reduced-motion → fade=0 run=false
- Палітра (paper brand): --paper #FAF6F0, --card #F2EEE8, --line #DEDBD5, --ink #231C15, --ink-soft #6A5D49, --faint #9A8B71, --terra #C24E2C, --terra-deep #9E3C20, --olive #6E7C39, --parm/gold #CE9E45, --tomato, --stage #0C0704 (sea backdrop), --red #C21A1F (ONLY prices + active tab). --spectral: linear-gradient(90deg in oklch, #9E3C20,#C24E2C,#E8A544,#CE9E45,#C24E2C) — "threads whole arc", animates FASTER when .attending (pan 2.4s vs 7s + glow)
- Type: --serif Iowan Old Style/Palatino (display h1/h2 clamp 40-88px); --sans SF Pro Rounded/ui-rounded (body); --mono SF Mono tabular-nums (prices/meta/kcal)
- Easing: --ease cubic-bezier(0.32,0.72,0,1) SNAPPY; --tide cubic-bezier(0.37,0,0.63,1) WAVE/TIDAL. dive = easeInOutCubic
- MONEY SACRED: "Çmimet asnjëherë të rrumbullakosura" (never rounded), tabular-nums, --red reserved prices, NEVER tweened
- DIVE transition: click "View menu" → __seaDive(cx,cy) shader plunge anchored on BUTTON (diveClick), camera push-in then settle (620ms sheet surfaces while under, 1400ms done), ring wipe from button, refract, chromatic. reduced-motion → plain scrollIntoView
- Feedback: __seaSwell (energy→0.5 on add), __seaPhase (order state → wave phase), ripples on tap, spectral .attending faster
- FLOW (client): hero → menu (tabs=categories w/ counts + search "Kërko" + sort chips Çmimi↑/Proteina/Kalori) → dish card (thumb gradient, badge, tags veg/hot, price --red, +add) → detail (bottom-sheet mobile / centered modal desktop @720px, ingredients chips "Çfarë përmban", nutrition protein/kcal mono, stepper −q+, "Shto në shportë") → checkout sheet (order lines, address input, comap canvas mini-map, entrance/floor note for courier, phone → SMS OTP hint, cash-on-delivery "e vetmja mënyrë"=only way, "Vendos porosinë") → tracking (stylized courier tmap canvas of-a-piece with sea, step pills, ETA mono, back to menu)
- Util toggles header: currency (L·ALL) + language (SQ/EN, --red active) — mirrors real dowiz storefront
- Card whole tappable→detail; add button pointer-events auto; grip handles (44×5 rounded) on sheets
- a11y: focus-visible 2px terra outline, aria-modal dialogs, reduced-motion respected, no-gl fallback (radial gradient bg)
- ЦЕ БУКВАЛЬНО field-UI теза реалізована локально: море=фізичне поле, стан→wave params, dive=cinematic transition, money-never-tween. Вже частково в проєкті (particle-cloud.js схожий).

## Повний список сторінок (26):
- CLIENT: MenuPage, CheckoutPage, OrderStatusPage + ClientLayout + (CartDrawer, CartFAB, ClientUI, OrderProgress, OTPModal, ProductCard, StateChip)
- COURIER: LoginPage, ShiftPage, TasksPage, DeliveryPage, EarningsPage, HistoryPage, CourierInvitePage + (CourierShell, CourierUI, SwipeToComplete, TaskCard, types)
- OWNER/ADMIN: Dashboard, MenuManager, RecipeEditor, AllergenEditor, Analytics, Branding, Couriers, CRM, Promotions, Settings, SupplyLibrary, Onboarding, Activation, Login, AuthCallback, FlowTest + MenuFirstOnboarding, Privacy

## Агенти (5):
- INV-client-courier (a9a3d56) · INV-owner-admin (a31cd22) · hub-state-order (a36a4a4) · multimodal-crossplatform (a8b1d22) · design-language (a0515555)

## INV client+courier — DONE (MASTER FEATURE CHECKLIST — жодну не втратити):
### THEMING/SHELL: per-tenant palette derive(primary/bg/text→full); logo; SunlightToggle light/dark; CurrencySwitcher(+EUR rate); LanguageSwitcher gated supportedLocales; branding-preview iframe live postMessage; draft-theme URL params; embed mode; paper-skin; serif heading; sticky cart bar (hidden checkout) animated total + count badge + bounce
### MENU: fetch min-skeleton-dwell 300ms; 404/error/empty distinct+retry; HERO Google rating stars+reviews + geo ETA "~N min" (OSRM) + StateChip open/closed/busy + banners; category tabs w/ counts + scroll-spy IntersectionObserver + smooth-scroll + Chef's Picks ✦; SEARCH persisted per-slug; SORT default/price↑↓/name (flat on non-default) persisted; allergen filter chips persisted colored; no-results reset; responsive staggered grid; reduced-motion всюди
### CARD+DETAIL: shared-element layoutId image morph, lazy, no-photo gradient fallback, allergen corner badges +overflow, kcal badge, chef-pick, ingredient chips, taste-axis repeated icons, prep-time, price, quick-add(+), unavailable dim. DETAIL bottom-sheet: scroll-lock, rich media gallery/renderer (ADR-0002 lazy+gated 4s abort), Canvas-2D cinematic reveal overlay, kcal/macro overlay, taste, nutrition 4-col (BOM-computed), allergen labeled list, ingredients, MODIFIER GROUPS radio/checkbox/select/quantity (required/min/max, price deltas, availability), selected-summary, qty stepper, add-to-cart live price, toast+bounce+navigator.vibrate(12) haptic. Activation-embed postMessage edit. Footer address+Maps/IG/FB
### CART engine: localStorage per-location versioned corrupt-recovery; cross-tab storage-event sync; add-dedupe product+options; qty(0=remove); clear; bounce event; reconcileToMenu re-price/remove drifted + toast
### CHECKOUT: empty guard; delivery/pickup tabs (scheduled scaffold coming-soon); contact name+phone Albanian→E.164 normalize+validate; optional messenger TG/WA/Viber; entrance photo R2 upload preview (delivery); MapWithPin draggable + "My Location" geo; street address; entrance+apartment required-validated; "how to find you" notes required; dropoff instruction chips (leave-at-door/call/ring/hand/text) + custom; payment CASH cash-amount (min=total, change/too-low), courier tip optional (+cash-to-courier line); summary subtotal/delivery-fee-or-"calculated at checkout"(distance-tier degrade)/tax/tip/animated-total/pre-order-wide-ETA-25-45/client-total-mirror ADR-0005; NutritionRing SVG donut; Wikipedia "did you know" city fact; draft+last-delivery persist/restore; PLACE order idempotency-key + server-priced items + OTP flow (send/verify/order_intent_hash/x-otp-verified header/acknowledged_codes) + ERROR MATRIX (422 MIN_ORDER, NOT_DELIVERABLE, CASH_TOO_LOW, item_unavailable/hard_block, humane server msg, 5xx→call-restaurant fallback-config phone), location-load-fail guard, success checkmark+navigate 1.5s, push subscribe (Notification→SW /sw.js→VAPID→/customer/push/subscribe), privacy notice, dev mock gated
### TRACKING: status fetch; tracking-link ?t= exchange JWT; live WS order:${id} (order.route polyline / order.courier_updated position/name/masked-phone / order.status merge+*At timestamp TERMINAL-LOCK ignore late / order.message); 30s watchdog refetch; reconnect refetch; CourierLiveMap tweened+ROTATED marker (bearing) + route polyline + dest pin + WS status dot (marker only after real fix); HONEST ETA range {low–high} never single/0, overdue/pre_assign/terminal variants; status headline+accent+pulse pickup-specific; OrderProgress stepper (delivery/pickup branch, terminal red, timestamp fill, step times, active pulse halo); SR role=status announcers + status-change toasts (skip cold-open); SHARE-MY-LOCATION watchPosition→WS client_location auto-stop DELIVERED; call courier masked; message deep-link; terminal CTAs (not-charged/order-again/call); order details line totals+tip; DELIVERED rating stars+feedback + un-gated Google review invite; nutrition snapshot; MessageThread; offline/WS banner + call fallback (owner-gated)
### COURIER: BottomTabBar Tasks/Earnings/History/Shift; full-bleed delivery/login; dev-mode propagate. LOGIN email/pw error-shake 401-map. INVITE redeem (expired/used/revoked/invalid states, role-aware courier/dispatcher, name/email/phone-E164/pw≥12/16-char code, 410/401). SHIFT live HH:MM:SS timer, start/end, on/off pulsing dot, today's stats grid (deliveries/earnings/distance/online-time staggered), messenger save PATCH. TASKS assignment fetch + real online-status from shift + WS courier:${id} task_assigned + playPing() sound + dedupe, accept→delivery/reject optimistic-restore, online/offline empty states paper-illustration, AnimatePresence popLayout. TASKCARD 60s offer countdown shrinking-bar + AUTO-DECLINE expiry (Bolt §F7), urgent≤10s red, pickup→dropoff timeline, accept/reject. DELIVERY live GPS watch + 12s heartbeat POST /courier/shifts/ping (server-gated), GPS/WS dots + unavailable banner, CourierLiveMap (courier/dest/CLIENT-shared-location pins + route restaurant→customer), WS order:${id} client_location plot/remove + order.status CANCELLED/REJECTED mid-delivery banner, drop-off card address/ETA/instructions, entry-photo thumb→fullscreen modal, tip display, cash breakdown (total/customer-pays/change), call+message customer, MessageThread, mark-picked-up, cash-collected input, SwipeToComplete (keyboard Enter/Space, resets-on-failure never-fake, idle nudge), delivery reconciliation 409/422 closed-vs-retry, AnimatedCheck celebration. EARNINGS count-up today/week/month+tips, payout history+StatusBadge, empty/error. HISTORY completed list (orderId/restaurant/address/amount/locale-date sq/en/uk/5-star/feedback quote), count, empty/error
### SHARED RT: useWebSocket (token auth, room sub/unsub, infinite backoff 2s→15s+jitter, resume online/focus/visibilitychange, status states); use-courier-marker rAF tween (interp, out-of-order guard, snap first-fix/large-jump, bearing rotation, tab-hidden pause); MessageThread preset quick-replies (role×status matrix, param pickers minutes/location/action/amount, mark-read, terminal lockout); use-geolocation (watch accuracy/heading/speed, permission states); messenger deep-link TG/WA/Viber; push (permission→SW→VAPID→subscribe); reduced-motion every surface; 44px targets; focus-visible; SR/aria-live; safe-area
### OWNER FRONT-DOOR public: MenuFirstOnboarding /start — menu upload file+drag-drop PDF/photo ≤10MB → /owner/menu/import/anonymous heuristic parse preview (item count/categories/samples), slugify ë/ç transliterate + reserved-word+min-len validate, slug availability 409, .dowiz.org suffix, Telegram claim (/auth/telegram/start deep-link + poll /auth/telegram/poll 5-min 410), authed direct /owner/onboarding/start, start-without-menu, AccessRequestGate, phases choose/parsing/review/blank/submitting, error matrix

## HUB/STATE/ORDER — DONE:
- STATE MACHINE (SPINE): order_machine.rs 10 states Pending/Confirmed/Preparing/Ready/InDelivery/Delivered/Rejected/Cancelled/Scheduled(disabled)/PickedUp; wire UPPER_SNAKE; terminal={Delivered,PickedUp,Rejected,Cancelled}. allowed_next: Pending→{Conf,Rej,Canc}; Confirmed→{Prep,InDelivery}; Preparing→Ready; Ready→{InDelivery,PickedUp}; InDelivery→Delivered. Happy: P→Conf→Prep→Ready→InDel→Deliv; pickup fork Ready→PickedUp. assert_transition 3 errors SameStatus/ScaffoldDisabled/Illegal stable codes. fold_transitions replay stop-at-first-illegal. МІРРОРЕНО byte-for-byte у web/src/lib/channel.js:20-69 → браузер валідує transition БЕЗ kernel/server call
- ⚠️ ROLE→TRANSITION UNGUARDED: kernel+server перевіряють лише LEGALITY не ACTOR (apply_event/routes.rs order_event беруть будь-який next_status від будь-кого). Role attribution = UI/product convention. Redesign: додати role→transition mapping на UI/capability layer (bebop SignedFrame scope model)
- HUB = owner data-hub: kernel=decide/fold Law (pure I/O-free wasm32, integer money no-float, NO courier-scoring). place_order→Pending subtotal=Σunit·qty; apply_event→new Order immutable event-sourced. analytics.rs ChannelLedger = перший reader attribution (ingest idempotent channel-locked, orders_by_channel, funnel 10-stage, reduce_anomalies reuse fold Law). OwnerDashboard renders (currently hardcoded sample via channel.js)
- LOCAL-FIRST CONFIRMED: (1) Astro SSG no server adapter → static HTML; (2) kernel in-browser wasm-bindgen web-target, place_order_js/apply_event_js/channel_ledger_js entirely WASM; (3) order placement NO network call (grep: only fetch = push.js + .wasm loader); (4) proven headless kernel.test.mjs no server. Server needed ТІЛЬКИ: persistence (SQLite orders/push_subs/venues — AVAILABLE but web NEVER POST /api/orders, unused), push subscribe, cross-device sync. TO MAKE FULLY LOCAL: persist event log client-side (IndexedDB/OPFS/wa-sqlite) + replay fold_transitions on load (reducer exists), server/transport = OPTIONAL sync peer коли navigator.onLine
- particle-cloud.js = ВЖЕ physics field substrate: 4096 ring, velocity integration + exp damping, GLOBAL energy scalar (field temperature, bursts raise :144, time lowers :209), pointer-repulsion, additive glow, auto-sleep idle. VOCAB :31-37 мапить 5 events→{color,energy,burst,swirl}
- REACTIVITY CATALOG (core of state-reactive field):
  (a) INTENT: toggle modifier→ripple; add-cart→ingest pulse toward cart-attractor; qty→cart-region mass ∝ subtotal; cash→STATIC no-anim (money); submit→order_created amber burst; select channel→funnel re-form; pointer-move→LIVE repulsion (radius 9000dpr² force 0.6); reduced-motion→burst×0.25
  (b) EVENT (VOCAB status→physics — прямий terracotta→gold гачок): order-placed→order_created amber[1.0,0.64,0.13] burst1.4; →InDelivery/PickedUp→courier_assigned teal[0.18,0.85,0.78] SWIRL1.6 (tangential=courier motion); →Delivered→delivered gold[1.0,0.82,0.27] BURST1.8 (bloom); Pending/Conf/Prep/Ready→pending_aging ember[0.95,0.45,0.20] energy0.3 (slow drift wait); →Rejected/Cancelled→dispatch_failed blood[0.8,0.1,0.16] SWIRL3.4 (turbulence); illegal→hard recoil red shock (not wired); anomaly count→field agitation; channel→per-channel attractor mass. REDESIGN: replace discrete 24-particle burst з continuous field whose color/energy/swirl = f(OrderStatus)
  (c) STATE: Svelte 5 runes cart/selected/cashPayWith/submitting; derived cartLines/subtotal; FIELD-GLOBAL energy = damped scalar = field temperature (single most important knob — cart/shift/availability modulate energy + region mass)
  (d) ENVIRONMENT: ✅reduced-motion, ✅dpr(≤2), ✅resize, ✅?ch= channel (index.astro→order.channel); ⚠️push VAPID placeholder; ❌online/offline (GAP — kernel offline-capable nothing listens navigator.onLine), ❌open-hours (GAP no hours model), ❌GPS/courier-location (GAP CourierTrack static text no geo/marker). BUILD: online/offline listener (field syncs vs pure-local), open-hours model (ambient brightness/dormancy), geolocation feed (marker kinematics)
- TRANSPORT: canonical dowiz has NONE (REST+SPA fallback only, poll-only). bebop bebop2/proto-wire: Transport trait connect/accept/send/recv(SignedFrame); WssTransport REAL+tested (tokio-tungstenite, hybrid gate RequireBoth Ed25519+ML-DSA-65 PQ, AnchorRoster delegation, channel-binding F7 anti-replay, adversarial RED-tested); iroh QUIC = TODO stub (drop-in same Transport). Push = only live path (sw.js). REDESIGN: field subscribes Transport::recv() stream signed order-event frames → each verified → fold step → physics burst
- CROSS-ROLE: shared Law all roles (client Storefront/owner OwnerDashboard/courier CourierTrack same OrderStatus enum+table, channel.js byte-mirror); server single-source-truth when used (orders.status column authoritative, 409 illegal, single-writer Mutex); NO WS rooms today (islands independent, no cross-observe); decentralized future = matcher.rs pure deterministic replicable match_orders + fingerprint (open matcher not single dispatcher)
- TAKEAWAYS: (1) state machine small+total+JS-mirrored → status→physics fully local zero-server; (2) real physics field exists (particle-cloud) → redesign = EXTENSION not greenfield; (3) render path genuinely local-first; (4) 2 gaps: environment signals (online/offline/hours/GPS) + live transport binding (WssTransport exists untethered, iroh stub); (5) role→transition unguarded → add at UI/capability layer

## ⚠️ ОПЕРАТОРСЬКЕ УТОЧНЕННЯ (mid-turn): дизайн-план МУСИТЬ спиратись на:
- План field-UI двигуна (docs/design/field-ui-engine/ FIELD-UI-ENGINE-PLAN + BLUEPRINTS-FIELD-UI) — ОДИН оператор M Ü+Γ U̇+c²LU=S, wave_energy/LaSalle сертифікат, Laplacian-модальна, (ω,ζ) словник, ζ=1 critical easing=governor; 5 аспектів=layout/motion/feedback/focus/structure; hybrid межа field+constraint+SDF+state; money-never-tween за конструкцією; DOD SoA + zero-copy Float32Array view+writeBuffer + fixed-timestep + lazy-render-on-settle + thin-shell; beachhead канонічний web/, island-by-island
- Фізика (particle-cloud DOD ring, damped wave, bebop field math)
- pgrust філософія = Rust-native local-first інфраструктура → data-шар дизайну = Rust-native embedded event-log (IndexedDB/OPFS/wa-sqlite→майбутнє native), no-server, replay fold_transitions
- DOD (SoA, fixed-timestep DT=0.02)
- egui + wgpu (immediate-mode GPU renderer, WebGPU+WebGL2 fallback, MSDF text, SDF shapes)
### СИНТЕЗ-ПРИНЦИП: дизайн-мова = ШАР ДОСВІДУ на двигуні. Море=поле; переходи(dive/sheet-rise)=ζ-motion; feedback(ripple/bloom)=Green's function; brand content-sheet=SDF content layer; focus=potential well; drill-down layout=spectral/force layout; дані=local-first event-log. Кожен design-блюпринт мапиться на engine-блюпринт FE-01..17.

## DESIGN LANGUAGE "Sea & Sheet" — DONE:
- ТЕЗА: dowiz = ambient physics FIELD; brand = content SHEET що піднімається над ним. Кожен інтерфейс = 2 шари: continuous living SEA (tinted field: arrival/transitions/tracking/feedback) + opaque SHEET (venue paper/type/words: choosing). Sea = dowiz IP/moat (one field engine, one law, one motion vocab, під кожним екраном кожної ролі кожного бренду). Sheet = venue world (frame not picture). Elegance = air (3-sec hierarchy) + repetition (tiny palette+components). Nav = drill-down STORY (кожен клік глибше в бренд, кожен рівень винагороджує). Choices HEAVY (hero + field відповідає ≤1 frame; money НІКОЛИ не рухається)
- TWO-LAYER MODEL (backbone): LAYER A DOWIZ AMBIENT (Sea/Field — dowiz-owned brand-TINTED never brand-authored, рендериться field-UI engine M Ü+Γ U̇+c²LU=S): arrival full-bleed entrance, transitions (dive/sheet-rise/page), tracking (wave amplitude grows + color travels terracotta→gold зі станом — CourierTrack вже binds particle vocab до kernel OrderStatus), feedback (Green's function tap→ripple/success→heat/error→shake/loading→sustained), focus (одне V(x) potential-well drives scale/brightness/blur). LAYER B BRAND CONTENT (Sheet/Paper — brand-owned tenant-themed): content menu/cart/forms/tables, brand identity accent/ink/paper/type/radius, decision. SEAM = SPECTRAL EDGE (--spectral oklch terracotta→gold re-derived per brand, top rim every sheet + ring-wipe every dive + progress thread tracking, faster коли attending — ЄДИНИЙ dowiz mark на brand Sheet)
- ASSIGNMENT RULE: ambient/physical/transition/tracking/feedback→SEA; content/word/price/form/decision→SHEET; motion between states→SEA; number/money→SHEET NEVER MOVES; seam→SPECTRAL. MONEY GUARANTEE ARCHITECTURAL (field↔state boundary can't interpolate + <Money> component no tween prop + ESLint no-number-anim on price)
- TOKEN 3 TIERS: T1 BRAND-OWNED (owner торкається ЛИШЕ 5, Hick's law owner теж): accent --brand-primary(+auto hover/strong AA/light), ink --brand-text/-muted (AA validated), paper --brand-bg/surface/raised, type-pair --brand-font-heading/body (2 max serif+rounded-sans), radius --brand-radius (sheet-rise 26px derives). НЕ per-component overrides. T2 DOWIZ-FIXED (Sea+grid never overridable): --spectral oklch + --spectral-speed 6s/attend 1.4s, --sea-backdrop (color-mix brand-bg 12% #060402) + --sea-tint=brand-primary, --field-c/gamma/mass (NEW engine params), --font-mono (NEW REAL GAP — tokens.css uses tabular-nums але немає --font-mono token!) + --money-ink/--price-red #C21A1F (hue-only brand shift, role locked), --ease-snap ζ=1 cubic-bezier(0.32,0.72,0,1)/--ease-out expo/--ease-tide ζ>1(0.37,0,0.63,1)/--ease-soft, springs motion.ts ARE (ω,ζ), --space 4px/--text 8-step/--tap 44/48/56/--z/--safe/--status 10×4 (DOWIZ-FIXED — lifecycle identical every brand)/--chart. T3 DOWIZ INTERNAL BRAND (product's own chrome marketing/login/settings/owner-tool-frame) = Warm Cosmo-Noir default #d69a3d/#061b1a/#f5efe5 + data-skin=paper. Один accent → 4 placements (Sheet direct / Sea tint / spectral re-derive / backdrop 12%) coherent zero manual
- MOTION ζ VOCAB (single source of feel, ніколи raw cubic at call site): SNAP ζ=1 critical fastest-no-overshoot (--ease-snap/spring press{400,25}enter{260,24}) = DEFAULT UI taps/toggles/hero; FLUID ζ≈0.8 (--ease-out/gentle{180,22}) entrances/sheet-rise; PLAYFUL ζ≈0.5 overshoot (bounce{500,18}) RARE delight only (Emil: never frequent list) cart-success/first-order; TIDE ζ>1 overdamped never-settles (--ease-tide) = THE SEA (drift/amplitude-growth/terracotta→gold travel, continuous ambient); SPREAD heat-kernel monotone (staggered delay τ_j=dist/√α) reveals/theme-swap (items DIFFUSE from tap not pop)
- 3 CINEMATIC TRANSITIONS: (1) DIVE (Act1→2) shader plunge anchored on pressed button, camera push-in→settle, ring-wipe spectral + refraction + chromatic, easeInOutCubic≈snap, Sheet revealed THROUGH sea; reduced→scroll. (2) SHEET-RISE paper rises over sea, --brand-radius top corners spectral edge, FLUID gentle, grip handle drag-down returns. (3) SEA-DEVELOPS-WITH-STATE (Act3) SAME sea continues driven kernel OrderStatus: amplitude grows + color travels terracotta→gold + waves turn = TIDE reading state machine (tracking = entrance field MATURING not progress bar) — dowiz most distinctive moment
- STANDING LAWS: "don't be shy animation BUT money never tweens" (Sea generous continuous, Sheet numbers frozen, ripple carries "happened" never digits); air>expensive-animation (transition earns place by clarifying WHERE you went); exit faster than enter; reduced-motion first-class never load-bearing (Sea→static gradient, state legible via pills/color/text)
- NAV = 3-ACT universal shell grammar ВСІ ролі (3 acts=3 URL states drill-down + working back): CLIENT arrive(hero+top items)→choose(menu Sheet tabs+search+sort)→receive(sea matures+step pills+ETA); OWNER arrive(dashboard pulse=field IS business, order=ripple, volume=amplitude)→choose(menu/orders SPLIT-PANE nav-left/content-right + accordion)→act(order action→field ripples, venue accent tints Sea); COURIER arrive(shift/queue calm+task ripple)→choose(task Sheet big --tap-courier/critical)→deliver(SEA IS THE MAP CourierTrack bound to status). INVARIANTS: split-pane for density (φ₂φ₃ spectral / 2-col collapse <bp-tablet); progressive disclosure accordion ≤7 (Hick's); breadcrumbs+URL-state+working-back (load-bearing); every level rewards click (category shows top items+brand voice); hybrid serves both types (lazy top-items Act1 Pareto 80/20, curious drills)
- MEANINGFUL AGENCY: one hero per view 3-sec rule (brightest/largest = deepest potential-well V(x), scale/brightness readouts not hand-tuned); instant consequence=field answers ≤--motion-instant network-indep (add→ripple+cart-pill exact-value snap, category→sheet re-diffuse SPREAD, advance→amplitude jump); presets 80% (cash default/remembered address/top-items/portion) + customization power-user behind progressive disclosure; Hick's grouping (tabs/chips/levels ≤7); attending state visible (spectral speeds up where agency needed); automate inconsequential (formatting/layout/reflow = field silent)
- COMPONENT GRAMMAR (tiny vocab repeated well, strict palette): field-backdrop(Sea full-bleed z-under), sheet(opaque --brand-surface 26px spectral-edge --elev-3 grip bottom-sheet/modal), spectral-edge(only dowiz mark on brand), grip(32×4 drag-dismiss), card(one idea, cardEntry crisp never bounce, pad ≥space-4/6), stepper(symmetric −val+ ≥tap-min SNAP mono-if-count), chip(pill multi-select active=primary), pill(status --status-* ONLY never brand-accent), Money(STRUCTURAL: --font-mono tabular-nums --money-ink kernel integer, NO tween prop exists, never rounded), tab-bar(active brand-red underline drives SPREAD), ripple/particle(2 renderers 1 field source never decorative-only). PALETTE DISCIPLINE: brand accent+derived / ink 2wt / paper 3surf / --gold / spectral / --status / --money-ink — NO new hue per screen. AIR LAW: negative space = component (min pad space-4 card/space-6 sheet/space-8 section), crowded→cut content or add drill-down level never shrink air
- 9 COHERENCE RULES (mechanically checkable): (1) Sea always dowiz (one field behind content every surface tinted venue accent); (2) spectral edge threads every flow; (3) money sacred (--font-mono tabular --money-ink kernel-integer never-tween never-round, 3-way enforced); (4) 3-second hierarchy (one hero deepest well); (5) air ratios + limited palette (no new hue, tiny component set); (6) ζ governs all motion (no raw cubic); (7) consequence ≤--motion-instant network-indep; (8) drill-down story + reversible (URL-state breadcrumb back-button state-intact); (9) reduced-motion never loses meaning
- CROSS-BRAND: Brand A (pasta terracotta cream serif warm) + Brand B (cosmo-noir gold teal-black) share ~95% (identical Sea physics/law/ζ/components/scales/status/money/spectral-identity), differ ONLY 5 T1 tokens. Coherence BY CONSTRUCTION (not enough brand-authorable surface to diverge). Paper look = Brand A's 5 tokens NOT dowiz skin; dowiz internal paper/cosmo = T3 product chrome. Rule: Sea tints active venue, Sheet wears content-owner brand
- CROSS-PLATFORM (2-layer defined PHYSICALLY field+surface not pixels → ports без redesign): web/mobile (full WebGL2 field, bottom-sheet, thumb CTA tap-44); laptop/owner (same field wider, SPLIT-PANE nav-left/content-right, centered modal, denser tables, density via split-pane+accordion never shrink air); AR/spatial glasses (Sea LITERAL spatial 3D, tracking waves spatial, Sheet = fixed floating panel comfortable focal depth, money pinned mono panel — field↔state matters MORE here); outdoor/glare courier (Sea flat high-contrast, sunlight-mode AAA 7:1 existing tokens.css). Invariants §8 hold every platform
- BUILD: REUSE tokens.css cascade + all scales + motion.ts springs/variants + paperSkin(rescope T3) + CourierTrack status→field + PaperScene fallback. ADD: --font-mono (real gap), --money-ink/--price-red, --spectral+speeds, --sea-backdrop/tint, --field-c/gamma/mass, --ease-snap/tide, <Money> component + ESLint no-tween-price, shared <Shell> 3-act Sea+Sheet all roles. FLAG: reference max-width:390px mobile-mock → production responsive split-pane at bp-tablet/desktop

## MULTIMODAL + CROSS-PLATFORM — DONE:
- BINDING CONTRACT (spine of coherence): УСЕ = field impulse. FieldPos(u,v)∈[0,1]² (+w AR curved-panel). enum Intent: Point{p,strength,id} (weak potential-well test particle) / Impulse{p,force,amp} (ripple damped-wave) / Select{target,p} (well collapse add/confirm) / Navigate{axis Dive|Surface|Lateral} (drill-down = cinematic dive) / Scrub{p,delta} (advect/pan) / Command{verb,object,slots} (voice → resolver expands). field.apply(&intent) → boundary/source PDE term (Gaussian packet Impulse / moving Dirichlet Point / well-depth Select / advection Navigate) = ОДИН code path. Modalities differ тільки HOW produce FieldPos+confidence; physics one path. Це інваріант що робить voice/finger/pinch/gaze/controller = same UI
- VOICE local-first: engines — Web Speech processLocally (on-device коли OS lang-pack, 17 langs Chrome, default=CLOUD must opt-in) / whisper.cpp WASM (tiny 31MB Q5, 2-3× realtime WASM 5-8× WebGPU) / transformers.js Whisper ONNX WebGPU (5-10× WASM) / Moonshine Web (6-61MB SUB-SECOND streaming English) / Vosk (offline grammar) / wake-word Porcupine|openWakeWord (~1-2MB always-on gate). ARCHITECTURE: mic → wake-word gate → ring buffer AudioWorklet→SharedArrayBuffer → ASR Web Worker (Moonshine live / Web Speech / whisper) → transcript → LOCAL intent parser (command-grammar/FST/slot-filler NO LLM) → Intent::Command → resolver → Select|Navigate|Impulse → field.apply. Web Worker Pipeline (audio+inference own workers, zero-copy PCM, render loop never stalls, NEVER ASR main thread). Deictic grammar: "show me pasta"→Navigate{Dive(pasta)}, "add cacio e pepe"→Select{dish} well-collapse, "checkout"→Navigate{Dive(checkout)}. Voice resolves SAME Navigate/Select as finger → field animates identically. Rec: wake-word→Moonshine (English live) + Web Speech processLocally (zero-download) + whisper quantized (multilingual fallback), COMMAND GRAMMAR not free dictation
- HAND GESTURES touchless: MediaPipe Tasks Vision (@mediapipe/tasks-vision, on-device WASM+GPU delegate) — Hand Landmarker 21 keypoints normalized+worldLandmarks metric, Gesture Recognizer (Closed_Fist/Open_Palm/Pointing_Up/Thumb_Up/Victory/ILoveYou + custom via Model Maker), recognizeForVideo. WebXR Hand Input 25 joints AR/VR (pinch=select near/squeeze far). PIPELINE: camera getUserMedia → MediaPipe landmarks+gesture @30fps → state machine (debounce/confidence-gate/hysteresis) → index-tip→FieldPos → POINT(index extended)=Intent::Point moving Dirichlet / PINCH(thumb-index<ε)=Select well-collapse / OPEN_PALM push(z-vel)=Impulse amplitude / SWIPE(wrist vel)=Navigate Lateral|Dive / 2-hand spread=Scrub zoom. Gesture→field force NATURAL fit (extended index IS moving Dirichlet, pinch IS well-collapse, palm-shove IS amplitude impulse — pointer-ripple physics = touchless substrate). Browser fully on-device ~30fps mid-laptop production-ready. AR headset: DON'T camera pipeline — WebXR Hand Input direct. LIMITS: needs lighting, single-RGB depth noisy, debounce hard (pinch flickers), continuous vision HEAVIEST battery lane — gate like wake-word
- AR GLASSES: WebXR immersive-ar TODAY on Quest3/3S (full-color passthrough + Plane/Anchor/Depth) + Chrome Android ARCore; Vision Pro VR-only browser (AR module NOT enabled, gaze+pinch → Point+Select); iOS Safari NO WebXR. wgpu→WebXR: WebGPU Baseline Jan2026 (multi-view stereo); XRGPUBinding = Editor's Draft 15 Jun 2026 NOT shipped (adapter xrCompatible, createProjectionLayer, getViewSubImage). wgpu NO upstream WebXR (issue #8329 open). WORKING 2026: (a) wgpu fork expose GPUDevice + wrap external GPUTexture (no WebGL no copy), (b) WebGL2+XRWebGLBinding fallback (Quest/Android today, loses compute + forces copy), (c) native OpenXR+wgpu (MOST MATURE, matthewjberger/wgpu-example Win/Linux/mac/Web/Android/OpenXR + hand tracking). FLAT→SPATIAL: render field → off-screen wgpu texture → curved/cylindrical panel floating XR ("menu in space"), same shader+DOD+1 vertex transform, FieldPos.w=panel-normal (crests pop toward user). INPUT=RAY: hand-ray/gaze-ray → intersect panel → hit (u,v)=FieldPos (controller ray + WebXR hand-ray + Vision Pro gaze ALL reduce to ray-hits-panel→FieldPos identical 2D pointer!). Pinch/air-tap=Select. SAME engine flat+spatial, only input front-end + 1 panel transform differ. VERDICT: realizable Quest/Android today (WebGL2+XRWebGLBinding OR native OpenXR), clean WebGPU path AS XRGPUBinding ships. DON'T block — ship flat+native-OpenXR, WebGL2 XR fallback, swap when lands
- CROSS-PLATFORM one Rust codebase: SHARED (write once) = field engine PDE solver/DOD SoA/fixed-timestep/Intent + WGSL shaders + wgpu one API. PLATFORM-SPECIFIC = shader variant (WebGL2 no compute → wave step CPU/fragment ping-pong), backend (Vulkan/Metal/DX12/WebGPU/WebGL2), windowing (winit desktop; iOS PREFER embed wgpu UIKit/SwiftUI view NOT winit-owns-app; Android winit surface-loss on suspend MUST handle Suspended/Resumed recreate surface; web canvas; XR no window), input backends, A11Y (canvas invisible screen readers, AccessKit Win/mac/Unix production but WEB EXPERIMENTAL → parallel hidden DOM semantic tree (dishes as real <button>) NON-OPTIONAL WCAG food-product), persistence. BREAKS: iOS winit fragile+no WebXR; WebGL2 no compute (field slow); Android surface-loss crash on backgrounding; web a11y black-box; XR WebGPU binding not shipped
- InputSource TRAIT (coherence layer): poll(&FrameCtx)→SmallVec<[Intent;4]>, kind()→Modality, confidence_floor() gate noisy. FrameCtx carries FieldProjection (screen→field pointer, camera-normalized→field MediaPipe, ray→panel→field XR). InputRouter.tick → for src.poll → field.apply(&intent) ONE code path physics identical. Backends: PointerSource(winit)/TouchSource(multi=Scrub)/VoiceSource(transcript→grammar→Command)/HandCameraSource(MediaPipe confidence_floor)/XrHandSource(joint→pinch=Select ray=Point)/GazeSource(Vision Pro)/GamepadSource. Multimodal FUSION trivial (voice "cacio e pepe" Command + finger Point same region → same well REINFORCE)
- LOCAL-FIRST DATA (nothing render/input loop hits server): MUST be local = entire menu/scene graph (dishes/prices/themes/layout memory hydrated boot), cart/order-in-progress (field reads every tick money+wells), Intent→field 100% local compute. Server ASYNC out-of-band (submission/payment/dispatch behind outbox drains online). Storage: browser OPFS+SQLite-WASM (@sqlite.org/sqlite-wasm real relational near-native, Worker sync-access, survives reload; IndexedDB KV fallback; Cache API assets) / native SQLite rusqlite (already bundled server crate reuse) / sync later CRDT Automerge 2.0 (Rust ~10× faster, offline-first converges no coordinator; Yjs alt). OFFLINE FLOW: boot hydrate OPFS-SQLite → interact Intents mutate local cart+field → checkout write LOCAL outbox pending → field "placed" optimistic well-collapse money-settle → async online outbox drains POST/peer mark synced CRDT-merge conflict. Render loop + modalities operate local exclusively, server = sync target NEVER render dependency (matches kernel/oracle split deterministic core owns truth locally)
- HONEST LIMITS: Web Speech lang-pack can't-bundle (17 langs device-dep → ship own Whisper/Vosk 30-140MB multilingual); free-dictation capped tiny/base (command grammar); single-RGB hand-track noisy depth + battery heaviest gate; wgpu↔WebXR NOT shipped (XRGPUBinding June draft, fork/WebGL2/native-OpenXR); Vision Pro VR-only + iOS no WebXR (AR-in-browser = Quest+Android); WebGL2 no compute (field resolution limited); web a11y UNSOLVED (parallel semantic DOM tree required WCAG); CRDT money-merge careful (naive schema → double-charge)
- SHIP ORDER: flat web (WebGPU+WebGL2 fallback) + native desktop → voice (Moonshine+grammar) + MediaPipe gestures → native OpenXR Quest → WebXR-WebGPU when XRGPUBinding lands. Local-first OPFS-SQLite/native-SQLite, CRDT deferred

## INV owner/admin — DONE (MASTER CHECKLIST — жодну не втратити):
### SHELL: auth guard (no token→/login, ?dev bypass); entry redirect (new→onboarding/draft→activation/published→dashboard/error→dashboard); 11-nav (Activation/Dashboard/Orders/Menu/Supplies/Promotions/Couriers/Analytics/CRM/Branding/Settings); collapsible desktop sidebar + mobile bottom-tab 4-primary + More sheet 7; logout; currency/language(al/en)/sunlight toggle; DEV Flow-Test easter-egg (5 logo clicks); paperSkinAttr
### ORDERS (Dashboard+OrderCard): 5 KPI (Pending/Preparing+Confirmed/Ready/Delivery/Revenue — ⚠️money COUNT-UP TWEEN reconcile); order search(name/shortId/items)+status-filter chips+sort(newest/oldest/highest)+Live/History toggle; export CSV; copy storefront link; OrderCard (shortId/time/status pill/timeline deltas confirm-prep-deliv min/OTP-verified signal/reputation-score signal/customer+masked-phone+address/item list+total/courier/order rating★+feedback/Overdue!/hollow-card shimmer); LIFECYCLE ACTIONS Accept→CONFIRMED/Reject→CANCELLED/Mark Preparing/Mark Ready/Assign Courier→IN_DELIVERY (Delivered/Rejected API-courier-driven NOT in card); reject/cancel danger-confirm "cannot undo"; optimistic+rollback+toasts; order detail modal; per-order PRESET messaging (MessageThread unread); readiness checklist 7 + progress; new-order alert banner + looping ping + haptics + arm/blocked states; Telegram-nag banner
### RT: WS dashboard room order.created/order.status (regression-guarded merge STATUS_RANK, PII claim-check + 800ms debounced authed refetch); WS courier room courier.position_updated/shift_updated; WS status dot; live courier map (NO-PRESENCE ADR-0006 no online dot); activation 8s draft polling; branding postMessage live preview; settings QR live-theming + locale auto-sync
### MENU: category CRUD (tab strip counts, empty-cat rule); product CRUD (name/price ALL/prep-time 1-1440/description/stock unlimited/taste 5-axes×3); photo upload ≤5MB jpeg/png/webp preview/remove; AVAILABILITY 86/stop-list toggle = de-facto "publish"; RecipeEditor BOM (supply picker by kind, qty steppers, auto nutrition Kcal/P/F/C, BOM allergen chips) — reads localStorage supplies; product allergen chips derived; MediaManager rich (presign→PUT→confirm, image/video/spin/model, hero/primary, reorder, hide, flag VITE_MEDIA_RICH); search/sort/availability filter; KitchenBusyToggle (raise ETA 30min); MenuScheduleEditor (per-category daily windows); AI import wizard (upload modes merge/add_only/replace→preview→commit→done ≤10MB); product preview card. ⚠️ NO real draft→published state machine (publish=per-product available boolean); DORMANT AllergenEditor attestation gate + ReadinessIndicator "cannot publish until allergens declared" IMPORTED NOWHERE (wire or drop); BOM→allergen cross-check advisory unwired
### SUPPLIES: SupplyLibraryPage localStorage-only (dos_supplies, food/condiment/packaging/utensil, nutrition, allergens, reorder threshold) — reconcile persistence; active-toggle+nutrition-confirmed latent no UI trigger
### ANALYTICS: period 7d/30d; 4 KPI (Revenue ⚠️count-up/Orders/Avg/Delivery-time) trends+no-data; revenue trend bar chart; top products + per-product drill-down; ingredient consumption + reorder flags + copy-list (HARDCODED data); order heatmap day×hour; delivery geo-map MapLibre; CSV exports. No WS/polling
### CRM: customer list (name/phone-masked/orders/LTV/last-order)+search+sort; contact reveal (audit-gated PII, one-shot)+redacted CSV; high-value badge >15; customer detail (top prefs/recent orders/ordering heatmap). No GDPR-erasure on admin page (PrivacyPage separate)
### PROMOTIONS: CRUD (code/type %-fixed-free_delivery/value/min-order/valid-from-until datetime-local/max-uses/description); active toggle; Active/Inactive/Expired (Scheduled implied no badge); usage badge used/max ≥90% warn; percentage≤100+value>0+valid-from validation; delete confirm; unexposed model fields applicable_product_ids/max_uses_per_customer; NO discount-stacking single-code
### BRANDING (⚠️ token editor критично): 3 ColorInput Primary/Background/Text (native color+hex) — ТІЛЬКИ 3 з 10 tokens editable (7 derived derivePalette); AA contrast advisory non-blocking; logo upload ≤2MB png/jpeg/svg + Logo URL + preview; auto-generate brand from website/logo POST /owner/brand/generate; Google Maps (rating/reviews/URL/place-id); social IG/FB; live phone-frame iframe preview postMessage theme+logo NO reload; shareable URLs SSR /s/:slug + SPA copy. ⚠️ REDESIGN GAP: NO font picker, NO radius/spacing, NO theme presets, NO skin(paper/bebop) selector despite [data-skin] system existing
### SETTINGS: store details (name/phone E.164/address); delivery config (fee ALL/min-order ALL + MapWithRadius zone draggable-pin+radius); working hours 7-day open/close; admin/notification language; Telegram (connect QR/deep-link + targets enable/disable + Send Test); notification category preference-centre (transactional always-on/operational/quality flag-gated); fallback phone (independent save); delivery pause/resume master toggle (immediate)
### COURIERS: list + active-account count (NOT presence) + search; invite (email + role Courier/Dispatcher + 48h TTL + 16-char security code + copy link+code); lifecycle invited→pending→active; detail (earnings today/week/month + recent deliveries w/ timing assigned/accepted/picked-up/delivered + recent shifts); order-detail modal status timeline; export CSV; live map no-presence; ⚠️ courier rating READ from API vs NO-COURIER-SCORING reconcile; courierPositions never populated ADR-0006
### ONBOARDING/ACTIVATION: menu-first (upload PDF/photo→AI parse anonymous_import_id→pre-fill name/phone/slug→review→claim authed create+seed / anonymous Telegram claim poll); start-without-menu manual; slug validation reserved/min-3/taken; AccessRequestGate flag. ACTIVATION trinity gate (menuConfirmed/notificationsConnected/fulfillmentReady) + pickup toggle (pickup-only publish) + test-order prompt; PUBLISH gated canPublish + "still needed" missing[] + live/draft iframe preview + inline product edit from preview postMessage dos_activation_edit_product; 8s draft polling
### AUTH: email+password (/auth/local/login 401); Telegram deep-link poll; Google OAuth flag; dev one-click ?dev; session-expired banner; tokens session+safeStorage; OAuth callback code-exchange; error matrix 401/410/timeout
### DEV: FlowTestPage 7-step lifecycle runner (Create→Confirm→Preparing→Ready→Assign→Pickup→Deliver) — end-to-end state-machine harness confirms IN_DELIVERY→pickup→delivered absent from OrderCard
### CROSS-CUTTING: i18n al/en inline fallbacks (gaps AuthCallback/FlowTest hardcoded); currency ALL minor-units integer PriceDisplay; cash payment model; loading/empty/error/success + reduced-motion every page; Zod validation (some z.any() tighten); Tirana/Durrës geo 41.331,19.817
### KEY REDESIGN FLAGS: no real draft→published (publish=available boolean), dormant allergen publish-gate built-but-unwired; branding 3/10 tokens no fonts/radius/presets/skin; supplies localStorage-only; money-tween legacy Dashboard+Analytics; couriers no-presence + rating-read reconcile NO-COURIER-SCORING; OrderCard no Delivered/Rejected button

## ВСІ 6 ЗВІТІВ DONE. Готовий синтезувати design plan + blueprints для ВСІХ інтерфейсів.
