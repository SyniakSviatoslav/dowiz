# RAW PROMPT 2 — Батч 4 (Frenet-Serre/Kepler/Hooke/Charles/Banach) + Truthfulness Criterion + Refactoring/Distributed-Systems Expansion

**Saved verbatim on operator request, 2026-07-19.** Second capture in this arc — additive to,
NOT a replacement for, `RAW-PROMPT-agentic-swarm-safety-fluid-architecture-2026-07-19.md` and
`SWARM-SAFETY-DETERMINISTIC-CIRCUIT-BREAKER-SYNTHESIS-2026-07-19.md`. Operator explicitly said:
"expand the original prompt with this one... without rewriting the original researches & first
synthesis." Do not edit this file's body after the fact — verbatim capture only.

---

## Part 1 — "Мультидисциплінарний довідник — Батч 4" (8 screenshots, 6 clusters)

Full content: a compiled reference covering differential geometry (Frenet–Serret formulas — the
moving T/N/B frame, curvature κ and torsion τ, the skew-symmetric matrix form, the Darboux vector
ω⃗ = τT⃗+κB⃗, the fundamental theorem of curves), classical mechanics (Kepler's second law
dA/dt=½r²θ̇=const as a consequence of angular-momentum conservation for ANY central force, not
specifically gravity — Noether's theorem chain: isotropy of space → conserved angular momentum →
equal areas), Hooke's law (F=−kx, the Taylor-expansion argument for why every stable equilibrium
looks like a spring to first order, U(x)≈U(x₀)+½U''(x₀)(x−x₀)², k=U''(x₀)), Charles's law
(V∝T at constant P, absolute zero as a linear extrapolation, the 4-law ideal-gas-law family
table), and Banach space (complete normed vector space, Cauchy-sequence definition, the space
hierarchy Topological⊃Metric⊃Normed⊃Banach⊃{ℓᵖ, Hilbert}, Banach fixed-point/contraction-mapping
theorem with its convergence-rate bound ‖xₙ−x*‖≤qⁿ/(1−q)‖x₁−x₀‖, and the explicit question this
raises for a "generate→evaluate→refine" agent loop: "is my refinement loop a contraction? what is
its q? what is the metric I'm even measuring distance in?" — flagged as the open question that
Part 2 below answers with "time"). See the message delivered in this conversation turn for the
complete text (~4,000 words with 🟩 literal-content / 🟦 enrichment / ⚠️ epistemic-limit markers
already applied by the source itself). Table of contents also names three further sections whose
full body was NOT pasted, only topic-lists appended at the end of the message (see Part 2):
"6. 12 архітектурних концепцій" (distributed systems), "7. 12 патернів рефакторингу" (code
quality), "8. PDDL-INSTRUCT — навчання LLM планувати" (symbolic planning for LLMs).

## Part 2 — Operative instructions (verbatim)

> Faithfullness is rejected as criterion, also research additionally [Батч 4 content above],
> also consistent caching, auto-scaling, sharding, service discovery, magic numbers, nested
> conditionals, long parameter lists, duplicate code, deeply nested loops, boolean flag params,
> commented-out-code, god-functions, string-keyed-logic, callback hell, muttable shared state,
> vagues variable names. All of the quality coding rules should be researched & aplied, also some
> of them like "god functions" are needed in terms of my original prompt & researches - for
> example for the ability of emergency recovery & backdoor storing them, also the metric
> mentioned in the docs needed for the loop distance - my idea that it is time. Topology &
> chronology layers are present so time can be used inside the attractor. Research everything,
> expand the docs & after that synthesize with fable again. I also reject "faithfullness" as
> safety criterion - instead of I suggest to use "truthfullness" as the measerument of logic,
> mathematically correct result. For memory poisoning & hallucinations modules - there will be a
> need to combine existing methods & researches insights to create custom modules. My idea is to
> measure memory poisoning & hallucinations with chronological comparisons of the models results
> based on the exactly same injections & conditions - they should be always the same, otherwise
> direct signals of the poisoning or hallucibation. Same as with the red team testing - all the
> defence layers should be validated by actual acttacks, different aggresive injections, etc. For
> the model truthfullness the main criterion is similiar to the retrieval logic. So basically can
> a model give the exactly same - byte by byte answer with same conditions on different time. Any
> divergency from this is the signal of poisoning or hallucination. Also do not reject & miss my
> vision to use mechanical deterministic approaches to direct & wrap over the stochastic data
> flow. Now expand the original prompt with this one - research everything & then synthesize
> again with fable, but without rewriting the original researches & first synthesis
