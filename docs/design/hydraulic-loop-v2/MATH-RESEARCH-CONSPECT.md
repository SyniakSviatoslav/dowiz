# Дослідження ключових математичних мостів (Фаза 3)

## R1 (контракція k / Банах) — DONE:
- ⚠️ 1−cos НЕ метрика (порушує трикутник) → Банах НЕ застосовний. Використовувати d_g=arccos(⟨u,v⟩) (геодезична на S^{d-1}) АБО хордову √(2(1−cos)). Обидві order-preserving → ordering-логіка resonator НЕ міняється, лише ratio-арифметика k̂ стає валідною. Обмежити на геодезичну кулю радіуса <π/2 (cut locus / антиподи)
- Скалярний k̂=exp(slope log e_n vs n) — НЕОБХІДНА-лише умова. Вердикт: upper(CI_k)=exp(b̂+t·se)<1 (не точкова оцінка!). Ламається на e_n→0 (log singularity: weight w_n=e_n² або truncate <10·floor) і non-geometric transients (fit тільки asymptotic tail)
- ДОСТАТНЯ умова = ρ(J)<1, J=DT(x*). Оцінка online БЕЗ autodiff через difference-snapshot DMD: Δ_n=x_{n+1}−x_n (скасовує невідомий x* бо T афінний біля x*); Δ_{n+1}=J·Δ_n; Ĵ=YX† (Moore-Penrose); reduced: X=UΣV*, Ã=U*YVΣ⁻¹ (r×r), eig(Ã); ρ̂=max|λ|. Row-major flatten Ã → lyapunov::spectral_radius(a,r) ПРЯМО
- ⚠️ КОД GAP: lyapunov::eigenvals = СИМЕТРИЧНИЙ Jacobi sweep → misreports КОМПЛЕКСНІ DMD eigenvalues (2-cycle μ≈−1). Потрібен general real eigensolver (Schur/QR) для non-symmetric Ã
- Noise de-bias ОБОВ'ЯЗКОВИЙ: OLS Ĵ=YX† biased-toward-0 (errors-in-variables) → ρ̂ spuriously <1 (RED-B). Fix: TLS-DMD або forward-backward Ĵ=(Ĵ_f·Ĵ_b⁻¹)^½; self-check ρ_f·ρ_b≈1 на stationary
- kalman.rs P_k=ĴPĴᵀ+Q: covariance tube навколо траєкторії — flag коли residual > P-band (нелінійність, не вір ρ̂)
- k∈[0.5,0.8] НЕ оптимум: n(ε,k)=⌈ln ε/ln k⌉, при constant c_turn менше k завжди краще. Реальний оптимум: c_turn'(k)/c_turn(k)=1/(k ln k) (cost elasticity). Band = ALARM limits (≥1 divergence; >0.9 slow; дуже мале+high cost overspend)
- 5 RED falsifiers (k̂<1 але НЕ конвергує): A non-normal transient ρ<1 але ‖J‖≫1 (hump overruns fuse; detect ‖Ĵ‖₂>1); B noise attenuation (true ρ≥1→<1; detect fb-consistency + CI); C biased fixed point x∞≠x* (Banach до ЧУЖОЇ точки; detect termination==Stalled && final_error≥ε); D cosine mirage (1−cos спадає а arccos росте; detect recompute в arccos); E attracting 2-cycle (scalar fooled, is_chaotic MISSES бо resets; detect vector ρ̂≈1)
- КОМПОЗИТНИЙ вердикт "converged" ⟺ ВСІ 5: (1) metric=arccos в <π/2 кулі; (2) upper(CI_k)<1 на truncated tail; (3) ρ̂<1 з de-biased reduced DMD; (4) ‖Ĵ‖₂ bounded; (5) ¬is_chaotic ∧ termination==Converged ∧ final_error<ε

## R3 (well-posedness інтейку) — DONE:
- Модель: constrained discrete dynamical system x_{n+1}=F(x_n;D); розв'язок = fixed point F обмежений feasible set M; well-posedness = ТИХОНОВСЬКА коректність мінімізації (не PDE Hadamard буквально)
- Dirichlet=immutable reference (π_R(x)=r* const), Neumann=drift-rate cap ‖ΔV‖≤δ, Robin=adaptive budget α·d+β·‖ΔV‖≤B_n
- Finite well-posed: F1 SAT (feasible nonempty + bounded below = existence); F2 DET (унікальний мінімізатор, residual DOF d≤0 = uniqueness/strict convexity); F3 COND (verify pure/reproducible/Lipschitz, no knife-edge = continuous dependence)
- ¬F1→Unsatisfiable→осциляція; ¬F2→UnderDetermined→random convergence; ¬F3→IllConditioned/NonReproducibleVerify→chaotic
- Гейт = ОДНА admit(spec)→Result<Witness,IntakeError> 1:1 lift order_machine.rs decide/fold: static authority (T∧P єдиний), unknown⇒rejected never coerced, fold stop-at-first + return position, 5-tier typed taxonomy stable codes; замінює ПРОЗОВІ preconditions у loops/*.yaml
- UNSAT (§2) драбина cheapest-first: Tier A структурна O(n) (type conflict, empty enum ∩, empty interval a>b, required∧forbidden, const∉range, cardinality) → Tier B arc-consistency AC-3 O(#c·|dom|³) sound+incomplete (domain wipeout⇒UNSAT) → Tier C SMT QF_LIA/QF_LRA decidable NP-complete (SAT⇒Witness model; UNSAT⇒unsat core; TIMEOUT/nonlinear⇒Undecidable→human FAIL-CLOSED never claim SAT on timeout)
- Under-determined (§3): DOF d=|free|−|binding| (pinned=singleton adm; free=|adm|≥2); ρ=pinned required/total; entropy H=Σlog|adm(f)| (H=0⟺singleton⟺determinate); "#M≥2" via AllSAT-2: model m1 → blocking clause ¬(≈m1) → resolve, SAT distinct m2⇒UNDER-DETERMINED. Syntactic distinctness = sound one-way flag (may over-flag)
- Conditioning (§4) ЧЕСНА МЕЖА: intake decidable ТІЛЬКИ структурне (knife-edge verify no hysteresis band → require [θ−h,θ+h]; near-empty sliver κ~1/(a−b)); НЕ decidable = Lipschitz α чорного ящика F (runtime-only!). Intake=detection необхідний filter, НЕ sufficient stability
- Neumann clamp (§5) = bebop saturate/io_guard: V_clamped=V(x_n)+s·min(1,δ/r) = Евклідова проєкція на δ-кулю B(V(x_n),δ) (L2; saturate=L∞); 1-Lipschitz nonexpansive → тільки ЗМЕНШУЄ conditioning ніколи не збільшує; length cap так само; Robin ‖ΔV‖≤(B_n−α·d)/β shrinks з drift, budget exhausted⇒freeze pure Dirichlet. Intake=detection, clamp=enforcement
- RED (§6): verify з impure oracle (RNG/clock/network/LLM/mutable) проходить F1/F2/структуру але terminal predicate = random variable ⇒ немає stable fixed point. Detector: (1) static purity scan denylist (як clippy disallowed-methods) + (2) dynamic idempotence probe K≥2 evals під perturbed nuisance env, різні біти⇒NonReproducibleVerify⇒FORCE human-bypass fail-closed

## R4 (персистентність signal/noise) — DONE:
- ⚠️ ВИРОК: НЕ persistent homology — це survival/duration filter над point process claim'ів. Barcode-грамматика переноситься як scoring-евристика; теорема стабільності (d_B(Dgm f,Dgm g)≤‖f−g‖_∞) НЕ переноситься і її ВИСНОВОК ХИБНИЙ: один sub-threshold парафраз → discontinuous death+rebirth (реальна PH provably stable, claim-barcode provably UNSTABLE). Причина: barcode = функція ГЕНЕРАТОРА, не input data — немає ‖f−g‖_∞ що зв'язує з істиною
- Identity matching t→t+1 = assignment problem: Hungarian max-weight bipartite O(N³) (N=max claims≈10 trivial), edges w=cosine≥τ, post-filter <τ⇒death+birth. НЕ greedy (spurious rebirth). τ*=Bayes/equal-error point 2-компонентної суміші p(sim|same)/p(sim|diff); NOISE_FLOOR=0.35 = ЛИШЕ floor (замало для identity)
- Re-entry stitching ОБОВ'ЯЗКОВИЙ: gap≤w rematch зберігає original birth; bebop attic move+restore = ТОЧНО цей примітив (non-destructive REQUIRED); canonicalize S-P-O перед embedding
- Persistence(c)=D=last_seen−birth. НУЛЬ-МОДЕЛЬ (noise survives кожен turn з ймовірністю p незалежно): geometric P(D≥d)=p^d ⇒ p-value=p^D. Сигнал iff p^D≤α ⟺ D≥D*=⌈ln α/ln p⌉=⌈log_p α⌉. Приклади: p=0.5,α=0.05⇒D*=5; p=0.3⇒D*=3; α=0.01⇒D*=7
- Multiple-testing: Bonferroni D*_Bonf=D*+⌈log_{1/p}N⌉ (N=100,p=0.5,α=0.05⇒11); або BH-FDR. POWER CEILING: D≤n−1 ⇒ certifiable лише якщо n≥D*+1 (Bonferroni n≥D*_Bonf+1, N=100⇒n≥12); нижче — ABSTAIN "insufficient iterations" НЕ "no signal"
- p estimate: geometric MLE p̂=mean_span/(mean_span+1) або 2-component EM (noise geometric + core heavy-tail). Density refinement: count~Binom(D+1,q), Fisher combine −2Σln p_i~χ²_{2m}
- Anomaly (§2.9) formalized: (b,D) plane shear of (birth,death). CORE={b<b_thr, D≥D*}; ANOMALY={b≥b_thr, D*≤D≤n−1−b} (ТРИКУТНИК не rectangle: b_thr≤n−1−D* feasibility); b_thr=⌈β·n⌉ β∈(0.5,0.8]. Emergence vs entrenched hallucination — geometry НЕ розрізняє → verify/human ніколи auto-accept
- Memory decay = filter: bebop mod-7 hash eviction WRONG (MI(kept,persistence)=0, zero filtering). Correct: keep iff persistence_score>θ=D* else attic (non-destructive). Soft: s_{t+1}=s_t·exp(−Δt/τ)+boost·seen; half-life t_½=τ·ln2; steady s*≈boost·τ; min-salience PQ eviction. τ≈n/ln(s_max/s_min). ТРИ наявні bebop примітиви (salience field, attic move, restore) → correct filter одним рядком decay
- RED (§6): early-born false claim h, autoregressive loop re-emits кожен turn ⇒ D=n−1 MAX, p-value=p^{n−1} НАЙЗНАЧНІШИЙ ⇒ filter labels CORE + pins max salience РЕЙНФОРСИТЬ помилку. Persistence вимірює generator fixed points НЕ world; вісь generator-stable/transient ОРТОГОНАЛЬНА true/false
- КОМПОЗИЦІЯ AND-gate: Accept(c)=P(c)∧V(c); P rejects generator NOISE (cheap no oracle); V external verifier (tests/schema/typecheck/fact/cross-model) rejects FALSE. Небезпечна множина {stable}∩{false}=entrenched hallucination — тільки V ловить. §2.9 "anomaly→human" ПРАВИЛЬНЕ але under-scoped (ловить лише late; early-persistent hallucination найгірша сходить з max score). V мусить домінувати ВСІ persistent. Persistence=advisory prefilter (ранжує що verify), verify=authority. Bind enrich.rs confidence gate + agentic_git verify_integrity (tamper-evidence на record)

## R5 (ентропійний бюджет) — DONE:
- ⚠️ Аксіома 1 (conservation of sense) МАТЕМАТИЧНО ХИБНА → замінити Data Processing Inequality: Markov X→Y→Z ⇒ I(X;Z)≤I(X;Y); post-processing НЕ створює інформації. I(R;x_t) СУПЕРМАРТИНГАЛ під internal turns (R→x_0→...→x_t Markov): монотонно спадає; росте ЛИШЕ від зовнішнього O_t⟂̸R (retrieval/tool/verify/human). = Huang et al. ICLR 2024 (intrinsic self-correction не додає I(R;·))
- Ідентичність: I(R;x_t)=I(R;x_0)−L_t+G_t (L_t≥0 processing loss DPI, G_t≥0 grounding injection). "Conservation"→"monotone leakage refilled only by grounding" (dissipative open system)
- Ентропія (яка саме): (a) Shannon order-0 H_0=−Σp log2 p (knowledge.rs) ДИСКВАЛІФІКОВАНА (maximized by noise); (b) perplexity/cross-entropy L_q=−Σlog2 q (needs logprobs); (c) compression length C(x)=|gzip|·8 bits ≥K−O(1) КОРЕКТНА ledger unit (deterministic integer); (d) semantic entropy SE=−Σp(C_j)log p(C_j) кластери за bidirectional entailment (Farquhar/Gal Nature 2024)
- ДВА види "heat": verbosity/redundancy heat = ΔC (compression-length delta) → drained by RENORMALIZER; confabulation/meaning heat = SE → drained ТІЛЬКИ external grounding (ніколи internal reflection)
- Ренормалізатор R = rate-distortion @ D=0 на claim-manifold: distortion d(x,x̂)=0 iff claims(x̂)=claims(x); R(x)=argmin L(x̂) s.t. claims(x̂)=claims(x); floor R_claim(0)=H(claims). Алгоритм: C0=claims_extract(x) [SEPARATE non-generative verifier], x'=LLM_rewrite, C1=claims(x'); RED: C1⊉C0 (dropped)⇒rollback, C1⊋C0 (new/hallucination)⇒rollback, L(x')≥L(x)⇒no-op; idempotent R(R(x))=R(x) як ledger replay
- Ледер = БЮДЖЕТ не Σ=0 (entropy НЕ conserved: source+external sink). Bind money.rs integer bits: H_max∈ℤ⁺; ΔH_in=max(0,L(x_raw)−L(x_{t-1})); ΔH_out=max(0,L(x_raw)−L(R(x_raw))); D_t=clamp₀(D_{t-1}+ΔH_in−ΔH_out), D_0=0; ALARM D_t>H_max⇒HeatOverflow force hard-renorm/reground/halt. Bind ledger.rs machinery (SHA id, idempotent replay, reject) АЛЕ invariant 0≤D_t≤H_max НЕ Σ=0
- Стаціонарність (§Q5): D_t random walk, drift μ_t=E[ΔH_in]−E[ΔH_out]. Strict negative μ≤−δ<0 ⇒ nonneg supermartingale CONVERGES a.s. (Foster-Lyapunov ergodic, E[D_∞]≤c²/2δ Lindley); positive μ≥δ⇒D_t→∞ linearly overflow a.s.; ZERO μ=0 null-recurrent Var∝t STILL overflows a.s. (потрібна СТРОГА негативність). ⟨ΔH_out⟩>⟨ΔH_in^gen+ΔH_in^crit⟩
- RED (§Q6): H(out)≤H(in) ALONE gamed by R(x)="" (max reduction I=0). DPI: I(claims;x̂)≤I(claims;x) ceiling; pin floor I(claims;x̂)≥I(claims;x) ⇒ pincer to EQUALITY. R_cheat drops persistent claim (price line/allergen/negation polarity flip): L↓ але claims⊉ ⇒ guardrail MUST reject. Gate = H↓ ∧ claim-set-equality НІКОЛИ H↓ alone. SE↓ теж gameable (collapse distinct clusters)

## R2 (спектральні моди дрейфу) — DONE:
- ⚠️ PCA-на-Δ МАТЕМАТИЧНО ХИБНИЙ proxy → DMD (data-driven Koopman operator approx). Ansatz x_{k+1}≈Ax_k, A=finite-rank Koopman compression
- Exact DMD (Tu/Rowley): X=[x_0..x_{m-1}], X'=[x_1..x_m]; X=UΣV* (thin SVD rank r); Ã=U*X'VΣ⁻¹ (=U*AU, НІКОЛИ не форм A∈ℝ^{n×n}); ÃW=WΛ; μ_i=DMD eigenvalues; modes φ_i=X'VΣ⁻¹w_i (exact) або Uw_i (projected); λ_i=ln(μ_i)/Δt=σ_i+iω_i (σ=growth ln|μ|, ω=freq arg(μ)); recon x_k≈ΦΛᵏb, b=Φ⁺x_0
- Classify: |μ|<1 damped (ignore), |μ|≈1 resonant (monitor), |μ|>1 UNSTABLE (intervene)
- PCA-на-Δ ТОЧНИЙ gap: Δ_k=(A−I)x_k ⇒ C_Δ=(A−I)C_x(A−I)ᵀ; ρ_i(PCA)=|μ_i−1|²·s². ЗГОРТАЄ двобічну вісь на |μ−1|: (1) μ=1.5 unstable і μ=0.5 damped дають ОДНАКОВЕ |μ−1|=0.5 — не розрізнити ЯКИЙ бік; (2) СЛІПА до μ=1 accumulation (Δ-variance=0, DC-drift/verbosity mode invisible); (3) C_Δ symmetric PSD → real eigenvalues, немає phase, сліпа до rotation. Coincides з DMD ЛИШЕ якщо A symmetric+всі μ real+same side of 1+isotropic
- КОД caveat: field::jacobi_eigen/kalman::real_eig SYMMETRIC only. Path A (real modes verbosity/decay): symmetrize Ã_s=(Ã+Ãᵀ)/2 → jacobi_eigen, ν_max>1⇒growth (reuse as-is, blind rotation). Path B (rotation): r=2 closed form μ=τ/2±√((τ/2)²−δ), |μ|=√det(Ã) unstable⇔det>1; r>2 Francis QR double-shift на tiny Ã (ОДНА чесна extension)
- Project/damp mode: c_i=algebra::project(d,Φ); d_corrected=d−c_iφ_i; inject −φ_i correction. Trip-wire |μ|_max≥1+ε⇒escalate
- Online DMD (Zhang-Rowley 2019): POD coords, rank-1 RLS Sherman-Morrison: γ=1/(1+x̃ᵀPx̃), Ã_{k+1}=Ã_k+γ(ỹ−Ãx̃)x̃ᵀP, P_{k+1}=P−γ(Px̃)(Px̃)ᵀ; exponential forgetting P/ρ; O(r²)/turn FLAT in loop length — realizable INSIDE loop; bind kalman::matmul/invert
- Verbosity = 1-D DMD: L_{k+1}=aL_k+b, a=Cov/Var, μ=a; a<1 settles L*=b/(1−a), a=1 ramp L_k=L_0+kb, a>1 runaway aᵏ. Ln=len ratio детектор FALSE-ALARMS коли b>0 (settling ratio>1); FIX regress affine separate a from b, test a not ratio; augment state [x;1] affine/control DMD captures μ=1 mode
- FFT vs DMD: FFT = stationary shift-invariant, poles ON unit circle |μ|=1 (limit cycle tone ping-pong); circulant_eigenvalues=FFT(row) ⇒ Fourier modes=eigenvectors of shift-invariant. DMD = non-stationary growth+freq μ off unit circle. Decision: mathx::classify_trajectory LimitCycle→FFT (cheap O(N log N)), Divergent/drift→DMD; FFT valid ⇔ ВСІ |μ_i|=1; half-split spectra stationarity test коли Undetermined. DMD strictly dominates (дає ω І σ)
- Conditioning (§7): truncate Σ Gavish-Donoho τ=2.858·σ_median (unknown noise) / 4/√3·√n·σ (known); noise bias A=X'X⁺ toward INTERIOR (hides unstable, |μ| pulled <1) → forward-backward μ_fb=√(μ_f/μ_b) або tlsDMD (SVD stacked [X;X'])
- RED: slow outward spiral A=ρR(θ) ρ=1.02 θ=0.05: DMD |μ|=√det=1.02>1 UNSTABLE catch; PCA-на-Δ ‖A−I‖≈0.054, harmless damped transient μ=0.3 має |μ−1|=0.7 ~13× більше ⇒ PCA ranks spiral LAST discards as noise MISS. Assert DMD_flag≠∅ AND DMD_flag⊄PCA_flag

## R6 (ортогональність + PID) — DONE:
- A1 Ортогональність (Коші-Ріман discharged): u(x)=cos(x,r) meaning coord, v(x) style. Generator flows ⟂∇u (g∈ker(Du)); orthogonal ⟺ ⟨g,∇q⟩=0 ⟺ ∇q∥∇u ⟺ ∂q/∂v=0 (critic invariant along generator DOF). Degeneration = ∇q набуває v-компоненти
- Естиматор: e_⊥=e_style−proj_∇u (Gram-Schmidt, algebra::project); D_style q≈[q(x+εe_⊥)−q(x−εe_⊥)]/2ε; ρ_gc=⟨g,∇q⟩/(‖g‖‖∇q‖) ENFORCE |ρ_gc|≤τ_orth≈0.1. Binary q: PARAPHRASE-INVARIANCE test — N meaning-preserving paraphrases, flip_rate=(1/N)Σ1[q(x'_i)≠q(x)] orthogonal⟺≈0 (meaning const ⇒ будь-яка variance = ∂q/∂v≠0). resonator reflect бачить output+reference НЕ reasoning (structurally good але НЕ гарантує ∂q/∂v=0)
- A2 corr(q,Δs) = Goodhart detector: q_t=a·s_t+b·style_t+ε; healthy a>0,b=0 corr>0; degenerate b≠0 corr→0; reward-hack corr<0. Diff series r_Δ=corr(Δq_t,Δs_t) sliding W; Spearman/point-biserial r_pb для binary; Fisher-z: r_lower=tanh(artanh(r̂)−z/√(W−3)) ALARM r_lower<r_min≈0.3-0.5; CUSUM C_t=max(0,C_{t-1}+(r_min−r_t))>h. = Gao-Hilton-Schulman reward-overopt R_gold(d)=d(α−βd), peak d*=α/2β; d≈total_drift. ⚠️ s_t МУСИТЬ бути held-out/decorrelated (не той метрик що loop оптимізує)
- A3 Крос-модель НЕОБХІДНИЙ: Q_g=Q*+η_shared+η_g, Q_c=Q*+η_shared+η_c; REWARD-HACK HEADROOM=Cov(Q_g,Q_c)−Var(Q*)=Var(η_shared). hacking-gain≤Var(η_shared); =0 ⟺ Cov(η_g,η_c)=0 ⟸ independent models. Self-preference bias B=P(Y'=1|S=1,Y=1)−P(Y'=1|S=0,Y=1); GPT-4 B≈0.520 (Wataoka arXiv:2410.21819); механізм: judge scores lower-perplexity/self-like higher ∝−log p_judge = max η_shared. 3 механізми: (1) different model η_shared→0 (eval-layer openrouter_judge = correct surface); (2) disjoint context (resonator reflect не бачить reasoning correct); (3) rubric в reference-terms pins ∇q∥∇s
- B4 PID stability: C(z)=Kp+Ki·z/(z−1)+Kd·(z−1)/z; integrator plant G=Kg/(z−1); char z³+a₂z²+a₁z+a₀, a₂=−2+Kg(Kp+Ki+Kd), a₁=1−Kg(Kp+2Kd), a₀=Kg·Kd. JURY: (1)Ki>0✔; (2)Kg(2Kp+Ki+4Kd)<4 → 9.02≥4 VIOLATED; (3)|a₀|=Kg·Kd<1 → 1.5>1 VIOLATED; (4)inner det. ⚠️ Kd=1.5 РОБИТЬ GOVERNOR НЕСТАБІЛЬНИМ на integrator plant (2 умови fail). Static plant margin ЛИШЕ 0.053 (весь з'їдений Kd)
- Binary derivative kick: e_t∈{−0.9,+0.1}, flip Δe=±1.0, D-term=Kd·Δe=±1.5 > весь authority budget [0,1] ⇒ bang-bang не linear; один reject Kp·(−0.9)=−1.26 slams to 0. FIX: filtered derivative D(z)=(1−γ)Kd(z−1)/(z−γ) pole z=γ; γ≥0.765 caps kick≤0.2; γ≈0.8; act on approval-rate p_t∈[0,1] НЕ raw {0,1} verdicts (D well-posed тільки на p_t)
- B5 critical damping ζ=1: (1+KgKd)s²+KgKp·s+KgKi=0; ω_n²=KgKi/(1+KgKd), ζ=(Kp/2)√(Kg/(Ki(1+KgKd))). Governor ζ=0.7·√(1/0.55)=0.944 (6% UNDER-damped). Kp_crit=2√(Ki(1+KgKd)/Kg)=1.483 або Ki_crit=0.196. Q=1/(2ζ); ζ<1 Q>0.5 oscillatory; ζ>1 Q<0.5 over-damped converges-to-banality (dominant pole→0); ζ=1 fastest non-overshoot
- B6 gain scheduling = annealing: q_p=Σ_{j∈M(p)}w_jφ_j, M₁hard⊂M₂+soft⊂M₃+style; Kp(1)>Kp(2)>Kp(3). ∇q_p в span dim|M(p)| → β(p)∝1/|M(p)| inverse-temp = simulated annealing / homotopy hard→soft→style; E(p)=Kp(p)|M(p)| bounded ⇒ style ніколи не out-competes hard (scheduling-level orthogonality enforcement)
- B7 RED: G* x_{t+1}=x_t+α·e_v−γ·e_u (climbs style rewarded, drifts off ref); C* same model B≈0.52 ⇒ q↑ monotone while s=cos(x,r)↓ ⇒ corr(q,Δs)<0. Detector 3 gates: (1) r_Δ<0 internal; (2) external-verify V decorrelated dominance (accept iff q↑ AND V non-decreasing, else rollbackToBest) — self-critic CANNOT detect own capture; (3) paraphrase-invariance floor ρ_gc>τ_orth ⇒ freeze (lyapunov_guard). Runnable: RED q_t=t,s_t=−t⇒r_Δ=−1 fire; GREEN q_t=s_t+noise⇒r_Δ>0.9 pass
