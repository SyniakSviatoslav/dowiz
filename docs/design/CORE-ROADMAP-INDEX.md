# CORE-ROADMAP INDEX — the one navigation root for all dowiz/bebop2 planning (2026-07-17)

> **Read [`ROADMAP.md`](ROADMAP.md) first** — the chronological master roadmap and canonical
> "what's the status right now" entry point (2026-07-20). This file is the detail layer beneath
> it: the full P-number/blueprint cross-reference table. `ROADMAP.md` tells you WHEN and WHAT
> happened; this file tells you WHERE the detail lives for any given item.
>
> **This is the WHERE. The WHAT/WHEN is now `ROADMAP.md` Part II** (2026-07-20: the former
> `MASTER-ROADMAP-SOVEREIGN-ARCHITECTURE-2026-07-16.md` was fully inlined there and deleted as a
> standalone file — canonical roadmap, execution phases P01–P56, its own §9/§10 still point back
> here — **2026-07-18 update: extended P31–P46 → P31–P56 across §11–§15**, see §10 there for the
> phase index).
> Every planning
> document in the corpus is reachable from this file in ≤2 hops; a plan not listed here does not
> exist for navigation purposes (add a row when adding a doc). Built by the Layer-I consolidation
> pass ([`CORE-ROADMAP-2026-07-17/BLUEPRINT-P-I-consolidation.md`](CORE-ROADMAP-2026-07-17/BLUEPRINT-P-I-consolidation.md));
> honesty rule inherited from it: rows marked **OPEN** or **MISSING** are never turned into links
> to files that don't exist.
>
> **Numbering, once and canonically:** `P01–P30` is the sole execution numbering (P01–P19 =
> numbered blueprint files in `sovereign-roadmap-2026-07-16/`; **P20–P30 exist only as standalone
> blueprint files indexed from SOVEREIGN §8.1–§8.12** — auditing "the P0x files" alone misses a
> third of the roadmap). **2026-07-18 addendum:** SOVEREIGN §10 extends the execution numbering
> with `P31–P46` (ecosystem-component consolidation phases; §10.2 there is the phase index) —
> `P01–P30` semantics unchanged. `Layer A..I` is an orthogonal **altitude axis** over clusters of those
> phases — formerly spelled "P-A..P-I", renamed to kill the P-D/P04 lexical collision (ruling:
> P-I audit §4). On-disk `BLUEPRINT-P-X-*.md` filenames keep their provenance names.
>
> **2026-07-18 addendum (later same day, consistency pass):** SOVEREIGN §11–§14 extend the
> numbering further to **P47–P53** (payment rails, owner hub, customer identity, compliance
> gate, open map/routing, courier surface, Tor/onion) — every "P01–P46"/"P31–P46" range in this
> file predates that extension; §10.2 there is now the full P31–P53 index. Same day, the wave
> swarm landed first real code for P40/P41/P42/P47/P49 (+G3 render shell), so §0's AGENT
> "executor loop … is 0%" line is historical — see SOVEREIGN §10.2's corrected status cells.

---

## 0. The Ecosystem-Component axis (2026-07-18)

Co-equal in importance to the Layer A–I altitude axis (§1) and the P01–P46 numeric axis — a
THIRD lens for navigation, not a replacement for either. Ask "which ecosystem part" first (this
table), then "which phase" (§2 below or MASTER-ROADMAP §10.2), then "which file" (per-phase
Blueprint links) — three lenses, same underlying phases. Full component sections with per-phase
DoD/anti-scope: [SOVEREIGN §10.5](ROADMAP.md). Row
order IS the critical path.

| Component | Mission | Phases | One-line status (live-verified 2026-07-18) |
|---|---|---|---|
| **CORE** | decide/fold Law, event-log, money, capability primitives, spectral math, self-tuning control loops | P31–P33 | ~90% done, not the bottleneck; dominant failure mode is built-but-unwired code |
| **PROTOCOL** | mesh, capability issuance, crypto, transport, delivery-domain (bebop2) | P34–P36 | ~70% built and PROVEN but 100% stranded from dowiz's own kernel — the single biggest lever in the whole roadmap (P34 = #1 next action) |
| **DELIVERY** | the dowiz product surface: UI, order/courier/payment flow, auth, demo/marketing, app-shell | P37–P39 | ~0% deployable (no HTTP server, no rendered UI, no live deployment) but math/domain logic mostly done in CORE+PROTOCOL — wiring-heavy, not from-scratch |
| **AGENT** | local/network AI, tool-use loop, MCP; three operating modes (no-AI / local-offline / connected) | P40–P42 | substrate (LlmBackend/Ollama) shipped; executor loop connecting it to anything is 0% — a chat backend today, not an agent |
| **ECOSYSTEM/OPS** | external integrations, deployment, monitoring, multi-product platform | P43–P46 | explicitly and deliberately LAST — near-zero built, correctly so, since nothing exists yet to integrate/deploy/monitor |

## 1. Crosswalk — Layer A..I ↔ P01–P30 (the anti-double-numbering artifact)

A Layer *rolls up* numeric phases + standalone blueprints + research batches; it never replaces
them. Layer I maps to zero numeric phases; several phases appear under two layers (a lens, not a
partition).

| Layer | Altitude scope | Rolls up (numeric phases · standalone blueprints · batches/arcs) |
|---|---|---|
| **A** | Core kernel primitives — equations-not-primitives, tensor/sparse/branchless memory | P04 · P11 · P28 · eqc-rs wiring (`geo.rs`/`domain.rs`) · [BLUEPRINT-EIGENVECTOR-REFACTOR-PLAN-2026-07-17.md](BLUEPRINT-EIGENVECTOR-REFACTOR-PLAN-2026-07-17.md) · [BLUEPRINT-CACHE-REFERENCE-GRAPH-TENSOR-ARENA-2026-07-17.md](BLUEPRINT-CACHE-REFERENCE-GRAPH-TENSOR-ARENA-2026-07-17.md) · masterwork Batches 1+8 · P30 lanes W1-L1/L5/L10 |
| **B** | State/consistency + living memory — event log, content-hashing, snapshots/epochs | P07 (exactly-once port — LIVE money-red-line bug) · P12 · P28 snapshot seam · P30 W1-L2/L11 · masterwork Batch 2 + doc 19 |
| **C** | Safety / self-healing / self-terminating — breakers, invariants, authority boundary | P10 (adjacency) · P27 · masterwork Batch 3 + doc 19 Part 2 · hydra hysteresis + restart-intensity (T-6/W3-L4) |
| **D** | Consensus / trust / capability — Sybil-resistance, DecisionUnit gossip, capability issuance | P03 · P06 · P10 · P14 · P29 §2 (Decision Compiler) · masterwork Batches 4/6/7 · R-3 `RootDelegationPolicy` docket |
| **E** | Network / hardware / crypto-in-core | P03 · P09 · masterwork Batch 5 v2 (target-corrected) · W2-L5 AVX2 parallel-independent-verify (batch-accept REJECTED on a real SSR-2020 forgery) |
| **F** | Local AI / MoE mesh | P21 · P15 (E13-cpu) · P29 · masterwork doc 21 (distributed-inference rejection) · `harness-2026-07-16/HARNESS-LLM-BACKEND.md` |
| **G** | Product/UI on kernel — wasm bridge, physics-UI, greenfield `web/` beachhead | P16 · P17 · P20 · [living-interface-2026-07-16/LIVING-INTERFACE-ROADMAP.md](living-interface-2026-07-16/LIVING-INTERFACE-ROADMAP.md) · masterwork Batch 9 |
| **H** | Ops / telemetry / benchmarks / regression | P01 (`ci.yml:23` fix) · P08 · P24 · P25 (· P26 soft) · chaos harness · `docs/regressions/REGRESSION-LEDGER.md` |
| **I** | Cross-repo consolidation (meta — this index, banners, fold-ins) | **no numeric phase** — proof the letters are a lens, not a renumbering |

**Former cross-cutting blocker, now CLOSED (corrected 2026-07-19):** P06 `key_V` (`HybridSigner`)
closed `58987d79d` (2026-07-18), survived the later merge wave (`5a97e1f6f`), and now has real code
consumers (`kernel/src/decision/import.rs`, `tools/ci-truth/{main,v1}.rs`). It no longer gates
Layer C's independent-verification leg, Layer G's product-safety story, E3-Phase-B (spectral arc),
or P30's signed DecisionUnit import — **but "unblocked" ≠ "built": E3-Phase-B's own work is
confirmed still fully unstarted** (see `ROADMAP-RECHECK-SESSION-SYNTHESIS-2026-07-19.md` §3/§4);
Layer C/G were not re-verified in that pass. Re-check each consumer individually before assuming
any of them shipped just because the blocker is gone. **Correction (P-D audit §3, 2026-07-17):**
the earlier "P06 gates Layer D's capability issuance" edge is **withdrawn** — P06 is a dev-time merge
fence over code diffs; `RootDelegationPolicy` is runtime courier onboarding. They share substrate
(`load_genesis`/`verify_chain`) and the (bebop-side, now-CLOSED per the same recheck §6) **C4b**
signing-path hardening, but neither functionally blocks the other; Layer D ships P06-independent.

## 2. Layer blueprints (Wave 2 + Wave 3, `CORE-ROADMAP-2026-07-17/`)

| Layer | Blueprint | One line |
|---|---|---|
| A | [BLUEPRINT-P-A-kernel-primitives.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P-A-kernel-primitives.md) | eqc-rs equation wiring + eig2x2 dedup + normalize-before-hash groundwork; IS masterwork Wave 1 formalized to the 20-point contract |
| B | [BLUEPRINT-P-B-state-consistency.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P-B-state-consistency.md) | The three correctness closures of tile→normalize→hash→snapshot: exactly-once `commit_after_decide` port, hash-canonicalization as a type invariant, drift-gated snapshot admission |
| C | [BLUEPRINT-P-C-safety-self-healing.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P-C-safety-self-healing.md) | `integrity_check` hysteresis band (LANDED `a50d44ab0`) + restart-intensity as a launch-path predicate; finite-anchored-authority doctrine. **+2026-07-18 fold-in (§0 context, §13):** verification CRITICALs (budget-NaN degrade-open, `noether` fail-open, drift-gate unauthenticated-`intervention`) + round-2 CSC-LAW containment/eviction-breaker — all instances of one law: zero-authority arithmetic is conditional on input totality |
| D | [BLUEPRINT-P-D-consensus-capability.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P-D-consensus-capability.md) | **WRITTEN (reconstructed 2026-07-17; index row corrected 2026-07-18 — was "OPEN").** Budgeted anchor-rooted issuance: `OperatorSigned` + monotonic per-anchor `IssuanceBudget` (Option A, buildable now, **P06-independent**), `FirstContactQr`+hw-attestation as an operator-gated overlay (B), `WebOfTrust` deferred on Cheng–Friedman (C). **+2026-07-18 fold-in (§12; was "§11" on its source branch):** the closed gap is now a LIVE red-team HIGH (agentic-mesh A5 unbounded Sybil / B-3 secret-leaking `RefSigner`); A7/B-6 admission red-line-scope-check gap named as a sibling Layer-D item. **R-3 status (merge reconciliation):** blueprint §11 records the R-3 ruling — Option A adopted 2026-07-18 (operator-overridable), mechanism landed `e08eb07` in bebop-repo |
| E | [BLUEPRINT-P-E-network-crypto-core.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P-E-network-crypto-core.md) | AVX2 SIMD crypto-verify lane (parallel-independent-verify, NEVER batch-accept); crypto is 8–20× the whole packet stack. **+2026-07-18 fold-in (§0 context, §14; was "§13" on its source branch):** round-2 FEC (`reed-solomon-simd`, L1 QUIC-datagram/L2 BPv7-shard, below-crypto-verify) + the 32-byte `LaneFrameHeader` wire format (no Confidence/CRC, recompute-as-authority) land in Layer E |
| F | [BLUEPRINT-P-F-local-ai-mesh.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P-F-local-ai-mesh.md) | Domain experts as DecisionUnit FAMILIES (DomainTag × compiled units × hub-only build-time oracle — operator-confirmed), NOT per-node models; three gossip extensions to P29 §2 (epoch max-merge, key_V-shaped import replay, lineage-in-one-log); checkpoint-STARK deferred with triggers. **+2026-07-18 status note (§13):** the Mistral/local-LLM audit re-confirms every verdict against the live daemon (Mixtral rejected, dense+Ollama stack green, 12+3 tests pass) — zero design change |
| G | [BLUEPRINT-P-G-product-ui.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P-G-product-ui.md) | Greenfield `web/` build-out (NOT a migration): wire `FieldSim`, bind the remaining 21 kernel exports, first real DOM surface; money-flip explicitly gated out. **⚠ SUPERSEDED as product-UI track (MASTER-ROADMAP §16.30/§18.4, 2026-07-18): wgpu-canvas-only, zero DOM — the DOM `web/` path is legacy/reference; its money-recompute findings below remain valid.** **+2026-07-18 fold-in (§0 context, §13):** the E1 forged-order-total CRITICAL (STILL LIVE) makes §8's server-recompute an owned Layer-G money-recompute DoD; V3 1.3–1.6 money-arith cluster surfaced as Layer-A fixes with §3 validators as boundary containment |
| H | [BLUEPRINT-P-H-ops-telemetry.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P-H-ops-telemetry.md) | Chaos/fault-injection harness + the one real CI bug (`ci.yml:23`) + benchmark CI gate + ledger migration (all LANDED `f4802927e`/`a952af354`); P24/P25 folded in by reference. **+2026-07-18 fold-in (§9):** GitHub-hygiene/versioning docket (CalVer `2026.07.0` + independent in-code `KERNEL_PROTO`/`MESH_WIRE` version, branch cleanup, PAT scope — operator-gated) + GROUND-TRUTH is 41 commits / test-count stale |
| I | [BLUEPRINT-P-I-consolidation.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P-I-consolidation.md) | This consolidation, formalized + executed: banners, fold-ins L1–L6, this index, the Layer ruling. **+2026-07-18 correction (§1):** its §1 G5 "Layer D/F not written" is stale — both blueprints now exist on disk |

## 3. Wave-1 audits (Opus, ground-truth passes)

| Audit | Status |
|---|---|
| [P-I-audit-cross-repo-consolidation.md](CORE-ROADMAP-2026-07-17/P-I-audit-cross-repo-consolidation.md) | ON DISK — the authority behind §§5–8 of this index |
| [P-H-audit-telemetry-regression-benchmarks.md](CORE-ROADMAP-2026-07-17/P-H-audit-telemetry-regression-benchmarks.md) | **RECONSTRUCTED ON DISK (2026-07-17)** — full re-verification fresh on `main @ caba2203c`; supersedes the stale line numbers embedded in `BLUEPRINT-P-H`/`BLUEPRINT-P-A` (delta table in its §6). Confirms the `ci.yml:23` bug and the `FaultyStore` precedent both still hold |
| [P-D-audit-root-delegation-policy.md](CORE-ROADMAP-2026-07-17/P-D-audit-root-delegation-policy.md) | **ON DISK — reconstructed 2026-07-17** (was lost pre-commit; rebuilt by harvesting `BLUEPRINT-P-E` §1's surviving quotes + re-verifying every cite fresh — all still match). Corrects the "P06 gates Layer D" edge (§3): the capability-issuance leg is **P06-independent** |
| [P-G-audit-product-ui-post-decommission.md](CORE-ROADMAP-2026-07-17/P-G-audit-product-ui-post-decommission.md) | **RECONSTRUCTED ON DISK (2026-07-18 correction — was "MISSING")** — the full audit is restored, re-verified fresh on `main @ caba2203c`; its scope statement and G1–G3 gap table also survive adopted verbatim in `BLUEPRINT-P-G` §0–§1 |

## 4. The quality contract

[`CORE-ROADMAP-STANDARD-2026-07-17.md`](ROADMAP.md) — the operator's
standing 20-point quality bar for ALL future planning ("not a guidance — a quality constant, zero
divergencies"). Every blueprint in §2 above carries a compliance map against it. Its §0 inventory
and §3 header carry the 2026-07-17 Layer-I corrections (6th master doc; Layer naming; P01–P30).

## 5. The mesh-masterwork corpus

[`bebop2-mesh-tensor-hermetic-2026-07-17/INDEX.md`](bebop2-mesh-tensor-hermetic-2026-07-17/INDEX.md)
— navigates the 9-batch tensor/state/safety/consensus/network/equations/product audit, the V1/V2
syntheses (V2 governs), and the correction docs. Roadmap entry point: Phase 30 (SOVEREIGN §8.12).
Not duplicated here — that INDEX is authoritative for its own directory.

## 6. Fold-in ledger (would-be-lost items, all six dispositioned — P-I audit §3)

| ID | Item | Where it lives now |
|---|---|---|
| L1 | Update-blob code-signing (ML-DSA vs pinned root; `kernel/src/pq/codesign.rs` live) | [BLUEPRINT-P10 § Addendum L1](sovereign-roadmap-2026-07-16/BLUEPRINT-P10-hub-runtime-kill-switch-boot.md) |
| L2 | Transport bake-off rationale (Zenoh/Reticulum/TCPCLv4/BIBE; libp2p rejected) | [`../transport-research-2026-07-12.md`](../transport-research-2026-07-12.md) (restored from blob `94e257fe9`), cross-linked from [BLUEPRINT-P09](sovereign-roadmap-2026-07-16/BLUEPRINT-P09-confidential-self-healing-wire.md) |
| L3 | Courier out-of-app notification/wake path | [BLUEPRINT-P13 § Addendum L3](sovereign-roadmap-2026-07-16/BLUEPRINT-P13-delivery-on-protocol.md) — delivery semantics dissolved-by-mesh; device-wake kept as a P13 sub-unit |
| L4 | Anonymous `.onion`/Tor tier | **ACTIVATED 2026-07-18** — direct operator request supersedes the old trigger ("vendor-node tier ships AND a venue requires anonymity"; the demand leg is the request itself, the vendor-tier leg is honored by the phase's wave split). Now phase **P53**, SOVEREIGN §14; blueprint [BLUEPRINT-P53-tor-onion-integration.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P53-tor-onion-integration.md) — C-tor sidecar hosting (arti-hosting deferred on the Tor Project's own maturity warnings), Onion-Location + QR convenience layer, mesh-transport-over-Tor deferred with trigger |
| L5 | "Lost reports" honesty ledger (13 + ~20 reports) | One line, SOVEREIGN §9.2 — closed-as-lost, decisions survive in `UNIFIED-DELIVERY-PROTOCOL-BLUEPRINT-v3`; never resurrected |
| L6 | Self-development research queue (causal/do-operator · category-theory functorial map · info-geometry · integer laws) | **Cross-track, NOT in P01–P30:** MEMORY → `physics-math-exploration.md` — the operator's always-running growth axis, parallel to the product roadmap |

## 7. Cross-cutting arcs (own consolidated docs, not numeric phases)

| Arc | Doc | Note |
|---|---|---|
| Agentic mesh protocol (B1–B4) | [agentic-mesh-protocol-2026-07-17/AGENTIC-MESH-PROTOCOL-CONSOLIDATED.md](agentic-mesh-protocol-2026-07-17/AGENTIC-MESH-PROTOCOL-CONSOLIDATED.md) | Authored in worktree `/root/dowiz-agentic-mesh`, merged in (`cabc01f6a`); Wave 0 landed `f30189262`; canon-diffs CD-1..8 operator-merge pending |
| Spectral energy-flow evolution (E1–E3) | [spectral-energy-flow-evolution-2026-07-16/SPECTRAL-EVOLUTION-CONSOLIDATED.md](spectral-energy-flow-evolution-2026-07-16/SPECTRAL-EVOLUTION-CONSOLIDATED.md) | Authored in worktree `/root/dowiz-spectral-evolution`, merged in (`230cc6998`); E1+E2 landed `6bd181a02`; E3-Phase-B gated on P06 `key_V` |
| Living Interface (feeds Layer G / P16) | [living-interface-2026-07-16/LIVING-INTERFACE-ROADMAP.md](living-interface-2026-07-16/LIVING-INTERFACE-ROADMAP.md) | In-repo and easy to overlook — listed here so no reader needs prior knowledge of the directory |
| Native pgrust tenant-schema rebuild (red-line, RLS) | [BLUEPRINT-P-NATIVE-PGRUST-TENANT-REBUILD.md](BLUEPRINT-P-NATIVE-PGRUST-TENANT-REBUILD.md) | Added 2026-07-18 (`f9b2eb9bb`), not yet /council-reviewed. **Deliberately NOT a Layer A-I item** — RLS/migrations/auth are explicitly out of kernel-autopilot scope (per P30's own note, MASTER-ROADMAP §8.12). Proposal-only, 0 code; the reactivation gate for `attic/` tenant-table revival once a server tier exists. Own DoD/anti-scope already in the doc's §0/§4 — no separate blueprint needed |
| Fail-operational / layout-versioning (R1 + round-2: FEC/CSC-LAW/CWR/LaneFrameHeader/DeltaPatch) | [fail-operational-layout-versioning-2026-07-17/round-2/BLUEPRINT-ROUND-2-MASTER-SYNTHESIS.md](fail-operational-layout-versioning-2026-07-17/round-2/BLUEPRINT-ROUND-2-MASTER-SYNTHESIS.md) | Self-mapped to Layers B/C/D/E (its own §6); folded into those blueprints' 2026-07-18 sections. **RC-2-broad residual pinned open** (B-T4/E-T4); NaN fix owed to Layer B (URGENT — defeats the shipped drift-gate) |
| Session verification / red-team + roadmap refresh (2026-07-18) | `ROADMAP-UPDATE-SESSION-SYNTHESIS-2026-07-18.md` — **deleted 2026-07-20, merged into `ROADMAP.md` §7** | The patch doc consolidating this session's per-repo verification CRITICALs, GitHub/local-LLM audits, and fail-operational R1+R2 into the Layer A–I structure; source of every "+2026-07-18 fold-in" in §2 above |
| hermes-agent-kernel-rewrite (dev-tooling, separate repo) | MEMORY `hk05-hk09-routing-status-2026-07-16.md` | Tracked **out-of-band by design** — Layer A–I is the dowiz/bebop2 kernel-product axis; hermes is in scope only "where cited" (STANDARD §1). No Layer home is warranted (synthesis §1.6). Open: H-0 GitHub-home decision (16 unpushed commits disk-only); T1 group-chatter prompt-injection MED-HIGH |
| Swarm-safety / deterministic circuit-breaker architecture (self-development research, 2026-07-19) | [SWARM-SAFETY-DETERMINISTIC-CIRCUIT-BREAKER-SYNTHESIS-2026-07-19.md](SWARM-SAFETY-DETERMINISTIC-CIRCUIT-BREAKER-SYNTHESIS-2026-07-19.md) (+ Synthesis II, master roadmap, 2 blueprints, 2 raw-prompt captures, all same-date filenames) | **Cross-track, NOT in P01–P96 — same L6 pattern as the self-development research queue** (§6 above): AI-agent-safety architecture research (deterministic circuit breaker, truthfulness-as-reproducibility, time-as-Lyapunov-metric), gated 4-phase (plan→implement→red-team→everything-else) behind its own `G3`. Not a dowiz/bebop2 product item; listed here only so it isn't undiscoverable per this file's own "every planning doc gets a row" rule. Added on a default call, unconfirmed by the operator — remove this row if that call was wrong |
| Space-grade kernel architecture synthesis (zero-external-dep, max-kernel, 100%-determinism-where-possible, 2026-07-19) | [SPACE-GRADE-KERNEL-ARCHITECTURE-SYNTHESIS-2026-07-19.md](SPACE-GRADE-KERNEL-ARCHITECTURE-SYNTHESIS-2026-07-19.md) (+ [SPACE-GRADE-KERNEL-EXECUTION-ROADMAP-2026-07-19.md](ROADMAP.md) — dependency-tiered execution order, all 5 open decision gates ruled same-day; + its own raw-prompt capture, same date) | **Unlike the swarm-safety row above, this is NOT purely cross-track** — 27 sections, all file:line-cited against the REAL dowiz kernel (`order_machine.rs`, `event_log.rs`, `spectral.rs`, `token_bucket.rs`, `decision/import.rs`, the live NTT wire-in on `openbebop/main`) and the real workspace (a 20-crate dependency audit, item 31). Headline finding: `kernel/Cargo.toml` is not actually zero-dep (`tracing`/`tracing-subscriber`/`regex`, 25 transitive crates) despite the kernel's own hand-rolled-crypto precedent proving it doesn't need to be — the flagship "upgrade something already working" case. Cross-checks its own proposals against the project's real Seven Hermetic Principles doc, audits `spectral.rs`'s eigendecomposition for the one-entry-point claim, finds `kernel/src/breaker/` does not exist (Blueprint A above is still paper), and finds the mesh/gossip protocol layer's own stranding is already this roadmap's #1-lever finding (§0 component table above), now folded into a hardening checklist. Everything is PROPOSED pending 32 falsifiable next-steps and 6 explicit `NEEDS-OPERATOR-DECISION` gates — zero code changed. Added same-session as the swarm-safety row, same caveat: unconfirmed by the operator, review before treating as roadmap-authoritative |
| Space-grade kernel — Item 2 verification + filed defect (`FileEventStore` wiring gap, 2026-07-19) | [BLUEPRINT-ITEM-02-file-event-store-verification-2026-07-19.md](BLUEPRINT-ITEM-02-file-event-store-verification-2026-07-19.md) (verification) + [BLUEPRINT-P-FILE-EVENT-STORE-WIRING-GAP-2026-07-19.md](BLUEPRINT-P-FILE-EVENT-STORE-WIRING-GAP-2026-07-19.md) (filed defect) | **Item 2 of the SPACE-GRADE execution roadmap (§A Tier 0), CLOSED-as-defect-filed.** Dual check re-verified adversarially against live `HEAD`: **(b) PASSES** — `EventStore::insert` is `Result`-typed and `FileEventStore` propagates every IO step via typed `StoreError` + `?`, index advances strictly after `sync_all()?`, fix landed `4dec04218` ("H1 fail-open") with a red→green regression test at `hydra.rs:1188-1218`; **(a) FAILS** — the durable `FileEventStore` (`hydra.rs:923`) is constructed in **zero** production paths (all 6 sites under `#[cfg(test)]` / integration tests), and **no binary builds any durable `Hydra`/`EventLog` composition root**. Latent-not-live wiring gap (same class as P91): a correct+tested+unreachable audit trail. Adversarial correction to the source blueprint recorded in the defect doc §1 (`hub_supervisor.rs:366` `MemStateStore` is a test-only stub, not production — strengthens the FAIL). Fix scoped as a follow-up Tier-1 build item (§3 there), NOT built here (Tier-0 read-only audit) |
| Space-grade kernel — Item 22 verification (`mesh.rs` wired-vs-stub classification, 2026-07-19) | [AUDIT-ITEM-22-mesh-classification-final-2026-07-19.md](AUDIT-ITEM-22-mesh-classification-final-2026-07-19.md) (proof artifact) + [BLUEPRINT-ITEM-22-mesh-classification-2026-07-19.md](BLUEPRINT-ITEM-22-mesh-classification-2026-07-19.md) (preliminary) | **Item 22 of the SPACE-GRADE execution roadmap (§A Tier 0), verification half CLOSED.** Blueprint independently re-verified and CONFIRMED (no correction). `kernel/src/mesh.rs` (387 lines, `#[cfg(feature="pq")]`, `pq` NOT default → absent from a default build) is a real, tested ML-DSA-65 signed-log primitive (5 red/green tests, reuses KAT-gated `pq::dsa` + `event_log::sha3_256`, no invented crypto) with **ZERO production kernel callers**: `MeshLog`/`MlDsaSigner`/`Signer` are bench-only (`benches/mesh_verify.rs`), `SignedEntry`/`MeshError`/`HubTransport` uncalled (0 `HubTransport` impls outside the in-module test); protocol layer (sync/consensus/capability) absent. `kernel/tests/verify_chain_wire.rs` ruled out as a false positive — it exercises `event_log`, not `mesh`. Extra finding: the only protocol-adjacent crate `mesh-adapter` doesn't enable `pq` (cannot see `mesh.rs`) and vendors bebop path-deps — exactly the §0 REIMPLEMENT-ZERO-DEP target. Item-23 scoping (gated strictly after): keep `SignedEntry`/`MeshLog`/`MlDsaSigner` + the `HubTransport` seam as-is, build everything above the signed log near-scratch (gossip admission must extend `import_unit()`, not fork it). Not built here (Tier-0 read-only) |
| Space-grade kernel — Item 31 dependency audit (rusqlite · cosmic-text · sha2/keccak, 2026-07-19) | [AUDIT-ITEM-31-dependency-findings-2026-07-19.md](AUDIT-ITEM-31-dependency-findings-2026-07-19.md) (findings) + [BLUEPRINT-ITEM-31-dependency-audit-2026-07-19.md](BLUEPRINT-ITEM-31-dependency-audit-2026-07-19.md) (blueprint) | **Item 31 of the SPACE-GRADE execution roadmap (§A Tier 0), investigative half CLOSED.** All three sub-findings independently re-verified against live `HEAD`: **(a) `rusqlite`** (`tools/deep-clean/Cargo.toml:10`, bundled) — KEEP-and-contain, cat-2 foreign format (reads Hermes `state.db` only, all usage `main.rs:92/147/158/776`, dowiz data is SQLLESS/pgrust per the crate's own `main.rs:16-22` note; in no kernel/engine/default build path); docs-only ruling. **(b) `cosmic-text = "*"`** (`engine/Cargo.toml:30`, optional, feature `text`) — DEFECT CONFIRMED + **FIXED**: pinned to already-resolved `0.19.0` (`engine/Cargo.lock:138-141`, checksum `be17b688…`); `cargo check --features text --offline --locked` green, `git diff Cargo.lock` empty (no churn); committed `c2d0f306a`, pushed to `exec/space-grade-tier0-2026-07-19`. **(c) `sha2` vs kernel Keccak** (`native-spa-server/Cargo.toml:35`, body/frame digests `api.rs:437/441/647/715`) — NOT a defect, KEEP `sha2`. **Blueprint corrected**: it claimed `pub mod pq` is unconditional and "already linked" — false; `kernel/src/lib.rs:13-14` is `#[cfg(feature="pq")]` and native-spa-server enables only `json-api`, so the swap would require enabling `pq` (nfc-pod-codec precedent `Cargo.toml:27`), pulling `aes-gcm`+`curve25519-dalek` — a NET dep *increase*. No browser minter exists (`apps/web/src` SHA-256/subtle grep empty; P59 mint is server/provision-time). **Bonus flag (confirmed, filed not fixed):** dual in-kernel Keccak-f[1600] — `event_log.rs:67` local `keccak_f` vs `pq/keccak.rs:156` `sha3_256` — dedup ticket owed to item 25 (design decision). Enactment half (Tier 2) allowlists rusqlite+sha2 |
| Space-grade kernel — Item 30 state-machine proliferation audit + `resume()` owner defect fixed (2026-07-19) | [AUDIT-ITEM-30-state-machine-final-2026-07-19.md](AUDIT-ITEM-30-state-machine-final-2026-07-19.md) (proof artifact) + [BLUEPRINT-ITEM-30-state-machine-audit-2026-07-19.md](BLUEPRINT-ITEM-30-state-machine-audit-2026-07-19.md) (blueprint) | **Item 30 of the SPACE-GRADE execution roadmap (§A Tier 0), CLOSED.** All four candidate modules re-verified by grep/read against live `HEAD` (baseline `94c29146b`): **0 SHARED** with the `order_machine.rs` FSM proof kit (`grep order_machine\|has_cycle\|topological_order\|GOLDEN_SIGNATURE` across all four → zero hits) — §22's "five copies of the same machinery" fear does **not** materialize; four structurally distinct shapes. 5-column proof table + **4 PARITY-PIN tickets** (I30-T1..T4), each with a stated forcing reason why COLLAPSE doesn't fit — `capability_cert.rs` `RotationState` `:542-571` (2-state clock predicate, no production transition fn); `hub_provisioning.rs` `PoolSlotState` `:152-162` (4-state, transitions scattered `:658/:811/:838/:858` — the only COLLAPSE candidate, deferred beyond Tier 0); `hub_supervisor.rs` `UpdateState` `:442-476` (7-state linear DAG, unrepresentable-by-type); `hydra.rs` `OrganismState` `:76-79` (2-state ρ-hysteresis, const-asserts `:103-113` are the in-situ pin). **True defect count: exactly 1** (the in-session "2 confirmed silent defects" phrase re-confirmed UNSOURCED — no hit in docs/git/memory). **Defect I30-D1 CONFIRMED + FIXED:** `hub_provisioning.rs` `resume()` `:858-864` overwrote state to `Claimed { owner: [0u8;32] }` then tried to restore the real owner by matching `Suspended` on the already-overwritten state — dead code, owner silently zeroed (capability loss, §16.57). Red test `red_resume_preserves_owner_not_zeroed` RED before / GREEN after; fix captures owner in the initial match, deletes the dead block; committed `707848dfd`, pushed to `exec/space-grade-tier0-2026-07-19`. Full kernel suite after fix: 899 passed / 0 failed / 3 ignored; order_machine golden signature stays green. Main still carries the buggy lines (fix branch not yet merged) |
| Space-grade kernel — Items 15/16/17/19 eigen-surface + GraphSpectrum collapse + engine-seam + retrieval audits (2026-07-19) | [BLUEPRINT-ITEMS-15-16-17-19-eigen-spectrum-engine-audit-2026-07-19.md](BLUEPRINT-ITEMS-15-16-17-19-eigen-spectrum-engine-audit-2026-07-19.md) (blueprint) + [AUDIT-ITEMS-15-17-19-followup-tickets-2026-07-19.md](AUDIT-ITEMS-15-17-19-followup-tickets-2026-07-19.md) (tickets + item-16 resolution) | **Items 15/16/17/19 of the SPACE-GRADE execution roadmap (§A Tier 0).** **Item 16 = the cluster's one P2, RESOLVED by refactor (not deferred).** `graph_spectrum()` triple-computed the adjacency spectrum (directly + inside its own `classify_drift(adj)`) and `graph_energy_report()` made four passes, both while claiming "single eigenvalue pass" — false. Collapse landed option (b) (internal, zero public-signature change — the 7 standalone functionals have many independent external callers, so routing them through `graph_spectrum` would be a regression): extracted `drift_guards_ok`/`drift_band`, added `classify_drift_with_rho(a, rho)`, `graph_spectrum` now makes EXACTLY 2 passes (adj + Laplacian), `graph_energy_report` 4→2. Proof = thread-local `EIGEN_CALLS` counter asserting exactly-2 (proves single-computation, not value agreement) + a field-consistency test. Kernel suite **902 / 0 / 3** (was 899; +3); engine builds green. Committed `e125f0c97`, pushed to `exec/space-grade-tier0-2026-07-19`. **Items 15/17/19 = read-only audits → 3 tickets filed (not built, each new scope):** I15-T1 single-eigen-surface HOLDS (no `lowrank.rs`) but parity is values+dominant-residual only (`spectral.rs:1254` `let _ = dvecs;`) → vector-scope cross-solver pin owed; I17-T1 RC-4 triple has DriftClass+dt CLOSED, L-operator OPEN (`field_frame.rs:10-40` engine 5-point Neumann Laplacian unpinned to kernel `csr.rs:552`) → engine-boundary pin owed (cross-refs item 18's intra-kernel pin, does not duplicate); I19-T1 retrieval independent-by-design (no GraphSpectrum consumer) but `ppr.rs:3-5` is a comment-bound unpinned mirror of `markov.rs:162-170` → parity-pin owed, NOT collapse (`retrieval/mod.rs:14` red-line forbids touching markov) |
| Space-grade kernel — Items 1+13 CI zero-dependency gate (allowlist + monotonic-shrink + lockfile-hash, 2026-07-19) | [BLUEPRINT-ITEMS-01-13-ci-zero-dep-gate-2026-07-19.md](BLUEPRINT-ITEMS-01-13-ci-zero-dep-gate-2026-07-19.md) (blueprint) | **Items 1+13 of the SPACE-GRADE execution roadmap (§B Tier 1, §G.7 batch), CLOSED — real CI config+script+data landed.** Blueprint's baseline independently re-verified against live `HEAD`: `cargo tree --manifest-path kernel/Cargo.toml -e no-dev --locked --offline` resolves to **exactly 24 external crates** (3 direct — `regex`/`tracing`/`tracing-subscriber` — + 21 transitive), matching the blueprint's list byte-for-byte (lockfile unmoved since 2026-07-19). Three deliverables built + tested: **(1)** `kernel/ZERO-DEP-ALLOWLIST.txt` (24 names, comments stripped by the gate, names-only so version bumps don't churn); **(2)** `scripts/zero-dep-gate.sh` (`chmod +x`) — **Gate A** default-tree name-set ⊆ allowlist (catches direct *and* transitive, which `decart-dep-lint` cannot); **Gate B** monotonic-shrink `comm -13 origin/main HEAD` (growth ⇒ RED unless the gate job is edited in the same diff); **Gate C** sha256 of `kernel/Cargo.lock` unchanged across the run (item 13 / §10-P6); **(3)** new `zero-dep-gate` job in `.github/workflows/ci.yml` after `supply-chain`, runs the script under `sudo -E … unshare -n` (networking-disabled proof is *continuous*, `unshare -r -n` rootless fallback documented). **Red-proofs executed (real output in commit body):** Gate A goes RED naming a throwaway `libc` dep (then reverted, lockfile byte-identical); Gate B RED on a simulated grown allowlist vs a scratch `origin/main` baseline, GREEN on a legitimate shrink; clean tree GREEN at 24 crates; `unshare -r -n` and `unshare -n` both yield the identical 24-crate verdict + stable hash. **Scope boundary held:** `dowiz-kernel` default no-dev ONLY — no engine/tools/apps/mesh/agent-loop files touched (that is item 31, Tier 2). Committed `01acd673e`, pushed to `exec/space-grade-tier0-2026-07-19` |

| Space-grade kernel — Item 25 dependency-replacement standing procedure (finalized + re-verified, 2026-07-19) | [PROCEDURE-DEPENDENCY-REPLACEMENT-STANDING-2026-07-19.md](PROCEDURE-DEPENDENCY-REPLACEMENT-STANDING-2026-07-19.md) | **Item 25 of the SPACE-GRADE execution roadmap (§B Tier 1), CLOSED.** A near-final Fable draft was independently re-verified against live `HEAD` and finalized. **Both precedents confirmed:** (1) slot-arena/thunderdome opt-in adoption under operator override — every cite green (`slot_arena.rs:10-24/26-31/33-49/72-73/209-226/274-284`, `Cargo.toml:64-77/74-76/77/146-151`, `arena.rs:25-27`, deep-dive at `docs/research/OPUS-PERF-ARENA-DEEPDIVE-2026-07-18.md`); (2) QRNG entropy provider never-replace rule — `entropy.rs:5-11/58-66/86-107/112-115/154-180`, `pq`-gated via `lib.rs:13-14`. **The distilled 10-step procedure is now BINDING** for Items 4+29 (`tracing`/`tracing-subscriber` logger/FDR rewrite, incl. the `telemetry` `SpanMetricsLayer` consumer) and Item 5 (`regex` retirement) — §3 explicitly binds them. **Flagged finding RESOLVED + strengthened:** the `qrng` feature is undeclared in `kernel/Cargo.toml` (proven: `grep` no-match + `cargo build --features qrng` → "does not contain this feature"), so the whole `#[cfg(feature="qrng")]` provider (incl. the sanctioned `master_seed()`) is dead code that never compiles in ANY build — a "QRNG-seeded-never-replace" standing-rule-vs-reality inconsistency (same correct-and-unreachable class as item 2's `FileEventStore`), filed as an owed follow-up ticket in the procedure doc §3 (new scope, NOT an item-25 fix). Docs-only; committed + pushed to `origin/main` |

| Space-grade kernel — Item 14 toolchain pin + structural compiler-bump gate (2026-07-19) | [BLUEPRINT-ITEM-14-toolchain-pin-2026-07-19.md](BLUEPRINT-ITEM-14-toolchain-pin-2026-07-19.md) (blueprint) + `docs/audits/toolchain/spot-check-1.96.1.md` (baseline audit artifact, on the exec branch) | **Item 14 of the SPACE-GRADE execution roadmap (§B Tier 1, §G.8), CLOSED — real CI config + toolchain file landed.** Blueprint's baseline independently re-verified against live `HEAD`: `rustc`/`cargo` = **1.96.1**, and `find rust-toolchain*` = **zero hits** repo-wide before the change (CI floated on the runner's preinstalled stable; no toolchain-setup step in `ci.yml`). Three deliverables built + tested on `exec/space-grade-tier0-2026-07-19`: **(1)** root `rust-toolchain.toml` — `channel="1.96.1"` (exact pin, not floating `stable`), `profile=minimal`, `components=[rustfmt,clippy]`; **(2)** `toolchain-bump-gate` job in `.github/workflows/ci.yml` — ALWAYS runs (required-check safe, NOT `paths:`-filtered — a paths filter deadlocks non-bump PRs), enforcement fires ONLY when the pinned `channel` value changed vs the comparison base, and a bump then REQUIRES `docs/audits/toolchain/spot-check-<new>.md` with `## Assembly spot-check` + `## Full-suite re-run` headings in the same diff (pin's own introduction counts as `<absent>→1.96.1`, so it carries the baseline artifact); **(3)** `docs/audits/toolchain/spot-check-1.96.1.md` — an **HONEST baseline**: real source-level constant-time audit of all 6 pq surfaces (dsa NTT branch-free w/ constant-time `caddq` mask; `kem`/`hybrid` FO tag-compare are plain `!=` variable-time compares, **pre-existing + compiler-independent**, owed to P91.2 ACVP; both `keccak_f` copies branch-free; x25519 delegated to audited `curve25519-dalek`), assembly audit PARTIAL (one symbol disassembled, method shown) with the full per-branch taint proof **explicitly DEFERRED to Tier 2 item 7 (Kani)** — no fabricated "clean" claim. **Proofs:** kernel `cargo test --offline` **902/0/3**, engine **117/0**, release build green under 1.96.1 (pin is a no-op vs installed toolchain); gate logic unit-tested **6/6** against simulated before/after channel values + an end-to-end `git show BASE:$FILE` extraction test (non-bump→vacuous-green, bump-without-artifact→RED exit 1, bump-with-artifact→GREEN, malformed-artifact→RED, `<absent>→1.96.1` with/without baseline) — maps 1:1 onto §G.8. Committed `bb1e9e8dc`, pushed to `exec/space-grade-tier0-2026-07-19`. **Owed (G5):** mark `toolchain-bump-gate` a required status check in branch protection — server-side, not in-repo; until then the gate is advisory (same caveat as every other `ci.yml` gate) |

| Space-grade kernel — Items 4+29 (+§1.2 JsonWriter) hand-rolled logger/FDR, `tracing` pair retired (2026-07-19) | [BLUEPRINT-ITEMS-04-29-logger-fdr-rewrite-2026-07-19.md](BLUEPRINT-ITEMS-04-29-logger-fdr-rewrite-2026-07-19.md) (blueprint) | **Items 4+29 of the SPACE-GRADE execution roadmap (§B Tier 1) — the largest/keystone Tier-1 item — CLOSED. Real kernel code landed** as three isolated, individually-revertable commits on `exec/space-grade-tier0-2026-07-19` (`f04142f89` build → `4f4872a54` flip → `eb350464e` remove), each pushed green. Built `kernel/src/fdr/` (json = single escape authority absorbing markov `esc()` + the `{:?}` span escaper; schema = `FdrEvent` with `hw:HwStamp` first-class + `Reading<T>`=Value|Unavailable(closed Absence enum) + NEW RAPL `joules_uj` reader; ring = A/B segment files + hand-rolled CRC32 + read-only recovery tolerating one torn tail; macros = tracing-grammar subset `%`/`?` sigils; mod = `Level`/sink/`SpanObserver`/`init`). Then flipped the 13 call sites (`tracing::`→`crate::fdr::`), ported `SpanMetricsLayer`(`tracing_subscriber::Layer`)→`SpanMetricsObserver`(`fdr::SpanObserver`) deleting the thread-local Extensions-lock deadlock workaround + the outer-span-under-nesting bug, deduped the double `mldsa_verify` wrapper (`mldsa.rs` deleted), `init_tracing`→`fdr::init`, markov `esc()`→`fdr::json::escape`. Then removed both deps + shrank `ZERO-DEP-ALLOWLIST.txt` 24→5. **Proofs discharged (blueprint independently re-verified first — usage map all-in-`kernel/`, 19-crate drop, dual-`mldsa_verify`, two-escaper inconsistency all confirmed):** `cargo tree -e no-dev` **25→6 crates (19 dropped, exceeds ≥13)**; `metric.jsonl` golden row + markov CLI JSON **byte-identical** before/after; **kill-9 durability test** (real child `fdr_recorder` SIGKILLed after 300 no-fsync writes → fresh reader recovered **300/300 events, torn_tail 0, crc_failures 0, dirty stop, PostMortem emitted**) + clean-shutdown control; `hw` first-class with `joules_uj:{"unavailable":"no_rapl_interface"}` named-absence on this RAPL-less host; **wasm32 cdylib green** (`Instant::now()` gated off wasm — the one shipping-cdylib trap); full kernel suite **938 passed / 0 failed**; `scripts/zero-dep-gate.sh` **GREEN** (5 external crates, shrink-only OK, lockfile hash stable). Ruling recorded three places (procedure step 9): `fdr/mod.rs` module doc, `kernel/Cargo.toml` removal-site comment, this blueprint. Reopen trigger named: external tracing/OpenTelemetry collector export requirement. NOT this pass (deferred, honest): PostMortem→`EventLog` routing (blocked on item 2), `RUST_LOG` per-target grammar, span hierarchy, `metrics.rs`/`typed_metrics.rs` struct dedup (own ticket) |
| Space-grade kernel — Item 5 `regex` retired, kernel reaches ZERO external dependencies — CLOSES ALL OF TIER 1 (2026-07-19) | [BLUEPRINT-ITEM-05-regex-retirement-2026-07-19.md](BLUEPRINT-ITEM-05-regex-retirement-2026-07-19.md) (blueprint) | **Item 5 of the SPACE-GRADE execution roadmap (§B Tier 1) — the FINAL Tier-1 item — CLOSED. Real kernel code landed** as three isolated commits on `exec/space-grade-tier0-2026-07-19` (`18152ef84` build → `c6b5d2176` flip → `6605166cd` remove), each pushed green. `regex` was the kernel's last external crate; its entire production surface was ONE function (`TrigramIndex::query_regex`) with **zero production callers** (independently re-verified by full-workspace grep across `kernel/ engine/ apps/ tools/ agent-loop/ agent-adapters/` — the only caller anywhere was its own test; the only pattern ever compiled was `note-.*-recall`). Ruling per item 25's procedure = terminal state (a) removed outright, replaced by a kernel-owned restricted matcher (`kernel/src/retrieval/pattern.rs`) for the used subset **{literal, `.`, `.*`}** as an unanchored contains-match (greedy leftmost segment placement — no backtracking exists ⇒ no pathological blowup, proven by the glob exchange lemma + a backtracking naive-reference cross-check), with typed rejection (`PatternError::UnsupportedMeta{byte,pos}`) of every other metacharacter (degrade-closed). **Parity proven BEFORE cutover:** differential vs the live `regex` crate over the 20-doc FIXTURE + 2000-doc synthetic corpus + a proptest sweep (random subset patterns × ASCII docs) — all bit-identical over the newline-free ASCII domain (one honest documented divergence: this matcher's `.` matches any byte incl. `\n`, regex's default `.` excludes it — coincident on the note-name domain, which has no newlines); post-removal a permanent independent naive recursive reference differential + a frozen golden (`query_pattern("note-.*-recall")==vec![7]`) carry the guarantee. **Proof:** `cargo tree --manifest-path kernel/Cargo.toml -e no-dev --locked --offline` = **`dowiz-kernel` root ONLY, ZERO external crates** (regex's whole subtree — regex, regex-automata, regex-syntax, aho-corasick, memchr — dropped; regex survives only as a `criterion` dev-dep transitive in `Cargo.lock`, outside the no-dev proof surface); `ZERO-DEP-ALLOWLIST.txt` shrunk **5→0**; `scripts/zero-dep-gate.sh` **GREEN "0 external crates"** — and a **latent gate abort at the true zero-dep end state was fixed** (its root-package/blank filter greps returned exit 1 when they filtered every line out, aborting under `set -euo pipefail` before it could report GREEN; now `|| true`-guarded, cargo resolution failures still loud, gate A/B/C semantics unchanged); full kernel suite **925 lib unit / 0 failed / 3 ignored** (+22 integration). Ruling recorded five places: `pattern.rs` module doc, `kernel/Cargo.toml` tombstone, allowlist header, `fdr/mod.rs`, `lib.rs`/`retrieval/mod.rs`, + the blueprint. **With this, every §B Tier-1 item (1+13, 14, 25, 4+29, 5) is DONE — the kernel's default build has genuinely zero external dependencies.** Scope boundary held: `tools/skillspector-rs`'s own `regex` is untouched (item 31) |
| Space-grade kernel — Item 26 batching measurement pass (event-log fsync · FDR ring · import_unit, 2026-07-19) | [AUDIT-ITEM-26-batching-measurements-2026-07-19.md](AUDIT-ITEM-26-batching-measurements-2026-07-19.md) (results) + [BLUEPRINT-ITEM-26-batching-research-2026-07-19.md](BLUEPRINT-ITEM-26-batching-research-2026-07-19.md) (blueprint) | **Item 26 of the SPACE-GRADE execution roadmap (§C Tier 2). Measurement pass CLOSED 2026-07-19 as measurement-only (scope law held at the time) — SUPERSEDED 2026-07-20: the operator later authorized the batching this measurement pass flagged as BATCH-WORTHY-but-gated, and it landed as real code** (`kernel/src/hydra.rs`, `with_batch_size`/`flush_pending`, opt-in, default `batch_size=1` byte-identical to prior behavior; commit `85022e49d`; verified `DONE-VERIFIED` in `SPACE-GRADE-VERIFIED-STATUS-LEDGER-2026-07-20.md` — "group-commit batching; 53x durability throughput; opt-in"). Inventory §1 citations independently re-verified against the live worktree — all accurate. Real numbers (ext4 block device, ~500 µs/fsync — not tmpfs), direct per-op percentiles + criterion cross-check + `strace -c`: **M1 event-log commit** p50 637 µs / p99 1343 µs / **1,513 ev/s**, strace confirms **exactly 1 fsync + 1 open + 1 close per event**; bench-crate group-commit model shows **~53× throughput at batch-64** (568→10.8 µs/rec) with the fsync barrier itself staying ~500–860 µs regardless of batch size → **BATCH-WORTHY but operator-gated** (acknowledged-before-durable changes the crash contract; also stop re-opening the fd per event). **M2 FDR ring** normal 2.56 µs vs alarm-fsync 571 µs (~148×), seg-cap sweep 1 MiB→4 MiB buys only ~11% on a burst → **KEEP AS-IS** (design already amortizes the fsync over a ~8k-record segment; the missing baseline the module landed without now exists and validates it). **M3 `import_unit`** 0.87 µs p50, ~0.6 ns marginal per replay-case, ~1M units/s, dominated by 2× SHA3-256 not the replay loop → **measured DON'T-BATCH** (blueprint hypothesis confirmed). **M4 arena** skipped per its own gate (M3 shows allocation is noise). **PMU/perf UNAVAILABLE** (`/proc/sys/kernel/perf_event_paranoid=4`, no `perf` binary) — wall-clock (`Instant` per op) + `strace -c` fallback, zero fabricated cache-miss/IPC counters. Scaffolding (`kernel/benches/batching.rs` + `kernel/tests/item26_latency_probe.rs`, both measurement-only) committed to `exec/space-grade-tier0-2026-07-19`; results doc pushed to `origin/main` |
| Space-grade kernel — Item 27 classifier-input half: PMU companion stamps for `Verdict`/`DriftClass` (2026-07-19) | [BLUEPRINT-ITEM-27-pmu-classifier-input-2026-07-19.md](BLUEPRINT-ITEM-27-pmu-classifier-input-2026-07-19.md) (blueprint) | **Item 27 of the SPACE-GRADE execution roadmap (§C Tier 2), classifier-INPUT half CLOSED — real kernel code landed** as `03887462a` on `exec/space-grade-tier0-2026-07-19` (5 files, +851/-1). All blueprint citations independently re-verified against the live worktree: `markov::Verdict` (`markov.rs:42-50`, sole producer `analyze_detailed:110`), `spectral::DriftClass` (`spectral.rs:674-682`, sole producer `classify_drift`, `DRIFT_BAND=1e-6`, pinned `wire_code()`), the unrelated `verify_retrieval.rs:20` `Verdict{Pass,Fail}`, and the Tier-1 FDR machinery (`Reading<T>`/`Absence`/`HwStamp`/`StampPolicy`, `f04142f89`). **Built `kernel/src/fdr/pmu.rs`:** `PmuStamp` (all `Reading<u64>`), a sibling of `HwStamp` on the SAME machinery. **Tier A** (`rdtsc` via `core::arch::x86_64::_rdtsc` + `/proc/self/stat` minflt/majflt/nswap + `/proc/self/status` ctxt-switches, the same `comm`-aware parse as `typed_metrics`) reads real data zero-permission. **Tier B** (instructions/cycles/cache-misses/branch-misses) via a hand-rolled zero-dep `perf_event_open(2)` raw syscall (`core::arch::asm!`, syscall 298, 64-byte VER0 `perf_event_attr`, `exclude_kernel|exclude_hv`), every failure mode → a named `Absence` (new `NoPmuInterface` variant for ENOENT/ENODEV/ENOSYS/EOPNOTSUPP; EPERM/EACCES → `PermissionDenied`) — never a fabricated 0, never a panic/SIGSEGV. **Wired** via `PmuStation::bracket`: the `markov_attractor` bin window-brackets `analyze_detailed` and logs ONE `markov_verdict` `FdrEvent` carrying `verdict_str()` + the PMU delta on the SAME record (optional `pmu` field on `FdrEvent`, absent everywhere else so all other FDR records stay byte-identical; opt-in durable via `DOWIZ_FDR_DIR`, no-op with no sink). `analyze_detailed`/`classify_drift` stay pure — P6 preserved; PMU is recorded input, not a classifier input. **Proofs:** 6 `fdr::pmu` unit tests + 1 end-to-end integration test (spawns the real bin, recovers the real FDR ring, asserts the companion record) green; full kernel suite **955 passed / 0 failed / 3 ignored** (was 945); verified **self-contained on clean HEAD** via an isolated detached worktree (diff applied + built + tested green without the branch's other in-flight changes). **Independent-verification CORRECTION to item 26's "PMU unavailable / paranoid=4" premise:** the self-management agent process runs as **root with `CAP_PERFMON`/`CAP_SYS_ADMIN`**, which BYPASSES `perf_event_paranoid=4` — so Tier B `perf_event_open` actually SUCCEEDS in that context and returns real hardware counters (measured live: **IPC ≈ 3.7**, instructions/cycles/cache-miss/branch-miss all real, hardware-plausible). A genuinely unprivileged process still sees `permission_denied`; that named-absence path is proven deterministically (errno-table + forced-absence serialization tests), not by the live privilege level. **Operator note (informational, non-blocking):** `sysctl kernel.perf_event_paranoid=2` OR `CAP_PERFMON` (kernel ≥5.8) on the kernel's process unlocks Tier B's real IPC/cache-miss data on the unprivileged production path too — host-level knob, no decision required for this half to stand. Response half stays Tier 4 (after items 9 + 21), untouched. Docs pushed to `origin/main` |
| Space-grade kernel — Item 31 enactment: per-crate zero-dep gate + `kernel::json` primitive + Phase-A serde cutover (2026-07-19) | [BLUEPRINT-ITEM-31-enactment-per-crate-gate-2026-07-19.md](BLUEPRINT-ITEM-31-enactment-per-crate-gate-2026-07-19.md) (blueprint) | **Item 31 of the SPACE-GRADE execution roadmap (§C Tier 2), enactment half CLOSED — real CI config + a new kernel module landed** as three commits on `exec/space-grade-tier0-2026-07-19` (`ae2da4a9d` gate → `dd6876a73` json+oracle → `c64ca923b` cutover), each pushed green. **All four blueprint corrections independently re-verified, TWO further corrected against real evidence:** **(1) Workspace = 26 crates** (synthesis said 20; six `tools/telemetry/*` missed) — confirmed; **12 already zero-external-dep**. **(2) Gate**: `scripts/zero-dep-gate.sh` parametrized `[<crate-dir>]` (no-arg=kernel, backward-compatible); the hardcoded `dowiz-kernel` path-dep filter generalized to `grep -v ' (/'` (verified against real `cargo tree --prefix none` — root + every path dep carry an abs path in parens, registry crates don't, `(*)`/`(proc-macro)` markers pass through). Added `scripts/zero-dep-crates.txt` (24-crate roster) + 25 `<crate>/ZERO-DEP-ALLOWLIST.txt` (12 empty floors, 13 frozen closures w/ item-25 ruling headers — rusqlite KEEP-and-contain, sha2 KEEP, cosmic-text pin carried forward). CI `zero-dep-gate` job loops the roster under one `unshare -n`; **mesh-adapter** rides its existing dual-checkout job; **agent-governance-wasm EXCLUDED** (absolute-path `/root/bebop-repo` dep — CI-unresolvable, filed as own portability defect). Also regenerated **10 downstream `Cargo.lock` removals-only** (pruning the regex/tracing closure the kernel dropped in items 4/5/29 so `--locked` resolves; audited zero version bumps). **A CI-poison bug root-caused + fixed**: a FAILING `git origin/main:<untracked-path>` access corrupts cargo's next `rustc -` target-info probe in a shared `.git`; Gate B now probes with a no-pathspec `git ls-tree`. **Proof**: full roster **GREEN 24/24 (5×)**; Gate A **RED** on an injected unlisted dep (`cfg-if`→`tools/eqc-rs`), **GREEN** on revert; Gate C lockfile-hash stable. **(3) Serde carriers = NINE** (not seven; +`rust-spool`, +`topics`) — confirmed. **(4) `kernel::json` — honest scope-down**: built (`kernel/src/json.rs`, always-compiled pure-std bounded recursive-descent RFC 8259 parser + serializer, degrade-closed, MAX_DEPTH/MAX_LEN), SEPARATE from `fdr::json` (serialize-only). `serde_json` kept as a **dev-dep differential oracle** (outside `-e no-dev` → kernel allowlist stays empty; lock unchanged). Oracle (`kernel/tests/json_oracle.rs`): **50-item real-carrier corpus, all 50 agree (31 accept / 19 reject), 31 round-trip** + a **2000-case proptest fuzz** over the carriers' real number/string/nesting distribution. **Phase-A cutover of the carriers that BOTH shrink the tree AND are sound: `agent-facade` (ext deps 11→0, now zero-dep) + `skillspector-rs` (15→5; the 3 `serde::Serialize` derives → `to_value()` builders, shape-locked by new tests). Serde carriers 9 → 7 (real decrease).** **`cargo tree -i serde_json`-verified shrink set**: only 4 of 9 can shrink at all (agent-facade, agent-adapters, wasm, skillspector); the other 5 keep serde_json via `axum`'s default `json` / `ureq`'s `json` feature regardless of our code. **Correction to the blueprint's projected 3rd (wasm): NOT Phase-A** — its `a11y_build_mirror` site (de)serializes the SHARED `dowiz-engine` `SemanticScene`/`A11yTree` through engine's `serde` feature; a cutover would couple `kernel::json` to engine's schema. **wasm deferred to Phase-B** (trigger: engine exposes a serde-free codec); `agent-adapters` deferred (adversarial JSON-RPC surface). **`rust-spool` deletion DEFERRED (corrects the blueprint's "referenced by nothing")**: `tools/telemetry/lib.sh:37` hardcodes + `tg_spool_ensure` LAUNCHES `rust-spool/target/release/telemetry-spool` as the LIVE Telegram telemetry drainer — deleting it would break the live pipeline; retire only after `async-spool` is deployed + `lib.sh` cut over. Docs pushed to `origin/main`. **With this, item 31 is fully closed (both halves).** |
| Space-grade kernel — Item 6 §4 hardening checklist codified + `hardening-gate` CI + minimal dudect harness (2026-07-19) | [BLUEPRINT-ITEM-06-hardening-checklist-ci-2026-07-19.md](BLUEPRINT-ITEM-06-hardening-checklist-ci-2026-07-19.md) (blueprint) + `docs/audits/hardening/CHECKLIST.md` + `docs/audits/hardening/HOT-PATHS.tsv` (on the exec branch) | **Item 6 of the SPACE-GRADE execution roadmap (§C Tier 2), CLOSED — real CI config + a new dudect harness landed** as `ae4964e61` on `exec/space-grade-tier0-2026-07-19` (9 files, +606/-1). Codifies SYNTHESIS §4 as CI-enforced standing law with the §10/P7 correction built in (**re-execute, never presence-check**). **Existing-coverage list independently re-verified** — the cited oracles all exist and pass (ACVP 64 per-tcId + 3 count pins, Keccak FIPS-202 both copies, x25519 RFC 7748, FSM golden-signature drift gate, eigen bit-capture/parity, KEM/hybrid red-green) — **but a blueprint-premise CORRECTION:** the pq-gated KATs do **NOT** re-execute in the default `cargo-test` job (`pq` is not a default feature, so `cargo test --offline` never compiles them — they were **dark in CI**). Three deliverables: **(1)** `CHECKLIST.md` (the 4 items as standing law + the P7 clause + the one sanctioned presence-check exception, `toolchain-bump-gate`); **(2)** `HOT-PATHS.tsv` (machine-read manifest, 14 rows — `path_prefix·features·filter·min_tests·mode·checklist·gap`; single-token module-path filters because cargo takes ONE positional; gaps ledgered in the row: `MISSING(P91.2)`/`KNOWN-RED(P91.2)`/`N/A(corpus-oracle)`/`MISSING(item-8)`); **(3)** `hardening-gate` job (`scripts/hardening-gate.sh`) — diff-triggered row obligation (touched `@ZONE` w/o row = RED), named-filter re-execution asserting parsed `N passed >= min_tests` (**zero-match = RED, anti-forgery core**), an UNCONDITIONAL oracle floor that now re-runs the pq KATs with `--features pq` every build (closing the dark-in-CI gap), the dudect gate incl. its planted-leak self-test in release, and a gap-ledger warning pass; P6: every cargo call `--locked --offline`, `kernel/Cargo.lock` hash asserted unchanged. **RED/RED/GREEN proven with real command output** (isolated temp worktree): unregistered pq file → exit 1 at step B; a row whose filter matches zero tests → exit 1 (`matched 0 passing tests, need >= 1`); my real commit's diff (3 registered rows) → exit 0. **dudect (real gap — BUILT):** `kernel/src/ct_gate.rs`, zero-dep Welch-t harness + reusable `ct_eq` constant-time primitive + planted-leak self-test — the variable-time `naive_eq` is DETECTED (|t|≈273–384), `ct_eq` PASSES (|t|<1.3), separation >290× (hard gate is detection + ratio, robust to a loaded runner's noise floor). **item 3 (debug_assert differential):** wired for `order_machine::assert_transition` (slice-vs-`FSM_ADJ`) + `householder::eig2x2` (Vieta trace/det) as the pattern, exercised by the existing 25+14 oracle tests. **Scoped vs honestly deferred:** dudect crypto-surface coverage → items 7/8; `kem.rs`/`hybrid.rs` variable-time tag compares → `KNOWN-RED(P91.2)` (NOT silently fixed — the CT fix adopting `ct_eq` is the gate's first customer); GCRA differential oracle → item 8; exhaustive assembly/taint → item 7 (Kani). Full kernel suite **955/0/8** at the commit. Roadmap + this index pushed to `origin/main` |
| Space-grade kernel — Item 7 Kani wiring + native exhaustive contracts (Keccak · FSM · NTT arithmetic, 2026-07-19) | [BLUEPRINT-ITEM-07-kani-wiring-2026-07-19.md](BLUEPRINT-ITEM-07-kani-wiring-2026-07-19.md) (blueprint) + [RESEARCH-NATIVE-KANI-REPLACEMENT-FEASIBILITY-2026-07-19.md](RESEARCH-NATIVE-KANI-REPLACEMENT-FEASIBILITY-2026-07-19.md) (v2 rescope authority) + `docs/audits/hardening/HOT-PATHS.tsv` `mode=kani` rows + `scripts/kani-gate.sh` (on the exec branch) | **Item 7 of the SPACE-GRADE execution roadmap (§C Tier 2), EXECUTED — mostly done, two honest ledgered limits. Real kernel code + CI landed** as `df92f0c16` (kernel proofs + 15 native exhaustive `#[test]`s + behavior-preserving `event_log::keccak_f` lift + planted-fault self-test) → `23f583b3e` (kani-gate CI job + manifest rows), pushed to `exec/space-grade-tier0-2026-07-19`. **KANI TOOLCHAIN BOOTSTRAP SUCCEEDED in this environment** — `cargo-kani 0.67.0` (its own `nightly-2025-11-21` rustc + CBMC/CaDiCaL SAT backend), a real hours-if-unlucky install that completed; CBMC verification genuinely executes here. **ZERO-DEP mechanically preserved:** every harness is `#[cfg(kani)]`, nothing added to `Cargo.toml`/`Cargo.lock` (only a `[lints.rust]` check-cfg for `cfg(kani)`), `cargo tree -e no-dev` still 0 external. **7 Kani harnesses verified SUCCESSFUL (real `cargo kani` output):** `proof_rotl_contract` (rotl ≡ rotate_left ∀u64), `proof_keccak_f_total` (permutation panic-free ∀2^1600 states), `proof_reduce32_contract`, `proof_montgomery_reduce_contract` (overflow-free + result ∈ `[−Q,Q]` — Kani exhibited that my first open-interval `(−Q,Q)` assertion was wrong, the boundary is inclusive), `proof_ntt_butterfly_lemma` + `proof_invntt_butterfly_lemma` (lazy-reduction growth bound; full-`ntt` capped at lemma + documented 8-layer induction per §3.3 ladder), and `proof_selftest_planted_overflow` (deliberate i32 overflow under `#[kani::should_panic]` — SUCCESSFUL only because caught; **RED-path demo verified**: removing `should_panic` → `VERIFICATION: FAILED`). **15 native exhaustive `#[test]`s** (Bucket-B per the v2 rescope, zero new tooling, riding item-6 lib rows): FSM ×4 (144-pair `assert_transition` vs `FSM_ADJ`, all-12 `reachable`, fold corollary, const-graph no-UB), dsa ×4 (`caddq`/`power2round`/`decompose`/chknorm-abs over full domains), kem ×5 (`red` edges incl. `i64::MIN`, poly add/sub + `poly_mul` body-lemma over `[0,Q)²`, compress/decompress, byte codec). **CI:** separate `kani-gate` job (pinned `KANI_VERSION 0.67.0`, cached `~/.kani`) — NOT folded into item-6's fast `--locked --offline` hardening-gate (Kani = network toolchain + minutes-scale); `scripts/kani-gate.sh` re-executes each `mode=kani` row asserting parsed "N verified >= min" (zero-match = RED); `hardening-gate.sh` skips kani rows with a notice. **TWO HONEST LIMITS LEDGERED (not silently dropped):** (a) the STRONG full-state Keccak cross-copy equivalence (2^1600) **exceeded the 25-min CI budget** (measured, exit 124) — shipped the §3.1 fallback rung: a native machine-checked **index-map equivalence** (proves the ρ/π rotation tables, round constants, and π destinations are identical — the ONLY divergence between the two copies) + `proof_rotl_contract`; the strong form is preserved (correct, not deleted) in `kani_proofs_strong`, runnable nightly. (b) the Montgomery **congruence** `r·2^32 ≡ a (mod Q)` is **not** machine-checked (symbolic modulo over ±1.8e16 timed out >7 min in both i128 and i64 forms) — stays covered by ACVP KATs; the harness proves the overflow/panic fault class the synthesis §7 actually names. **Item 8 inherits two design requirements** (recorded in the roadmap item-8 entry + the `token_bucket.rs proof_gcra` `min=0` placeholder row): pure `fn gcra_decide(now_ns,tat_ns,cost_ns,burst_ns)->Option<u64>` + integer nanoseconds (never f64) in the transition. Full kernel suite `--features pq` **1131/0/8** (was 1116) |

| Deterministic AI inference — synthesis + roadmap items 33–44 (Fable planning pass, 2026-07-19) | [DETERMINISTIC-AI-INFERENCE-SYNTHESIS-2026-07-19.md](DETERMINISTIC-AI-INFERENCE-SYNTHESIS-2026-07-19.md) (synthesis) + [RAW-PROMPT-4-deterministic-ai-inference-self-verifying-code-2026-07-19.md](RAW-PROMPT-4-deterministic-ai-inference-self-verifying-code-2026-07-19.md) (verbatim source) + SPACE-GRADE roadmap **§H items 33–44** | **PLANNING ONLY — zero code; extends the SPACE-GRADE arc, appended after item 32.** Grounding headline, both repos swept: **(1) no AI/ML inference engine, model, or ML crate exists anywhere** (candle/burn/tract/ndarray/onnx = zero hits in every manifest + Cargo.lock) — greenfield subsystem, but every needed discipline has live precedent (`attention.rs` deterministic softmax, `simd.rs`/`householder.rs` `core::arch` AVX2 with bit-identity falsifiers, `mat.rs` one-matmul, `micrograd.rs`/`online.rs` edge learners, BumpArena, eqc-rs `emit_fixed_rust` Q-format, and the CORDIC ledger-row-25 precedent — the kernel already retired float sin/cos for digest determinism once); **(2) the raw prompt's telemetry report is GROUNDED-names / UNVERIFIED-numbers** — `fold_transitions/5_hops`, `empirical_identify/20k_samples`, `absorbing/fundamental_matrix_16`, bebop `loop_cycle`/`wire`, and the perf/bus-contention branches are all real (dowiz branch merged `8c865805b`; bebop branch live with an uncommitted bus fix), but the specific figures (+30% wire, 3.02x@N=64, +16.6%/+14.3% regressions, "123 passed" engine) match **no committed artifact in their claimed context** → item 33 re-measures before anything acts on them (re-verify correction: "123 passed" IS a real committed count — but for `bebop-proto-cap` (`WAVE-CLOSEOUT-P57-P74:36`, P65), not `engine`, which is 112/116/117/121 across docs, never 123 — a cross-wired attribution, not a fabrication). **All 5 open questions resolved under the operator ruling "безпека і передбачуваність понад швидкість":** own-kernel zero-dep engine (not TVM/Burn); inference-only (training stays edge/build-time); embedded weights as generated committed Rust `static [i8]` + `repr(align(64))` + SHA3 init self-check + `pq/codesign.rs` (objcopy/link_section parked with named trigger — **no `link_section`/`build.rs` precedent exists in-repo**, verified); i8-symmetric per-tensor quantization (65536-pair-exhaustive provable core law, `div_half_up` requantization); fold_transitions pinning MOOT pending item 33 (separate-core rejected on the `core_pinning.rs` DECART precedent; item 3's const-adjacency IS the static-pin shape). Binding laws recorded: zero new crates (the now-EMPTY allowlist gate enforces structurally), §4 hardening checklist unchanged (no parallel checklist), item-25 procedure for any dep question. 12 new items 33–44, dependency-ordered (33 ∥ 34 → 35 → {36 ∥ 37 ∥ 38} → 39 → 40 → 42 → 43 → 44, 41 off 35), each with a real proof condition; item 40's fail path composes with item 9's breaker but nothing gates on it. **Item 34 (pilot workload, the arc's one `NEEDS-OPERATOR-DECISION` gate) is now RESOLVED (2026-07-19): operator ruled SYNTHETIC/TOY PILOT FIRST** — a small hand-built synthetic classifier (toy MNIST-style/pattern classifier, KB-scale, zero product data/PII/risk) proving the whole determinism pipeline end-to-end before any real product workload; the three real-product surfaces (reranker/anomaly/ETA) are deferred to a follow-on pilot; consequently items 35–44 carry no PII/product-data handling and item 43's constant-time gate takes its cheap-optional branch for this pilot. Build execution of items 35–44 still awaits operator dispatch — synthesis → roadmap → ruling → THEN execution |
| Whole-system determinism & AI-optional invariant — research + synthesis + roadmap items 45–49 (Opus grounding / Fable synthesis split, 2026-07-19) | [RESEARCH-CRASH-CONSISTENCY-FORMAL-VERIFICATION-GUARDIAN-2026-07-19.md](RESEARCH-CRASH-CONSISTENCY-FORMAL-VERIFICATION-GUARDIAN-2026-07-19.md) (Opus grounding, 11 findings) + [CRASH-CONSISTENCY-FORMAL-VERIFICATION-GUARDIAN-SYNTHESIS-2026-07-19.md](CRASH-CONSISTENCY-FORMAL-VERIFICATION-GUARDIAN-SYNTHESIS-2026-07-19.md) (Fable synthesis) + [RAW-PROMPT-5-crash-consistency-formal-verification-fail-fast-guardian-2026-07-19.md](RAW-PROMPT-5-crash-consistency-formal-verification-fail-fast-guardian-2026-07-19.md) (verbatim source) + SPACE-GRADE roadmap **§I items 45–49** | **PLANNING ONLY — zero code; third wave of the SPACE-GRADE arc, first pass under the operator's Opus-research/Fable-synthesis role split.** Operator directive: 100% predictable + mathematically deterministic + safeguards, WHOLE system (not just the §H AI subsystem), AND the system must run with AI entirely absent — an enforced invariant, not an accident. Grounding headlines (research doc, all file:line-cited): kill-9 durability = pure **Sequential Append-only Log** (A/B 1 MiB segments + CRC-per-line; NOT pointer-swap, NOT hybrid — closes the raw prompt's open architecture question); no LSM/WAL+snapshot exists; Kani/TLA+ = zero usage (planned-only, items 7/10); seL4-"capability" and OTP-"supervisor" are terminology COLLISIONS with `capability_cert.rs` (authority-token) and `hub_supervisor.rs` (update-rollback) — flagged so never re-imported; TMR absent (owned by item 12); **AI-optional already TRUE** (`attention.rs` "the kernel stays non-AI"; core FSMs import zero AI modules); non-AI kernel numeric plane is f64 throughout. Synthesis rulings: Hybrid/LSM parked per-surface (FDR = rejected permanently, replay bounded by construction; event-log = measure-first, gated on item 2's wiring fix); watchdog scoped honestly to the HANG class (the one failure FDR is blind to — k3 deadlock precedent) as heartbeat + platform-restart, kernel never self-heals; Guardian = typed `Option<Proposal>` → parse-don't-validate `ValidatedProposal` gate + named static procedures (extends item 9, distinct from item 40's bit-integrity — semantic plane vs bit plane); static-procedure testing = exhaustive/oracle primary + proptest sweep + mandatory planted-rejection (house pattern, no new framework); **float judgment call: kernel-wide f64→fixed rewrite REJECTED as disproportionate** (the one real float bug ever = libm sin/cos ULP, fixed by Q30 CORDIC, ledger row 25; pinned-toolchain+zero-dep posture already delivers deployed-binary determinism) — scope = transcendental inventory + golden-coverage re-run under the pinned toolchain by CI's always-on full-suite + item-6 hardening-gate (item 14's bump gate itself only requires the spot-check artifact, verified), full conversion parked with named triggers; **AI-optional enforcement = item 45 `ai-optional-gate`** (non-default `inference` feature + default-build full-suite CI + dependency-direction check + P7 red-proof); Coq/Lean/proof-carrying-code OUT OF SCOPE (BITE/runtime-verification primary — where the source dialogue's own self-correction landed). 5 new items 45–49, dependency-ordered (45 ∥ 46 ∥ 48 ready; 47 after 35/42 composing with 9; 49 after item-2 fix), each with a real proof condition. Execution awaits operator dispatch — same synthesis → roadmap → ruling → THEN execution law |
| Validity (K3 admission), live-struct Sentinel & proportionate open-source hardening — research + synthesis + roadmap items 50–54 (Opus grounding / Fable synthesis split, 2026-07-19) | [RESEARCH-KLEENE-TRUTHFULNESS-OPENSOURCE-HARDENING-2026-07-19.md](RESEARCH-KLEENE-TRUTHFULNESS-OPENSOURCE-HARDENING-2026-07-19.md) (Opus grounding, 8 findings + terminology adjudication) + [KLEENE-TRUTHFULNESS-VALIDITY-SYNTHESIS-2026-07-19.md](KLEENE-TRUTHFULNESS-VALIDITY-SYNTHESIS-2026-07-19.md) (Fable synthesis) + [RAW-PROMPT-6-truthfulness-logical-deduction-kleene-unknown-2026-07-19.md](RAW-PROMPT-6-truthfulness-logical-deduction-kleene-unknown-2026-07-19.md) / [RAW-PROMPT-7-opensource-hardening-miri-kani-sentinel-shadow-mode-2026-07-19.md](RAW-PROMPT-7-opensource-hardening-miri-kani-sentinel-shadow-mode-2026-07-19.md) (verbatim sources, one combined dialogue) + SPACE-GRADE roadmap **§J items 50–54** | **PLANNING ONLY — zero code; fourth wave of the SPACE-GRADE arc.** **Terminology RULING (binding):** the two same-word "Truthfulness" concepts are GROUNDED-orthogonal (swarm-safety's = content-FREE byte-reproducibility; RAW-6's = content-BASED logical-deductive checking) — the NEW concept renames to **"Validity" (derivational validity)**; "Truthfulness" stays exclusively the swarm-safety arc's property (earlier-established + load-bearing there; the rename is also a logic-textbook CORRECTION — the checked property is validity-from-axioms, not world-truth — and lands on item 47's existing `ValidatedProposal` type, zero new vocabulary). Grounding headlines: no 3-valued logic exists in-tree (`Reading<T>` is 2-state named-absence); Miri/Kani/clippy/fmt run NOWHERE in CI; **kernel unsafe = 19 real blocks in 4 modules (arena 6, simd 5, fdr/pmu 5, householder 3) — independently re-verified 2026-07-19; the research/RAW "21 blocks/7 modules" figure was WRONG (messenger/slot_arena/chaos/bounded_drainer are unsafe-FREE, comment mentions only; `fdr/pmu` was omitted); `pq/` crypto ZERO — raw prompt's crypto guess also corrected**; no read-time live-struct sentinel exists (all integrity machinery is at-rest); shadow-mode is genuinely novel (every existing differential fails/rejects on disagreement); ADR-0020 public-flip + EUTM operator-gated, NOT imminent. Five scope RULINGS: (1) K3 = INTERNAL `TruthState` combinator inside item 47's `admit` + `RejectionClass::{Refuted,Undecidable}` — no third public control-flow arm, no parallel type, exhaustive 9-case truth-table proof, Evidence-based Unknown adopted (item 50); (2) shadow-mode ADOPTED full-design — FDR `Kind::ShadowDivergence`, digest-only, bounded emission, advisory-never-authority (item 51); (3) Miri ADOPTED scoped to the real unsafe modules (arena + scalar simd/householder; `fdr/pmu` post-FDR-merge) with the SIMD/PMU-intrinsic limitation stated honestly, own pinned analysis nightly, build pin untouched (item 52); (4) Sentinel read-time integrity check on critical LIVE structs **ADOPTED as item 54** (operator-reversed 2026-07-19 from an earlier draft rejection; deployment target corrected to **local/offline consumer hardware that typically LACKS ECC** → in-memory bit-flip risk real, not negligible — the "space-grade" quality standard binds regardless of substrate) — reuses the in-kernel CRC32 (zero new primitive/dep, max-nativeness law), checked at transition points not per-field-read, scoped to the live mutable authority structs item 40's read-only weight checksum does NOT cover (item 47 `Invariants` table, item 21 gain-schedule, live inference config); genuine item-40 overlap is a boundary, not a skip; (5) clippy/fmt/miri-required gates REAL but LOW-priority-LAST, trigger-promoted to pre-flip blocker on public-flip authorization (item 53). **Operator-facing flag (Part 4): items 1–49's actual CODE still lives ONLY on unmerged `exec/space-grade-tier0-2026-07-19` — `main` has docs only.** Execution awaits operator dispatch — same synthesis → roadmap → ruling → THEN execution law |

| Space-grade kernel — triple consistency audit + triple research wave (deployment/nativeness · binary-vs-Kleene · telemetry/AI-optional · OS-patterns · Kani-feasibility · resource-footprint/relational-telemetry, 2026-07-19) | [AUDIT-SPACE-GRADE-CONSISTENCY-DEPLOYMENT-NATIVENESS-2026-07-19.md](AUDIT-SPACE-GRADE-CONSISTENCY-DEPLOYMENT-NATIVENESS-2026-07-19.md) + [AUDIT-BINARY-VS-KLEENE-LOGIC-2026-07-19.md](AUDIT-BINARY-VS-KLEENE-LOGIC-2026-07-19.md) + [AUDIT-TELEMETRY-EVERYWHERE-AI-OPTIONAL-OS-2026-07-19.md](AUDIT-TELEMETRY-EVERYWHERE-AI-OPTIONAL-OS-2026-07-19.md) + [RESEARCH-OS-ARCHITECTURE-PATTERNS-ADOPTION-2026-07-19.md](RESEARCH-OS-ARCHITECTURE-PATTERNS-ADOPTION-2026-07-19.md) + [RESEARCH-NATIVE-KANI-REPLACEMENT-FEASIBILITY-2026-07-19.md](RESEARCH-NATIVE-KANI-REPLACEMENT-FEASIBILITY-2026-07-19.md) + [RESEARCH-RESOURCE-FOOTPRINT-ZERO-BLINDSPOT-RELATIONAL-TELEMETRY-2026-07-19.md](RESEARCH-RESOURCE-FOOTPRINT-ZERO-BLINDSPOT-RELATIONAL-TELEMETRY-2026-07-19.md) | **Six parallel read-only passes over the space-grade arc, 2026-07-19 — the inputs to the master-synthesis row below.** Headlines: (1) consistency audit found 4 deployment-context corrections (SIHFT §6 ECC-cloud valuation = the uncaught twin of the reversed Sentinel rejection; "x86 Hetzner" claim; BP-27 dev-host/deploy-target conflation; item-12 inherited rows) + the `kernel::json`/`fdr::json` unfiled dedup ticket + item-51's unnamed digest primitive — and cleared 18 scope-downs + 7 deployment statements as legitimate; (2) Kleene audit: 46 decision points → 8 SHOULD-BE-3-VALUED (headline: item 33's forced binary on noise-bound benches; `markov::Verdict` fail-open-to-Healthy conflating couldn't-measure with measured-healthy in FDR) / 27 correctly binary / 11 already correct; (3) telemetry audit: 13 real gaps G1–G13 (headline: agent-loop counts tokens, records zero wall-clock, bypassing the ONE timed path) + the work-normalized cost ledger design + the honest AI-optional verdict (item 45 = spec not flag; kernel AI-free by convention, not gate); (4) OS-patterns research: full seL4/OTP/IPC-bus/preemptive-RTOS = category mismatches (single-process synchronous library, F1–F5 grounded); 3 genuine adoptions — capability-secure declarative composition root (backed by item 2's proven defect — strongest), typed in-process AI-capability boundary, temporal TMR (merges into item 12) — + periodic durable-log scrub as the one journaling gap; (5) Kani feasibility: 16/22 targets need zero Kani (native exhaustive idiom suffices), 0/22 need a SAT solver, Kani = CI-time terminal-state-(c) boundary → enacted as item 7's blueprint v2 rescope (16 native + 4 Kani + 2 → item 8); (6) resource-footprint research: water/carbon = derived constant-multiplied views of joules (never new measured fields); "zero blind spots" honestly = zero UN-NAMED blind spots; FDR schema confirmed FLAT/UNLINKED (span_id/parent_span_id proposed); the 10-step completeness procedure; COVERAGE-COMPLETE-PRECISION-HONEST oracle principle (ORACLE-EXACT/BOUNDED/MEASURED-ONLY, cost as a correctness-proof byproduct, spectral reuse genuine at graph level / forced metaphor per-leaf); digital twin split into buildable state-mirroring (A) vs aspirational auto-optimizing (B) with one small honest first step (B′) |
| Space-grade kernel — MASTER SYNTHESIS pass: corrections applied + items 55–78 + telemetry-completeness procedure + governed self-evolution (2026-07-19) | `MASTER-SYNTHESIS-CONSISTENCY-TELEMETRY-DIGITAL-TWIN-2026-07-19.md` — **deleted 2026-07-20, merged into `ROADMAP.md` §8** (operator-facing summary) + [PROCEDURE-TELEMETRY-COMPLETENESS-STANDING-2026-07-19.md](PROCEDURE-TELEMETRY-COMPLETENESS-STANDING-2026-07-19.md) (NEW binding-on-ratification 13-step procedure, item-25 pattern) + `ROADMAP.md` Part V **§K items 55–72 + §L items 73–78** | **STALE ROW — superseded by `ROADMAP.md` Part V's 2026-07-20 reconciliation table and D11 (`DECISIONS.md`): items 55–56 are now DONE-VERIFIED, items 67–72 are now DONE-VERIFIED, items 73–78 have a full apply-token design ruled (D11) though still spec-only for code dispatch. This row's "PLANNING ONLY, all files staged NOT committed" describes 2026-07-19, not today — read `ROADMAP.md` Part V's reconciliation table for current truth, not this row.** Merges the six audit/research inputs above + a seventh mid-pass operator directive (AI may build/change the internal OS, never core/red-lines, only under explicit human approval, same space-grade telemetry standard; self-healing + self-upgrading included; the gate itself structurally unbypassable). **Corrections applied in place (staged):** SYNTH §6/§9.12/§11-T2 retro-corrected (SIHFT ECC-cloud valuation withdrawn — same treatment as the item-54 reversal; "x86 Hetzner" → local/offline consumer x86_64+wasm32); BP-27 §5 dev-host/deploy-target conflation fixed (KVM cloud dev guest ≠ local target; availability typically inverts there); roadmap item 12 re-scoped in place as **temporal TMR** (audit finding + OS-patterns research MERGED — one item, no new number) with `VoteOutcome` FDR class; item 7's entry aligned to its enacted v2 rescope; `kernel::json` dedup ticket FILED (dual-Keccak format); item 46 fleet-heterogeneity sentence + item 51 digest primitive named. **New items, dependency-ordered, real proof conditions:** §K 55–72 (K3 verdict-class spec retrofit; markov/spectral epistemic-basis retrofit; procedure ratification + HOT-PATHS `eff` column; work-normalized cost ledger; agent-turn/engine/kernel-counter gap closures; FDR `span_id`/`parent_span_id` relational linkage + wasm clock leg; item-45 disposition-table extension; capability-secure composition root (subsumes item-2 fix); typed AI-capability boundary; periodic scrub; cost-oracle backfill; cost-as-proof-byproduct; water/carbon derived views; state-mirroring digital twin (A) REAL; cost-aware eqc-rs extraction (B′) small+gated; auto-optimizing twin (B) EXPLICITLY ASPIRATIONAL, entry-criteria only); §L 73–78 (Governed Self-Evolution: the Gate-Root Invariant FIRST — gate/registry/verification structurally unmodifiable by the pipeline they govern, out-of-band-only changes, the corrigibility problem addressed structurally; grounded red-line registry, every row cited; typed `ChangeProposal` pipeline — kernel's own gates re-executed, explicit human apply-token, silence ≠ consent; FDR lineage + cost-classified impact at the approval seam; self-healing = same pipeline for one trigger class — health classifiers grounded as mostly-already-built (`DriftClass` has a real fail-closed consumer, `markov::Verdict` observational-only), restart-recovery stays automatic, code-level fixes never autonomous, MAPE-K cited with the human-as-Execute deviation; self-upgrading = same gate, no beneficial-change exception). G10 (bebop NTT zero perf data) + G13 (apps/api) recorded as named out-of-arc flags. All files staged on main, NOT committed — operator review gates everything |
| Space-grade kernel — Verified-Status Ledger: operator-authorized autopilot-to-completion execution pass over the remaining blueprint items (2026-07-20) | [SPACE-GRADE-VERIFIED-STATUS-LEDGER-2026-07-20.md](SPACE-GRADE-VERIFIED-STATUS-LEDGER-2026-07-20.md) | **Previously unlinked from this index (found + fixed 2026-07-20 by the roadmap-gap re-audit below).** Operator ruling that unblocked the row above ("continue on autopilot until all are done... give permission to work on all... red-line/operator can work on them too — if decision is needed, stop & ask"). Discipline: verify-what-already-works-first, never re-implement shipped code, register the missing `HOT-PATHS.tsv` rows the blueprints require, mark genuine ceilings with upgrade triggers. Evidence is live `cargo test --offline --lib [--features pq]` on `exec/space-grade-items-2026-07-20`, main baseline 1046→1052 after item 20. Status-key discipline (`DONE-VERIFIED` / `DOC/CI-ONLY` / `GATED/HOLD` / `NEW-BUILD`) mirrors the Q-series `verified-by` pattern rather than inventing a parallel one. Items closed this pass include 8 (Kani GCRA), 9, 20-24, 26 (group-commit — see the item-26 row above), 48, 50, 54, 55-56, and more per the ledger's own tables. **Independent verification caveat (2026-07-20, this re-audit):** items 45 (`ai-optional-gate`) and 73/74 (Gate-Root Invariant + red-line registry) have real, substantial standalone scripts on disk (`scripts/ai-optional-gate.sh`, `scripts/gate-root-invariant.sh`, `scripts/red-line-classifier.sh`, `scripts/red-line-monotonicity.sh`, 56-239 lines each, landed via `cb00706b1`) but **none are wired into any `.github/workflows/*.yml` job** — the CI-enforcement these items' own spec text describes is not yet live; see the correction added directly to those items in `SPACE-GRADE-KERNEL-EXECUTION-ROADMAP-2026-07-19.md` |
| Two 2026-07-19/20 docs previously reachable only at 3 hops from this index (violated this file's own ≤2-hop rule; found + fixed 2026-07-20 by the roadmap-gap re-audit below) | [LIVING-MEMORY-WAVE-PROPAGATION-FINISHING-LAYER-SYNTHESIS-2026-07-20.md](LIVING-MEMORY-WAVE-PROPAGATION-FINISHING-LAYER-SYNTHESIS-2026-07-20.md) + [OPTICAL-COMPRESSION-DECISION-2026-07-19.md](OPTICAL-COMPRESSION-DECISION-2026-07-19.md) | **Living memory:** "Status: RESEARCH SYNTHESIS ONLY — not a blueprint, no build items, no DoD, no code," per its own header — reconciled via ~30 operator rulings, see MEMORY `living-memory-finishing-layer-status-2026-07-20.md`, `main`@`5506d6647`. **Optical compression:** Phase-A operator ruling — **IN (archival-plane-only, pilot scoped per Phase B)**, dated 2026-07-19, blueprint is `BLUEPRINT-ITEM-28-optical-compression-2026-07-19.md` (already index-linked via the space-grade roadmap doc; this ruling doc itself was the missing direct link) |
| Product surface wave: spatial storefront/voice hub, concurrency architecture, offline resilience, media/comms/agentic autonomy, intent interface (5-doc synthesis series, 2026-07-20) | [BLUEPRINT-SPATIAL-STOREFRONT-VOICE-HUB-SYNTHESIS-2026-07-20.md](BLUEPRINT-SPATIAL-STOREFRONT-VOICE-HUB-SYNTHESIS-2026-07-20.md) + [CONCURRENCY-ARCHITECTURE-SYNTHESIS-2026-07-20.md](CONCURRENCY-ARCHITECTURE-SYNTHESIS-2026-07-20.md) + [OFFLINE-RESILIENCE-SYNTHESIS-2026-07-20.md](OFFLINE-RESILIENCE-SYNTHESIS-2026-07-20.md) + [MEDIA-COMMS-AGENTIC-AUTONOMY-SYNTHESIS-2026-07-20.md](MEDIA-COMMS-AGENTIC-AUTONOMY-SYNTHESIS-2026-07-20.md) + [BLUEPRINT-INTENT-INTERFACE-ONE-SCREEN-2026-07-20.md](BLUEPRINT-INTENT-INTERFACE-ONE-SCREEN-2026-07-20.md); rulings in `DECISIONS.md` **D13/D14** | **PLANNING — two code changes landed same-day in the SPACE-GRADE track, unrelated to this row's own scope (see the item-26 row above and §M in `SPACE-GRADE-KERNEL-EXECUTION-ROADMAP-2026-07-19.md`, NOT "item 5" — item 5 is the already-closed regex-retirement item; cross-mesh replication is tracked out-of-band as §M, "not one of the original 78 items"); everything else here is design.** Five same-day Opus-research/Fable-synthesis docs, each independently code-grounded against live `HEAD` (not assumed), reconciled into one coherent DeliveryOS product-surface arc, **all operator decisions RULED same-day (D13/D14)** — nothing here is pending confirmation. **(1) Spatial storefront + voice hub:** 5 Opus passes (photogrammetry/capture, AR/volumetric display, SMPL/GVHMR/FaceAnything, RAG+CAG vs the actual retrieval code, local voice) → browser AR ships first (`<model-viewer>`→AR Quick Look/Scene Viewer, zero new hardware); SMPL/GVHMR/FaceAnything REJECTED (GPU-bound, non-commercial licenses, courier-analytics use would violate the no-scoring CI gate); agent has ZERO retrieval tools today despite the kernel already having real hybrid BM25+PPR retrieval + a wired-but-unused local embed port — the real gap; voice = sync thread cascade (confirmed valuable by operator), 6 phases. 7/8 open decisions ruled directly (O1-O4,O6-O8); O5 escalated into (2). **(2) Concurrency architecture (D13's scoping pass, CONFIRMED):** reconciles the operator's D13 tokio-reversal ruling against 2 Opus passes (native-concurrency/exokernel-inspired design; the end-to-end principle) — evidence does NOT support literal "tokio everywhere": `ToolPort`/`agent-loop`/every kernel port stay synchronous **permanently** (`ToolPort` is the kernel's fail-closed capability-check endpoint — an inserted async executor would weaken the same-call-stack enforcement property); LLM streaming ships via blocking SSE on the existing worker pool, zero runtime change; tokio's *only* evidence-backed entry point is the not-yet-built `mesh-adapter` networking layer, gated on a ~1,000–2,000-concurrent-peer-socket threshold; io_uring/kernel-bypass prohibited (binding, io_uring's own documented exploit history + D0 reliability-over-latency). Resolved as **"async only where it brings value"** after the operator asked "why not async everywhere" and received the full reasoning. **(3) Offline resilience:** code audit REFUTES two hypothesized gaps — payment is cash-only and structurally bulkheaded from lifecycle progress (order completes with settlement-pending, never blocked on it); the storefront/owner-hub already run the full kernel as WASM client-side (no per-action server round-trip) — a warm session already works fully offline. 3 REAL gaps: no live mesh transport exists anywhere (offline≡online today, nothing to lose yet); `web/` has no Service Worker (cold loads need network, **ratified as an in-doctrine JS exception** per D14); no browser-side persistence (IndexedDB adapter mirroring `FileEventStore`'s role). CAP/PACELC framing: dowiz's order path is explicitly **PA/EL** (Partition→Availability, Else→Latency), a deliberate D0 consequence, not a gap to fix with a quorum. **(4) Media/comms + granular agentic autonomy:** `kernel/src/chunker.rs`+`backup.rs` already ARE ~80% of a Perkeep/Git-shaped content-addressed media backend (confirmed by code reading) — the gap is a metadata/manifest layer + a `Media` capability resource, not a from-scratch build. SimpleX Chat researched precisely (unidirectional per-queue addresses, PQ-hybrid double-ratchet, 2× Trail-of-Bits-audited) — 3 specific ideas borrowed (invitation bootstrap, per-contact addressing, PQ-ratchet-as-reference), wholesale no-identifier adoption REJECTED (fights the event log's deliberate `actor_pubkey` attribution money/order authority needs). **Every capability — media/file/communication/inventory/menu/ordering — is a symmetric human-OR-agent path, owner's per-layer/per-feature/per-action choice** (the hub agent modeled as just another delegated keypair — zero rework to the existing person-agnostic capability substrate); **red-line resources (`Ledger`/`Auth`/`Secret`/`Migration`) are a hard, non-configurable, human-only ceiling — structurally excluded from any agent-keypair delegation, not merely denied-by-default** (D14). Customer-leg chat encryption ruled **native PQ-hybrid ratchet** on dowiz's own KAT-gated primitives (not the SimpleX CLI sidecar) — real new crypto-surface work accepted for full sovereignty. Media storage ruled **local-disk-only, off-hub backup/CDN is explicitly the hub owner's own responsibility to connect**, diverging from the synthesis's own recommendation (operator's stated reason: keep the dowiz-provided surface minimal). Staff model: `StaffMember` + Owner/Kitchen/Counter-Manager presets (capability substrate needs zero rework — a staff member is just another narrowed `CertDelegation`). **(5) Intent interface (newest, same-day):** corrects a FALSE claim already caught by an internal audit (`docs/research/AUDIT-2026-07-18-FEYNMAN-meta-patterns-math.md` FEYNMAN-01) that "ONE operator MÜ+ΓU̇+c²LU=S draws the whole UI" — replaced with the true, stronger story: one shared, well-tested Laplacian primitive (`kernel/src/csr.rs::laplacian_spmv`) reused honestly across several genuinely different, individually-proven dynamics (screened-diffusion `field_frame.rs`, uncoupled `motion.rs::Spring`, modal `field_modal.rs`). Headline: the requested "one-screen, intent-driven, minimal-chrome" interface is NOT a new architecture — `engine/src/intent.rs`+`compose_ui.rs` already implement it (built, tested, wired into the production loop, consequential-action firewall proven by tests, 122/122 green), just never connected to real content or styled; the unbuilt "Sea & Sheet" visual grammar (`docs/design/dowiz-interfaces/`) is the missing middle layer. Two operator-provided external reports (browser GPU neural-field rendering+sonification; native Rust+wgpu AR compositing) triaged: GPU report's architecture (not its neuroscience) maps onto the existing `field_frame` substrate, gated on one named unlock (operator network grant for `cargo add wgpu` — `engine/Cargo.toml`'s `gpu`/`webgpu` features are a real off-by-default seam, not unsolved design); native AR report split into 3 buckets — already-shipping via (1)'s `<model-viewer>` Quick Look, a real unmade native-companion-app decision (full camera-compositing stack, deliberately NOT proceeded), and cheaply-reusable-in-browser ideas (curl-noise particles, brand-token LUT grading, degrade-to-match-camera as a shader discipline). 3-stage build sequence (Stage A = wire two already-tested endpoints together, ~zero technical risk, 6 falsifiable RED→GREEN acceptance criteria given; Stage B = gated on the wgpu grant; Stage C = gated on the native-app decision, does not proceed). **Cross-doc dependency, stated in both places so they stay consistent:** offline resilience's future mesh-transport N-peer daemon defers its runtime choice to concurrency architecture's §5.4 tokio-entry-gate; intent interface's Sea parameters render media/comms' `PendingIntent`/autonomy-mode state as first-class field states, never a popup. **Not yet built anywhere in this row** — every doc is PLAN status; this row exists so the whole wave is discoverable from the one navigation root per this file's own "every planning doc gets a row" rule |
| Remaining-queue wave: Telegram ops-hub build-out, payment adapter residual, omnichannel order intake, local-model wiring (4-doc batch, 2026-07-20, PLAN except Telegram item 1) | [BLUEPRINT-TELEGRAM-OPS-HUB-REMAINING-BUILD-ORDER-2026-07-20.md](BLUEPRINT-TELEGRAM-OPS-HUB-REMAINING-BUILD-ORDER-2026-07-20.md) + [CORE-ROADMAP-2026-07-17/BLUEPRINT-P60-payment-adapter-residual-2026-07-20.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P60-payment-adapter-residual-2026-07-20.md) + [CORE-ROADMAP-2026-07-17/BLUEPRINT-P48-INTAKE-omnichannel-order-intake-2026-07-20.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P48-INTAKE-omnichannel-order-intake-2026-07-20.md) + [CORE-ROADMAP-2026-07-17/BLUEPRINT-LOCAL-MODEL-WIRING-TESTING-USAGE-2026-07-20.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-LOCAL-MODEL-WIRING-TESTING-USAGE-2026-07-20.md) | Closes the last 4 items of this session's task queue. **(1) Telegram ops-hub:** formalizes the existing design doc's build-order items 2-7 into 5 DoD-bearing phases (CI/CD+pinned live-board upgrade, S0/S1 mirror-rule formalization, LOGS topic, KERNEL/MESH+MEMORY/DOCS topics, roadmap-gated enrichment); item 1 (`topics resources`) already shipped this session (`9f94547ca`). **(2) Payment adapter (P60 residual):** headline correction — the online-fiat payment core (`IdempotencyKey`/`ClientHandoff`/webhook-as-sole-truth-writer/N-leg saga/refund routing, `kernel/src/ports/payment_provider.rs`) is already built and proptest-gated; residual scope is the out-of-kernel `payment-adapters` crate (ureq+hmac+sha2, `async-stripe` explicitly rejected), webhook-inbound routes on `native-spa-server`, the D12 §4-A owner-desktop webview shell, and Albania/EU PSP selection (Stripe as reference-only, likely not the launch rail — Paysera/Viva Wallet named). Direct-vs-destination charges recorded as an open decision, not silently resolved; a new `no-agent-capture-path` gate specified alongside the existing `firewall_p47`/`no_card_data` firewalls. **(3) Omnichannel intake (P48-INTAKE):** builds the inbound mirror of P43's outbound-only channel port, reusing P48's own pre-defined `InboundMessage`/`IntentOutcome`/`OrderIntent`/`IntentParser` vocabulary verbatim (zero new types invented). Tiers channels by real owner-achievability: Telegram+website first (self-serve, free, build first), SimpleX dowiz-facilitated (cross-referenced to the media-comms synthesis, not redesigned), WhatsApp dowiz-shepherded (Business Verification can be denied — named honestly as a real support-cost commitment, not a switch), Instagram/Facebook deferred (Meta App Review is a multi-week deniable gate). `ChannelKind::Linktree` explicitly ruled out as a hard guard — Linktree is an outbound-redirect-only discovery surface with no inbound-message API, never an intake source; reused instead as a generated button-page (`kernel/src/messenger.rs` deep-links). **(4) Local-model wiring:** smallest-scope of the four — confirms `llm-adapters`' existing HTTP-client-to-Ollama architecture (sync, `ureq`, no tokio, ~2-worker dispatch) is correct and locks it in as the one thing NOT reopened, then closes one concrete wiring gap (`AiMode`/`BackendConfig` exists and is fully unit-tested in `kernel/src/ports/llm.rs` but `compose.rs` still hardcodes `OllamaAdapter::new(&self.base)`, never consulting it — `StackBuilder::from_config` closes it) plus one new Rust-native golden tool-calling test suite (BFCL-style, scored on tool selection not prose, includes negative/ambiguous cases) proposed to replace the stranded, CI-disconnected `eval-layer/` Python prototype. Fine-tuning ruled out now with a named future trigger (a narrow intent/order classifier that measurably plateaus under prompting); model-routing/cascading explicitly deferred to the separate HK-05/HK-09 research arc (`hermes-agent-kernel-rewrite` repo) rather than redesigned here. All three of (2)-(4) independently code-grounded against live `HEAD` before writing (file existence, line numbers, type signatures checked, not merely asserted by the synthesis pass) |

## 8. Former master roadmaps (2026-07-20: deleted, fully merged into `ROADMAP.md`)

This section used to list six master docs, one canonical and five superseded-but-kept. As of
2026-07-20 (operator directive: "merge all roadmaps into one, delete the old ones") they no longer
exist as separate files: the canonical one is fully inlined verbatim as `ROADMAP.md` Part II; the
five historical ones (plus 8 more top-level roadmap-shaped docs found by the same pass) are
deleted outright, their essential facts already folded into `ROADMAP.md`'s chronological
narrative (§1–§9 there). Recover any of them via `git log --all --diff-filter=D -- 'docs/design/<name>.md'`
if ever needed — nothing is destroyed, just no longer part of the live tree.

| Doc (deleted) | Where its content lives now |
|---|---|
| `MASTER-ROADMAP-SOVEREIGN-ARCHITECTURE-2026-07-16.md` | `ROADMAP.md` Part II (verbatim), summarized in `ROADMAP.md` §5 |
| `MASTER-BUILD-SEQUENCE-UPDATED-2026-07-11.md` | `ROADMAP.md` §4 (fact only — this one was already dead weight, not inlined verbatim) |
| `MASTER-EXECUTION-PLAN-2026-07-13.md` | `ROADMAP.md` §4 |
| `MASTER-INTEGRATION-PLAN-2026-07-14.md` | `ROADMAP.md` §4 |
| `MASTER-ROADMAP-10-PHASES-2026-07-14.md` | `ROADMAP.md` §4 |
| `[`../../MASTER-ROADMAP-MVP-2026-07-12.md`](../../MASTER-ROADMAP-MVP-2026-07-12.md) (repo root)` | **Not part of this pass** — lives outside `docs/design/`, out of scope for this specific merge; still on disk, still bannered historical as before |
| `CORE-ROADMAP-STANDARD-2026-07-17.md` | `ROADMAP.md` Part III (verbatim) |
| `GROUND-TRUTH-2026-07-17.md` | `ROADMAP.md` §6 (fact only) |
| `GROUND-TRUTH-2026-07-19-FINAL.md` | `ROADMAP.md` Part IV (verbatim, historical) |
| `ROADMAP-GROUND-TRUTH-2026-07-11.md` | `ROADMAP.md` §2 |
| `ROADMAP-GROUND-TRUTH-2026-07-14.md` | `ROADMAP.md` §4 |
| `ROADMAP-LIVE-STATUS-2026-07-18.md` | `ROADMAP.md` §7 |
| `ROADMAP-UPDATE-SESSION-SYNTHESIS-2026-07-18.md` | `ROADMAP.md` §7 |
| `ROADMAP-RECHECK-SESSION-SYNTHESIS-2026-07-19.md` | `ROADMAP.md` §8 |
| `TELEGRAM-ROADMAP-SUMMARY-2026-07-18.md` | `ROADMAP.md` §7 |
| `MASTER-ROADMAP-SWARM-SAFETY-TELEMETRY-FIRST-2026-07-19.md` | `ROADMAP.md` §7 |
| `MASTER-SYNTHESIS-CONSISTENCY-TELEMETRY-DIGITAL-TWIN-2026-07-19.md` | `ROADMAP.md` §8 |
| `SPACE-GRADE-KERNEL-EXECUTION-ROADMAP-2026-07-19.md` | `ROADMAP.md` Part V (verbatim, reconciled) |

## 9. Previously-orphaned arcs — now absorbed into SOVEREIGN §10 (2026-07-18)

The 2026-07-18 audit's biggest finding: the ten arcs below had **ZERO reference in this index**
before today. Each is now owned by a P31–P46 phase (SOVEREIGN §10.2 = fast phase index;
§10.5.1–5 = full per-phase DoD/anti-scope). MEMORY-only arcs are cited by filename, not linked
(they live outside the repo, in the agent memory corpus).

| Arc dir/doc | Absorbed into | One-line note |
|---|---|---|
| [field-ui-engine/](field-ui-engine/) + [field-ui-engine.md](field-ui-engine.md) | P38a | FE-01..17; math substrate DONE (compose/zerocopy/widget_store/loop_/motion/money_guard), GPU path 0% — gated on the O18a `graphics-unlock` (`cargo add wgpu`) |
| [dowiz-interfaces/](dowiz-interfaces/) | P38b | DZ-01..12 Sea & Sheet surfaces; DZ-10 voice deliberately Phase-9b-deferred, not lost |
| [rust-engine-rewrite/](rust-engine-rewrite/) | P37 + P38a | RW-01..12; RW-02/03/06/07/08 DONE substrate; RW-09 thin-shell rule → P37; RW-01/04/05/10/11 → P38a |
| integration-ports arc — [`integration-ports/BLUEPRINTS-INTEGRATION-PORTS.md`](integration-ports/BLUEPRINTS-INTEGRATION-PORTS.md) *(real on-disk blueprint, previously cited as MEMORY-only despite existing — found + fixed 2026-07-20)* | P42 + P43 (+ existing P22) | IP-08→P42, IP-11/12/13/14/19/20→P43, IP-10/15/16→P22 (not renumbered); IP-01/02/03/04/05/07→already covered by P40/P42 (cross-ref fix, resolved 2026-07-19); IP-06→future blueprint candidate (deferred, no GPU pipeline yet); IP-09→P95 (living-memory); IP-17/18→operator-gated crypto red-line; IP-21→verification scaffolding, downstream — see `GAP-A1-DISPOSITION-AUDIT-2026-07-19.md` |
| ecosystem-strategy arc — [`ecosystem-strategy/BLUEPRINTS-ECOSYSTEM.md`](ecosystem-strategy/BLUEPRINTS-ECOSYSTEM.md) *(real on-disk blueprint, previously cited as MEMORY-only despite existing — found + fixed 2026-07-20)* | P44 + P46 | EC-05 + scale-out units → P44 (LOW PRIORITY); EC-17 + remainder → P46 (FURTHEST FUTURE); shared-KernelFacade concept already built in PROTOCOL |
| ops-reliability arc — [`ops-reliability/BLUEPRINTS-OPS-RELIABILITY.md`](ops-reliability/BLUEPRINTS-OPS-RELIABILITY.md) *(real on-disk blueprint, previously cited as MEMORY-only despite existing — found + fixed 2026-07-20)* | P45 | OPS-01..22; attic-revival path dead twice over — formally superseded by the native pgrust rebuild (§7 row above) |
| [`realtime-change-intelligence-2026-07-17/`](realtime-change-intelligence-2026-07-17/) *(previously unreachable within ≤2 hops despite being cited by 2 already-linked blueprints — found + fixed 2026-07-20)* | P26/P28 (LLM cache) | Cache-reference-graph + fault-isolation feeder research; see the P26/P28 rows above for build status |
| [`hermes-kernel-rewrite-2026-07-15/`](hermes-kernel-rewrite-2026-07-15/) · [`organism-status-2026-07-15/`](organism-status-2026-07-15/) · [`tech-synthesis-2026-07-15/`](tech-synthesis-2026-07-15/) *(previously unreachable within ≤2 hops — found + fixed 2026-07-20)* | AGENT / infra | Agent-tooling rewrite plan / a closed status snapshot / a tech-adoption survey; one landed deliverable traced to the first — `kernel/src/harmonic.rs`'s own doc comment confirms the port |
| Standalone design docs previously unreachable within ≤2 hops (found + fixed 2026-07-20) | various | [`AUTONOMOUS-ORGANISM-SYNTHESIS-2026-07-14.md`](AUTONOMOUS-ORGANISM-SYNTHESIS-2026-07-14.md) · [`BLUEPRINT-W17-web-ui-restore.md`](BLUEPRINT-W17-web-ui-restore.md)/[W19](BLUEPRINT-W19-kalman-trigram-integration.md)/[W20](BLUEPRINT-W20-vertexbridge-gpu-gate.md)/[W22](BLUEPRINT-W22-governance-doc-retro.md) (siblings W18/W21 already linked; the whole W17-W22 wave already confirmed shipped/green by the 2026-07-19 audit — this is link-hygiene only) · [`launch-design-brief.md`](launch-design-brief.md) · [`spectral-graph-fsm.md`](spectral-graph-fsm.md) · [`SWARM-QUANT-BLUEPRINT-2026-07-15.md`](SWARM-QUANT-BLUEPRINT-2026-07-15.md) · [`SYSTEMS-GPU-ML-KERNEL-SYNTHESIS-2026-07-16.md`](SYSTEMS-GPU-ML-KERNEL-SYNTHESIS-2026-07-16.md) · [`WEB3-SYNTHESIS-INVISIBLE-AGENTIC-LOCAL-INFRA-2026-07-17.md`](WEB3-SYNTHESIS-INVISIBLE-AGENTIC-LOCAL-INFRA-2026-07-17.md) |
| `docs/research/` docs previously unreachable within ≤2 hops (found + fixed 2026-07-20) | various | [`../research/BLUEPRINT-W13-pgrust-adapter.md`](../research/BLUEPRINT-W13-pgrust-adapter.md) · [`../research/BLUEPRINT-W14-mesh-discovery-gossip.md`](../research/BLUEPRINT-W14-mesh-discovery-gossip.md) · 3 sibling council critiques to the already-linked FEYNMAN one: [`../research/AUDIT-2026-07-18-ARCHITECT-whole-system-hostile.md`](../research/AUDIT-2026-07-18-ARCHITECT-whole-system-hostile.md) · [`../research/AUDIT-2026-07-18-HERZOG-interface-design.md`](../research/AUDIT-2026-07-18-HERZOG-interface-design.md) · [`../research/AUDIT-2026-07-18-TORVALDS-code-quality.md`](../research/AUDIT-2026-07-18-TORVALDS-code-quality.md) |
| [mesh-real/](mesh-real/) | P34 + P34B | MESH-01..13 (~70% built + PROVEN, 100% stranded pre-P34 — the roadmap's #1 lever); MESH-14 small docs+CI-lint blueprint written → `mesh-real/BLUEPRINT-MESH-14-DISPOSITION-2026-07-19.md` (parts 1–3 doable in-repo; part 4 bebop-repo gated; part 5 folds into Q1) — GAP-A1 closed for closeable items, see `GAP-A1-DISPOSITION-AUDIT-2026-07-19.md` |
| [docker-swap/](docker-swap/) | P35 | DK-01..10; DK-01/02/03/07 DONE with real `wasip2` component + deny-by-default WASI host — previously zero index reference despite working code |
| [math-first-architecture-blueprint.md](math-first-architecture-blueprint.md) | P31 | S0/S1/S2/S4 DONE; S3→P31c, S5→P31d, S6(=B3, counted once)→P31e, S7 parked→PROTOCOL |
| [hydraulic-loop-v2/](hydraulic-loop-v2/) | P32 + P33 | BP-01..23; resonator/DMD wiring gaps → P32b/P32c; BP-03/04/11/12–21/23 unconfirmed-status audit → P33b |

## 10. New phases P47–P56 (+P97, 2026-07-20) + operational runbooks (2026-07-18, same-day follow-ups)

Genuinely new work, not absorbed-orphan-arc registration — each links to its blueprint file
directly. Full DoD/anti-scope lives in the blueprint; SOVEREIGN §11–§15 carry the phase-entry
summaries (Absorbs/Status/Role/DoD/Anti-scope/Depends-blocks) in the same template as §10.5.

| Phase / doc | Component | Blueprint | One-line note |
|---|---|---|---|
| P40 | AGENT | [CORE-ROADMAP-2026-07-17/BLUEPRINT-P40-agent-loop-tool-wiring.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P40-agent-loop-tool-wiring.md) (2026-07-18, the executor build) + [CORE-ROADMAP-2026-07-17/BLUEPRINT-P40-AGENT-EXECUTOR-PRODUCT-WIRING-2026-07-19.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P40-AGENT-EXECUTOR-PRODUCT-WIRING-2026-07-19.md) (2026-07-19, the product-wiring follow-up) | **Both previously unlinked from this index (found + fixed 2026-07-20 by the roadmap-gap re-audit below), despite being heavily cited in prose by P41/P42/P43/P44/P48/P54/§1's crosswalk and the WIRING WAVE row above.** First doc: `AgentLoop` executor (`kernel/src/agent/loop.rs`, 651 lines, fail-closed) built same-day it was written. Second doc: fills the gap the Wave-1 recon flagged — the executor had zero product callers until `native-spa-server` was wired to it (DECART-**B** proxy, money-law firewall, `e375043d7`, see the WIRING WAVE row above) |
| P47–P50 | DELIVERY / ECOSYSTEM-OPS | [CORE-ROADMAP-2026-07-17/BLUEPRINT-P47-P50-gap-closing-phases.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P47-P50-gap-closing-phases.md) + [BLUEPRINT-P48-owner-hub-surface.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P48-owner-hub-surface.md) (P48 promoted to its own file) | Payment rails, owner hub, customer identity, legal/first-order gate — all 3 operator rulings RESOLVED (SOVEREIGN §11) |
| P51 | DELIVERY | [CORE-ROADMAP-2026-07-17/BLUEPRINT-P51-open-map-routing.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P51-open-map-routing.md) | OSM vector + field-rendered routes; satellite imagery rejected on cited resolution physics |
| P52 | DELIVERY | [CORE-ROADMAP-2026-07-17/BLUEPRINT-P52-courier-working-surface.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P52-courier-working-surface.md) | Closes the MVP audit's #1 finding — courier had zero owned UI despite the most-built protocol side. Kernel-side fold/law/capture (K1-K8) only — **P71 (below) is "P52-rev", the rendered voice-primary surface built on top; disposition confirmed 2026-07-20, see `ROADMAP.md` §15.4 G1** |
| P53 | DELIVERY | [CORE-ROADMAP-2026-07-17/BLUEPRINT-P53-tor-onion-integration.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P53-tor-onion-integration.md) | Activates fold-in ledger L4; system-`tor` sidecar not arti (arti hosting still experimental) |
| P54 | AGENT | [CORE-ROADMAP-2026-07-17/BLUEPRINT-P54-llm-agent-verification-harness.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P54-llm-agent-verification-harness.md) | LLM/agent adversarial probes; fine-tuning DEFERRED against the operator's own glossary criteria |
| P55 | PROTOCOL/CORE | [CORE-ROADMAP-2026-07-17/BLUEPRINT-P55-protocol-ecosystem-testing.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P55-protocol-ecosystem-testing.md) | Regression taxonomy from 4 real same-session bugs; proptest confirmed already-live (400 cases) |
| P56 | ECOSYSTEM-OPS | [CORE-ROADMAP-2026-07-17/BLUEPRINT-P56-verification-harness-infrastructure.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P56-verification-harness-infrastructure.md) | Shared storage/scheduling/meta-verification substrate for P54+P55; `hetzner:dowiz/test-results/` sync |
| P97 (added 2026-07-20) | AGENT (mobile half feeds DELIVERY P52/P71) | [CORE-ROADMAP-2026-07-17/BLUEPRINT-P97-local-mobile-model-selection-topology-2026-07-20.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P97-local-mobile-model-selection-topology-2026-07-20.md) | Local/mobile model selection + CPU-only serving topology: LFM2.5-VL-450M vs SmolVLM-256M mobile pick (LFM Open License $10M revenue cap = named operator ruling O-1, not a footnote), Ollama-native E/S/G/C residency plan + static TaskClass routing + exactly one deterministic intake-assist cascade, 5-cell measured concurrency matrix before shipping (bandwidth-honest, 4-physical-core box); P54 training-deferral REAFFIRMED (AMX absence live-verified on the box) + optional CPU-LoRA wall-clock probe (≤24h falsifiable criterion). Companion to the local-model-wiring blueprint — wiring vs which-models split; learned routing stays out (HK-05/HK-09 non-duplication ruling stands) |
| P21 Part 2 | AGENT | [CORE-ROADMAP-2026-07-17/BLUEPRINT-P21-local-llm-hermes-native.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P21-local-llm-hermes-native.md) §11 | Tiered-Intelligence architecture evaluated against operator proposal — Tier-0-model rejected, Ollama stays, model verdict grounded in real workload |
| Disk-ops runbook | ECOSYSTEM-OPS | [CORE-ROADMAP-2026-07-17/BLUEPRINT-DISK-OPS-CLEANUP-2026-07-18.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-DISK-OPS-CLEANUP-2026-07-18.md) | Operational runbook, not a numbered phase — 90%→65% disk fix executed same pass; `hetzner:dowiz` remote confirmed live |
| §16 Deployment topology | ALL | [MASTER-ROADMAP-SOVEREIGN-ARCHITECTURE-2026-07-16.md §16](ROADMAP.md) | Post-audit dialogue pass: 3-mode hosting (CF Pages/Hetzner/self-host), CF Tunnel remote access, venue-owned couriers confirmed, single assistant-role in-hub agent, unified multi-channel order-flow, isolated-hub MVP topology (dowiz.org=directory), hybrid auto-posting, Fly.io fully retired (teardown blocked on operator `flyctl auth login`) |
| §17 Long-term ecosystem | ALL | [MASTER-ROADMAP-SOVEREIGN-ARCHITECTURE-2026-07-16.md §17](ROADMAP.md) | Governance (BDFL now, revisable), crypto-agility from day one, 4 dowiz/vendor-forever dependencies found+closed as swappable ports (CF-tunnel account, cert-root, Cloudflare-as-company, Hetzner-as-company) |
| §18 Launch-blocker program | ALL | [MASTER-ROADMAP-SOVEREIGN-ARCHITECTURE-2026-07-16.md §18](ROADMAP.md) | 5 Opus research passes → Fable synthesis → 2 canon-diffs (P38-rev, P39-rev) + 18 Opus blueprints (P57-P74) across 4 build waves to milestones M1-M4; index below |
| §19 Perf/physics/mesh research wave | ALL | [MASTER-ROADMAP-SOVEREIGN-ARCHITECTURE-2026-07-16.md §19](ROADMAP.md) | Roadmap registration of the 2026-07-18/19 wave — points into the three file rows below (ledger/meta-gap/Q-series): four clusters (mesh M1/P92/P93/P94, perf P75–P91, product P95/P96, Q-series governance), the two-lane dowiz ∥ bebop sequence (bebop gate-0 = P85+C3), all LOCAL/UNPUSHED, zero product code, 3 registered code artifacts (I1 NTT process-red, I2 slot_arena, I3 contention-bench) |
| P57-P64 | CORE/PROTOCOL/DELIVERY | [CORE-ROADMAP-2026-07-17/BLUEPRINT-P57-canvas-text-input.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-P57-canvas-text-input.md) · [P58](CORE-ROADMAP-2026-07-17/BLUEPRINT-P58-a11y-mirror-everywhere.md) · [P59](CORE-ROADMAP-2026-07-17/BLUEPRINT-P59-capability-cert-chain.md) · [P60](CORE-ROADMAP-2026-07-17/BLUEPRINT-P60-payment-adapter-core.md) · [P61](CORE-ROADMAP-2026-07-17/BLUEPRINT-P61-notification-fabric.md) · [P62](CORE-ROADMAP-2026-07-17/BLUEPRINT-P62-catalog-multivendor-data-model.md) · [P63](CORE-ROADMAP-2026-07-17/BLUEPRINT-P63-shell-platform-spike.md) · [P64](CORE-ROADMAP-2026-07-17/BLUEPRINT-P64-intent-engine-friction-voice.md) | Wave W1 (foundations, build-parallel): text input, a11y-mirror, capability-certs, payment core, notifications, catalog/multi-vendor, shell spike, intent/friction/voice |
| P65-P66, P69-P70 | DELIVERY | [P65](CORE-ROADMAP-2026-07-17/BLUEPRINT-P65-dispatch-orchestrator.md) · [P66](CORE-ROADMAP-2026-07-17/BLUEPRINT-P66-data-wallet-offline-drafts.md) · [P69](CORE-ROADMAP-2026-07-17/BLUEPRINT-P69-customer-storefront-checkout.md) · [P70](CORE-ROADMAP-2026-07-17/BLUEPRINT-P70-owner-surface.md) | Wave W2 (assembly to M1): dispatch orchestrator, data wallet/offline drafts, customer storefront/checkout (M1 critical path), owner surface |
| P67-P68, P71, P73 | DELIVERY/ECOSYSTEM-OPS | [P67](CORE-ROADMAP-2026-07-17/BLUEPRINT-P67-hub-provisioning-claim.md) · [P68](CORE-ROADMAP-2026-07-17/BLUEPRINT-P68-hub-supervisor-update-backup.md) · [P71](CORE-ROADMAP-2026-07-17/BLUEPRINT-P71-courier-surface.md) · [P73](CORE-ROADMAP-2026-07-17/BLUEPRINT-P73-dowiz-org-landing.md) | Wave W3 (delivery+automation to M2/M3): hub provisioning/claim, hub supervisor (update+backup), courier surface ("P52-rev" — preserves P52's K1/K4-K8, supersedes 3 named items, adds the real `apps/courier` build; build P52's folds first), dowiz.org landing |
| P72, P74 | DELIVERY | [P72](CORE-ROADMAP-2026-07-17/BLUEPRINT-P72-foodcourt-checkout-nleg.md) · [P74](CORE-ROADMAP-2026-07-17/BLUEPRINT-P74-moderation-reports-blocklist.md) | Wave W4 (multi-vendor to M4): food-court N-leg checkout, moderation reports+blocklist |
| P38-rev, P39-rev | CORE/DELIVERY | [BLUEPRINT-P38-webgpu-render-engine.md §12](CORE-ROADMAP-2026-07-17/BLUEPRINT-P38-webgpu-render-engine.md) · [BLUEPRINT-P39-app-shell-installability.md §1.2](CORE-ROADMAP-2026-07-17/BLUEPRINT-P39-app-shell-installability.md) | Canon-diffs (write first, unblock the rest): strikes the IME DOM-overlay + adds AR/VR hard requirements; records the winit+wgpu no-webview desktop shell decision |
| Launch-blocker synthesis | ALL | [CORE-ROADMAP-2026-07-17/SYNTHESIS-LAUNCH-BLOCKERS-2026-07-18.md](CORE-ROADMAP-2026-07-17/SYNTHESIS-LAUNCH-BLOCKERS-2026-07-18.md) | Fable's reconciled build plan: 12 cross-cutting dependency resolutions (X1-X12), 4 milestones, 4 operator decisions closed, the full blueprint breakdown |
| Launch-blocker research (R1-R5) | ALL | [docs/research/OPUS-R1-INTERFACE-RENDERING-2026-07-18.md](../research/OPUS-R1-INTERFACE-RENDERING-2026-07-18.md) · [R2](../research/OPUS-R2-PAYMENT-MONEYFLOW-2026-07-18.md) · [R3](../research/OPUS-R3-HUB-PROVISIONING-IDENTITY-2026-07-18.md) · [R4](../research/OPUS-R4-ORDERFLOW-COURIER-NOTIFICATIONS-2026-07-18.md) · [R5](../research/OPUS-R5-MULTIVENDOR-ECOSYSTEM-OPS-2026-07-18.md) | 5 parallel Opus research passes grounding the whole §18 program in real 2026 prior art |
| Master status ledger — 2026-07-18/19 research wave (M1, P75–P94 + all closed scans) | ALL | [CORE-ROADMAP-2026-07-17/MASTER-STATUS-LEDGER-2026-07-19.md](CORE-ROADMAP-2026-07-17/MASTER-STATUS-LEDGER-2026-07-19.md) | The one-glance status of the entire perf/physics/mesh research day: 18 blueprints to write (P75–P83, P85–P91, P93–P94), 2 fully-blueprinted (M1 inside P92, P92 itself), the validated-design vs no-target closed split, the merged dependency sequence (dowiz ∥ bebop lanes; bebop gate-0 = P85+C3), 15 outstanding operator decisions (§5 there), and the §0 recovered-docs incident note |
| Meta-gap audit — 2026-07-18/19 wave (coverage against 8 dimensions) | ALL | [CORE-ROADMAP-2026-07-17/META-GAP-AUDIT-2026-07-19.md](CORE-ROADMAP-2026-07-17/META-GAP-AUDIT-2026-07-19.md) | Second adversarial read of all 21 P75–P96 blueprints: found 4 HIGH (G1 P95/P96 orphaned from ledger, G2 GPU WebGL2-floor DoD, G3 P81/P82 gate DoDs unmeetable, G4 P91 conformance vectors absent) + G5–G17 hygiene. **STATUS 2026-07-19: G1–G5 all CLOSED** (G1–G4 pre-session `e7cfd0f1b`; G5 constant-time review line — the one genuinely new gap — closed this session across P85/P91/P92; G4's remaining port-source pin also closed this session, real SHA `f38f2c5`) — see `ROADMAP-RECHECK-SESSION-SYNTHESIS-2026-07-19.md` §1. Confirms agent/model verification is a non-gap owned by P54/P55/P56 |
| Roadmap-wide blueprint-gap audit — full coverage check (2026-07-19) | ALL | [ROADMAP-BLUEPRINT-GAP-AUDIT-2026-07-19.md](ROADMAP-BLUEPRINT-GAP-AUDIT-2026-07-19.md) | Applies the meta-gap-audit lens to the ENTIRE roadmap, not just P75–P96: blueprint coverage is complete (every P01–P96 either has a file or is P84, deliberately reserved); one genuine coverage gap found (GAP-A1, un-homed arc-units) — **already closed pre-session** (`06ac90c65`/`63a1cb364`); G2–G5 re-surfaced as still-open in its §5 — **that re-surfacing was itself stale**, see the meta-gap-audit row above and `ROADMAP-RECHECK-SESSION-SYNTHESIS-2026-07-19.md` §1 for the correction |
| Roadmap-wide blueprint-gap + landed-status audit, delta pass (2026-07-20) | ALL | [ROADMAP-BLUEPRINT-GAP-AUDIT-2026-07-20.md](ROADMAP-BLUEPRINT-GAP-AUDIT-2026-07-20.md) | Successor delta audit covering the full extra day of work since the 07-19 pass. Coverage still effectively complete; GAP-A1 confirmed closed (the 07-19 doc's "still open" text had already gone stale same-day). Found + fixed directly in this index: 1 dangling link, 4 under-linked/orphaned docs (both P40 blueprints, the space-grade verified-status ledger, 2 docs at 3-hop depth), 1 item-number mislabel, 1 stale item-26 status cell. Fresh live verification: HEAD `499869e55` matches `origin/main` exactly, kernel default 1137 / pq 1310 / engine 128 all passing zero failures, all 13 spot-checked commit hashes confirmed real and matching their claims. One real non-blueprint finding, reported not silently fixed: space-grade items 45/73/74 have substantial standalone CI-gate scripts on disk that are not yet wired into any GitHub Actions workflow, despite those items' own spec text describing a live CI gate — annotated directly on those items in the space-grade roadmap doc. Also surfaced + partially resolved a disk-full incident (99 leftover autopilot-swarm worktrees, 77 removed safely, freed 25GB) |
| **Q-SERIES — Roadmap Verification & Observability (governance layer over P75–P96)** | ALL (governance — no numeric phase) | [CORE-ROADMAP-2026-07-17/BLUEPRINT-Q-SERIES-VERIFICATION-OBSERVABILITY-2026-07-19.md](CORE-ROADMAP-2026-07-17/BLUEPRINT-Q-SERIES-VERIFICATION-OBSERVABILITY-2026-07-19.md) | **A cross-cutting Q-namespace (distinct from P-series to signal governance-not-feature)** that ensures each P-item's OWN stated DoD was actually met as it gets built: **Q1** claim-verification checkpoint (new `DONE-VERIFIED` status + `verified-by` evidence field on the ledger; automated backstop = P56 §4f.3 `StaleGround`; would have caught G1/G3/G4 as a standing gate — and would have caught the G2–G5 "already fixed but re-flagged as open" staleness above, exactly the class of drift Q1 exists to prevent); **Q2** feature telemetry by extending the real closed `LogEvent` enum (`kernel/src/metrics.rs`) + P83 spans (closes G14/G15 — no new logging system); **Q3** code-review gate — honestly mostly EXISTS (ci.yml gates + `v5c-reexec` + per-blueprint `D-REVIEW` + safety-floor); only add = `reviewed-by` pointer, no CODEOWNERS ceremony; **Q4** interface verification — almost the whole wave has no UI, only P86/P87/P88 (cite G2 WebGL2-floor DoD) + P96 (cite G15) touch a surface; reuse P38 render-floor DoD + Playwright, invent nothing. Proportionate: Q1 deep (real gap), Q2–Q4 cite-and-wire |
| Roadmap recheck — session synthesis (2026-07-19, afternoon) | ALL | [ROADMAP-RECHECK-SESSION-SYNTHESIS-2026-07-19.md](ROADMAP.md) | Consolidates a full "what's missing or not done" recheck: G2–G5 closure confirmation, P06 unblock ripple-effects, 4 active-arcs cross-checked vs. live git (agentic-mesh B2/B3 still open, B1 fuel-wiring landed but calibration placeholder remains, E3-Phase-A unbuilt, spectral E3-Phase-B blocker dissolved but work unstarted, math-first S0.5 done, hydraulic-loop-v2 resonator fix done), dowiz not-done items GROUND-TRUTH's summary dropped (M1 open red-team correctness bug, P95 no-blocker not-done, P84/P86/P87/OD-1/2/4 correctly gated), and bebop-repo status (C3/C4b closed, NTT-wiring still genuinely frozen, ⚠ an uncommitted in-progress diff was observed deleting the frozen NTT block — unresolved as of this writing, re-check before trusting that item) |
| **WIRING WAVE 2026-07-19 — built-but-unwired CORE surface → WIRED + MERGED + PUSHED** | CORE/AGENT (P59·P64·P66·P68·P83·P89·P40) | `built-but-unwired-core-surface-wired-2026-07-19.md` *(memory-system file, lives outside this repo at `/root/.claude/projects/-root-dowiz/memory/` — not a resolvable repo-relative link; see the Claude memory corpus, not a path under `docs/`)* | **DONE + MERGED @ `17d65f315`, confirmed pushed** (verified 2026-07-20: `17d65f315` is an ancestor of current `origin/main` HEAD). Previously "built but unwired" CORE capabilities now have real callers + red→green wire tests: **k1** P59 cap-verify + P74 moderation/blocklist (`95023efbd`); **k2** P68 hub_supervisor drive + P66 wallet reconnect→outbox double-charge + B4 backup + P-H verify_chain_before_trust (`e12a5e323`); **k3** P83 span-metrics wiring + **deadlock root-caused+fixed** (thread-locals, was self-deadlock hang) (`67851b2f3`); **e** P64 engine_loop intent-router + P89 field_modal live (`98cad8bf0`); **p40** agent-executor DECART-**B** proxy native-spa-server→agent-loop + money-law firewall (`e375043d7`); **json-api build-enabler** unblocked native-spa-server (`593bc60a4`). Combined main green at the time: kernel default 912 / pq 1071 / telemetry 3-3 / engine 123 / agent-loop 8 / spa 20 — only pre-existing `r5` dockerfile test fails (unrelated); **current live counts (2026-07-20) are higher — kernel default 1137 / pq 1310 / engine 128, 0 failures**, see the remaining-queue-wave row below for the fresh verification pass. P95 HOLD/NO-GO; mesh-auth + bebop-perf excluded (bebop-side). |
| **Blueprint-writing wave 2026-07-20 — closes the one real gap-audit finding + 3 operator-requested/flagged blueprints** | CORE (item 3) / ECOSYSTEM-OPS (telemetry) / PROTOCOL (mesh-adapter) / DELIVERY (native-spa-server) | [`BLUEPRINT-ITEM-03-order-machine-zero-alloc-proof-2026-07-20.md`](BLUEPRINT-ITEM-03-order-machine-zero-alloc-proof-2026-07-20.md) · [`BLUEPRINT-PREDICTIVE-RESOURCES-TELEMETRY-2026-07-20.md`](BLUEPRINT-PREDICTIVE-RESOURCES-TELEMETRY-2026-07-20.md) · [`BLUEPRINT-MESH-ADAPTER-BEBOP-CROSSREPO-DRIFT-2026-07-20.md`](BLUEPRINT-MESH-ADAPTER-BEBOP-CROSSREPO-DRIFT-2026-07-20.md) · [`BLUEPRINT-NATIVE-SPA-SERVER-LISTENER-HARDENING-2026-07-20.md`](BLUEPRINT-NATIVE-SPA-SERVER-LISTENER-HARDENING-2026-07-20.md) | Registered in `ROADMAP.md` §15.3 items 11-13 and §15.4 G1 (resolved)/G4 (closed)/G5 (accepted limitation). **Item 3**: the sole genuine gap found across all 78 space-grade items by this pass's roadmap-wide re-audit — 2 of 3 proof clauses already green, the counting-allocator proof was never built, now fully specced against the existing `arena::counting_alloc`/item-38 precedent. **Predictive telemetry**: operator-requested — Kalman prediction (`kernel::kalman`) + a newly-generalized `kernel::markov` regime detector (same engine `tools/loop-signals` proves, parameterized not duplicated) + graph-Laplacian cross-metric coherence (`kernel::spectral`), narrated via `llm-adapters` only on a fired alert. **mesh-adapter/bebop drift**: real `cargo check --features kernel-rlib` run confirms `bebop-delivery-domain` does not compile against current `dowiz-kernel` (`OrderItem`/`OrderStatus` evolved, companion repo didn't) — ⚠ needs operator sign-off between a cross-repo PR and a dowiz-side vendored-fork stopgap, both fully specified. **native-spa-server hardening**: closes the listener-wide (not just per-connection) DoS gap `00436dedc` left open, via a `tokio::sync::Semaphore` connection cap + a `kernel::token_bucket`-based per-IP limiter, defense-in-depth alongside the existing `MAX_INFLIGHT_API`/`MAX_BODY_BYTES` layers. |
| **Blueprint-writing wave 2026-07-20 (pass 2) — full-file PLANNED-marker sweep across all 5 ROADMAP.md parts** | CORE (P31d verification ladder · P32d cross-model critic) | [`BLUEPRINT-P31d-verification-ladder-kani-money-organs-2026-07-20.md`](BLUEPRINT-P31d-verification-ladder-kani-money-organs-2026-07-20.md) · [`BLUEPRINT-P32d-cross-model-critic-2026-07-20.md`](BLUEPRINT-P32d-cross-model-critic-2026-07-20.md) | Extends the prior Part-V-exhaustive / Part-II-spot-check pass to a systematic sweep of Parts I–IV. Two genuinely-unblueprinted open items got real docs. **P31d** (was flagged "Blueprint: none — needs one before build"): the first-deliverable it called for — a ranked ≥5 candidate-invariant inventory, honest kani-vs-z3 selection (lands on Kani, grounded in the 5 existing `#[cfg(kani)]` sites), and one `#[kani::proof]` harness over `eqc_gen.rs`'s float-free integer tax organs wired into the existing item-7 kani-gate. **P32d** (was flagged "needs a short design note first"): that note — a decorrelated (two `(provider,model)` judges, research-verifier precedent) advisory-only critic over `markov::Verdict`, never-gates per GROUND-TRUTH-over-PROXY, P6 boundary preserved. The same pass also fixed 8 stale link/status gaps directly in `ROADMAP.md` §10.5/§15 (P37 + P40 statuses corrected against live code — HTTP order surface and AgentLoop both shipped; P37/P44/P45/P46/P54/P55/P56 on-disk blueprints linked) — the same reachability-gap shape the prior pass fixed for P51/P52/P53. |

---

## 11. New phases P97–P98 — AR/VR consolidation + audio design layer (2026-07-20)

A completeness audit of `ROADMAP.md`/this index found two real gaps: AR content real but scattered
across four places with no first-class item number (DZ-12, P38-rev §12.2, the §17.5 ruling, and
the spatial-storefront-voice-hub synthesis's Lane B), and audio/sound design consisting of exactly
one accessibility ruling-bullet (§16.50) plus one already-built partial instance (P64's friction
audio channel). Both close this pass with genuinely free P-numbers — verified: every `P\d+`
reference across `docs/design/` confirms P01–P96 are all claimed (P84 deliberately reserved), and
P99/P100 only ever appear as latency-percentile notation, never as roadmap item numbers.

| Phase / doc | Component | Blueprint | One-line note |
|---|---|---|---|
| P97 | DELIVERY/CORE | [BLUEPRINT-P97-ar-vr-spatial-interface-2026-07-20.md](BLUEPRINT-P97-ar-vr-spatial-interface-2026-07-20.md) | Consolidation, not a new build: reconciles two different "AR" concepts the four scattered sources conflated under one name — near-term browser product-AR (`<model-viewer>`→AR Quick Look/Scene Viewer, zero render-engine involvement, ships without any wgpu work) as Track 1, vs. long-term XR-headset spatial-panel readiness (P38 §12.2's four structural render-core constraints + DZ-12's OpenXR-Quest-first/WebXR-deferred sequencing) as Track 2. Native camera-AR compositing stays triaged into 3 buckets (already-shipping / deferred-on-an-unmade-decision / reusable-non-AR-techniques) per the intent-interface synthesis's own disposition, not re-decided here. DZ-12, P38-rev §12.2, §17.5, and the spatial-storefront-voice-hub synthesis's Lane B all now carry an append-only pointer to this doc |
| P98 | DELIVERY/CORE | [BLUEPRINT-P98-audio-sound-design-2026-07-20.md](BLUEPRINT-P98-audio-sound-design-2026-07-20.md) | Greenfield but code-grounded: extends P64's already-proven friction-only `AudioParams` pattern (derive audio from the same field data the visual channel already animates from) to the rest of the DZ-04 (`OrderStatus`)/DZ-05 (event vocab) visual-signal surface, with an exhaustive-`match` compile-time parity proof rather than a manually-maintained checklist. Explicitly declines to build the separately-triaged whole-field ambient sonification idea (`BLUEPRINT-INTENT-INTERFACE-ONE-SCREEN-2026-07-20.md` §6 — real prior art, already ruled speculative/non-load-bearing/deferred by the operator; not re-opened here for want of new evidence). No new TTS: OS screen readers already speak P58's `role=Status`/`role=Alert` mirror text for free, and conversational-agent TTS is already owned by the voice-hub arc's Phase 4/6 (Kokoro-82M) — both cross-referenced, neither duplicated |

---

*Maintenance rule (from BLUEPRINT-P-I §8): new planning doc ⇒ one new row here, Layer chosen by
which numeric-phase cluster it serves. DoD checks D1–D5 in BLUEPRINT-P-I §5 are the drift alarms.
Index built 2026-07-17 on `feat/p19-growth-engine` (HEAD `b64a2c1c6`). **Refreshed 2026-07-18** in
worktree `dowiz-verify-redteam` (branch `research/dowiz-verify-redteam-2026-07-17`): Layer-D row
corrected from "OPEN" to the now-on-disk blueprint; P-G-audit row corrected from "MISSING" to
"RECONSTRUCTED"; §2 descriptions updated with each blueprint's 2026-07-18 session fold-in; two
cross-cutting arcs added to §7 (fail-operational round-2, session-verification synthesis) plus the
hermes out-of-band pointer. No P01–P30 renumbering; no Layer added.*
