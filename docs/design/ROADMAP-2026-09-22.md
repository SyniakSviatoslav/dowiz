# dowiz — the roadmap, 2026-09-22

**This is the entry point. Read this file to know what is true and what is next.**

`ROADMAP.md` beside it is 5,556 lines whose own "current live status" section is dated
**2026-07-20** — two months stale at the time of writing. It is the merged archive of nineteen
earlier roadmap-shaped documents and it is still worth reading for the P01–P30 narrative and the
landing-wave detail, but **it is history, not status.** It did exactly this to the nineteen files
before it; this does it to that one.

**WHY THIS FILE IS SHORT.** The document it replaces is 545 KB. A roadmap nobody finishes reading
is a roadmap that gets contradicted by a summary of itself, which is how a status claim about a
commit that did not exist survived three sessions here (`bebop-status-claims-unverified`). Every
number below was produced by a command on 2026-09-22 and can be reproduced by the command named
beside it.

---

## 0. What is true today — measured, not carried forward

```
clock       0 sites decide the time for themselves, outside four allowed places
no-sql      0 .prepare( + 0 .d1( + 0 bindings + 0 migration files
one-image   0 handler bodies write more than one image
one-venue   0 handlers decide the venue twice
paths       0 cited paths that do not exist, across four documents
vocab       vocab.js is what tools/gen-vocab emits — 12 statuses, 3 currencies, 0 hand copies
vocabulary  12 kernel statuses, all present in 3 languages and 3 stylesheets
idempotent  3 queueable routes, 0 without a server guard
unreached   4 public capabilities that no shipping code references
file-size   worst 1737 lines (workers/api/src/hubstore.rs)
```

Reproduce: `for g in tools/gates/*.sh; do sh $g; done; python3 tools/gates/unreached.py`

**Tests:** `workers/api` 221 · `dowiz-hub` 317 · `dowiz-core` 3,647 · `native-spa-server` 166.
Each is `cd <crate> && cargo test` — **there is no cargo workspace**, and running from the root is
a documented false-green trap (`CLAUDE.md`, "Build model").

**Deployed** 2026-09-22, version `53778a00`, verified by reading the live platform back rather than
by the deploy's exit code: both storefronts answer 200, `/api/owner/health` carries the two new
fields, and the conservation audit's eight laws run against the real venues.

**Status tags below.** **LANDED** — code on `main`, tested, and a gate holds it. **IN FLIGHT** — a
lane is working on it now. **PLANNED** — a blueprint exists and nothing is built. **OPEN** — named,
not yet designed. **AGAINST** — decided not to build, with a re-entry condition.

---

## 1. The ordering principle

Three rules decide the order of everything below, and they are the reason it is not sorted by
excitement:

1. **A room cannot open without the room.** A waiter who cannot exist, a bill that cannot be
   amended and a table that cannot hold a tab are not features of a POS; they are the POS. Nothing
   downstream of them is worth building first.
2. **A wrong number is worse than a missing one.** Tax is absent from the order path entirely.
   Adding channels and prices on top of that multiplies a wrong figure across three channels
   instead of one, so tax comes before pricing and both come before fiscalisation.
3. **A record you may not use is not a record.** A customer profile without a consent record cannot
   lawfully be sent anything, so CRM is last — not because it matters least, but because building
   it earlier produces something that has to sit unused.

---

## 2. What landed on 2026-09-22

Fifteen commits, all pushed. Each closed a defect that was measured before it was fixed.

| | What | The measurement that mattered |
|---|---|---|
| **D1 removed** | binding, 9 migrations, `migrate.rs`, and three constants that were D1's alone | the legacy seed was proved SPENT before deletion: `menuVersion 246` live against D1's frozen `generation 95` |
| **Commands in the object** | `place`, `advance`, `assign` execute in ONE object turn | `one-image` 3 → **0**; `with_hub` and `save` became dead code, which is the proof |
| **The clock** | `Router::with_data(Req { now_ms })`; helpers take the instant | `clock` 93 → **0**. The blueprint estimated 27 by counting a spelling; counted by the site that DECIDES, it was 93 |
| **Offline writes** | IndexedDB queue on the courier's phone + `idempotency::guard` on four routes | a naive client sends 10 requests under 10 keys against a 409; the outbox asks once and drops it |
| **The errlog** | every 500 was written to an image **no code ever read** | `/api/owner/health` answered `errors: []`, which is the same shape as a healthy venue |
| **Rebuild-and-diff** | projections refolded from bytes and crossed against the stock ledger | the conservation audit's 8th law; `StockLedger::stranded()` had never been called in production |
| **The outbox** | the kitchen's bell is written by the turn that writes the order | `notify::order_placed` became dead code |
| **Client vocabulary** | generated from the kernel by `tools/gen-vocab` | all four hand copies were short by `COMPENSATED_REFUND` |
| **The twin server** | three write routes with **no credential of any kind**; a summer timezone constant in four places | a real order walked to CONFIRMED by an anonymous request |
| **Dine-in** | a third fulfilment kind, and the one question four files each asked separately | the kind set was OPEN: any unknown word was charged the full delivery fee and never asked for an address |
| **The audits** | `unreached.py`, `paths.sh`, `idempotent.sh`, `clock.sh` | the dead-code audit was itself wrong twice (24 → 11 → 6) before it was worth acting on |

---

## 3. The track

### Phase A — the room (LANDED in part, the rest IN FLIGHT)

| | | Status |
|---|---|---|
| A1 | `dine_in` as a fulfilment kind; shared shelf proved by test | **LANDED** |
| A2 | Tables as data: zones, seats, per-slot availability, double-booking refused | **IN FLIGHT** — tables lane |
| A3 | Booking reachable from the storefront, time mandatory, three languages | **IN FLIGHT** — tables lane |
| A4 | The waiter role — `Principal` is exactly `Owner`/`Courier`/`Customer` today | **IN FLIGHT** — `BLUEPRINT-POS-THE-ROOM-2026-09-22.md` |
| A5 | The open tab, and `Amended` as an event kind | **IN FLIGHT** — same |
| A6 | Split bill, transfer between tables, void with a reason and a person | **IN FLIGHT** — same |
| A7 | The till as a shift, and its X/Z report as a **ninth conservation law** | **IN FLIGHT** — same |
| A8 | Print routing (kitchen / bar) and tip distribution | **IN FLIGHT** — same |
| A9 | The customer-facing surface for ordering at a table (a QR onto the storefront) | **OPEN** |

### Phase B — money, tax and channels (IN FLIGHT)

| | | Status |
|---|---|---|
| B1 | A tax rate as an integer, with the rounding rule stated as an equation | **IN FLIGHT** — `BLUEPRINT-TAX-PRICE-CHANNEL-2026-09-22.md` |
| B2 | Inclusive vs exclusive: which number is stored, which is derived | **IN FLIGHT** — same |
| B3 | Price per channel, and how it survives a menu import and a promotion | **IN FLIGHT** — same |
| B4 | Historical correctness: last March's receipt shows last March's rate | **IN FLIGHT** — same |
| B5 | `channel` on the order — a closed set, distinct from `fulfilment.kind` | **IN FLIGHT** — same |
| B6 | The seam a fiscal integration plugs into | **IN FLIGHT** — same |
| B7 | ebills.al: auth, endpoints, field mapping, what dowiz is missing | **IN FLIGHT** — `BLUEPRINT-EBILLS-INTEGRATION-2026-09-22.md` |
| B8 | The receipt itself (format, printing) | **OPEN** |

### Phase C — CRM (IN FLIGHT)

| | | Status |
|---|---|---|
| C1 | The customer as a RECORD, not only a fold | **IN FLIGHT** — `BLUEPRINT-CRM-CONSENT-LOYALTY-2026-09-22.md` |
| C2 | Consent: the wording shown, when, by what channel, and its withdrawal | **IN FLIGHT** — same |
| C3 | Erasure against an append-only, content-chained log | **IN FLIGHT** — same, and the hardest part of it |
| C4 | Identity merge — one person is several rows today | **IN FLIGHT** — same |
| C5 | Loyalty, or a reasoned refusal — is a tier a score under `DECISIONS.md`? | **IN FLIGHT** — same |
| C6 | Segments, campaigns, send history | **IN FLIGHT** — same |

### Phase D — the platform's own foundations

| | | Status |
|---|---|---|
| D1 | The stack audit: every crate and pattern proposed, with a verdict and a decart | **IN FLIGHT** — `BLUEPRINT-STACK-AND-DEPENDENCIES-2026-09-22.md` |
| D2 | The courier PWA's cold start: no service worker of its own, so "kill the app underground and reopen it underground" shows nothing | **OPEN** |
| D3 | `Currency::minor_units()` in the kernel, so the decimals table stops being a hand copy in two places | **OPEN** |
| D4 | `KINDS` into `dowiz-core` so `lib/fulfilment.js` can be generated like `vocab.js` | **OPEN** |
| D5 | The `unreached` baseline to zero: four capabilities nothing calls | **OPEN** |
| D6 | The 17 remaining `#[allow(dead_code)]` — the manual, worse version of that gate | **OPEN** |

---

## 4. What is recommended AGAINST, and when to re-open it

A roadmap that recommends everything is a wish list. These are decided, and each says what would
change the decision.

- **CRDTs for money and orders** — `DECISIONS.md:369` fences them out; a Durable Object is already
  a single writer per venue, and `queue + Idempotency-Key + a refusing FSM` is a deterministic
  merge by construction. **Re-entry:** a second writer that cannot share an object — *an offline
  POS till*. **That condition may now be met**, and the stack lane has been told to re-examine it
  rather than inherit the answer.
- **A general effect system in Rust** — the cost is rewriting 519 + 185 awaits over `!Send` wasm
  futures for a property the kernel and hub already have by construction. Injecting the clock
  bought most of the rest, and that is now done (93 → 0). **Re-entry:** a second runtime target
  that cannot link the kernel directly.
- **A general event bus between in-process services** — `BLUEPRINT-MODULAR-ARCHITECTURE` §9: "a
  direct typed call is debuggable; a bus is a distributed system with none of the benefits."

---

## 5. The gates, and what each one holds

Every rule that matters here is a ratchet that may only fall, and **each was proved to fire in both
directions before it was committed** — the change that should refuse it, and the restore.

| Gate | Holds |
|---|---|
| `no-sql` | SQL and the D1 binding cannot come back |
| `clock` | a handler cannot decide what time it is |
| `one-image` | a handler cannot write two images in one body |
| `one-venue` | a handler cannot authorise one venue and act on another |
| `vocab` + `vocabulary` | the browsers cannot retype the kernel's status list |
| `idempotent` | a route a phone can replay must be idempotent on the server |
| `paths` | a document cannot cite a file that does not exist |
| `unreached` | something cannot be built that nothing calls |
| `file-size` | a file cannot cross 300 lines without splitting |
| `conservation` (8 laws) | the register adds up, live, per venue |

**Three of these gates counted their own epitaph** — the comment recording a deletion — before
being fixed to strip `//` first. The first one's header had warned about exactly that trap and its
replacement still fell into it. Any new gate strips comments before counting.

---

## 6. What is NOT verified

Said out loud, because silence reads as health:

- **`file-size` is at 41 against a baseline of 40** as this is written: a lane's new file has
  crossed 300 lines mid-work. It is not a regression on `main`.
- **One live breach in the conservation audit**: `dubin-durres` costs 619 log cells per order
  against a budget of 120, over 217 orders. Pre-existing, a capacity finding rather than a
  correctness one, and **not investigated**.
- **`rebuild` reported `unheld: [one order]`** on a live venue — a live, unprepared order with no
  reservation. Not a breach by design, and not chased down.
- **No end-to-end order has been placed through the new command path on the live platform.** The
  path is covered by 221 native tests and the read paths were probed after deploy; placing a real
  order would ring a real kitchen, so it was not done without asking.
- **Every blueprint marked IN FLIGHT above is a lane's opinion until it is read and verified.**
  Five of six lanes this session produced something that had to be corrected on inspection, and
  one of those corrections was to an audit I wrote myself.

---

## Wave F — launch gaps (2026-09-24)

Evidence for every row: `BLUEPRINT-LAUNCH-GAPS-2026-09-24.md` (HEAD `b154b97c`, read-only, sources cited
there). Two of the seven operator-listed gaps turned out narrower than stated — the courier invite flow exists
(§3) and the hybrid X25519+ML-KEM-768 seal is wired into the nightly copy since `8ae71778` but is `Off` until
`BACKUP_SEAL_PK` is set (§6) — and the rows reflect what is actually missing. **Size:** S ≤ a lane-day, M a few,
L a week or blocked on a third party. **CHECK** is what makes a row done, in the shape the phases above use.

| | Goal | Files it owns | CHECK | Depends on | Size |
|---|---|---|---|---|---|
| **F1** | Supplies and recipes come in **in bulk** from a CSV, dry run then apply, through the writers the console already uses | `crates/dowiz-hub/src/import.rs` (+ `import/supplies.rs`, `import/recipes.rs`), `workers/api/src/services/catalogue/import.rs`, `services/operations/supplies.rs`, `public/admin/stock.js`, `public/admin/menu.js`, new `e2e/gates/recipes.mjs` | native: a supplies CSV in sq/en/uk headers with `1.200,00` and `1200` parses to the same minor units, and a row whose unit is not g/ml/unit is a warning naming the row; a recipes CSV naming a dish by its normalised name lands as `bom` through `update_product`'s path so `derive` runs (kcal/weight/cost appear on the owner product); `one-image` stays 0. Live on the QA venue: 80 supplies + 165×~6 lines imported in two requests; `e2e/gates/recipes.mjs` prints `with recipe 165`; `stock-cycle.mjs` green on an imported dish | F7 for the live half | **M** |
| **F2** | Recipe **templates per category** and a **suggestion** for a dish with no recipe, always a draft the owner applies | new `tools/recipes/templates/sushi/*.csv`, `workers/api/src/recipe.rs` (`suggest`), `public/admin/menu.js` | native: for a dish with no `bom`, `suggest` answers the same-category nearest-name recipe scaled to its `weightG`, else the category template, else nothing — each case a test; nothing is written until apply (the product's `bom` is unchanged after `GET …/suggest`); no scoring (`no-scoring` gate unchanged) | F1 | **S** |
| **F3** | A **supplier invoice** as one receipt: priced `received` lines in one object turn, idempotent on `(supplier, doc)` | `workers/api/src/services/operations/stock.rs` (`receipt`), `crates/dowiz-hub/src/stock/cost.rs` (unchanged, used), `public/admin/stock.js`, `workers/api/src/lib.rs` (one route) | native: the same `(supplier, doc)` posted twice lands once (`idempotent` gate counts the route, 0 without a guard); two receipts at 1,000 and 1,200 per kg → the WAC fold says 1,100 and an order between them stamps 1,000 (BLIND-SPOTS §2.10's case, now reachable from a route); law 8 rebuilds the stamps | F1 | **S** |
| **F4** | **Wolt intake**: signed webhook → allow-listed fetch → `aggregator::Entry` → placed and accepted; status pushed back through the outbox; catalogue + availability pushed to Wolt | new `workers/api/src/wolt/{client,judge,fetch,hook,map,push}.rs`, `hubdo/wolt.rs`, `public/admin/wolt.js`, `outbox/rails.rs` (one kind), `lib.rs` (routes) | native: a body with a wrong `WOLT-SIGNATURE` → 401 and no image written; a valid `CREATED` for an unknown `venue_id` → 404 loud; the same webhook twice → one order, `existing: true`; an unmapped SKU is placed flagged and draws no stock; `total ≠ lines − discount` refused (law 3); a fetch failure answers 5xx (so Wolt retries); an `OutOfStock` refusal enqueues exactly one availability entry per SKU. Sandbox: a Wolt test order reaches CONFIRMED in the console with no typing. **Glovo** is the same row again with `Bearer` + `chain_id`; **Baboon** AGAINST until a bilateral API exists | **BLOCKED (external)**: Wolt's integration form → T&Cs → sandbox credentials; B5 (`channel` closed set) is landed | **L** |
| **F5** | The courier invite is **delivered**, not only shown: sent by the venue's WhatsApp/Telegram rail; a claim link carrying the phone (never the code); the Couriers pane names its venue | `workers/api/src/services/courier/hiring.rs`, `public/admin/couriers.js`, `public/courier/app.js` (hash → claim form) | native: `invite` with `send: "telegram"` writes one outbox entry and answers `sent: true`, and shows the code only when nothing could send it; a link `#claim=<phone>` opens the claim form with the phone filled and the hash cleared. Live (operator's word): F6's `courier.mjs --claim` on `sushi-durres` mints, claims, logs in on the venue host and reads `locationId == "sushi-durres"`; and the existing `QA_COURIER_*` pair does the same | — | **S** |
| **F6** | The **walk harness**: `e2e/walk/{_harness,guest,owner,waiter,courier}.mjs` + `FEATURES.md`, one browser and N contexts, every UI step read back through the API, every TEST order closed in `finally`, a report with the exit code in it | new `e2e/walk/**`, `e2e/walk/FEATURES.md` | the four flows run green on the QA venue and each prints its read-backs; a deliberately broken seam (rename one console route in a scratch build) turns the matching step red; `ensureClosed` leaves `rebuild.stranded == []` and no `QA-WALK` order live even when a step throws; `FEATURES.md` has no `NOT WALKED` row for A1–A8, B5, B9, A10–A13; the CSP listener fires on a `style-src` break (the 2026-09-16 incident replayed on the scratch build) | F7 for the default host | **M** |
| **F7** | A **QA venue on the platform** (`qa-durres.dowiz.org`) for walks and soaks: created by the two documented steps, seeded with the sushi menu and F1's supplies/recipes, its bell in the operator's own chat | `tools/platform/qa-venue.sh` (create + attach + seed, idempotent), `e2e/walk/_harness.mjs` (`HOST` default) | `curl -sI https://qa-durres.dowiz.org/` → 200; `GET /api/owner/health` on it shows 165 dishes, N supplies, `errors: []`; a walked order rings no real kitchen (the venue's Telegram chat id is the operator's); the script run twice changes nothing | F19 removes the attach step, not required | **S** |
| **F8** | Split `workers/api/src/hubstore.rs` (1671) into `hubstore/{place,images,seed,fold,archive,backup,tests}.rs`, zero behaviour change | `workers/api/src/hubstore.rs`, `workers/api/src/hubstore/**` | `cargo test -- --list` (workers/api) identical as a set of names before/after; every gate identical except `file-size`, whose baseline falls in the same commit (worst 1671 → next); `git diff --color-moved=dimmed-zebra` shows only moves plus `mod`/`use` lines; `cargo check --target wasm32-unknown-unknown` green; no new file over 300 | — | **M** |
| **F9** | Split `crates/dowiz-hub/src/lib.rs` (1640): the three test modules out (737 lines), then `kinds.rs` / `error.rs` / `record.rs` / `hub.rs` | `crates/dowiz-hub/src/lib.rs`, `src/{tests,audit_tests,usage_tests,kinds,error,record,hub}.rs` | as F8 for `dowiz-hub` (317 tests, same names); the golden image tests untouched and green (they are the byte proof for this crate) | — | **S** |
| **F10** | Split `workers/api/src/hubdo.rs` (1560) into `hubdo/{view,storage,commands,bell,routes,sockets,tests}.rs`, `impl HubImages` spread across files | `workers/api/src/hubdo.rs`, `workers/api/src/hubdo/**` | as F8; plus the live read-back after deploy: `/api/owner/health`, both storefronts 200, `conservation.mjs` prints `CONSERVATION HOLDS` on both venues, sockets open once (`socket-probe.mjs`) | — | **M** |
| **F11** | Split `workers/api/src/owner.rs` (1477) into `owner/{orders,dashboard,product,i18n,location}.rs` | `workers/api/src/owner.rs`, `workers/api/src/owner/**` | as F8; `one-venue` stays 0 (`owner_and_venue` stays in the root and every handler still calls it) | — | **M** |
| **F12** | Split `crates/dowiz-hub/src/stock.rs` (1418): inline tests out, then `stock/{codec,log,bom,ledger}.rs` | `crates/dowiz-hub/src/stock.rs`, `src/stock/**` | as F9; `stock::decode` of the old-format bytes test (EBILLS §8.1 "old logs fold unchanged") green; `stock-cycle.mjs` green on the QA venue | — | **S** |
| **F13** | Split `workers/api/src/storefront.rs` (1391) into `storefront/{input,menu,manifest,place}.rs`, `place` cut along validation / pricing / the command call | `workers/api/src/storefront.rs`, `workers/api/src/storefront/**` | as F8; `clock` stays 0; `idempotent` unchanged; F6's `guest.mjs` green before and after | reads `command/place.rs` first | **M** |
| **F14** | Split `workers/api/src/booking.rs` (1152) along its own rulers into `booking/{model,reservations,floor,pass,tests}.rs` | `workers/api/src/booking.rs`, `workers/api/src/booking/**` | as F8; the tables lane's tests (A2/A3) unchanged | after the tables lane merges | **S** |
| **F15** | `pq::hybrid` made **X-Wing-exact** (`SHA3-256(ss_M‖ss_X‖ct_X‖pk_X‖label)`, no public tag, one-seed keygen) and gated on the draft's vectors; dwzseal v2 with v1 still readable | `crates/dowiz-core/src/pq/hybrid.rs`, `pq/hybrid/xwing_tests.rs`, `pq/kat/xwing/**` (vendored, provenance README), `pq/backup_seal.rs`, `tools/seal-open/src/**` | `cd crates/dowiz-core && cargo test --offline pq::hybrid` green with every X-Wing vector byte-exact and the group count asserted (the ACVP file's shape); a v1 sealed fixture still opens; a v2 file with any header byte flipped is refused by the AEAD; `ct-gate` on in `seal-open`'s release profile | — | **M** |
| **F16** | **Switch the seal on** (operator): keygen off-platform, `BACKUP_SEAL_PK` set, deploy, and one real nightly copy opened on this box | none in the tree; `docs/design/BLUEPRINT-LAUNCH-GAPS-2026-09-24.md` §6.4 is the runbook | after the next 03:17Z: `/api/owner/health → backup.seal.sealed == true`; the bucket lists `…json.gz.sealed`; `tools/seal-open open` on that object yields a gzip that `gunzip -t` accepts and whose witness matches; the nightly `cloud.seal` loud line stops | F15 if the combiner is to change first (or v1 now, v2 later — both readable) | **S** |
| **F17** | The **words**: README Security and the landing say "off-site copies sealed with hybrid X25519+ML-KEM-768 (X-Wing) + AES-256-GCM; the secret key never exists on the platform", and nothing broader | `README.md`, `workers/api/public/platform/**` (landing copy, three languages) | `grep -rn "post-quantum" README.md workers/api/public/platform` shows only the qualified sentence; the memory rule (`dowiz-pq-and-investment-decisions`) is satisfied: KAT green AND F16's live proof precede the commit | F16 | **S** |
| **F18** | The crons stop fanning out: **per-venue DO alarms** for the outbox drain, the ebills tick and the nightly; a `cron` record on the platform object first, so the serial loop's wall time is a number before it is replaced | `workers/api/src/hubdo.rs` (alarm), `hubdo/alarm.rs`, `outbox/rails.rs`, `ebills/poll.rs`, `cloud.rs`, `lib.rs` (`scheduled`), `platform_store.rs` (`cron`) | native: an enqueue sets the alarm and a drained outbox clears it; a venue with ebills off never wakes for it; the nightly runs at 03:17 venue-local (`tz`). Soak (F20) at N = 50: `cron.ms` recorded under the sweep, then per-venue; the sweep's DO requests per minute fall from 2N to the count of venues with work; `clock` stays 0 (one clock read per alarm) | F20 to measure | **M** |
| **F19** | The **wildcard route** (operator): `Workers Routes:Edit` on the deploy token, `*.dowiz.org/*` back in `wrangler.toml`, `attach-host.sh` retired | `workers/api/wrangler.toml`, `tools/platform/attach-host.sh` (deleted), `docs/design/*` (onboarding text) | a hub created by `POST /api/platform/hubs` answers on `https://<slug>.dowiz.org/` with no second step; the two existing hosts still answer; the deploy no longer aborts on a `routes` key (the 403 trap in `dowiz-platform-on-dowiz-org`) | the token (operator) | **S** |
| **F20** | A **soak on the preview Worker**: N synthetic venues (10 / 50 / 200), k orders each, read the platform `cron` record, per-venue health gauges, storefront/console latency and the registry's `used_per_mille`; refuses to run against a production host | new `e2e/soak/venues.mjs`, `workers/api/wrangler.toml [env.preview]` | the script refuses any `HOST` not matching `/preview\.|qa-/`; at N = 200 it reports the four numbers and the cron wall time, and every venue's `errors: []`; the report is committed under `docs/design/` with the date; the preview namespace is deleted after | F18's record, F19 for hosts (or preview-only paths) | **M** |
| **F21** | The **registry's real capacity**: `K_LOC` records into a `Table` at `REGISTRY_BYTES` until `arena_full`, the count printed | `workers/api/src/platform_store.rs` (test only), `workers/api/src/platform.rs` (registry gauge on `/api/platform/health`) | the test prints `registry holds N venues`; if N < 1,000 the follow-up (Kv packing, phase 4b, or a larger ceiling) is opened as a row with that number in it | — | **S** |

**Order.** F7 → F6 first (the harness is what makes every other row's CHECK sayable, and `b154b97c` found six
defects this morning that no suite saw); F16 next (an hour, and the only thing between the code and the claim);
then F1 (the ledger has been proved and holds nothing for three weeks); F8 and F9 in parallel lanes (the two
worst files, and F9 is mostly moving tests); F15 before F17; F4 the day Wolt answers; F18–F21 before the
tenth venue, not after.

---

## Wave P — privacy (GDPR) and MCP (2026-09-24)

Evidence for every row: `BLUEPRINT-GDPR-AND-MCP-2026-09-24.md` (HEAD `5abeb0ac`, read-only, sources cited there).
The law was read from the Commissioner's official English text of **Law 124/2024**: it is "fully aligned" with the
GDPR, but its deadlines are **30 days** (Art. 12(4) answers, Art. 15(2) erasure), not "one month" — and the forget
route's promise that backups "expire within one month" is false today (nightly copies kept **35 days**,
`cloud.rs:264`). C3 (forget) landed in `c8e82907` and reaches `people`, `consent`, the log and its archives; seven
other venue stores and every platform record are not reached, and a restore brings a forgotten person back. MCP
exists as an owner-only door (`mcp.rs`, protocol `2025-06-18`, a one-year unscoped API key), with the right
kernel — every tool is one in-process call to an existing route — and no OAuth, so no role but the owner can
connect and no client can connect as a person. Sizes and CHECK as in Wave F. **§6 of the blueprint lists what
needs a lawyer; P8 and P9 do not ship text without it.**

| | Goal | Files it owns | CHECK | Depends on | Size |
|---|---|---|---|---|---|
| **P1** | A **personal-data registry in code** (every store: fields, subject, purpose, basis, retention, eraser, exporter) and a **gate** that fails when a new store, external host or browser key appears without a row | new `workers/api/src/privacy/registry.rs`, new `tools/gates/personal-data.sh` (+ `.baseline`, `.prove.sh`) | the gate prints `personal-data 0` on HEAD with every `IMAGE_*`/`K_*` in `workers/api/src`, every `https://` host and every `dw_*` storage key accounted for; `.prove.sh`: adding `pub const IMAGE_X: &str = "x";` turns it RED naming `x`, removing it turns it GREEN; comments stripped before counting; a registry row whose eraser is `Retain` must carry a reason string (native test) | — | **S** |
| **P2** | **Erasure reaches every venue store** in the same one-turn forget: bookings, inbox, threads, outbox, idem, campaign marks, audit text; ledger kept with the reason in the answer; plus the missing law **redacted = declared** | `workers/api/src/hubdo/forget.rs`, `services/customers/forget.rs` (+ `forget/tests.rs`), `booking.rs` (redactor only), `channels.rs` (predicate rebuild), `e2e/gates/conservation.mjs` (one law) | native: a fixture venue with one person in all eleven stores → after `forget`, a byte search of every image for the phone's three spellings finds 0, the order money/status folds unchanged, and a second run changes nothing; a pending outbox ticket for an ended order is dropped, for a live order the call is 409 as today; `conservation.mjs` gains the law `chain.redacted == declared` (health already prints both, `services/operations/mod.rs:168-169`) and it fires RED on a scratch image with one undeclared tombstone | P1 | **M** |
| **P3** | **Backups and restores honour erasure**: `KEEP_WEEKLY_MS` 35 d → 21 d; a pseudonymous **erasure register** on the platform object, replayed after every `restore`/`import` and in the PITR runbook; the forget answer's sentence made true | `workers/api/src/cloud.rs` (one constant + its test), `services/operations/mod.rs` (`restore` replay), `platform_store.rs` (`forgot` kind), `services/customers/forget.rs` (notice text) | native: `keys_to_drop` over 60 nightly stamps keeps none older than 21 d; restoring a bundle taken before an erasure → the person's phone is absent from every image afterwards and `declared` equals the pre-restore value; the register holds no phone or name (a test greps the record for digits runs ≥ 7) | P2 | **S** |
| **P4** | **Request intake with a 30-day clock**: a public privacy-request route per venue, identity by WhatsApp thread or past order + phone, `due_at` on the dashboard, overdue = red on health | new `workers/api/src/privacy/requests.rs`, `lib.rs` (2 routes), `public/admin/customers.js`, `public/kit/screens/privacy.js` (form) | native: a request files one `dsr` record with `due_at = received + 30 d`; the route performs no erasure or export (grep of the handler for `forget`/`export` = 0); health shows `privacy.overdue` > 0 when `now > due_at` and the conservation run prints it | P1 | **S** |
| **P5** | **Access, portability, restriction, objection**: `export` (JSON + sq/en/uk HTML) walking the registry, audited as a reveal; a `restricted` flag honoured by campaigns and reveals; "forget me on this device" in the storefront/kit | new `workers/api/src/privacy/export.rs`, `services/customers/record.rs` (flag), `services/campaigns/audience.rs` (exclude), `public/kit/me.js` (device clear) | native: export of the P2 fixture person lists every store the registry marks for them and nothing of another customer (two-person fixture); a restricted person is absent from every campaign audience and the `consent` gate still holds; the export writes one `Revealed`-class audit event; live on `qa-durres`: export of a TEST customer opens in three languages | P1, P4 | **M** |
| **P6** | **Retention jobs**: nightly redaction of order contacts after 24 months (lawyer to confirm), bookings and inbox/threads after 12, waitlist after 24; every retention tombstone declared like an erasure | new `workers/api/src/privacy/retention.rs`, `cloud.rs` (nightly hook), `waitlist.rs` (prune) | native: a log with orders at 23 and 25 months → only the 25-month contacts are redacted and one `Forgotten{reason:"retention"}` declares them; P2's law holds after the pass; `clock` gate stays 0 (the pass takes `now_ms`) | P2 | **M** |
| **P7** | **Couriers, staff, owners, prospects have rights too**: forget/export for courier, staff, owner accounts and waitlist emails; till/fiscal keep the id, not the person | `services/courier/hiring.rs`, `services/identity/staff_admin.rs`, `platform.rs` (operator routes), `waitlist.rs`, `identity_store.rs` | native: forgetting a courier removes `courier`, `invite`, `roster` records and sessions, the courier cannot log in, and `conservation.mjs` law 4 (cash accounted to a courier) still holds on the fixture; a forgotten waitlist email is absent from `GET /api/platform/waitlist` | P1 | **S** |
| **P8** | A **real privacy notice per venue** in sq/en/uk at `/privacy`, rendered from the registry + venue settings (controller, processors switched on, 30-day answer, 22-day backups, complaint to the Commissioner, 16-year line for marketing consent); linked from checkout, booking and every consent prompt | `workers/api/public/kit/screens/privacy.js` (lorem removed), new `workers/api/src/privacy/notice.rs`, `public/store/checkout.js` (link) | `grep -c "Lorem" public/kit/screens/privacy.js` = 0; `curl https://qa-durres.dowiz.org/privacy?lang=sq|en|uk` → 200 and each names every processor ON in that venue's settings and none that is OFF (switch Telegram off on QA → it disappears); **a lawyer signed the text** (the row is not done without it) | P1, lawyer | **M** |
| **P9** | **Processors and transfers**: a processor register (Cloudflare, Meta, Telegram, S3 provider, Stripe, ebills, tax authority, venue AI endpoint), the dowiz↔venue DPA click-accepted at hub creation with its version on the `loc` record, and the live venues' Durable Object location measured | new `docs/privacy/PROCESSORS.md`, `platform.rs` (`create_hub` stores `dpa_version`), `docs/design/BLUEPRINT-GDPR-AND-MCP-2026-09-24.md` §2.4 updated with the measurement | every `https://` host in `workers/api/src` appears in the register (P1's gate enforces it); a hub created without accepting the DPA is refused 400 (native); the DO location of both live venues is written down with the command that produced it | P1, lawyer | **S** (+ legal **L**) |
| **P10** | **Records of processing** (Art. 30 / Law 124 Art. 27) rendered from the registry, per venue and for the platform | new `workers/api/src/privacy/ropa.rs`, `lib.rs` (2 routes) | `GET /api/owner/privacy/ropa` on the fixture lists one activity per registry purpose with basis and retention, and a registry row added in a test appears without touching `ropa.rs` | P1 | **S** |
| **P11** | **Minimisation**: no customer `contact` in assistant facts (courier keeps its own run's address line only); the Telegram ticket prints a phone only for delivery; the AI endpoint named as a processor when set | `services/engagement/assist.rs`, `notify.rs`, `privacy/registry.rs` (row) | native: the owner-assist facts for a fixture with 10 orders contain no phone digits and no names (`assist.rs:200, 265` no longer copy `contact`); a pickup order's ticket has no phone, a delivery order's has one | P1 | **S** |
| **P12** | **Breach runbook** with a recorded `aware_at` (the 72 h is a number), templates for the Commissioner, for each affected venue (processor → controller "immediately", Law 124 Art. 29(2)) and for subjects (binding ≈ Jan 2027) | new `docs/runbooks/BREACH.md`, `platform.rs` (`incident` record + route) | a dry run on the QA venue: `POST /api/platform/incidents` records `aware_at`, the platform health shows the hours left, and the runbook's three templates render with the venue's name; the runbook names the detection sources that exist (`/api/platform/errors`, witness contradictions, `conservation.mjs`) | lawyer for the wording | **S** |
| **P13** | **DPIA screening** for courier live location and for the CRM (EDPB criteria answered in writing); a full DPIA if two or more hit, before Law 124 Art. 31 binds (≈ Jan 2027) | new `docs/privacy/DPIA-courier-location.md`, `docs/privacy/DPIA-crm.md` | each document answers the nine criteria with the file that shows the fact; the courier one states retention of positions as measured (latest fix only, `hubstore.rs:1157-1180`) | lawyer | **S** |
| **P14** | **OAuth 2.1 authorisation server in the Worker**: RFC 9728 resource metadata, RFC 8414 AS metadata, CIMD (SSRF-guarded fetch) with DCR as fallback, `/authorize` reusing the existing logins + a consent screen (client, redirect host, role, venue, scopes), `/token` (form-urlencoded, PKCE S256, `resource` = canonical URI, rotating refresh), RFC 9207 `iss`; opaque access tokens stored hashed as **grants** (`mcpgrant`) | new `workers/api/src/oauth/{metadata,authorize,token,cimd,register,grants}.rs`, `lib.rs` (routes), `identity_store.rs` (kind), `public/admin/agents.js` (grants list + revoke) | `e2e/mcp/oauth.sh` (curl only): 401 carries `resource_metadata`; PRM `resource` equals `https://dowiz.org/mcp`; AS metadata carries `S256`, `client_id_metadata_document_supported: true`, `none`, the `iss` flag; a code redeemed with a wrong verifier → `invalid_grant`; a token issued for another `resource` → 401; a refresh token used twice kills its family; a CIMD URL resolving to 10.0.0.0/8 is refused; Claude Code's CIMD loopback redirect accepted on any port. Live: `claude mcp add --transport http` then `/mcp` authenticates against `qa-durres` | — | **L** |
| **P15** | **`/mcp` for every role, two protocol generations**: grant → the same `Principal` the HTTP door builds → inner token → `crate::route` in-process; `2025-06-18`/`2025-11-25` (`initialize`) and `2026-07-28` (`server/discover`, `_meta` version, `resultType`, `Mcp-Method` header, cacheable lists); per-venue authless guest endpoint with venue from `Host`; `/api/mcp` + API keys kept as legacy, keys gain scopes and lose `restore`/`forget`/admin routes unless scoped | `workers/api/src/mcp.rs` → `mcp/{protocol,dispatch,guest}.rs`, `auth.rs` (API-key scopes), `services/identity/keys.rs` | native: the same `tools/call` answered under both generations; a waiter grant calling an owner route through MCP gets the route's own 403/404 (proves no new privilege path); a guest call on `sushi-durres.dowiz.org/mcp` cannot name another venue (no venue argument exists in any schema — a test walks every schema); an API key without `admin` gets 403 on `POST /api/owner/restore`; `file-size` does not rise (`mcp.rs` is 384 lines today and is split) | P14 | **M** |
| **P16** | The **catalogue covers every route**: one table `(tool, route, verb, roles, scope, danger, schema, annotations)` for guest, waiter, kitchen, counter-manager, courier, owner, platform operator; a **gate** fails on a route with neither a tool nor a `NotAnAgentTool(reason)` | new `workers/api/src/mcp/catalogue.rs`, new `tools/gates/mcp-coverage.sh` (+ `.prove.sh`) | the gate prints `mcp-coverage 0` against `lib.rs`'s route list; adding a route in a scratch build turns it RED naming the path; `tools/list` for a kitchen grant contains no `refund` and for a guest contains no `/api/owner/*` tool (native); every schema parses and names are unique (the existing test, extended) | P15 | **M** |
| **P17** | **Resources and prompts**: menu, live orders, one order, floor, stock, health, ROPA, courier tasks; prompts `morning_check`, `close_the_day`, `eighty_six`, `privacy_request`, `courier_next_run` | new `workers/api/src/mcp/{resources,prompts}.rs` | `resources/read dowiz://venue/menu` equals the public menu route's body byte for byte; a courier grant cannot read `dowiz://venue/orders/live` (the route's 403); each prompt names only tools its role can see (native walk) | P16 | **S** |
| **P18** | **Safety layer**: scopes with step-up (`insufficient_scope` 403 listing the whole set); **two-phase dangerous actions** (refund, forget, restore, till close, campaign send, fiscal arm, delete dish/category, wallet repair) with a server-minted confirmation handle bound to grant + args hash, 5 min, single use, plus MRTR/elicitation where the client speaks it; idempotency keys forwarded; per-grant GCRA limits; a 90-day `agent` audit record for every MCP write | new `workers/api/src/mcp/{confirm,audit,limits}.rs`, `hubstore.rs` (audit kind retention) | native: `refund` without a handle → refused, with a handle for different args → refused, the same handle twice → refused once used; one `via:"mcp"` audit record per write carrying grant and client id and no phone; the 21st write in a minute → 429 with `Retry-After`; `idempotent` gate unchanged and a replayed `place_order` through MCP returns `existing: true` | P15, P16 | **M** |
| **P19** | **An e2e session per client** — Claude Code (`claude -p`), Codex (`codex exec`), OpenCode (`opencode run`) — against `qa-durres` only: guest orders, kitchen advances, courier delivers, owner refunds through the confirmation, a read-only grant hits step-up; every step read back over HTTP; TEST orders closed in `finally` | new `e2e/mcp/{claude,codex,opencode}.sh`, `e2e/mcp/{claude.json,config.toml,opencode.json}`, `e2e/mcp/README.md` (setup snippets) | each script refuses a host not matching `qa-`; each prints its read-backs and exits 0; `conservation.mjs` on `qa-durres` holds after all three; a deliberately wrong scope on the owner grant turns step 4 RED in every client; the audit image shows three client ids | P14-P18, F7 (QA venue) | **M** |
| **P20** | **Connected agents in the console** (sq/en/uk): the three setup snippets, grants listed with client/role/scopes/last use, revoke, QA-grant minting for the operator | `workers/api/public/admin/more.js` (replaces the API-key MCP hint), new `public/admin/agents.js`, `public/admin/i18n.js` | revoking a grant in the console makes the next MCP call 401 within one request; the old hint text ("with an API key as Bearer") is gone in all three languages; `design` gate green | P14 | **S** |

**Order.** P3 first — one constant and a replay, and it makes a promise already shown to customers true; P1 next
(everything else in the GDPR half is driven by it, and its gate is the "every store" proof); then P2 → P5 → P4, with
P11 in parallel (small, and it stops a live leak to a third-party model). P8/P9/P12/P13 start with the lawyer in
parallel and ship when the text is signed. On the MCP half: P14 is the long pole and blocks the rest; P15 and P16
can be written against a stub grant while P14 is in review; P18 must land before any grant with `money`,
`people` or `admin` scope is issued to a real owner; P19 is the definition of done for "all three clients, every
role".
