# R2 — MERGED PHASE ROADMAP (2026-07-16)

> Synthesis of R1-A (mesh/PQ-crypto), R1-B (hub-autonomy/agent-infra), R1-C (kernel/compute/storage),
> R1-D (product-on-protocol), R1-E (ecosystem/legal/growth) into ONE dependency-ordered phase
> sequence for the entire anchor set of `ARCHITECTURE.md` + `STRATEGIC-VECTORS-LOCKED-2026-07-16.md`.
> Every claim inherited from R1 was spot-verified where load-bearing (CI jobs, LICENSE, CONTRIBUTING.md,
> E10/E36 text, D5/D8 absence — all re-checked against the live tree this session).
> No pre/post-MVP split. Every anchor lands in exactly one numbered phase. Phases are real
> build-dependency order; parallel-safety is stated explicitly wherever it exists.

---

## 1. Preamble: anchor accounting + canon corrections to relay

### Resolved anchor count

- **Nominal canon count: 147** (M1-12 + V1-6 + D1-8 + S1-9 + E1-62 + F1-50).
- **E10 ≡ E36** (R1-A headline #7, re-verified: STRATEGIC-VECTORS:89 vs :98, identical three words
  "ML-DSA hybrid", no distinguishing text anywhere). Treated as **one anchor**; E36 is recorded as an
  alias of E10 in the accounting table and is NOT double-counted. → 146 distinct anchors.
- **D5 and D8 are undefined in every revision of both canon docs** (R1-E §D5/D8, re-verified: zero
  occurrences at HEAD; git archaeology across `8180b03eb`, `4aa71b725`, `0d1935d96`, `574f05604`
  shows the "8 (D)" count was overstated by 2 at inception and carried forward unverified). They are
  carried here as **operator-decision placeholders** in Phase 2 — that IS their coverage.
- **Exact resolved count: 144 defined distinct anchors + 2 operator-decision placeholders (D5, D8)
  = 146 distinct IDs; 147 nominal IDs all accounted for** (E36 maps to E10's phase as alias).
  If the operator ratifies definitions for D5/D8 (candidates: DECISIONS.md D5 roles+adapters,
  D8 newest-wins precedence), the total becomes 146 distinct; if renumbered, 144.

### Canon corrections to relay to the operator (found by R1, verified here; ARCHITECTURE.md is
### deliberately NOT edited by this synthesis — "merge, never append" edits are the operator's call)

1. **LICENSE is ALREADY AGPLv3** (full text at `/root/dowiz/LICENSE`, flipped at `ac1caba40`
   2026-07-14, also on `origin/main`). ARCHITECTURE.md §8 + S3 ("LICENSE Apache-2.0 vs AGPLv3") are
   stale. Likewise the force-push/history-scrub question is substantively RESOLVED (H8 runbook
   CLOSED 2026-07-13 + P10 decision 2026-07-16: origin already points at the scrubbed tip; SHA
   rewrite declined as redundant theater). Remaining D3 gates are EUTM + explicit public-flip go
   + one cheap all-origin-refs gitleaks sweep — NOT the scrub.
2. **The CI truth floor is REGRESSED.** `b10a7bfe3` added a gitleaks Tier-0 gate; `f9ab28ff1`
   ("drop ALL JS/TS; rewire CI") silently dropped it. Verified at HEAD: `.github/workflows/ci.yml`
   has exactly two jobs (`telemetry-selftest`, `eqc-proofs`) — no gitleaks, no cargo-test (kernel's
   337 + engine's 47 tests run NOWHERE in CI), no zero-OCI/supply-chain job, and
   `scripts/check-zero-oci.sh:8` references a CI job that does not exist. V3 is therefore false in
   practice; every downstream "GREEN" is unverifiable until this is restored → **Phase 1 for the
   whole roadmap**, not just cluster C.
3. **CONTRIBUTING.md:17 makes a false claim** ("CI rejects commits without a valid Signed-off-by" —
   no DCO job exists; verified). A documentation-integrity RED, folded into Phase 1 (the fix is to
   make the claim true, not to delete it).
4. Minor relays: memory says "kernel 152 tests" — actual is **337 passed / 0 failed** (re-verify
   counts from `cargo test`, never memory); `proto-cap/src/lib.rs:12` still calls the ML-DSA leg
   "a marked TODO" (stale — the leg is real and ACVP-verified); the Hermes cron gateway is DOWN, so
   all "automated hygiene" claims are currently false (fixed in Phase 12).

### Major merges performed (same work item under two cluster names)

| Merged into | From | Why it is one work item |
|---|---|---|
| Phase 1 | C-K1 + E-E2 + E-D3(gaps 1-2) | Both are edits to `.github/workflows/ci.yml` + scripts: gitleaks, cargo-test, DCO, cargo-audit, DECART-dep lint, zero-OCI — one CI rewrite, not two |
| Phase 1 → 6 | C-K1's V5-C re-exec harness + B-B1's "verifier runner" | Identical runner (fresh checkout, re-execute suites, RED/GREEN + rationale); Phase 1 builds it unsigned, Phase 6 adds ML-DSA K/V signatures + merge gate on top |
| Phase 9 | A-Phase-B mesh-healing (HRW-merge/Dijkstra/A*/UF/MST over peer graph) + B's M7 "zero implementations, assign one owner" | Verified: the SAME heal layer, named twice. Math primitives land ONCE in Phase 4 (kernel, zero-dep), consumed by Phase 9 (mesh heal), Phase 4/13 (product route F45, partition-tolerant delivery F46) |
| Phase 10 | B-B3 kill verb + A-Phase-C "unilateral operator kill-switch of M9 shape" (F28) | Same anchor-signed kill frame → COLD-backup-then-halt handler |
| Phase 8 | C-K3 + E's E46/E47 | One typed-metrics/OTel-opt-in/claim-latency-alert build |
| Phase 12 | C-K5 + E-E3 ops items (E49 OpenTofu, E50 restore drill, E11 pgrust drill, cron fix) | One durable-storage/deploy/ops-floor phase |
| Phase 13 | A-Phase-C (M1/M10/F17/S4 integration) + D-D2 (order/PoD/saga spine) | A-C's done-test #1 ("dowiz order frame verified+folded by a second hub") IS D-2's first done-test |
| Phase 14 | D-D3 (F44+F48) + E's E8/E51 + B's E16 cross-ref | One per-hub-knowledge + dispute phase; E16 was a seam anchor (see §5) |
| Phase 16 | D-D4 UI rebuild + E-E3's E12 i18n rebuild (≡ F49) | One i18n rebuild (recovered catalog, parity gate) inside the one UI rebuild |

---

## 2. THE MASTER PHASE TABLE

> "Depends on" = hard build-dependency. "Parallel-safe with" = zero ordering dependency, may run
> concurrently. Wave-0 phases (1-5) all start immediately in parallel.

| # | Name | Anchors covered | Depends on | Parallel-safe with | Current → target gap | Falsifiable done-test |
|---|---|---|---|---|---|---|
| **1** | **CI TRUTH FLOOR** (dev-time fences, SCOPE RULE stamped on every gate) | V2, V3, V5, S3, S6, D6, E2, E3, E40, E52, E58, E62 | — | 2, 3, 4, 5 | CI regressed to 2 jobs (`f9ab28ff1`); kernel 337 + engine 47 tests run nowhere in CI; gitleaks gate dropped; CONTRIBUTING.md claims a DCO check that doesn't exist; no cargo-audit/deny.toml, no vendoring policy, no NO-COURIER-SCORING dowiz wire, no DECART-new-dep lint, no claim-latency ledger, no V5-C re-exec harness. Target: restore/extend `.github/workflows/ci.yml` — cargo-test (offline, vendored), gitleaks (from `b10a7bfe3`), DCO check (makes CONTRIBUTING:17 true), cargo-audit + deny.toml with rsa-triage-form waivers, zero-OCI job, DECART-dep lint, NO-COURIER-SCORING grep, claim-latency ledger appender (V5-B), unsigned V5-C local re-exec harness; fix license metadata (kernel Cargo.toml missing `license`, two MIT tool crates decided+documented); write re-scoped reinstatement-trigger docs for the deleted-surface i18n/IDOR/OTP gates; fix stale `check-zero-oci.sh:8` comment. | On a probe branch: a planted secret, a failing kernel test, an nginx `FROM`, a commit without Signed-off-by, and an un-DECARTed new dep EACH independently turn CI RED; a clean signed commit is GREEN; the claim-latency ledger gains one entry per commit; the V5-C harness re-executes a diff in a clean checkout and emits RED on a planted failure; `grep` finds a DCO job so CONTRIBUTING.md:17 is now true. |
| **2** | **CANON REPAIR + OPERATOR DECISION BATCH** (docs + rulings only) | D5, D8, V4, V6, E35, E42, E53, E55 | — | 1, 3, 4, 5 | Canon carries known-false statements (LICENSE, scrub, DCO claim, "147") and ~15 rulings gate later engineering. Target: operator-approved canon merge fixing §8/S3 stale lines; restore dropped D3/D4 labels; **D5/D8: define (candidates = DECISIONS.md D5 roles+adapters, D8 newest-wins) OR renumber to 145/144 — this phase cannot close without the ruling**; ratify E10≡E36 merge; write ADR-020 as a real `docs/adr/` file; codify V4 split-track + closure-criterion template, retrofit active arcs; V6 metaphor audit + record G11 as the stable-track criterion; define-or-strike E35 "3-tier locality" (impl attaches to Phase 9 if defined); re-anchor E42 to `bebop2/delivery-domain` + blueprint v3 (delete/populate empty `delivery/`); rule on: F44 DECART, F48 merge semantics, D2/iroh direction, E1 hub-ring semantics, F10 depth-cap value, V1-B context-isolation bar, S7/S8 split, M12/F25 replay bound, "BD" expansion, M9 subtree-kill pre-F10, E13-20 per-item numbering, E39 rewording, F21 trust-root-PQ naming, F20 amendment (full list §4). E53 (rsa-triage template) codified as THE suspension form; E55 Manifesto confirmed built+linked. | `grep -n "Apache-2.0" docs/design/ARCHITECTURE.md` → 0; all "147" claims consistent with the ratified count; D-series enumerable (D1..D8 defined OR renumbered everywhere); each §4 decision has a written ruling or an explicit dated deferral in canon §8; every emergent/swarm/organism hit in canon sits within 3 lines of a named computed criterion; closure-criterion template exists and every active arc carries done-when + evidence + strand condition. |
| **3** | **PQ TRUST-ROOT HARDENING** (crypto correctness before anything rides on it) | M2, M4, M12, E10 (≡E36 alias), F19, F21, F24, F26 | — | 1, 2, 4, 5 | Frames are hybrid-signed but the trust root is classically forgeable: delegation links + genesis anchors are Ed25519-only (`roster.rs:114,167,179`), making the ML-DSA frame leg vacuous against exactly the F21 adversary (attacker forges chain classically, supplies own PQ key). C4b `mod_l` variable-time secret-dependent branch still open (HIGH, live classical side-channel on Ed25519 signing); three parallel "ML-KEM-768" stacks, two non-FIPS (proto-crypto η1=3, H(sk‖c), G=SHAKE256 vs core's FIPS-exact J(z‖c)); red-line gate maps only Money (Auth/Secrets/Migrations unmapped); classical KEM fallback (X25519) absent from bebop2; ladder/wycheproof harness = placeholder stubs; stale "PQ is TODO" header. Target: hybrid (ML-DSA) delegation links + genesis anchors; ONE canonical KEM/DSA + named oracles; C4b/C6b/C3 closed; full red-line category mapping; X25519 KEM leg. F19/F24 (fail-closed scope/expiry) already BUILT — regression-guarded here through the refactor. | (1) A classically-forged delegation chain is rejected under a quantum-attacker fixture (hybrid anchors + links); (2) dudect/CT gate GREEN on `mod_l` (C4b closed); (3) exactly ONE canonical ML-KEM-768 + ML-DSA-65 with ACVP KAT — every other copy deleted OR oracle-marked with a CI byte-agreement test (proto-crypto/dowiz-pq reconciled to FIPS or struck); (4) red-line gate denies Auth-, Secrets-, and Migrations-scoped capabilities, not just Money; (5) `proto-cap/lib.rs` no longer claims PQ is TODO; F19/F24 suites stay GREEN. |
| **4** | **KERNEL PRODUCT-MATH PRIMITIVES** (the single graph-math implementation) | F45, E61 | — | 1, 2, 3, 5 | No route computation exists in the dowiz kernel (`geo.rs` projects onto given polylines only); Dijkstra/A*/DSU/MST absent from dowiz AND bebop2 — but a real zero-dep Dijkstra/A*/CH router exists stranded in `crates/bebop/src/cost_estimate.rs:205-290`. Target: port the router into the kernel with a road-graph ingestion port onto `csr.rs`; DSU primitive + Kruskal MST (parity-swap `cgraph.rs` BFS `c_components`); the six GS §2.6 geo functions; wasm exports for all of it + `field_frame::compose` RGBA (currently unexported — physics can't reach a canvas). This is the ONE implementation of the M7/E32/E33/F45/F46 math; Phases 9 and 13 consume it. E61 (kernel math-first) is substantively BUILT — confirmed here as the exemplar. | Route on a hand-oracle 10-node graph = known shortest path; A* result == Dijkstra result (admissibility); DSU `c_components` byte-identical to BFS on existing cgraph fixtures; GS P0.1 six-item acceptance verbatim (pin-drop / floor-slice / open-space degrade / <1° bearing / 0-360 seam / LOS rectangle); `compose` reachable from node via wasm with bit-identical frames across two runs. |
| **5** | **ROUTING ORGANISM WIRING** (P0-C1: 100% compute → live) | E13, E14, E15, E19, E20, F6 | — | 1, 2, 3, 4 | harmonic_centrality + kelly_fraction tiering is 100% built + tested (`routing.rs`/`control.rs`/CLI) and 0% wired: `governance.sh` never calls `classify_complexity`/`rank_models`; lane width never fed by live telemetry; no per-agent spend budget; paired-debate primitives (deliberate(), jury_aggregate) unenforced at routing tier; session-close verification_gate not invoked. Target: exactly P0-C1 (SYNTHESIZED §2-C verbatim: three governance.sh calls, `bucket` column, live lane width) + TokenBucket/Budget spend enforcement in gov_route reject path (F6/E19) + Complex-bucket routes pass deliberate()/jury with verdicts in the precedent store (E20) + HK-03 gate at session close. E13/E14 formalized as managed-advisory current tier; self-host llama.cpp/vLLM execution is gated on GPU-unlock and ships via Phase 15's blueprint. | The same task classified `Complex` receives a different (wider/costlier) route than `Simple`; lane width changes when injected arrival-rate telemetry changes; an over-budget call is refused; a session that edited code with failing evidence cannot emit "complete"; a Complex route's debate verdict appears in the precedent store. |
| **6** | **V1 SPLIT-IDENTITY + ADVERSARIAL VERIFIER** (epistemic bedrock) | V1, E9 | 1, 3 | 5, 7, 8 | Zero split-identity code exists (no key_K/key_V anywhere); all verifier fragments are self-context (three-model-review orchestrated by the same session, HK-03 self-checks); no merge gate tied to any verifier signature — while the entire substrate (ACVP ML-DSA, hybrid gate, canonical TLV, genesis loader) sits finished and idle. Target: K/V keygen ceremony (load_genesis pattern + derive_pq_seed; hybrid keys ⇒ needs Phase 3's C4b closed); diff-signer (TLV over commit-sha3 + diff-sha3, key_K); key_V-signed RED/GREEN verdicts from Phase 1's re-exec harness run in an independent context (bar per Phase 2 ruling); CI merge gate (both sigs, K≠V, GREEN on red-line paths); claim-latency feed into FalseClaimMeter; standing "enforced approximation: identity ≠ person" residue line in every verdict. | A diff with a failing test cannot obtain a key_V GREEN (re-execution goes RED); a GREEN verdict signed with key_K is rejected by the gate; a PR without a verifier signature cannot merge — all three REDs demonstrated, then GREEN on a clean diff, each verdict carrying the residue line. |
| **7** | **MONEY-LAW CLOSURE** (red-line correctness before persistence) | S5, S9 | 1 (verifier-strengthened by 6) | 5, 6, 8 | event_log dedup bug: `commit_after_decide` hashes before prev-chaining while `append` rebinds prev then hashes — replaying a zero-prev event onto a non-empty log commits a SECOND event (existing idempotency test only covers genesis). FSM is happy-path only (`InDelivery→[Delivered]`, no compensation edges); `money.rs` has no reversal primitive; money is not evented; `estimate_order_total` silently swallows tax overflow to 0; engine panic-as-fail-closed undocumented. Target: P0-A2 fixed correctly (bind prev BEFORE dedup check + the RED replay-on-non-empty-log test — the blueprint's own acceptance was insufficient); P0-A4 compensation states + reversal primitive + DELIBERATE golden-signature drift-gate re-key; money ledger-entry events through commit_after_decide; both S5 deviations decided + documented. First real consumer of the V5-C verifier (red-line diff). | Cancel-after-confirm reaches a terminal compensated state whose ledger entries net to exactly zero; the replay-on-non-empty-log RED test fails on pre-fix code and passes after; the drift-gate re-key is a named, reviewed diff (never a silent constant change); money-event replay reproduces identical state. |
| **8** | **TYPED LOCAL OBSERVABILITY** (M8 made real, not vacuous) | M8, S7, S8, D7, E46, E47, F29, F31, F32, F36, F39, F40 | 1 | 5, 6, 7 | The only existing metrics pipeline EXFILTRATES: bash `resource_sample()` → Telegram, unsigned, untyped. No Rust typed CPU/GPU per-process metrics; no claim-latency alerting; F39 vacuously green; OTel absent (compliant default, but the opt-in local sink itself unbuilt); F40 signed self-report unbuilt. Target: Rust typed-metrics module (per-process CPU from /proc/self, GPU as typed Option-none); strict typed log-line schema (reject-on-untyped, F32); local JSONL sink via spool.rs adapter; claim-latency anomaly alert consuming Phase 1's ledger (F36/E47 — near-duplicate anchors, one build); ML-DSA-signed envelope + explicit opt-in marker for ANY remote sink (resolves the M8-vs-Telegram tension as F40 operator-opt-in self-reporting); OTel opt-in local-only (F29/E46); S7/S8 split documented per Phase 2 ruling. | Metrics accrue locally with zero network by default (grep + capture proves nothing leaves the host without the opt-in marker); an untyped log line is provably rejected; `kill -9` mid-write loses zero acked spool records; the Telegram report's signature verifies against the operator key and fails on 1-bit corruption; a synthetic 52s-on-1610-line-diff pattern raises the claim-latency anomaly flag. |
| **9** | **CONFIDENTIAL, SELF-HEALING WIRE** (transport substrate completion) | M3, M6, M7, D2, E31, E32, E33, E34, E38, F11, F12, F13, F15, F16, F18, F20, F22, F23, F25, F30 | 3, 4 (rulings from 2) | 5, 6, 7, 8, 12 | ML-KEM is implemented but NEVER invoked for transport (zero encaps/decaps in proto-wire; default feature `insecure-tls` accepts any cert; WSS runs plaintext `ws://`; `NoopPayloadEnc` sits where the ML-KEM→XChaCha20 layer belongs) — the wire is authenticated but readable. D2 inverted vs code (iroh absent, quinn is the only carrier — F20 impossible as written). The entire M7 heal layer (partition detect, HRW-merge, shortest-path re-route, MST overlay) = 0%. Revocation gossip named but not running; F25 replay ledger in-process only; F30 at-rest crypto stranded in legacy vault.rs; QRNG beacon feature-gated but unwired (M3/F13/F22); F18 batching test-only. F11/F12 BUILT — regression-guarded here. Target: ML-KEM session encryption to just-met peers (F16); heal module over the peer graph consuming Phase 4's math (M7/E31-33/F15); D2 resolved via written DECART (land iroh or amend canon + named trigger, F20/E34 with it); revocation gossip loop (F23/E38); operator beacon toggle + beacon-down test (M3/F13); F25 bound blessed-or-persisted per Phase 2 ruling; per-hub at-rest store (F30); library batching (F18); stdio/HTTP transports (M6 extension). | Two nodes that just met derive an ML-KEM session key and a passive on-path capture is ciphertext, not plaintext (F16); `--features anu` on mixes beacon bytes, beacon down still succeeds floor-only (M3/F13/F22); two islands with divergent roots deterministically merge via HRW (F15/E31); a revoked key gossips to a 3rd node and is rejected there (F23/E38); a dropped node is routed around via recomputed shortest path + spanning tree within the heal budget (M7/E32/E33); a written D2 DECART exists (iroh landed OR canon amended with a named network-unlock trigger). |
| **10** | **HUB RUNTIME: POLICY-AS-DATA, KILL-SWITCH, BOOT** | M5, M9, E37, F1, F2, F5, F8, F28 | 3, 6, 9 | 11, 12 | No named kill-switch exists anywhere (the one mandated global bound is the one not built — today only `systemctl kill`); the legacy `guard.rs` KillSwitch is a ≥2/3 consensus registry, the OPPOSITE of M9's unilateral operator kill; no HubPolicy runtime entity (transport policy = compile-time consts); genesis loader built but not wired into MeshNode boot, `config/genesis.example.txt` missing, no key ceremony; revocation gossip loop absent (F5's missing half); no dynamic listener open/close; no insecure-bridge flag. Target: HubPolicy module (ports/bridges/model-endpoints/access-lists/RedLinePolicy/HybridPolicy as operator-editable hot-reload data, F1); dynamic listeners deny-by-default + TokenBucket (F2); operator kill verb = anchor-signed frame → COLD-backup-then-halt, restartable (F28/M9, frame kind from Phase 9's wire format, signing per Phase 6's ceremony); boot wiring of load_genesis + example file + ceremony (E37); gossip loop live (F5); local-telemetry insecure-bridge flag when ClassicalUntilPqAudit active (F8). | A kill frame signed by a genesis anchor halts a running node only after a COLD snapshot exists (RED: unsigned/non-anchor kill ignored; RED: kill without completed backup refuses to halt-lose-data); a HubPolicy edit changes the accepted-port set without restart; MeshNode boot loads a genesis file and fail-closes on empty; a revoked peer's frames are dropped by a second node after one gossip round; the insecure-bridge flag is visible in local telemetry. |
| **11** | **COMPUTE BUDGET & CACHE** (P0-A1 + E-compute, throttles on Phase 8 rails) | E21, E22, E23, E24, E25, F33, F34, F35 | 1, 8 | 9, 10, 12 | E21 boundary BUILT (honest gpu Err stub); everything else absent: no spectral-decomposition cache (recompute on hot paths) + two fresh Vec allocs per field step; no Rust TokenBucket for GPU/spend (F33); no Modal adapter/budget ceiling (E22/F34); no webgl/webgpu features (E23); SIMD only the AVX2 dot-product (E24 batch lanes missing); no NUMA pinning (E25); F35 tiny-edge-model not conflated with hermes dev-tooling. Target: snapshot_root-keyed decomposition cache (analytic DCT where closed-form) + allocation-free step(); bounded spool drainer for heavy ops; TokenBucket budget rails; feature-gated Modal job port (offline = honest Err, degrade-closed ceiling); webgl/webgpu empty features; SoA f64x4 batch Kalman + softmax reduction; NUMA behind a trait port, crate by DECART bake-off, adopted only on measured win. | Unchanged topology ⇒ decomposition counter stays 0 across 1000 steps and a snapshot_root change makes it exactly 1; default `cargo build` dependency graph byte-identical to today; TokenBucket property test never exceeds budget under concurrency; Modal stub returns Err offline and the ceiling degrade-closes in a fake-billing test; SIMD lane bit-identical to scalar reference. |
| **12** | **DURABLE STORAGE, DEPLOY & OPS FLOOR** (persist only what Phase 7 made correct) | D1, S1, S2, E11, E26, E27, E28, E29, E30, E48, E49, E50, F37, F38 | 7 | 9, 10, 11 | Only MemEventStore exists (nothing durable to replay from — E28); COLD archives are terminal-ops-only, NO restore drill has ever run (E27/E50); deep-clean built but its Hermes cron gateway is DOWN (hygiene loop dead); pgrust binary not installed, adapter never exercised (E26/E11); zero .tf files (E49) and the planned pg-backend needs an advisory-lock falsifier on pgrust first; systemd units exist only for the absent pgrust binary; no in-repo timers (fresh hub can't reproduce the schedule). E48 H8 runbook BUILT — confirmed + reused. Target: disk BlockStore adapter (E29); COLD zstd archiver + restore-verify subcommand (E27/F38, 3-2-1-1-0's "0 errors" leg automated) + drill incl. pgrust leg (E50/E11); durable EventStore (file-JSONL first, PgEventStore after pgrust proven) STRICTLY after Phase 7's dedup fix (E28); pgrust install+smoke as backup/fallback (E26, promotion stays a hub choice); in-repo systemd units + timers (S1, E30/F37) + revive the hygiene cron; OpenTofu single-host module with de-risked state backend, or an honest DECART declining it (E49); microVM launcher stays probe-gated until fleet>5 (S1/S2 confirm). | `kill -9` mid-append loses zero acked events and replay reproduces the exact tip id; COLD archive → restore-verify is byte-identical + drill log shows `integrity_check=ok` (incl. pgrust restore leg); `tofu apply` creates one microVM from a clean checkout and `tofu plan` is then empty OR a dated DECART declines it; `systemctl list-timers` shows deep-clean from the in-repo unit and the hygiene job actually fires. |
| **13** | **DELIVERY ON PROTOCOL** (dowiz rides the mesh: integration + order spine) | M1, M10, S4, E1, E39, F17, F41, F42, F43, F46, F50 | 4, 7, 9, 10 | 11, 12, 18 | The biggest structural gap: dowiz has ZERO dependency on the protocol (one comment at `domain.rs:524`); no second-service-hub proof exists; "hub-ring" is two words with no spec (adopted reading per Phase 2 ruling: consistent-hash order/region ownership over HRW — a literal star hub would contradict M7); PoD absent from the product (primitive stranded in `crates/bebop/src/pod.rs`); payout is legacy-central-DB history; double-entry ledger stranded in `crates/bebop/src/ledger.rs`; no tonic/prost anywhere (S4/F17 premature until now — this is the phase where two in-host services first exist); F46's order-state half (who finalizes during a split) leans on the I-FINAL quorum-intersection proof, whose home (bebop consensus path vs dowiz eqc dir) must be decided here — P0-A5 currently cites a file that doesn't exist. Target: dowiz→protocol port (SignedFrame produce/consume); order intake → signed envelope → DoD gate → delivery-domain fold across two real hubs with divergent internals (M1/M10); hub-ring ownership overlay (E1/F41); DeliveryClaim ported, edge-ML-DSA-signed, multi-signal (geo + optional photo — one capture flow feeding PoD AND the splat bootstrap), k-of-n threshold settle (F42); payout saga on the ported double-entry ledger with compensation + cash-reconciliation feature parity (F43, consumes Phase 7); first `.proto` contract + tonic DECART + gRPC-internal/REST-edge (S4/F17); E39 hash-chain/signed-frame unification per Phase 2 rewording; F50 standing invariant: no rebuilt flow may reintroduce a mandatory central service. | An order placed on hub A folds to an identical `DeliveryStatus` on hub B running a different internal store (M1+M10); forged `Pending→Delivered` rejected; a tampered PoD claim is rejected and a valid k-of-n claim settles; the ledger sums to zero across one delivered order AND one cancel-after-confirm compensation; a gRPC-internal call and a REST-edge call serve the same order projection from the pinned .proto (golden-encoding test); solo-hub island test: the full flow completes with zero non-hub services running (F50/F12); partition-then-merge double-finalization test goes RED by design pre-fix and GREEN after (F46 closed, not half-closed). |
| **14** | **DISPUTE/ESCROW + PER-HUB GRAPH-WIKI** | E8, E16, E51, F44, F48 | 2 (F44+F48 DECARTs — HARD), 13 | 15, 16 | F44: zero implementation (0 grep hits for escrow/arbitrat/dispute in any .rs) but a full spec exists (`fable-protocol-2026-07-11/F2-dispute-arbitration.md`: 6-state fail-closed machine, timeouts, default-refund invariant, RED test) — carrying TWO canon contradictions that ONLY the operator may resolve (jury→reputation.rs vs M12/NO-COURIER-SCORING; UMA/Kleros vs M6 zero-deps; candidate M12-consistent resolutions are named in R1-D but NOT chosen here). F48/E8/E51: single-hub knowledge substrate built (living_knowledge PRIMARY recall@5=1.0, spine, trigram, csr, BlockStore) with ZERO replication — no sync, no per-hub instancing, no merge semantics (content-address vs CRDT = Phase 2 ruling; dormant crdt-fence intent checked first). E16 (spectral+BD memory) = the seam anchor, owned here: substrate built, "BD" expansion ratified in Phase 2, consumers = F48 + Phase 5 rank folding. Target: F2 state machine as protocol messages, escrow HOLD = paired ledger entries (reuses Phase 13's ledger), default-refund-on-timeout, arbiter model per DECART; per-hub graph instance + delta sync via signed envelopes + content-address dedup, merge policy per ruling. | F2's own RED test verbatim: dispute opened, no evidence, timeouts elapse → escrow auto-refunds claimant, funds conserved; ledger still sums to zero with an open HOLD; two hubs diverge offline then sync → both hold the union with no central node consulted; kill either hub → the other retains a complete graph (no-SPOF falsifier). |
| **15** | **LIVING ORGANISM UNBOUNDED** (general self-mod, sub-hubs, model autonomy) | M11, E17, E18, F3, F4, F7, F9, F10, F27 | 10 (HARD: the single mandated bound must exist first), 5, 9 · E13-execution additionally on GPU-unlock (operator/external) | 14, 16 | M11 is dependent, not independent: "unbounded" is only permissible once M9's kill-switch exists (Phase 10) and each F-line's LOCK-qualifier is built. Self-mod effector is one scalar wide (Kalman q_scaler only); no sub-hub concept or depth cap (F10, cap value = Phase 2 ruling); no sha3-verify-or-deny model ingestion (F3/F27); MCP built but tool calls not capability-bound (E17/F4 qualifier); the mature proto-cap system stops at the wire — agents/tools/subagents carry NO minted capabilities (E18); no eqc-gated self-update (F9); F7 vacuous (no consensus rule to defect from) — recorded as satisfied-by-absence. Target: generalize SelfModEffector to HubPolicy revisions (deliberate-gate + floor-gate + audit kept, red-lines stay HumanGated); sub-hub spawn with capability-carried depth + cap refusal; model manifest {url, sha3} → verify-or-deny; per-agent capability minting with attenuation on spawn + MCP binding + signed MCP-surface announcement; eqc-gate-or-deny self-update; llama.cpp/vLLM provider blueprint behind a port (sha3 weights + budget ceiling), executed when GPU-unlock fires. | A floor-violating HubPolicy self-revision is refused and logged REJECTED while a mirror-agreed floor-clean one applies; sub-hub spawn at depth > cap is refused; a model blob with a wrong sha3 is refused at load; a self-update with a failing eqc proof is denied; an MCP tool call without a minted capability is refused; F7 recorded vacuous in canon. |
| **16** | **PRODUCT UI REBUILD** (deterministic wasm Sea & Sheet + i18n + WCAG) | D4, E12, E41, E43, E44, F49 | 4, 13 (i18n gate hosted by 1) | 14, 15 | apps/web + packages/ui + i18n were FULLY DELETED (79ef316f6/db766de47/fce5738b0) — this is a REBUILD preserving the feature inventory (As-Built summary + DOWIZ-INTERFACES-PLAN master checklist = the behavioral oracle; git history recovers assets, e.g. the paid-for 631×3-string i18n catalog via `git show db766de47~1:packages/ui/src/lib/i18n.ts`). The kernel-driven `web/` shell (20 VbM tests) + wasm surface exist; zero product pages, zero i18n, zero a11y. Target: 26 pages on the shell pattern, all math from kernel wasm (zero JS re-implementation, CI grep gate); field render as the Sea layer; i18n rebuilt from the recovered catalog (sq/en/uk key-major + parity CI gate — E12≡F49, one build, canonical-repo DEV-TIME fence per SCOPE RULE); WCAG-AA + responsive as page-level acceptance (E43/E44); address-picker v1 + courier photo capture shipped in courier pages (feeds Phase 13 PoD + Phase 17 splat). | Feature-inventory reconciliation: every As-Built shipped feature maps to a rebuilt page or an explicit operator-approved drop (the master checklist is the ledger); Playwright E2E full loop (client order → courier PoD → owner settlement) green against the wasm UI on a mesh hub; i18n parity gate RED on a missing key, GREEN ×3 locales; axe WCAG-AA zero critical; CI grep proves zero client-side money/geo/FSM arithmetic. |
| **17** | **DEMO, SPLAT TIERS & GPU-UNLOCK CLOSURE** | E4, E45, F14, F47 | 11, 16 + GPU-unlock trigger (operator/network `cargo add wgpu`) | 18, 19 | Demo half of E4 BUILT (web/ renders kernel math); no delivery SCENE (compose unexported until Phase 4; no scripted scenario); video correctly blocked on the W21 wgpu offline-ceiling — do not fake it earlier (E4 LOCK); F14 topology viz absent; splat P1 items (SplatReconstructionJob+Modal ← courier photos; tiered renderer; Tier-C pre-render) land exactly per the GS synthesis, rejections standing (no CuPy/trained-reranker/TimesFM-ETA/satellite/standing-GPU). Target: scripted wasm delivery demo (courier along a Phase-4 route, FSM ticking, field physics), bit-deterministic; post-unlock: real wgpu adapter behind the existing `gpu` feature, video capture of the same scenario, feature-gated webgl topology viz (F14), splat P1 per GS acceptance. | Demo runs offline (node + browser) with bit-deterministic frames for a fixed scenario; default `cargo build/test` dependency graph byte-identical to today; post-unlock: a video artifact rendered from the same scenario; one real address reconstructed ≤$2 with $0 cache-hit re-submission (GS P1.1 verbatim); topology viz absent from every default build. |
| **18** | **PUBLIC-FLIP READINESS & EXECUTION** (operator-gated one-way door) | D3, E5, E54, E59 | 1 (gitleaks CI green = flip precondition), 2 | 3–17 (prep work), 19 | LICENSE/NOTICE/DCO/TRADEMARK/MANIFESTO already in tree (canon §8 stale on this); scrub substantively resolved; remaining: SECURITY.md + named private disclosure channel, CODE_OF_CONDUCT, README, repo-settings script (clone bebop GOVERNANCE.md:39-56), the one residual all-origin-refs gitleaks sweep, CITATION.cff — all agent-preparable now. Operator-only: brand decision ("dowiz" vs weak descriptive "DeliveryOS") BEFORE EUTM e-filing (€850 one class, Fast Track, SME-Fund voucher check), then the explicit public-flip go. Discussions + social (E5) activate at flip. | Re-run the P10 GO/NO-GO table — all gates ✅ with evidence (gate = sweep artifact over ALL origin/* refs; gate = EUTM application number; + written operator go); post-flip `gh repo view` returns AGPL-3.0 license + Discussions enabled. |
| **19** | **ECOSYSTEM GROWTH ENGINE** | E6, E7, E56, E57, E60 | 18 (public repo is the surface) · soft: 14 (wiki demo strengthens dossier) | 17 | No positioning doc for the mesh-pivot era (BRAND-BIBLE memory pointer is stale — file doesn't exist); no grant dossier (NLnet NGI Zero verified: ≤€50k first-time, FOSS-license mandate already satisfied, EU/UA dimension fits E60; the 2026-06Z deadline passed — target the next call); pricing lives only in memory; E57 usage-PQ-api deliberately spec-only until a falsifiable demand trigger (first paying B2B inquiry) — building now violates ponytail/C8. Target: positioning + landing copy (bebop PQ protocol = headliner, delivery = by-product, E6); NLnet dossier submitted at next call (E7/E56/E60); pricing/B2B doc refreshed against the mesh pivot; usage-PQ-api spec + named build-trigger (E57). | Grant application submission receipt OR a dated, reasoned decision not to apply; positioning doc merged; pricing doc refreshed and in-tree; usage-PQ-api exists as spec ONLY with its build-trigger named; G11 (first real order — achieved via Phases 13/16) recorded in canon with its owner. |

---

## 3. DEPENDENCY GRAPH

### Adjacency list (phase ← hard dependencies)

```
P1  ← ∅                     P8  ← P1                  P14 ← P2*, P13
P2  ← ∅                     P9  ← P3, P4  (rulings P2) P15 ← P10(hard), P5, P9  [+GPU-unlock for E13-exec]
P3  ← ∅                     P10 ← P3, P6, P9           P16 ← P4, P13  (gate hosted by P1)
P4  ← ∅                     P11 ← P1, P8               P17 ← P11, P16  [+GPU-unlock, operator]
P5  ← ∅                     P12 ← P7                   P18 ← P1, P2  [operator: brand+EUTM+go]
P6  ← P1, P3                P13 ← P4, P7, P9, P10      P19 ← P18  (soft: P14)
P7  ← P1  (strengthened P6)
* P14's dependency on P2 is specifically the F44 and F48 DECART rulings.
```

### Waves (maximum wall-clock parallelism)

```
WAVE 0 (all parallel, start now):   P1 CI-truth  P2 canon+decisions  P3 PQ-hardening  P4 kernel-math  P5 routing-organism
                                       │              │                  │  │             │  │              │
WAVE 1:                             P6 V1 ◄──(P1,P3)  P7 money ◄──(P1)  P8 observability ◄──(P1)   P18-prep ◄──(P1,P2)
                                       │              │                  │
WAVE 2:                             P9 wire ◄──(P3,P4)            P11 compute ◄──(P1,P8)    P12 storage/ops ◄──(P7)
                                       │
WAVE 3:                             P10 hub-runtime ◄──(P3,P6,P9)
                                       │
WAVE 4:                             P13 delivery-on-protocol ◄──(P4,P7,P9,P10)
                                       │
WAVE 5:                             P14 dispute+wiki ◄──(P2,P13)   P15 organism ◄──(P10,P5,P9)   P16 UI ◄──(P4,P13)
                                       │                                                            │
WAVE 6:                             P19 growth ◄──(P18)                                  P17 demo/GPU ◄──(P11,P16)
```

The critical path is **P3 → P9 → P10 → P13 → {P14, P16} → P17** (crypto → wire → hub runtime →
delivery spine → dispute/UI → demo). P1/P2 gate everything epistemically but are cheap and parallel.
P5, P8, P11, P12, P18-prep are off-critical-path lanes that should be fanned out early.

---

## 4. OPERATOR DECISIONS REQUIRED

Every item below is blocked on a HUMAN decision, not engineering. Each lives inside a numbered phase
(coverage, not deferral) — listed here for visibility. **None are resolved by this document.**

| # | Decision | Phase | Blocks |
|---|---|---|---|
| O1 | **D5/D8**: define (candidates: DECISIONS.md D5 roles+adapters; D8 newest-wins precedence — hypothesis about intent, NOT canon) OR renumber the D-series and correct the total everywhere | P2 | The anchor arithmetic of every doc citing "147" |
| O2 | **E10≡E36**: ratify the merge (one anchor, alias) or write the distinguishing scope that was never written | P2 | Anchor count; P3 bookkeeping |
| O3 | **F44 DECART** (named blocking item): the only existing dispute spec (`fable-protocol-2026-07-11/F2-dispute-arbitration.md`) proposes jury→reputation.rs (contradicts M12 + NO-COURIER-SCORING law) and UMA/Kleros (contradicts M6 zero-protocol-deps). M12-consistent candidates exist (operator-gated arbiter capability; Schelling voting among staked capability-holders) — **operator must choose; not silently resolvable** | P2 (ruling) | P14 (dispute/escrow build) |
| O4 | **F48 merge semantics**: content-address-only vs CRDT for per-hub graph-wiki sync; dormant `crdt-fence` guard intent must be read first | P2 (ruling) | P14 (wiki replication) |
| O5 | **D2/iroh inversion**: land iroh, or amend canon to "quinn primary + named network-unlock trigger" (F20/E34 amended with it) | P2 pre-ruling, executed as P9's written DECART | P9 |
| O6 | **E35 "3-tier locality"**: define the three tiers or strike the anchor — cannot be built or falsified as written | P2 | P9 attachment |
| O7 | **E1/F41 "hub-ring" semantics**: ratify the consistent-hash order/region-ownership reading (a literal star-hub would contradict M7 no-SPOF) | P2 | P13 design |
| O8 | **F10 max-depth-cap value** for sub-hub recursion | P2 | P15 |
| O9 | **V1-B context-isolation bar**: fresh worktree / separate machine / different model family — one sentence of canon | P2 | P6 |
| O10 | **S7 vs S8 split** ratification (proposed: S7=tracing, S8=typed metrics) | P2 | P8 documentation |
| O11 | **M12/F25 replay-ledger bound**: bless the per-gate in-process bound or require persistence | P2 | P9 |
| O12 | **"BD"** in "spectral+BD memory" (E8/E16): one authoritative expansion | P2 | P14 |
| O13 | **M9 subtree-kill semantics** (undefinable until F10 hierarchy exists — sequence or redefine) | P2 | P10/P15 |
| O14 | **E13-E20 per-item numbering** ratification (session-dialog text is not on disk; R1-B's inferred mapping needs a lock) | P2 | Bookkeeping |
| O15 | Cheap canon rewordings: E39 ("signed event_log" → hash-chained log fed by hybrid-verified frames), F21 (name the trust-root PQ requirement), F20 (amend with O5), E42 re-anchor, M3-vs-M6 beacon boundary note | P2 | Doc truth |
| O16 | **EUTM**: brand decision FIRST ("dowiz" vs weak descriptive "DeliveryOS") → e-filing €850 + SME-Fund voucher check | P18 | P18 close |
| O17 | **Public-flip go** — one-way door, never autonomous (standing rule) | P18 | P18/P19 |
| O18 | **GPU-unlock** (network `cargo add wgpu`) — environment/operator trigger | external | P17 video/wgpu; P15 E13-execution |
| O19 | **I-FINAL proof home** (P0-A5 cites `eqc-proofs/lambda_max_of_d.rs` which does not exist anywhere): bebop consensus path vs dowiz tools/eqc emitted-proof dir | P13 | F46 full closure |

---

## 5. FINAL ANCHOR ACCOUNTING (every ID → exactly one phase)

**Seam check:** one anchor fell between the five clusters' ownership: **E16** (spectral+BD memory)
— R1-B documented it as a cross-reference and explicitly asked the merge step to assign a
storage/knowledge owner; no cluster owned it. Assigned: **P14** (substrate already built; remaining
work = per-hub instancing + BD ratification via O12). No other seams found — E1-E62, F1-F50,
M1-M12, V1-V6, S1-S9, D1-D8 are all covered by at least one R1 report and mapped below.

| Series | Anchor → Phase |
|---|---|
| **M** | M1→13 · M2→3 · M3→9 · M4→3 · M5→10 · M6→9 · M7→9 · M8→8 · M9→10 · M10→13 · M11→15 · M12→3 |
| **V** | V1→6 · V2→1 · V3→1 · V4→2 · V5→1 (ledger+harness; alert consumed in 8, signatures in 6) · V6→2 |
| **D** | D1→12 · D2→9 · D3→18 · D4→16 · **D5→2 (operator placeholder)** · D6→1 · D7→8 · **D8→2 (operator placeholder)** |
| **S** | S1→12 (zero-OCI CI wiring noted in 1) · S2→12 · S3→1 · S4→13 · S5→7 · S6→1 (deploy-unit half noted in 12) · S7→8 · S8→8 · S9→7 (CI-gate half in 1) |
| **E1-10** | E1→13 · E2→1 · E3→1 · E4→17 · E5→18 · E6→19 · E7→19 · E8→14 · E9→6 (tool half noted in 5) · E10→3 |
| **E11-20** | E11→12 · E12→16 · E13→5 (execution gated, blueprint in 15) · E14→5 · E15→5 · E16→14 (seam anchor, see above) · E17→15 · E18→15 · E19→5 · E20→5 |
| **E21-30** | E21→11 · E22→11 · E23→11 · E24→11 · E25→11 · E26→12 · E27→12 · E28→12 (dedup precondition fixed in 7) · E29→12 · E30→12 |
| **E31-40** | E31→9 · E32→9 · E33→9 · E34→9 · E35→2 (definition; impl attaches to 9) · **E36→3 (ALIAS of E10 — not counted separately)** · E37→10 (PQ-anchor format done in 3 under F21) · E38→9 · E39→13 (rewording in 2) · E40→1 |
| **E41-50** | E41→16 · E42→2 · E43→16 · E44→16 · E45→17 · E46→8 · E47→8 · E48→12 · E49→12 · E50→12 |
| **E51-62** | E51→14 · E52→1 (bebop parity noted in 18) · E53→2 · E54→18 · E55→2 · E56→19 · E57→19 · E58→1 · E59→18 · E60→19 · E61→4 · E62→1 |
| **F1-10** | F1→10 · F2→10 · F3→15 · F4→15 · F5→10 · F6→5 · F7→15 (recorded vacuous) · F8→10 · F9→15 · F10→15 (cap value = O8) |
| **F11-20** | F11→9 · F12→9 · F13→9 · F14→17 · F15→9 · F16→9 · F17→13 · F18→9 · F19→3 · F20→9 |
| **F21-30** | F21→3 · F22→9 · F23→9 · F24→3 · F25→9 (bound = O11) · F26→3 · F27→15 · F28→10 · F29→8 · F30→9 |
| **F31-40** | F31→8 · F32→8 · F33→11 · F34→11 · F35→11 · F36→8 (ledger half in 1) · F37→12 · F38→12 · F39→8 · F40→8 |
| **F41-50** | F41→13 · F42→13 · F43→13 · F44→14 (DECART = O3) · F45→4 · F46→13 (math from 4, heal from 9) · F47→17 · F48→14 (semantics = O4) · F49→16 · F50→13 (standing invariant, re-asserted in 14/16) |

**Tally:** P1:12 · P2:8 · P3:8 · P4:2 · P5:6 · P6:2 · P7:2 · P8:12 · P9:20 · P10:8 · P11:8 ·
P12:14 · P13:11 · P14:5 · P15:9 · P16:6 · P17:4 · P18:4 · P19:5 = **146 distinct anchors**
(= 147 nominal IDs − 1 E36 alias), of which 144 are defined and 2 (D5, D8) are operator-decision
placeholders. **Zero anchors deferred outside a phase; zero silently dropped.**

---

*R2 synthesis complete. Sources: R1-A/B/C/D/E (all read in full), ARCHITECTURE.md,
STRATEGIC-VECTORS-LOCKED-2026-07-16.md; load-bearing R1 claims (CI job list, LICENSE text,
CONTRIBUTING.md:17, E10/E36 duplication, D5/D8 absence) re-verified against the live tree
2026-07-16. This document orders work; it changes no canon — canon corrections in §1 are relayed
for the operator's merge (ARCHITECTURE.md: "merge, never append").*
