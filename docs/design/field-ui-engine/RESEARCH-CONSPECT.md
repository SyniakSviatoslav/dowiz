# Engine-UI task — conspect index
Мета: один план+блюпринти для переходу всіх проєктів на DOD + max-Rust інфраструктуру,
де UI малює двигун (wgpu+egui), а фізика/математика хвиль = фундамент малювання інтерфейсів загалом.

## Розвідка (зафіксовано):
- webgl/particle-cloud/particle-cloud.js — саморобний WebGL2 particle field; VOCAB подій (order_created amber burst / courier_assigned teal stream / delivered gold bloom / dispatch_failed blood turbulence / pending_aging ember drift), кожен {color,energy,burst,swirl}; GLSL 300es vert/frag; ring buffer MAX=4096; pointer force. = ЗАРОДОК "фізика керує UI"
- packages/ui/src/theme/: index.ts, ThemeProvider.tsx, palette.ts, paperSkin.ts, tokens.css — дизайн-система
- apps/web візуальні: DeliverySwan, PaperScene, SwanHero, media/{MediaGallery,RevealOverlay,SpinViewer}
- web/ (Astro/Svelte canonical): Storefront.svelte, OwnerDashboard.svelte, CourierTrack.svelte, lib/kernel/ wasm glue
- Фізичний субстрат bebop: field_physics.rs 1148 (damped wave), wavefield.rs 1031 (FR spring layout + heat-kernel + spectral), geometry_field.rs 466 (platonic + spherical harmonics), tui.rs 1285, mathx/panels/radio/optical
- bebop2 math: field.rs (eigenmodes+vectors), chebyshev (heat-kernel), fft, vsa, algebra
- rust-core/src/lib.rs = CSR heat-kernel WASM kernel (linear memory, f32, deterministic libm) — zero-copy substrate
- НЕМАЄ wgpu/egui досі — тільки WebGL2 particle-cloud

## Агенти (7):
- A1 frontend/animation surface (re-launched a37... ні — frontend agent)
- A2 design system + tokens (ac0ce56)
- A3 particle-cloud + physics substrate
- A4 bebop2 math + WASM boundary
- R1 immediate-mode GPU renderer (text SDF, shapes, wgpu, a11y, migration)
- R2 field-physics AS universal UI substrate (CORE — layout=equilibrium, motion=wave, focus=potential, feedback=ripple, structure=spectral)
- R3 DOD + zero-copy WASM↔GPU + fixed-timestep + thin-shell + low-end budget

## A2 (design system) — DONE:
- Single source = packages/ui/src/theme/tokens.css :root (597 рядків), 4-шаровий CSS-var cascade: :root defaults → .theme-* preset → @media dark → tenant inline (ThemeProvider setProperty) → [data-skin=paper] → html[data-sunlight] !important. Renderer МУСИТЬ відтворити цей override-stack точно
- Warm Cosmo-Noir DEFAULT = dark teal+gold: --brand-primary #d69a3d (gold), -bg #061b1a, -surface #152928, -text #f5efe5, -accent/#213433 teal, -border #334443, radius 12/8/78px (btn near-pill), Inter font. (⚠ palette.ts food-dark orange #ea4f16 = ЛЕГАСІ, tenant-selectable)
- Палітра ПОВНІСТЮ перелічувана + ESLint-примусова: ~11 brand + 40 status(10 станів×4 variant: base/-bg20%/-border30%/-light10%) + 8 semantic(success#059669/warning#D97706/danger#DC2626/info#2563EB) + 3 chart + paper. no-hardcoded-color (0 hex), no-hardcoded-tailwind-color, no-arbitrary-tailwind (4px snap), no-arbitrary-font-size (рівно 8 steps), no-raw-form-control
- Type scale text-step-{2xs 11/xs 12/sm 14/base 16/lg 18/xl 22/2xl 28/3xl 36}px (rem), leading tight1.15/snug1.3/normal1.5/relaxed1.7, weight 400/500/600/700. Fonts: Inter/DM Sans/DM Serif/Cormorant/Playfair/Fraunces(VARIABLE opsz9-144)/Yeseva One + Tabler icons (ti-*)
- Spacing 4px base (--space-0..64), radius none..full(9999), brand-radius separate. Breakpoints ЛИШЕ 768/1280 (390=design width НЕ breakpoint). Tap 44/48/56. Safe-area env() (OS-provided). Z 100-500 (painter order). Elevation --elev-1..4 multi-layer soft shadow + --blur-surface 12px
- Motion ДВІЧІ (CSS tokens + framer-motion mirror motion.ts, sync): durations instant80/fast150/base240/slow400ms; easing ease-out cubic-bezier(.16,1,.3,1)/ease-soft(.4,0,.2,1)/bounce(.34,1.56,.64,1); SPRINGS motion.ts press{k400,d25}/enter{k260,d24}/bounce{k500,d18}/gentle{k180,d22} — ФІЗИЧНО ГОТОВІ. AnimatedNumber = rAF cubic-out 1-(1-t)³ 240ms tabular-nums, ПОВАЖАЄ useReducedMotion (snap). reduced-motion wired 3 рівні
- i18n: 3 locales sq(Albanian DEFAULT)/en/uk(Cyrillic) — НЕ 2; LTR only no RTL; ~1296 keys × 3; t('key','fallback') {{var}} interpolation; catalog i18n-catalog.ts key-major
- Skin: ТІЛЬКИ data-skin=paper (grep: НЕМАЄ bebop skin!) — warm paper palette --paper-bg #F4ECDB/--ink/--action/--sand/--teal/--gold; grain overlay mix-blend multiply SVG 0.06; sunlight mode AAA high-contrast !important flatten
- Component atoms (renderer МУСИТЬ відтворити): Button(5 variant×4 size, rounded-full, min-h-11/56), Input/Select/Textarea (px-4 py-2.5 min-h-11, chevron ti), SegmentedControl (pill snap-x), StatusBadge/StateChip (color-mix tones), Status (Skeleton/Empty/Offline/WSDot), AnimatedNumber
- GPU mapping: ✅direct=flat colors(RGBA uniform/attr), alpha tints, type sizes(rem×16), spacing/radius(SDF rounded-box), motion durations(t=elapsed/dur)/easing(LUT/closed)/springs(integrator), reduced-motion(flag), gradients/shimmer, z(painter). ❌NO CLEAN ANALOG: box-shadow/--elev-* (blur pass/9-slice sprite), backdrop-blur (framebuffer gaussian), color-mix (CPU pre-resolve→token table, recompute on tenant switch), Fraunces variable font (MSDF не інтерполює → discretize N opsz atlases або CPU raster), env(safe-area) (OS query), CSS filter chains (post-shader), interaction states :focus/:hover/::selection (input-state layer над renderer)
- Text hardest: (M)SDF atlas per family+weight; Cyrillic(uk)+Albanian diacritics(ë/ç) в atlas ranges; tabular-nums потребує tnum GSUB feature at shaping

## A1 (frontend/animation) — DONE:
- ДВА роз'єднані стеки: (1) ЛЕГАСІ apps/web React 18 DOM SPA (живий продукт) — framer-motion (35 файлів), 32 CSS @keyframes, react-router, Zod; (2) КАНОНІЧНИЙ web/ Astro SSR + Svelte 5 islands на kernel WASM — НЕМАЄ framer/React, money=static integer-cent NO tween, WebGL2 particle cloud
- 4 Canvas/WebGL двигуни (всі bespoke): PaperScene (three.js custom GLSL, ОРФАН — 0 mounts), RevealOverlay (Canvas-2D 80-particle dissolve), particle-cloud.js (WebGL2 4096 ring), SpinViewer (img frame-swap), maplibre-gl (maps)
- Motion source: packages/ui/src/lib/motion.ts — easing ease.out[0.16,1,0.3,1]/soft/bounce, springs press{400,25}/enter{260,24}/bounce{500,18}/gentle{180,22}, durations instant.08/fast.15/base.24/slow.4, ~25 framer variants
- 35 анімацій каталогізовано; MONEY-TWEEN ПОРУШЕННЯ (4, всі apps/web): ClientLayout:154 (AnimatedNumber cart total), EarningsPage:47-65,176 (CountUpPrice), DashboardPage:421 (revenue), AnalyticsPage:262 (revenue+avg). Culprit = AnimatedNumber.tsx (rAF cubic) + CountUpPrice. Канонічний web/ ЧИСТИЙ. Fix = DELETE, snap.
- State→pixels: legacy React reconciler + framer parallel rAF; AnimatedNumber робить власний rAF→setState ПЕР FRAME (найгірший path). Канонічний: Svelte fine-grained + 1 GPU loop decoupled
- WASM boundary: kernel wasm.rs JSON-string RPC (place_order_js/apply_event_js/channel_ledger_js), НЕ zero-copy; wasm 191KB. Найбільший виграш: renderer у Rust → kernel in-process struct ref, БЕЗ JSON
- Component taxonomy → immediate-mode difficulty: text lists LOW, cards LOW-MED (layoutId shared-element HARD), forms/inputs HIGH (немає IME/autofill/keyboard), dashboards/charts LOW (bars=quads DOD), maps VERY HIGH (maplibre цілий renderer), hero/decorative LOW-MED (engine WINS, particle-cloud вже DOD), modals LOW, media MED-HIGH
- БЛОКЕРИ: (1) react-router deep-linking (/s/:slug SSR, ?ch=, ?embed), (2) forms/native input (HTML overlay layer), (3) A11Y DOM-semantics (role/aria-live/sr-only/focus-visible — egui AccessKit nascent, canvas ЗНИЩИТЬ screen-reader без ARIA mirror), (4) SSR/SEO/OG unfurl (canvas рендерить НІЧОГО server-side — public menu МУСИТЬ мати SSR HTML path), (5) i18n text-shaping (sq/en/uk, money/date locale-aware), (6) two-stack — beachhead = канонічний web/ (kernel WASM + money-discipline + particle DOD + no framer)
- ОРФАНИ до реконсиляції: PaperScene/SwanHero/DeliverySwan (0 mounts, найбагатший GPU/animation reference — north-star або delete)
- РЕАЛІСТИЧНИЙ шлях: composited hybrid (GPU canvas для cards/lists/charts/decorative + retained DOM overlay для inputs/a11y/SSR), kernel promoted JSON-RPC→in-process Rust struct

## A4 (bebop2 math + WASM boundary) — DONE:
- ТРИ Rust-поверхні: bebop2-core (чиста spectral lib, slice API, host-gated), rust-core (deployed zero-copy WASM kernel raw C-ABI linear memory), dowiz-kernel (JSON wasm-bindgen — АНТИ-ПАТЕРН для numerics). Engine = bebop2-core algos + rust-core ABI + GPU
- Spectral layout: field.rs LaplacianSpectrum eigenVECTORS column-major modes[rank*n+i] via jacobi_eigen (≤100 sweeps tol1e-14, O(n³)). Hall's algorithm: node i at (x=modes[1*n+i]=φ₁ Fiedler, y=modes[2*n+i]=φ₂) = мінімізує Σ‖p_i−p_j‖² — layout 2 row-reads away (~85%, gap: coords_2d helper + Lanczos для великих + incremental relayout). Вищі моди φ₃+ = standing-wave visuals cos(√λ_k·t)·φ_k
- Heat transitions (~90%): propagate_spectral analytic u(t)=Σe^{−λ_k t}⟨u0,φ_k⟩φ_k (t-SCRUBBABLE! set t=clock, no integration) + chebyshev matrix-free exp(−cLt) qp=64 recurrence (для великих графів O(n³) Jacobi infeasible, deg20-40 = diffusion у deg matvecs/frame) + active_diffuse (dt=0.02 stable, saturate-then-gate). Mass-conserving RED-tested. Gap: frame driver clock→t + field→color/opacity/size mapping
- FFT effects (~75%): fft radix-2 bit-exact; circulant_eigenvalues=FFT(kernel row) = blur/edge (convolve = IFFT(FFT(x)⊙FFT(h))). Gap: 2-D separable FFT (row+col pass) для image blur/bloom НЕ написаний; real-FFT opt; convolve wrapper
- ZERO-COPY WASM↔GPU (~55%): rust-core raw C-ABI field_build/matvec/spectral/rank/cost/sensitivity, buffers = caller-owned ptrs у linear memory, out:*mut f64 пише В ТОЙ САМИЙ ArrayBuffer. JS: new Float64Array(wasm.memory.buffer, ptr, n) = VIEW no alloc → queue.writeBuffer (1 host→device copy, НЕ serialization). Native wgpu: bytemuck::cast_slice linear memory → write_buffer TRUE zero-copy. f32 packing field_build_f32 halves upload. GAP: ВСЯ wgpu binding glue НЕ існує ("GPU/WGPU flagged next"=TODO), SharedArrayBuffer COOP/COEP, #[no_mangle] wrappers для bebop2 slice API
- Determinism (~85%): fexp/fcos in-tree Taylor shims (bit-identical cross-platform, no libm), no wallclock/RNG/network, fixed qp=64. ⚠️ rust-core fround BUGGY x<0 → port bebop2 C8-correct fexp (lib.rs:328) НЕ rust-core. f32 accum order pin для bit-identical; GPU FMA ≠ CPU → keep authoritative compute CPU-side, GPU=display
- VSA (~60%): bind=circular conv IFFT(FFT⊙FFT), unbind, bundle=mean. Для UI-state = solution-looking-for-problem (noisy retrieval, потрібен pow2 n≥256). Fit = spectral-frame FINGERPRINT/similarity (transition blending, memoize), НЕ primary state. Gap: cleanup memory/codebook
- dowiz JSON boundary anti-pattern: per call UTF8 encode+JSON parse+serialize+decode+2 malloc/free + Box::leak per event. Fine @order-freq, CATASTROPHIC @60fps. Fix: raw C-ABI numeric plane окремо від JSON control plane, 2 planes 1 module
- Build gaps: wgpu glue (0), 2-D FFT, Lanczos eigensolver, frame driver + field→visual mapping, SharedArrayBuffer, port C8-correct fexp

## A3 (particle-cloud + physics substrate) — DONE:
- ДВА half'и НЕ з'єднані: particle-cloud.js = renderer template (JS/WebGL2, GPU але no PDE); bebop Rust field math = physics engine (PDE але no GPU). Теза "physics-drives-all-UI" = merger цих двох
- particle-cloud.js: VOCAB подій→{color rgb, energy, burst, swirl} (order_created amber/courier teal swirl1.6/delivered gold/dispatch_failed blood swirl3.4/pending ember) — ПАТЕРН: будь-який enum→4-tuple physics params, setPalette override. GLSL 300es: VS 3 interleaved attr (a_seed/a_state xy vx vy/a_meta life maxLife r g), point size=2+life·6·energy, ⚠️ blue hardwired 1.0 (треба widen RGBA); FS procedural soft round r>0.25 discard alpha=(1−4r)·life, ADDITIVE SRC_ALPHA,ONE glow. Physics step: damp 0.92^(dt/16.6), pointer repulsion f=(1−d²/R²)·0.6 radius²9000dpr², semi-implicit Euler, life decay swap-remove dense, energy 0.95^decay idle-stop<0.02. Ring MAX=4096, 3 flat Float32Array (seed[*2]/state[*4]/meta[*4]) bufferSubData stream = ТОЧНО wgpu buffer layout. API init/burst/setReducedMotion/setPalette/setPointer/resize/dispose
- ЄДИНИЙ consumer CourierTrack.svelte: 24-particle burst on mount = one-shot decorative. Delta до substrate = весь engine
- Damped wave field_physics.rs (flagship): m·ü=c²Lu−γu̇+s; Body V-dim tensor (platonic 4-20 vertex=mass m0=vertex_count + κΣedge weights). step_wave: intra-solid Lap (1/deg)Σ(u_other−u) + between-node nb_mean + source amp(1+m)·1/hop² + a=(c²(L_solid+nb)−γv+src)/m, Euler v+=dt·a, u+=dt·v; fluid advection pressure p=Σu_c flow=p_i−p_j → position drift. WAVE_C2=1/WAVE_DAMP=0.08/FLUID_ADV=0.05. Energy Lyapunov E=Σ½mv²+½mc²(u_a−u_b)², field_stable refuse Ė>tol. STABILITY CORRIDOR proven dt≤0.02 stable, ≥0.03/0.05 DIVERGES. → UI: ripple/feedback (impulse at touched node), panel motion (advection), per-node Σ|u_c| activation → glow/scale/color будь-якого widget
- change_impact blast radius → highlight cascade "що змінення X торкає"; wave_bounce_path BFS shortest route → animated wayfinding
- heat-kernel coherence.rs (⚠️ single-step, +L sign sharpen НЕ exp(−Lt) smooth, O(nE)) — focus/attention diffusion, interfere constructive|ψ₁+ψ₂|²/destructive; gap true multi-step exp(−Lt)+CSR
- FR spring layout wavefield.rs (deterministic circle-seed no RNG): k=√(area/n) area=1, repulsion k²/d, attraction d²/k, cooling temp=0.1. + layout_circle 2πi/n + layout_grid. → auto-layout cards/graphs/mindmaps. Gap O(n²), unweighted, point-only no card-sizing/collision/anchor
- potential_well stabilizer.rs ½Σk(θ−b)² + ground_state + saturate limit·tanh → snap-to-grid/dock/drag-resistance/elastic-return (~80%). Gap: expose ∇V=k(θ−b) restoring force
- geometry_field platonic exact FEV Euler=2 + golden ratio vertices + spherical harmonics Y_l^m real → 3D node glyphs (complexity=mass tetra light/icosa heavy), harmonic auras, phyllotaxis. Gap: no faces/normals/index buffers для wgpu mesh
- mathx first-order ease K(1−e^{−t/τ}) + settling −τln(1−frac) + lagrange spline + classify_trajectory (motion self-test). Gap no cubic-bezier, no true spring
- DOD audit: particle-cloud YES (already flat), Body NO (ragged Vec u,v → flatten CSR + flat edge table), wavefield adjacency NO (Vec<Vec> pointer-chase → CSR), geometry easy, mathx/stabilizer N/A pure. optical=aHash NOT renderer, panels=TUI ASCII replace, radio=irrelevant
- Coverage для "draw WHOLE UI as field": particle emitter 30% (тільки point sprites, no lines/quads/SDF/text, no persistent/structural, no layout-binding, RGB truncated), damped-wave physics 60%/render 0% (no channel→pixel mapping), heat-focus 40%, FR layout 70%, spectral 30% (values only no vectors — але bebop2 field.rs МАЄ vectors!), platonic 50%, potential-well 80%, mathx easing 65%
- 4 фізичні стовпи: FR layout (positions) + damped-wave (motion/activation) + potential-well (snapping) + mathx first-order (easing); всі доведені+детерміновані, жоден не wired до пікселя

## R1 (immediate-mode GPU renderer) — DONE:
- ВЕРДИКТ: rendering повністю realizable сьогодні (WebGPU Baseline січень 2026: Chrome113+/Firefox/Safari26; wgpu dual-targets WebGPU+WebGL2 fallback). ЄДИНЕ невирішене = a11y (AccessKit НЕ має web backend 2026) → гібрид ОБОВ'ЯЗКОВИЙ
- Immediate vs retained: DOM retained Θ(N)×2 (fiber) persistent + reflow cascade unbounded; egui immediate Θ(K) state (K≪N stateful only, GC'd) + Θ(V) transient pooled (V=visible vertices, 20 bytes each). Приклад N=8000 DOM nodes → W_visible=300 widgets K=20 stateful. Low-end WINS: memory ceiling/GC pauses, bounded worst-case latency Θ(W_visible) flat, one compositor surface. CAVEAT: naive redraws 60fps=loses to idle DOM → egui repaint-on-demand (ctx.request_repaint) idle=0 CPU. ЗАГ. виграш: memory+tail latency, тільки MATCHES idle CPU ЯКЩО repaint-on-demand
- Render loop: INPUT (RawInput pointer/keys/dt/dpr) → RUN Context::run (per widget: LAYOUT allocate Rect + INTERACT hit-test + PAINT push Shape) → TESSELLATE → Vec<ClippedPrimitive>{Mesh Vec<Vertex>,Vec<u32>} → PAINT (scissor=clip, bind atlas, draw). run Θ(W), tessellate Θ(V). НЕМАЄ reflow cascade / style-invalidation
- ТЕКСТ (найважче): (a) bitmap atlas (glyphon+cosmic-text default, crisp тільки @rasterized size — OK для fixed text-step scale!); (b) single SDF d=tex.r−0.5, alpha=clamp(d/fwidth(d)+0.5) — corners round off; (c) MSDF median(RGB) sharp corners: median=max(min(r,g),min(max(r,g),b)); pxRange=4; toPixels=pxRange·inverseSqrt(dx²+dy²); alpha=smoothstep(−0.5,0.5, (median−0.5)·toPixels) — ОДИН atlas crisp @ANY scale/rotation/DPR, outline/glow free на sigDist. Stack: cosmic-text (shaper+BiDi+linebreak+fallback via rustybuzz 2221/2252 HB tests + unicode-bidi) → MSDF atlas render. RTL/Arabic (sq не Arabic але): cosmic-text capable сьогодні; ліміти — no Arabic fallback shaper (МУСИШ ship font з glyphs), IME input = hidden <input> overlay, find-in-page/select/spellcheck lost. Cyrillic(uk)+Albanian(ë/ç) OK
- SHAPES SDF (все = 1 instanced quad pipeline): sdRoundBox(p,b,r)= {q=abs(p)−b+r; min(max(q.x,q.y),0)+length(max(q,0))−r} (iq); per-corner radii by quadrant; AA analytic cov=1−smoothstep(−fwidth(d),fwidth(d),d) (замість egui CPU feather); border dBorder=abs(d)−t/2; gradients in-shader mix(c0,c1, clamp(dot(p−p0,dir)/len,0,1)) або 1×K LUT; SHADOWS cheap=blur SDF threshold АБО Levien blurred-rounded-rect erf7 branchless (r_eff=√(r_c²+1.25·r_b²)) — ZERO blur passes (low-end win). Кожен design token → instance attr {rect,cornerRadii,fill,border,borderW,shadow,gradient,flags}; CSS vars resolved once CPU→uniform/instance
- wgpu pipeline DOD: RectInstance ~64-80 bytes flat Vec (rect/corner/fill u32 RGBA8/border/shadow/gradient/flags). INSTANCED unit quad ×N = ВСІ rounded-rect widgets = 1 draw call. Single render pass painter-order pipelines: P0 shape SDF (1 draw N inst) → P1 text glyphon/MSDF → P2 icons → P3 particle-cloud custom shader (ВЖЕ "custom shader pass composited") → [P4 egui]. Bind group 0 per-frame UBO {screen,dpr,time,theme_tokens} (theme switch = 1 uniform write!); bind 1 static atlases (once, no churn); instance buffer DIRTY-FLAGGED (re-upload тільки on change, static screen=skip); clip = scissor або clip_id
- WebGL2 fallback (~5-10% tail): NO storage buffers NO compute → instance via vertex attr+UBO (shapes/text OK); bebop2 compute (Laplacian/Chebyshev/FFT/CSR) = WebGPU compute shader primary АБО deterministic WASM-CPU fallback (CSR kernel вже f32 linear memory). "one substrate two execution targets"
- A11Y (THE cost center, permanent tradeoff): GPU canvas = ZERO semantic DOM → втрачає screen readers/focus/Ctrl+F/autofill/password/translate/reader/SEO/selection. AccessKit = standard bridge (Win/Mac/Unix) АЛЕ НЕ web backend 2026 (roadmap "most difficult, no timeline"). Pattern = "thin DOM shim / semantic mirror" (Flutter CanvasKit): hidden transparent DOM mirror widget tree (role/aria-label/tabindex/rect/state), reconcile per-frame from immediate widget list. FORMS потребують REAL <input> overlay (autofill/password/IME/mobile-kbd cannot be ARIA-faked) — transparent над GPU-drawn field, keep type=email/tel. Solvable: roles/labels/focus/read-out via mirror. PERMANENT LOSS: Ctrl+F, translate, reader. SEO public /s/:slug → keep SSR DOM
- MIGRATION: island-by-island (particle=island#0), НЕ big-bang. Decision: Gain−Loss>0, Gain=M·(render+memory saved), Loss=A+S+(T?∞:0). T?∞ = будь-який text-input НІКОЛИ pure-GPU (hybrid overlay). Порядок: 1.decorative/animated M≈1 A≈S≈0 (particle/order-earnings viz/charts/transitions — FIRST); 2.visual containers (cards/lists + semantic mirror); 3.nav/chrome; 4.forms HYBRID always (GPU field + transparent <input>); 5.public SEO /s/:slug DO NOT migrate keep SSR. money-never-tween HELPS: immediate redraw settled number = no count-up (drop AnimatedNumber/CountUpPrice), money viz = first-wave якщо static final figure

## R2 (фізика як UI-субстрат — ЯДРО) — DONE:
- ВЕРДИКТ: ТАК для 5 аспектів (layout/motion/feedback/focus/structure) = ОДИН damped-field оператор + ОДИН Lyapunov сертифікат + ОДНА (ω,ζ) лексика. НІ для 3 (exact pixel align, crisp edges/glyphs, exact discrete semantics) = measure-zero/combinatorial що smooth field ПРОВАБЛИ не тримає → constraint solver + SDF + state machine
- МАТ-ОБ'ЄКТ: UI = дві зв'язані поля над графом G=(N,E) (nodes=containers/cards/controls, edges=parent-child+sibling+semantic, w_ij=coupling). G = дискретизація continuous field над Ω⊂ℝ² (спільний eigenbasis graph-Laplacian≈continuous-Laplacian → ОДНЕ поле). Position X:N→Ω (де), Value/activation U:N×t→ℝ^V (як виглядає). ОДИН закон (damped wave field_physics узагальнений): M Ẍ=−∇E(X)−Γ_x Ẋ (layout) + M Ü=−c²LU+S(t)−Γ_u U̇ (value). Rendering map R:(X,U)→pixels композує SDF shape + field readouts + particle tracers
- СЛОВНИК: layout=equilibria ∇E(X*)=0; motion=transients M Ü+Γ U̇+c²LU=0; feedback=impulse response U=G∗S (Green's function); focus=potential wells; structure=low eigenmodes φ₂,φ₃
- §1 LAYOUT (найважче, без jitter): E(X)=Σ_edges ½k_ij(‖x_i−x_j‖−d0_ij)² + Σ repulsion k²/d (FR узагальнений з rest-length). БЕЗ JITTER 3 ліки: (a) STRESS MAJORIZATION SMACOF — Guttman transform X^{k+1}=V⁺B(X^k)X^k, майоризація ГАРАНТУЄ σ(X^{k+1})≤σ(X^k) monotone non-oscillating (upgrade substrate FR→majorization); (b) symplectic Euler + Lyapunov: H=½ẊᵀMẊ+E(X), dH/dt=−ẊᵀΓẊ≤0 → LaSalle → converge to {∇E=0} — ТА САМА теорема що resonator/Lyapunov (γ той самий, wave_energy той самий сертифікат!); (c) dual rest-threshold freeze (‖∇E‖<ε_f AND ‖ẋ‖<ε_v) + pixel-snap ПРОЄКЦІЯ ПІСЛЯ convergence x_snap=g·round(x*/g) НІКОЛИ during. §1.3 constraint: minimize E(X) s.t. AX=b (align/equal-width) CX≤d (min gaps/contain) via constrained majorization + gradient projection + Cassowary incremental simplex (=Apple Auto Layout). Physics=soft global arrangement, constraint=hard pixel-exact
- §2 MOTION: heat kernel U(t)=e^{−αLt}U₀=Σe^{−αλ_k t}⟨U₀,φ_k⟩φ_k (monotone NO overshoot — theme swap/selection spread) vs damped wave (springy/ripple/bounce). MODAL DECOUPLING U=Σa_k(t)φ_k → незалежні осцилятори a_k''+2ζ_kω_k a_k'+ω_k²a_k=s_k/m, ω_k=c√(λ_k/m), ζ_k=γ/(2√(mc²λ_k)). ОДНОЕЛЕМЕНТНЕ easing = ẍ+2ζω ẋ+ω²x=ω²x_target: ζ<1 underdamped bouncy, ζ=1 CRITICAL fastest monotone no-overshoot snappy (x=x_t[1−(1+ωt)e^{−ωt}]), ζ>1 overdamped sluggish. ДОВЕДЕНО: критичне ζ=1 = фізика доброго UI easing = ТЕ САМЕ критичне демпфування governor. cubic-bezier НЕ може overshoot → springs витіснили. react-spring default {170,26}→ζ=0.997 critical; presets = точки на ζ осі. Designer (tension,friction,mass)=(ω,ζ,m). Рецепти: snappy ζ=1 ω≈30 τ_s≈130ms; fluid ζ≈0.65-0.8; playful ζ≈0.35 M_p≈30%. τ_s≈4/(ζω), M_p=exp(−ζπ/√(1−ζ²)). ГЛОБАЛЬНІ transitions = heat-kernel stagger: delay τ_j=dist(source,j)/√α (ripple-out reveal), wave_bounce_path BFS = eikonal limit
- §3 FOCUS = potential fields: V(x)=−Σ A_i exp(−‖x−c_i‖²/2σ_i²) + Σ B_m bumps (forbidden-zone repellers stabilizer). Cursor test particle m p̈=−∇V−γṗ + tanh saturate (магнітний pull не yank). Local minima=snap/dock, basins=Voronoi, saddle=decision boundary. ОДНЕ V драйвить ВСЕ emphasis (no per-component): scale s₀(1+β(−V)/V_max), brightness Boltzmann L₀+Δexp(−V/T), blur DoF b_max|V−V_focus|/range, saturation σ₀−κ|V−V_focus|. Focus change = well spring
- §4 FEEDBACK = Green's function unification: U(x,t)=∫∫G(x,t;x',t')S(x',t')dx'dt', G=impulse response=fundamental solution. Tap δ→ripple (2D damped wave: expanding front radius c(t−t₀) amplitude 1/√dist = Material ripple!). Дискретна G_ij(t)=Σφ_k(i)φ_k(j)g_k(t), g_k=(1/mω_dk)e^{−ζ_kω_k t}sin(ω_dk t) — delayed decaying oscillation через structure. Event→source vocab (узагальнення particle-cloud): tap=δ wave ζ0.6; success/gold-bloom=Gaussian blob HEAT no-overshoot; order/amber=impulse wave ζ<1; error=high-λ modes shake; loading=sustained oscillatory. Particles = tracers seeded ∝|U|/|∇U| advected by U̇. Ripples+particles = ДВА rendering ОДНОГО поля
- §5 STRUCTURE = spectral embedding (Hall 1970): x_i=(φ₂(i),φ₃(i)) низькі eigenvectors L (excl trivial φ₁). Мінімізує xᵀLx s.t. xᵀx=1, ⊥1 → Fiedler φ₂+φ₃. Natural resonant layouts, clusters separate, NO tangled local minima. Алгоритм bound bebop2 field.rs (вже має eigenvectors!): L=D−W → smallest nontrivial (λ₂,φ₂)... via field.rs eigensolver/Lanczos/Chebyshev filter → p_i=center+S(φ₂,φ₃) scale 1/√λ → hand to §1 constrained majorization+snap. Spectral=initializer що dodges FR local minima. Eigenvalue = рівень hierarchy (λ₂ coarsest nav/content, high λ fine). Decoration = same operator finer scale: Bessel drum J_m(kr)e^{imθ}, spherical harmonics Y_l^m, golden-angle phyllotaxis p_n=(√n cos ϕn,√n sin ϕn) ϕ=137.5° (principled particle seeding)
- §6 ЧЕСНА МЕЖА (falsifiable). Уніфіковано: ОДИН оператор + ОДИН wave_energy Lyapunov/LaSalle + ОДНА Laplacian модальна + ОДНА (ω,ζ). RED тести (field ПРОВАБЛИ не може): #1 exact align/equal-size (equality measure-zero, smooth flow drifts) → expect(colA.right).toBe(colB.left) FAILS; #2 crisp 1px stroke/glyph (band-limited O(σ) blob) → edge-width O(σ)≫1px; #3 exact discrete semantics — money=server integer NEVER tween (RED-LINE!), one radio selected, z-order — field-interpolated total $12.4999 mid-transition VIOLATES; #4 text line-break (Knuth-Plass combinatorial). ГІБРИД: field owns arrangement/motion/feedback/focus/structure; CONSTRAINT solver owns exact align/equal/contain (Cassowary/QP); SDF owns crisp shapes/glyphs (field тільки МОДУЛЮЄ position/scale/glow); DISCRETE state machine (kernel) owns money/selection/z-order (field тільки ПРЕЗЕНТУЄ decided states НІКОЛИ interpolate). Одностороннє per-frame: field→constraint project→SDF render→state supply. Physics=continuous skeleton, 3 layers=discrete corrections projected on top
- BUILD: present у substrate (damped wave+Lyapunov field_physics, heat/spectral/eigenvectors bebop2 field, FR/spectral wavefield, potential-well stabilizer, event-vocab+particles particle-cloud). MISSING: (1) constrained stress-majorization+gradient-projection Cassowary/QP bridge (RED#1); (2) SDF render layer (RED#2); (3) field↔state boundary contract (money/selection discrete НІКОЛИ interpolated — захищає money-never-tween RED-LINE). Upgrade FR→majorization; reinterpret particle bursts як Green's-function tracers

## R3 (DOD + zero-copy + fixed-timestep + thin-shell) — DONE:
- Ground: wasm.rs JSON boundary (per-frame catastrophic), bebop2 field.rs ВЖЕ SoA flat f32 (modes/row_ptr/col_idx/degrees), DT_STABLE=0.02 tested (b11_dt_corridor), resonator.ts:137 convergence detector (НЕ будувати новий), RevealOverlay AoS = anti-pattern
- DOD SoA: cache line 64B. AoS Widget ~32B, integrate touches тільки x,y(8B) → 25% line utilization. SoA pos_x/pos_y/vel_x/vel_y/size/color:u32/flags:u32 окремі Vec → 100% utilization, cache-miss 320KB→80KB = 40-60% speedup +SIMD. hot/warm/cold split. Vertex-emit corollary: SoA→interleaved pack cache-optimal+SIMD; AoS gather strided prefetcher-hostile
- ECS decision: HAND-ROLLED SoA НЕ bevy_ecs. bevy=SoA archetype але платить dynamic composition (TypeId/downcast/archetype churn) виправдано для героген runtime-discovered game. UI = small fixed compile-time component set → static dispatch, no TypeId, no bevy dep bloat. Rule: 1 WidgetStore SoA + 1 ParticlePool SoA ring (spawn overwrites head, ZERO alloc steady-state). ECS тільки якщо UI стане genuinely dynamic heterogeneous
- ZERO-COPY: дві копії. #1 marshalling JS↔WASM (JSON serde, V8 JSON.parse re-parse whole buffer per frame — CATASTROPHIC, FULLY ELIMINABLE); #2 upload CPU↔GPU (DMA/staging, essentially unavoidable унеможливлюється тільки generate-on-GPU compute). ZERO-COPY path: Rust пише flat Vec<f32> у linear memory → JS Float32Array::view(&slice) NO COPY (view into wasm memory) → queue.writeBuffer(buf,0,view) 1 upload copy. ⚠️ writeBuffer BEATS mapped buffers САМЕ з WASM (Toji.dev: mapped range = separate ArrayBuffer, fill from WASM = ДРУГА copy). Per-frame dynamic→writeBuffer; static set-once→mappedAtCreation; high-churn→staging ring. GENUINELY zero-copy: WGSL compute shader integrator → storage buffer read by render pass, positions НІКОЛИ не покидають GPU (WebGPU only, WebGL2 fallback keeps CPU writeBuffer). Two backends: simd128 CPU + WGSL compute, SoA feeds both. Quant: 10⁴ particles@60fps JSON = parse 240KB text 60×/s main thread = jank; view path = 0 text. НАЙБІЛЬШИЙ realizability lever. Rule: transactional→JSON (unchanged), per-frame numeric→view+writeBuffer. Boundary kind chosen by UPDATE FREQUENCY не data type
- FIXED-TIMESTEP (non-negotiable): variable dt → slow frame large dt → explicit integrator past stability → explode. field.rs dt=0.02 stable/0.05 divergent tested. Fixed guarantees integrator sees ТІЛЬКИ 0.02 no matter device stutter + preserves determinism. Accumulator (Gaffer): DT=0.02=field::DT_STABLE, MAX_FRAME=0.25 clamp (spiral-of-death guard), MAX_SUBSTEPS=5 cap. frame: clamp frame_time→accumulator+=; while accum>=DT && steps<MAX: prev=curr; integrate(DT); accum-=DT; steps++. alpha=accum/DT; render(lerp(prev,curr,alpha)). INTERPOLATION робить 50Hz physics smooth @60fps render (lerp = SoA SIMD pass). Update 50Hz constant device-indep, render=display refresh (0Hz коли settled). 3 guards: MAX_FRAME clamp (GC pause→blip не freeze), MAX_SUBSTEPS low weak, dt compile-const=DT_STABLE
- LAZY RENDER (key low-power): egui reactive by default (update тільки на input/animation), opt-into continuous via request_repaint. ⚠️ egui НЕ sub-region damage tracking → repaint re-tessellates whole; win = FRAME-GATING (skip whole frame ~0 cost). Policy: should_render = input_pending || !physics_settled || animation_active || external_state_changed; none→ DON'T schedule next rAF, DORMANT, wake on input. Static menu 60fps→~0fps instantly responsive. physics_settled = max per-step delta SoA < ε = resonator error<delta_threshold Converged = field.rs active_diffuse gate |Δu|<eps (field ВЖЕ знає коли зупинився). Hysteresis: settled K=3 consecutive ticks (resonator stall_patience) before dormant. Lyapunov watchdog: divergence НЕ settled → force render never dormancy. Battery: interact 5% + settle 0.5s → duty cycle few % не 100%. НЕ SoA/SIMD — ЦЕ найбільший battery lever
- THIN-SHELL: JS = bootloader + event forwarder, Rust owns ALL state. JS робить: (1) load/instantiate wasm + feature-detect SIMD/WebGPU; (2) raw event taps forward one-liner wasm.on_pointer (Rust queue = truth); (3) hold GPUDevice/Queue/canvas handles + writeBuffer(view); (4) re-arm/cancel rAF per lazy. MUST-STAY-IN-JS (honest, irreducible): clipboard (web_sys unstable gesture/permission), file dialogs (gesture-bound showOpenFilePicker), A11Y TREE (parallel DOM shadow — REAL cost canvas UI, screen reader reads DOM a11y tree, egui AccessKit→DOM bridge JS-side), text input/IME (hidden DOM input + mobile keyboard summon), native pickers select/date/a href, History/URL pushState/popstate, permission/gesture-gated chrome (share/notification/fullscreen/pointer-lock/vibration). Export surface <10: init/on_pointer/on_key/on_resize/on_url/on_visibility/on_external_data(ptr,len)/tick(now)->bool/vertex_view()->(*const f32,usize). NO JSON in any (numeric+memory views). Domain wasm.rs keeps JSON separate transactional
- LOW-END BUDGET: 33ms (30fps design) / 16ms (60 bonus). physics substeps capped never eat frame. Linear memory 32-64MB ceiling (memory.grow copies whole heap → pre-size), zero per-frame alloc. WebGL2 BASELINE universal + WebGPU enhancement (feature-detect requestAdapter SUCCESS не object presence). wgpu both from 1 codebase, design to downlevel_webgl2_defaults (tex 2048, 4 bind groups). simd128 f32x4 integrator (pos_x[i..i+4]+=vel_x*dt 4/instr ~4× throughput) + MANDATORY scalar fallback (bit-identical determinism). Decidable checklist (device passes iff ALL): 1.wasm loads; 2.GPU backend init (requestAdapter OR WebGL2); 3.meets downlevel limits; 4.active frame ≤ budget; 5.memory ≤ ceiling no mid-session grow; 6.idle draw≈0 rAF actually stops (BATTERY gate); 7.a11y tree present if accessible; 8.SIMD-or-scalar selected. Gates 4+6 = what weak HW actually fails. Two hard non-optional costs: (a) a11y shadow DOM (canvas otherwise inaccessible), (b) scalar/WebGL2 fallbacks alive (fast paths strictly optional). Nothing requires WebGPU/SIMD/compute to FUNCTION — accelerators over WebGL2+scalar baseline

## ВСІ 7 ЗВІТІВ DONE (4 code A1/A2/A3/A4 + 3 research R1/R2/R3)
