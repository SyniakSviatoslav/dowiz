# G11 — "first real order" recording mechanism (V6 stable-track criterion)

> BLUEPRINT-P19 §6. G11 = *"first real order"* is the stable-track closure criterion
> (`STRATEGIC-VECTORS:57`: "stable = delivery (G11 first real order)"). This phase does
> **not** cause G11; it defines *how G11 gets recorded in canon the moment it is true*.

## Recording template (entered into canon when true)

- **Claim:** G11 — first real order.
- **Date:** the order's event-log timestamp (not the claim date).
- **Evidence:** order id + hub id + the signed event-log fold proving
  `Pending → … → Delivered` on a real (non-fixture) order — the Phase 13 done-test
  artifact, not a screenshot.
- **Named owner (verifier):** the product/delivery-track owner is accountable for
  verifying G11 is genuinely real before it is written. **Default verifier: the
  operator (`SyniakSviatoslav`)** — the milestone must have an accountable human, not an
  anonymous "GREEN."

## Guardrail

G11 **cannot be pre-declared**. It is recorded only after Phases 13 / 16 land AND a real
order verifiably folds. Until then the canon carries the *template + named owner*
defined here, and the actual G-entry is appended when the evidence exists. This is the
anti-self-certification discipline applied to the project's own headline success metric.

## Status

Template + named verifier owner defined. The actual G11 G-entry is appended when Phases
13/16 produce a verifiable real order — not before.
