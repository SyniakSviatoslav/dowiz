# Rust-rewrite task — план рерайту всіх залишків TS/Node/JS інтерфейсу → нативна Rust/WASM бібліотека
Мета: інтерфейсний двигун справді растовий. channel.js + particle-cloud.js + усі TS/JS → Rust/WASM. Блюпринти. Telegram.
Стоїть на: field-UI двигун (FE-01..17) + dowiz-interfaces дизайн (DZ-01..12) + kernel/bebop Rust.

## РОЗВІДКА (LOC зафіксовано):
- Канонічний web/ (engine target): kernel.js 53 (generated glue REGENERATE), push.js 85 (Push API KEEP-thin-shell), channel.js 146 (byte-mirror analytics.rs DELETE), Storefront.svelte 211 + OwnerDashboard.svelte 226 + CourierTrack.svelte 215 (PORT→Rust field content), index.astro 29 + Layout.astro 41 (SSR shell KEEP minimal), sw.js 119 (service worker MUST-JS KEEP)
- particle-cloud.js 319 (WebGL2 → Rust/wgpu PORT = FE-04)
- packages/ui/src/lib: i18n.ts 74 (PORT logic), i18n-catalog.ts 3830 (DATA — locale, Rust reads/bakes), motion.ts 165 (PORT → field ζ params), money.ts 86 (DELETE dup kernel/money.rs)
- packages/ui/src/hooks: use-breakpoint/use-cart/use-courier-marker/use-delivery-eta/use-embed/use-geolocation/use-geo-stream/use-haptics/use-online/use-sound/use-voice-order (mix PORT logic + KEEP-thin browser-API)
- apps/web/src/lib: analytics/apiClient/CartProvider.tsx/cartReconcile/exportCSV/hooks/messenger/safeStorage/tileConfig/useWebSocket (mix)
- apps/web hooks: useMenuData
- ЛЕГАСІ apps/web/src: 68 files 17521 LOC React/TS (замінюється двигуном)
- packages/ui/src: 103 files 12140 LOC TS (design system — токени→Rust GPU table, components→Rust field, деякі logic port)
- РАЗОМ інтерфейсний TS/JS ≈ 30k+ LOC

## ВЖЕ ВІДОМО (з попередніх арків):
- particle-cloud.js повний аналіз (A3): VOCAB {color,energy,burst,swirl} order_created amber/courier teal swirl1.6/delivered gold burst1.8/dispatch_failed blood swirl3.4/pending ember; GLSL 300es VS point-size=2+life·6·energy (blue hardwired 1.0), FS soft round r>0.25 discard alpha=(1−4r)·life additive SRC_ALPHA,ONE; physics damp 0.92^(dt/16.6), pointer repulsion f=(1−d²/R²)·0.6, semi-implicit Euler, life swap-remove, energy 0.95^decay idle-stop<0.02; ring MAX=4096 3 Float32Array (seed*2/state*4/meta*4) bufferSubData; API init/burst/setReducedMotion/setPalette/setPointer/resize/dispose
- channel.js (hub report): byte-for-byte mirror analytics.rs ChannelLedger + reduce_anomalies; kernel WASM = authority → DELETE, call kernel channel_ledger_js
- kernel wasm.rs = JSON boundary (anti-pattern) → replace raw numeric ABI (FE-01 zero-copy Float32Array view+writeBuffer)
- field-UI FE-04 = particle→wgpu port; thin-shell JS <10 exports numeric+memory-view NO JSON
- money never tween: kernel money.rs integer authority, JS money.ts = duplicate DELETE

## Агенти (2):
- INV interface TS/JS/Node per-file verdict (a82824)
- ARCH Rust-native crate + channel/particle ports + thin-shell + toolchain (a47cd4)

## INV interface TS/JS/Node per-file — DONE:
- Rust authority вже є: kernel/src order_machine 250/money 142/analytics 312/domain 278/wasm 389; compiled dowiz_kernel_bg.wasm 191KB, 4 bindings place_order_js/apply_event_js/channel_ledger_js/reduce_anomalies_js
- SURFACE 1 web/ (engine target): kernel.js 53 KEEP-THIN (loader), channel.js 146 DELETE (byte-mirror analytics.rs — WASM вже exports channel_ledger_js+reduce_anomalies_js, header каже "before WASM binding lands" — landed!), push.js 85 KEEP-THIN (Push/VAPID), sw.js 120 KEEP (SW must-JS), Storefront.svelte 211 KEEP-THIN (view; cartLines/subtotal math→money.rs), OwnerDashboard.svelte 226 KEEP-THIN (repoint channel.js→channel_ledger_js), CourierTrack.svelte 215 KEEP-THIN (STATUS_TO_VOCAB=DATA), index.astro 29/Layout.astro 41 KEEP-THIN SSR, kernel/dowiz_kernel.js 320 REGENERATE + node cjs 229 + 6 .d.ts, kernel.test.mjs 47 KEEP (WASM contract proof)
- SURFACE 2 particle-cloud.js 319 KEEP-THIN (WebGL2 context+rAF browser-bound; physics step could PORT але feeds GL buffers/frame little gain; GLSL+gl.* stays JS; VOCAB=DATA) — АЛЕ field-UI FE-04 = full wgpu port (розбіжність: цей агент каже little-gain для WebGL2 keep, field-ui каже wgpu port. Синтез: wgpu port коли двигун Rust-wgpu, тоді particle-cloud стає Rust module)
- SURFACE 3 packages/ui/lib: auth.ts 194 KEEP-THIN (JWT/fetch/session), CurrencyProvider 57/currency.ts 34 DATA+shell, geo-anim.ts 134 PORT→kernel/src/geo.rs (PURE haversine/lerp/bearing/ema/progress/eta/snap — ideal), i18n-catalog.ts 3830 DATA (locale, largest, JSON/TOML Rust reads), I18nProvider 126 KEEP-THIN, i18n.ts 74 KEEP-THIN (lookup+{{var}}), money.ts 86 DELETE (mirror money.rs, red-line dedupe), motion.ts 165 DATA (framer config→field ζ), sound-prefs 36 KEEP-THIN
- SURFACE 4 hooks (wrapper dissolves, logic classifies): use-breakpoint 35 KEEP-THIN (matchMedia), use-cart 141 PORT→kernel/src/cart.rs (+dup CartProvider), use-courier-marker 132 KEEP-THIN (rAF; math in geo-anim PORT), use-delivery-eta 55 KEEP-THIN, use-embed 12/use-geolocation 59/use-geo-stream 62/use-haptics 22/use-online 27/use-sound 41/use-voice-order 97 ALL KEEP-THIN (browser APIs Geolocation/Vibration/Audio/NetworkInfo/WebSpeech)
- SURFACE 5 apps/web/lib: analytics.ts 76 DATA (taxonomy), apiClient.ts 216 KEEP-THIN (fetch/WebLocks/refresh/Zod), CartProvider.tsx 122 PORT+DELETE-dup→cart.rs (2nd cart impl overlaps use-cart), cartReconcile.ts 63 PORT→cart.rs (pure), exportCSV 16 KEEP-THIN (Blob download), hooks.ts 158 KEEP-THIN+DELETE-dup (re-impl packages/ui hooks), messenger.ts 38 PORT (pure deep-link TG/WA/Viber), safeStorage 27 KEEP-THIN+DELETE-dup, tileConfig 32 DATA, useWebSocket.ts 173 KEEP-THIN (WS lifecycle backoff+jitter)
- SURFACE 6 useMenuData 89 KEEP-THIN (fetch orchestration)
- BUCKET legacy apps/web/src 68 files 17521 LOC: pages/admin 19 files 7317 PORT-view (MenuManager 1244/Dashboard 795/Settings 782/Couriers 584/Analytics 525/... + dashboard-utils order-delta→kernel + AnimatedNumber money-tween smell), pages/client 3 files 3225 PORT/DELETE-superseded (MenuPage 1275/Checkout 1151/OrderStatus 799 — canonical web/ re-implements; red-line Checkout=money), pages/courier 7 files 1734 PORT-view (Delivery 503/Shift 298/Earnings 230 money-tween/...), pages root 2 files 431 PORT+DATA (Onboarding 380/Privacy 51 static), components/media 8 files 770 KEEP-THIN (video/canvas/3D-spin), components brand-scenes 4 files 1232 KEEP-THIN (PaperScene 808/DeliverySwan 201/... canvas/WebGL/SVG), routes 4 files 625 KEEP-THIN (React-Router→Rust router/shell, ClientLayout money-tween), api 2 files 595 DATA/DELETE (mockData 534 fixtures/devBootstrap 61 dev-only), main.tsx bootstrap KEEP-THIN
- BUCKET packages/ui/src 103 files 12140 LOC: components/molecules 27 files 2750 KEEP-THIN (Toast/Modal/Drawer/Sheet + MAP stack MapLibreBase 325/MapWithPin/CourierLiveMap/MapWithRadius 3rd-party JS browser-bound + AnimatedNumber money-tween DELETE-smell), components/client 7 files 959 KEEP-THIN (ProductCard 248/OrderProgress 224/OTPModal/CartDrawer/StateChip), components/atoms 13 files 524 KEEP-THIN (Button/Input/Select/StatusBadge → Rust component lib; PriceDisplay formatMoney→PORT), root scenes 7 files 772 KEEP-THIN, admin 6 files 405 KEEP-THIN (OrderCard 240), courier 5 files 363 KEEP-THIN (SwipeToComplete 170 gesture/TaskCard), layout 6 files 334 KEEP-THIN, theme TS 4 files 335 DATA (palette 146/index 79/paperSkin 26)+shell ThemeProvider 84, tokens.css 596 DATA, utils 6 files 362 PORT/DELETE mixed (index.ts 73 ORDER-MACHINE DUP + parse/ETA, delivery-zone.ts 59 PURE ray-cast PORT, embed/safeStorage dup, sunlight 29 DATA), allergenColors 32 DATA
- IRREDUCIBLE THIN-SHELL (15 browser APIs cannot-Rust): ServiceWorker sw.js; wasm-bindgen loader; Push/Notification/VAPID push.js; Geolocation watchPosition; WebSpeech SpeechRecognition; Vibration; HTMLAudio; navigator.onLine; matchMedia; WebSocket useWebSocket; fetch/WebLocks/token-refresh apiClient/auth; localStorage safeStorage; History/URL ?ch=?embed; File-dialog/Blob exportCSV; Clipboard/QR/canvas; WebGL2+GLSL particle-cloud; MapLibre 3rd-party; <input>/DOM-event-forward/a11y-DOM every view; rAF tweens
- REDUNDANCY (delete/consolidate): channel.js 146 DELETE (analytics.rs+order_machine.rs); money.ts 86 DELETE (money.rs mirror); utils/index.ts ~30 DELETE (ORDER_TRANSITIONS+assertTransition mirror) + PORT parse/ETA; dashboard-utils 71 PORT→kernel (STATUS_RANK forward-progression law); CartProvider 122 vs use-cart 141 = 2 cart impls PORT→ONE cart.rs; apps/web hooks.ts 158 vs packages/ui hooks DELETE-dup; safeStorage ×2 DELETE-dup; Storefront cartLines/subtotal PORT read-kernel; formatMoney→money.rs
- PURE-LOGIC PORT (no Rust yet, should be): geo-anim 134→geo.rs, cartReconcile 63→cart.rs, delivery-zone 59 ray-cast→geo.rs, messenger 38 deep-link→util
- TOTALS surfaces 1-6 (~8.6k itemized): PORT ~498 (geo-anim/use-cart/CartProvider/cartReconcile/messenger), DELETE ~232 (channel 146/money 86), KEEP-THIN ~2890 (islands+browser-API hooks+apiClient/WS/auth+particle), KEEP must-JS ~167 (sw 120/test 47), REGENERATE ~696 (wasm-bindgen), DATA ~4137 (i18n-catalog 3830/motion 165/analytics 76/currency/tile)
- LEGACY VIEW: apps/web 17521 → PORT ~13.5k views (client ~3.2k DELETE-superseded), KEEP-THIN ~3.4k (media/scenes/routes/bootstrap), DATA/DELETE ~0.6k. packages/ui 6881 TS+596 CSS → KEEP-THIN ~6107 (component lib incl MapLibre/gesture/scene), DATA ~963 (tokens/palette/allergen), PORT/DELETE ~130
- GRAND: ~33.6k LOC ~205 files
- HEADLINE "як Rust є двигуном": детермінований DOMAIN (order-machine/money/channel/cart/geo) вже WASM (kernel 1390 LOC Rust) АБО <1k pure JS/TS порт 1:1. Лише ~230 LOC ДУБЛЮЮТЬ Rust → delete outright (channel.js/money.ts/utils-table). ~9k irreducible browser-glue KEEP-THIN (15 Web APIs no WASM). ~5.7k DATA (locale 3830). ~0.7k regenerated. Решта ~18k = React/Svelte view rendering = ШАР що двигун МАЄ СТАТИ. Логіка що "думає" = 1.4k Rust сьогодні; усе що JS сам обчислює (cart totals/money/transitions/anomaly/ETA/geo) collapses у kernel, лишаючи JS = thin membrane browser-APIs + document/locale data
- FIRST CUTS zero-risk (kernel already authoritative): delete channel.js + money.ts + utils order-machine block; repoint OwnerDashboard+Storefront at WASM bindings; consolidate use-cart+CartProvider → kernel/src/cart.rs

## ARCH Rust-native crate — DONE:
- GROUND-TRUTH CORRECTIONS: (1) particle-cloud GLSL HARDWIRES blue=1.0 (v_col=vec3(meta.z,meta.w,1.0) :52, pushParticle stores лише r,g :133/156) → delivered [1,0.82,0.27] рендериться РОЖЕВИМ [1,0.82,1], dispatch_failed blood [0.8,0.1,0.16] рендериться синьо-фіолетовим → wgpu port full-RGBA FE-04 = ВИПРАВЛЯЄ latent bug (visual output legitimately changes); (2) a_seed attr (loc0 :42) declared+uploaded але НЕ читається в main() → Rust ParticlePool drops it, −1 з 3 uploads; (3) channel.js вже повністю redundant (channel_ledger_js/reduce_anomalies_js exist у wasm .d.ts:5,7, kernel.js:50-53 вже wraps, OwnerDashboard:17 still imports JS mirror — delete = wiring-only)
- CURRENT: kernel/ = STANDALONE crate (не workspace) crate-type cdylib+rlib, wasm-bindgen+serde, modules analytics/domain/money/order_machine/wasm → dowiz_kernel_bg.wasm 191KB --target web. bebop spectral у SEPARATE repo (cross-repo rule: НЕ reference bebop з dowiz → VENDOR)
- PROPOSED dowiz-engine Cargo WORKSPACE at crates/: promote kernel/ + add engine crates. ДВА wasm artifacts split by frequency: dowiz_kernel (JSON transactional unchanged) + dowiz_engine (numeric memory-views per-frame)
  | kernel (exists E3): discrete-state authority decide/fold+money+ChannelLedger, keeps JSON wasm.rs transactional, 100% reuse untouched
  | field-math (E2-math): VENDORED no_std spectral core (Laplacian eigenmodes+Chebyshev+FFT blur+algebra), core+alloc ZERO deps, port bebop2 field.rs DT_STABLE=0.02/chebyshev/fft/algebra — vendored NOT git-dep (cross-repo rule)
  | field (E2): 5 aspects layout(stress-majorization)/motion(critical spring)/feedback(Green's)/focus(potential well)/structure(spectral φ₂φ₃), owns fixed DT, reuse field-math, new FE-07/08/10/11/12
  | state (E3): thin adapter kernel + money-never-tween TYPE BOUNDARY FE-09 (money discrete channel never field channel), reuse kernel::{Order,apply_event,ChannelLedger,apply_tax}
  | store (E1): DOD SoA WidgetStore hot/warm/cold + ParticlePool ring FE-02 + fixed-timestep accumulator DT=0.02=field::DT_STABLE FE-03, pattern particle-cloud SoA ring
  | render (E4): wgpu pipelines particle billboards FE-04 GLSL→WGSL + SDF rounded-box FE-05 + MSDF text cosmic-text FE-06, single pass painter-order, deps wgpu+cosmic-text
  | input (E5): Intent/FieldPos/InputSource, pointer→field-force, event→source impulse
  | tokens (E4-data): design-token GPU table color-mix pre-resolved CPU→RGBA, 4-layer cascade→1 UBO write
  | i18n (E4-data): locale key tables sq/en/uk feeding shaper, ~1296 keys data-only
  | shell (E5/E0): ТІЛЬКИ crate з wasm-bindgen, <10 numeric/memory-view exports, integrates all, holds linear-memory staging, pattern rust-core *mut f64 shared ArrayBuffer
- DEP GRAPH: shell(cdylib ONLY JS boundary)→render(tokens,i18n,field-math)+field(field-math)+store+state(kernel)+input; kernel(cdylib+rlib own JSON wasm.rs transactional). Тільки shell+kernel cdylib, engine crates rlib. field-math = STRICT FLOOR #![no_std]+alloc zero-deps wasm32-clean (bebop2 field.rs вже meets)
- channel.js DELETE (proven byte-mirror): header ":1 port of analytics.rs"; STAGE_ORDER=funnel stage_order, ALLOWED_NEXT=allowed_next, assertTransition=assert_transition, ingest dup-lock=ChannelLedger::ingest, reduceAnomalies BTreeMap-fold=reduce_anomalies. Rust вже shipped wasm (channel_ledger_js wasm.rs:285, reduce_anomalies_js :293, .d.ts:5,7). Sole consumer OwnerDashboard:17 comment "until WASM-binding wave" (landed). Verdict DELETE not port (dashboard TRANSACTIONAL once at mount ~16 events, transactional→JSON correct). Rewire OwnerDashboard channel.js→channelLedger(SAMPLE_EVENTS) 3-line, funnel[selectedChannel]??ZERO. Warts: channel_ledger_logic Box::leak per event (harmless one-shot NOT per-frame), funnel only seen channels (client 10-stage zero fallback). DELIVERABLE −146 LOC, ~3-line Svelte, 0 new Rust
- particle-cloud.js FULL wgpu PORT (FE-04): store::ParticlePool (SoA ring+integrator) + render::particles (WGSL+pipeline) + 3 shell exports (on_event/on_pointer/instance_view); delete 319 LOC; swap CourierTrack:15,71-73 createParticleCloud→engine mount
  - GLSL 300es→WGSL: point sprites (gl_PointSize) NOT in WGSL → INSTANCED BILLBOARD QUAD (4 verts tri-strip). Inst {pos vec2, life vec2, color vec4 FULL RGBA fixes blue-bug}. VS: life01=life/maxLife, size=2+life01·6·energy=gl_PointSize, corner expand quad pixel-space, ndc y-flip. FS: d=uv·0.5=gl_PointCoord−0.5, r=dot(d,d), r>0.25 discard, a=(1−4r)·life01, additive pipeline blend SrcAlpha,One. naga compiles WGSL runtime; WebGL2 fallback wgpu/naga cross-compile WGSL→GLSL auto (no hand 2nd shader)
  - Ring SoA: MAX=4096, pos_x/y+vel_x/y+life+max_life+color[[f32;4]] (drop dead seed, widen 2-ch meta→RGBA), inst=flat interleaved staging linear-memory → ONE writeBuffer (collapse 3 bufferSubData FE-01), steady alloc=0 (spawn overwrite head)
  - VOCAB Rust table verbatim :31-37 (FE-04 OUT OF SCOPE не міняти): OrderCreated/CourierAssigned/Delivered/DispatchFailed/PendingAging {color,energy,burst,swirl}, palette runtime-mutable (setPalette)
  - Physics Rust integrator (ms-calibrated dt/16.6, under FE-03 fixed loop dt_ms=DT·1000=20ms): damp=0.92^(dt/16.6), pointer repulsion :182-185, semi-implicit Euler, life-decay swap-remove, energy 0.95^decay. burst :136-158 (energy min(1.6,+v.energy·0.5), center spawn random angle, spd=(0.4+rand)·burst·60·dpr, tangential swirl>1, life=(0.6+rand·0.8)·1000, reduced count·0.25). RNG Math.random→xorshift32 (sovereign no std::rand). SIMD damp+integrate f32x4 + scalar bit-identical FE-16
  - API→Rust: init→Particles::new+ParticlePool::new, burst(status,count)→pool.burst(kind:EventKind enum)→shell on_event(kind:u32,count:u32), setReducedMotion→set_flags, setPalette→pool.set_palette, setPointer→shell on_pointer (DOM forward raw px, *dpr in step), resize→shell resize, dispose→Drop
  - DUAL PATH: WebGPU optional WGSL compute integrator positions-stay-GPU FE-01 fast; WebGL2 CPU SoA step+writeBuffer FE-16 baseline. RED→GREEN visual parity EXCEPT corrected colors, blue!=1.0 proven, compute==CPU bit-identical
- IRREDUCIBLE THIN-SHELL: wasm bootloader+feature-detect (kernel.js:10-14 ready()), lazy rAF arm/cancel FE-14 (particle :255-260), DOM event→on_pointer one-liner (:279-285), browser-API shims Rust invokes through (Push/getUserMedia/WebSpeech/WebXR/clipboard/file/History/permissions), a11y DOM mirror FE-15 (AccessKit no web backend hand-roll), transparent <input> overlay (IME/autofill/mobile-kbd DOM-only), service worker sw.js (separate thread). ENGINE WASM EXPORT SURFACE <10 zero-JSON: memory, engine_new(), tick(frame_ms)->u32 dirty-bits FE-14, instance_ptr/len + widget_ptr/len (Float32Array view zero-copy), on_pointer(px,py), on_event(kind:u32,count:u32), set_flags(bits), resize(w,h,dpr). JS reads frame NO copy NO parse: new Float32Array(memory.buffer, instance_ptr(), instance_len())→writeBuffer. = rust-core ABI (*mut f64 same ArrayBuffer lib.rs:246-273). CONTRAST JSON wasm.rs anti-pattern (place_order_js/apply_event_js/channel_ledger_js/reduce_anomalies_js JSON String both ways → per-frame V8 JSON.parse 240KB 60×/s катастрофа) — kernel JSON boundary CORRECT stays TRANSACTIONAL only. Dividing line = update FREQUENCY not data type. TWO wasm modules two boundaries
- TOOLCHAIN: wasm32-unknown-unknown; field-math --no-default-features no_std; shell/render link wgpu+cosmic-text. wasm-pack vs trunk vs bindgen+vite → wasm-bindgen (as today, Astro/Svelte SSR не Rust-only-app so NOT trunk). Path: cargo build wasm32 → wasm-bindgen --target web → ES-module+.wasm committed web/src/lib/engine/ + ~1KB loader import init. Vite bundles ONLY thin loader NEVER logic (logic=prebuilt .wasm) = "no JS bundler for engine logic". WGSL: include_str! embedded, naga compiles runtime (removes GLSL compile particle :75-83), WebGL2 cross-compile WGSL→GLSL. SIZE budget ≤2MB gzip dowiz_engine (kernel 191KB; wgpu+cosmic-text cost centers). Levers opt-level="z"+lto+panic=abort+wasm-opt -Oz+strip+talc allocator+feature-gate cosmic-text sq/en/uk (MSDF atlas=DATA not wasm). LEAN first slice: FE-04 particle alone doesn't need full wgpu → raw web-sys WebGL2/WebGPU keeps island small, adopt wgpu at Wave 1 SDF+text. Astro SSR stays SEO /s/:slug DOM; engine = client:only svelte island onMount import engine loader→canvas→engine_new+arm rAF (=CourierTrack:67-93 backend swapped); astro.config +Vite wasm stanza
- MIGRATION island-by-island canonical web/ beachhead, big-bang forbidden Gain−Loss>0 text-input never pure-GPU:
  1. Workspace + field-math + store + shell skeleton (Wave0 FE-01/02/03): promote kernel→crates/, vendor bebop2 field/chebyshev/fft/algebra→field-math (re-run tests green field.rs:346-521), store ring zero-alloc + fixed loop DT=0.02 stutter-gate. No app change, engine wasm builds green headless
  2. DELETE channel.js EARLY (independent, transactional read kernel exports ship): rewire OwnerDashboard→channelLedger(). Proves "kernel authority, JS mirrors deleted" smallest change, de-risk drift. −146 LOC
  3. Particle port = ISLAND #0 FE-04 (decorative M≈1 A≈S≈0 T=0 rank#1, one island): store::ParticlePool+render::particles+3 exports. Keep running: CourierTrack static text stays, only canvas backend swap :15,:71, existing try/catch :77-81 = WebGL2 degrade. DELETE particle-cloud.js after parity gate (visual parity + corrected colors + zero-alloc)
  4. Render primitives (Wave1 FE-05 SDF+tokens/FE-06 MSDF): one Storefront card pixel-parity vs CSS (must DRAW before field MOVES)
  5. Field dynamics (Wave2 FE-07 layout/FE-08 motion/FE-09 money 🔴): one island Svelte→field stress-majorization+critical spring. FE-09 money-boundary red-line codifies existing integer-static invariant (CourierTrack:8) not behavior change
  6. Semantics+hybrid (Wave3-4 FE-10..16): Green's/potential-well/spectral + PERMANENT hybrid <input> overlay+a11y DOM mirror FE-15. Forms/a11y/SSR-menu stay DOM permanently architecture-not-debt
  7. Legacy apps/web LAST FE-17 🔴: React SPA untouched till end, 4 money-tween (ClientLayout:154/EarningsPage/DashboardPage:421/AnalyticsPage:262) killed separate red-line
  KEEP-RUNNING INVARIANT: each Rust island mounts behind SAME Astro mount as Svelte it replaces, lazy client:only WASM, degrade to Svelte/WebGL2 when WASM/WebGPU absent (CourierTrack:77-81 pattern). Two wasm modules coexist, kernel never rewritten only re-consumed
- BUILDABLE: one Cargo workspace + one vendored no_std math crate (tests green) + one cdylib shell <10 numeric exports + two deletes (channel/particle) + existing Astro-island + wasm-bindgen --target web reused verbatim

## ОБИДВА ЗВІТИ DONE. Готовий синтезувати rewrite plan + blueprints.
