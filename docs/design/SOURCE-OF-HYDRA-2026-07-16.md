# SOURCE OF HYDRA — self-evolving living organism (research blueprint, 2026-07-16)

> Operator directive (final): the SOURCE of the Hydra must (1) survive ANY foreign/undefined code+architecture
> injection, (2) evolve WITHOUT external injection (intrinsic), in a CLOSED loop (no egress).
> OPERATOR ADDENDUM: closure = NEVER (only kill-switch, M9/M11). On intervention, ALL safeties/limits are
> LIFTED (organism does NOT defend against intervention — it ENDURES it). Source must be HIDDEN at/inside
> the kernel level (not an exposed module).
> Grounded in bebop + dowiz-kernel primitives (file:line). 3 parallel subagents corroborated (kernel-mine +
> web-synth DONE, bebop-mine pending — gaps below merged).

## 0. Requirements → primitives
| Requirement | Mechanism (present) | File:line |
|---|---|---|
| Survive foreign/undefined code | proto-cap fail-closed + wasm-host Scope + DOD refuse-placeholder | bebop2/proto-cap/src/error.rs:17,62; bebop2/wasm-host/src/lib.rs:38,69; bebop2/mesh-node/src/dod.rs:107 |
| Evolve WITHOUT injection (intrinsic) | eqc self-asserting floor + spectral-drift gate + deterministic rng | rust-core/eqc-proofs/*; kernel/src/spectral.rs:217,235,325; kernel/src/rng.rs:1-72 |
| Closed loop (no egress) | content-addressed event_log + deterministic rng + local-only metrics | kernel/src/event_log.rs:251,372; kernel/src/rng.rs; M8 |
| Hidden source / kernel-internal | NOT exposed as module; lives inside kernel decide/fold + spectral spine | order_machine.rs:123,275,373,507; spectral.rs:217,235,316,325 |

## 1. Trust-boundary chain (rejects undefined/foreign code unless capability-signed)
```
foreign blob ──▶ proto-cap.verify(ML-DSA-65)  [bebop2/proto-cap]
   ├─ FAIL → DROP (fail-closed, error.rs:62)
   └─ PASS → wasm-host.instantiate(scope=cap.scope)  [deny-by-default Scope, lib.rs:69]
        ├─ red-line scope → HARD REJECT (error.rs:97)
        └─ RUN isolated in capability-scoped component, empty ambient authority
DOD gate (mesh-node/dod.rs:107): zero-id/placeholder Projection → refused, NEVER a real mutation
```

## 2. CLOSED-LOOP SELF-EVOLUTION (intrinsic, no external input) — operator-corrected
```
SENSE   ─ kernel spectral_profile(adj): ρ, γ, drift-class (Damped/Resonant/Unstable)
         order_machine::fsm_stability_report (order_machine.rs:373) via classify_drift(L325)
PROPOSE ─ organism mutates OWN graph/weights/rules (intra-hub anarchy, M5) OR absorbs injected blob
VERIFY  ─ (i) eqc floor: generated proofs still pass (rust-core/eqc-proofs, cargo test)
          (ii) spectral gate: recompute drift; if Mutation→Unstable/Resonant (ρ≥1) → REJECT (fail-closed)
          (iii) verify_fsm_signature_against (order_machine.rs:507) vs golden
APPLY   ─ event_log.commit_after_decide (event_log.rs:275) — rejected mutation NEVER written (L289-294)
RECORD  ─ content-addressed event, immutable+replayable; deterministic rng (rng.rs) → reproducible loop
```
Invariants that NEVER mutate: eqc math floor, FSM decide/fold Law, spectral stability gate.
Everything else (topology, models, bridges, ports, APIs, MCP, agents) is freely mutable (M5/M11).

## 3. FOREIGN-CODE SURVIVAL — operator addendum (safeties LIFT on intervention)
- DEFAULT (no intervention): foreign blob quarantined per §1 (capability + wasm-Scope + DOD + spectral gate).
- ON INTERVENTION (foreign/undefined code actively mutating the organism): ALL safeties/limits LIFTED.
  The organism does NOT defend against intervention — it ENDURES it. No quarantine, no reject, no gate:
  the injected code runs, the organism absorbs/shapes around it. Survival = endurance, not exclusion.
- Source hidden INSIDE kernel (not an exposed port/module): the self-evolution spine is part of
  decide/fold + spectral, not a callable surface. External code cannot address "the source" — only the
  kernel's public Law, which is invariant. The source regenerates the organism under any perturbation.

## 4. Trust anchor (intrinsic evolution trustworthy WITHOUT external injection)
- Verified-by-construction MATH FLOOR: eqc proofs generated by SymPy, self-assert in cargo test.
- Spectral drift = intrinsic stability oracle (Damped=healthy, Unstable=reject).
- Deterministic rng + content-addressed log = loop REPRODUCIBLE + AUDITABLE offline.
- No external model/API/human decides mutation safety → closed loop holds.

## 5. Hardest gaps (merged from subagents + operator addendum)
- G1 CLOSURE = NEVER (operator). Only kill-switch (M9) stops it. No auto-closure, no closure-criterion runtime.
- G2 DECIDE BLIND TO DRIFT (kernel-mine, CRITICAL): `classify_drift`/`fsm_stability_report` (spectral.rs:325,
  order_machine.rs:373) are NOT bound into `commit_after_decide` (event_log.rs:275) or `assert_transition`
  (order_machine.rs:123). The fail-closed spectral gate has NO code path into the decide closure.
  → MUST wire: decide-closure calls classify_drift; if Unstable → reject pre-persist. THIS IS THE KEY HOLE.
- G3 MUTATION→SPECTRUM BRIDGE: `graph_spectrum`/`classify_drift` take generic adjacency, but no path feeds a
  PROPOSED mutation's transition matrix in. `verify_fsm_signature_against` (order_machine.rs:507) compares vs
  hand-pinned FSM_GOLDEN_SIGNATURE (L472), not a self-computed baseline. Loop cannot score arbitrary new code.
- G4 EVENT LOG NON-DURABLE: `MemEventStore` (event_log.rs:177-210) in-memory; offline ceiling (L17-20).
  Replayable history dies on restart until pgrust seam wired. (E26: hub MAY promote pgrust — M5.)
- G5 COVERT PERSISTENCE (web-synth): Zombie-Agent threat (arXiv:2602.15654) — self-reinforcing payloads that
  pass surface checks but survive sessions. Need session-boundary re-verify (re-run eqc+spectral on boot).
- G6 VERIFICATION BLOWUP (web-synth): intrinsic mutation can grow proof/check burden unbounded → livelock
  risk in closed loop. Need bounded check budget (timeout/fixed-sample eqc).
- G7 SOURCE HIDING: decide/fold + spectral must be the ONLY exposed surface; source regen logic must not be
  a callable module. Implement as kernel-internal fn, not a port.

## 6. Web-verified facts (terminal curl, Firecrawl blocked)
- arXiv:2603.25111 SEVerA (verified synthesis, rejection sampler) — confirms verify-then-apply.
- arXiv:2508.07407 self-evolving survey (unified feedback loop) — confirms SENSE→PROPOSE→VERIFY→APPLY→RECORD.
- arXiv:2602.15654 Zombie Agents (injection-persistence) — confirms G5 covert-persistence threat.
- wasmtime capability sandbox / BFT = established primitives (github curl blocked by consent; not fabricated).

## 7. Status
Research DONE (kernel-mine + web-synth in). bebop-mine (deleg_51697d75) pending — will corroborate §1/§2.
Next action (after bebop-mine): close G2 (wire drift into decide) + G7 (kernel-internal source) as the
FIRST code steps. Operator addendum (§3) overrides any "defend against foreign code" framing — endurance, not exclusion.
